// This file is auto-generated by @hey-api/openapi-ts

export type ClientOptions = {
    baseUrl: 'https://partner.example.com' | (string & {});
};

/**
 * The status of the listing
 */
export type ListingStatus = 'AVAILABLE' | 'ACTIVE' | 'CLAIMED' | 'SUPPRESSED' | 'BLOCKED';

/**
 * List of business categories, in your taxonomy, ordered by importance / relevance
 */
export type Categories = Array<{
    /**
     * Category ID in your taxonomy
     */
    id: string;
    /**
     * Category name
     */
    name: string;
}>;

/**
 * The kind of listing
 */
export type LocationType = 'Location' | 'HealthcareFacility' | 'HealthcareProfessional';

export type SearchResult = {
    /**
     * The ID of this location, as assigned by you
     */
    id: string;
    status: ListingStatus;
    /**
     * The name of the result listing
     * For sites that track offices and professionals separately (e.g., a doctor’s office vs. individual doctors), please return both the office name and the individual professionals’ names.
     *
     */
    name: string;
    /**
     * The street address of the result listing
     * In cases where the business sets address.visible to false, hide the address on your page, but still return its value in the API response.
     *
     */
    address: string;
    /**
     * Address line 2 for the result listing
     *
     */
    address2?: string;
    /**
     * The sublocality (e.g., neighborhood or district) of the result listing
     *
     */
    sublocality?: string;
    /**
     * The city of the result listing
     *
     */
    city: string;
    /**
     * `required` for US, CA and some other countries.
     *
     * The state, region, or province of the result listing
     *
     */
    state?: string;
    /**
     * `required` for most countries.
     *
     * The ZIP or postal code of the result listing. Contact the Implementation team for valid formats.
     *
     */
    zip?: string;
    /**
     * The ISO 3166-1 alpha-2 code of the area of the result listing
     *
     */
    countryCode: string;
    /**
     * The latidude of the result listing
     */
    latitude: number;
    /**
     * The longitude of the result listing
     */
    longitude: number;
    /**
     * The primary phone number of the listing. For international numbers, the number should match how a person within the country would dial it (i.e., no area code or punctuation).
     */
    phone: string;
    /**
     * The URL where the listing’s details page can be viewed
     */
    url: string;
    categories: Categories;
    type: LocationType;
    /**
     * The business's website URL on the listing
     *
     */
    websiteUrl?: string;
    /**
     * The number of reviews posted on the listing
     *
     */
    reviewCount?: number;
    /**
     * The average of the ratings (e.g., 4.3 for an average of 4.3 stars) posted on the listing
     *
     */
    averageReviewScore?: number;
    /**
     * The date the listing was created (YYYY-MM-DD)
     *
     */
    listingCreationDate?: string;
};

export type ResponseErrorNew = {
    /**
     * Code that uniquely identifies the error or warning.
     *
     */
    code?: number;
    /**
     * Message explaining the problem.
     */
    message?: string;
};

/**
 * The hours associated with the listing.
 */
export type DetailsHours = Array<{
    day?: 'MONDAY' | 'TUESDAY' | 'WEDNESDAY' | 'THURSDAY' | 'FRIDAY' | 'SATURDAY' | 'SUNDAY';
    /**
     * A set of opening and closing times for the specified day
     */
    intervals?: Array<{
        /**
         * The starting time of the interval on the 24-hour clock (e.g., 09:00:00)
         */
        start?: string;
        /**
         * The ending time of the interval on the 24-hour clock (e.g., 14:00:00)
         */
        end?: string;
    }>;
}>;

/**
 * The closed status of the listing
 */
export type ClosedStatus = 'OPEN' | 'TEMPORARILY_CLOSED' | 'PERMANENTLY_CLOSED';

/**
 * Base fields for any type of listing
 */
export type Details = {
    /**
     * The ID of this location, as assigned by you
     */
    id: string;
    status: ListingStatus;
    /**
     * The name to be displayed on the listing
     */
    name: string;
    /**
     * The building number and street address of the location
     */
    address: string;
    /**
     * Second line of address (e.g., suite number)
     */
    address2?: string;
    /**
     * The sublocality (e.g., neighborhood or district) of the location
     */
    sublocality?: string;
    /**
     * City
     */
    city?: string;
    /**
     * The location's state, region, or province. Abbreviations are used for some countries.
     */
    state?: string;
    /**
     * The ZIP or postal code of the location
     */
    zip?: string;
    /**
     * The ISO 3166-1 alpha-2 code of the location
     */
    countryCode?: string;
    /**
     * The primary phone number of the listing. For international numbers, the number should match how a person within the country would dial it (i.e., no area code or punctuation).
     */
    phone: string;
    /**
     * The URL where the listing’s details page can be viewed
     */
    url: string;
    /**
     * The current rating for the listing (if your site has ratings). This is usually measured in “stars” and ranges from 0 to 5 in increments of 0.5.
     */
    rating?: number;
    /**
     * The total number of reviews for the listing (if your site has reviews).
     */
    totalReviews?: number;
    categories?: Categories;
    /**
     * The website associated with the result listing.
     */
    website?: string;
    /**
     * The business description associated with the result listing.
     */
    description?: string;
    hours: DetailsHours;
    /**
     * Array of urls of photos associated with the result listing.
     */
    photos?: Array<string>;
    /**
     * The year that the business opened (e.g., 1982)
     */
    yearEstablished?: number;
    /**
     * The "message" portion of the `specialOffer` that we send to you
     */
    specialOfferMessage?: string;
    /**
     * The "URL" portion of the `specialOffer` that we send to you
     */
    specialOfferUrl?: string;
    /**
     * The email address that consumers can use to contact a representative of the business
     */
    contactEmail?: string;
    /**
     * The latitude of the listing
     */
    latitude: string;
    /**
     * The longitude of the listing
     */
    longitude: string;
    /**
     * The URL of the business's YouTube video
     */
    video?: string;
    /**
     * A list of payment methods that the business accepts.
     */
    paymentOptions?: Array<string>;
    /**
     * The Twitter username used by the business
     */
    twitter?: string;
    /**
     * The URL of the business's Facebook Page
     */
    facebook?: string;
    type?: LocationType;
    closedStatus: ClosedStatus;
};

/**
 * The following additional fields are available if `type` is `HealthcareProfessional`.
 */
export type HealthcareProfessional = {
    /**
     * National Provider Identifier (NPI) of the healthcare professional.
     */
    npi: string;
    /**
     * The first name of the healthcare professional.
     */
    firstName: string;
    /**
     * The middle name of the healthcare professional.
     */
    middleName: string;
    /**
     * The last name of the healthcare professional.
     */
    lastName: string;
    /**
     * The name of the office where the healthcare professional works.
     */
    officeName: string;
    /**
     * The gender of the healthcare professional.
     */
    gender: string;
    /**
     * A list of the conditions treated by the healthcare professional.
     */
    conditionsTreated?: Array<string>;
    /**
     * Indicates whether the healthcare professional is accepting new patients.
     */
    acceptingNewPatients?: boolean;
};

/**
 * The following additional fields are available if `type` is `HealthcareFacility`.
 */
export type HealthcareFacility = {
    /**
     * National Provider Identifier (NPI) of the healthcare facility.
     */
    npi: string;
    /**
     * A list of the conditions treated by the healthcare professional.
     */
    conditionsTreated?: Array<string>;
    /**
     * Indicates whether the healthcare professional is accepting new patients.
     */
    acceptingNewPatients?: boolean;
};

/**
 * The structured address for the location
 */
export type Address = {
    /**
     * The building number and street address of the location
     */
    address: string;
    /**
     * Second line of address (e.g., suite number)
     */
    address2: string;
    /**
     * Additional information to help consumers find the location. This string should be presented in addition to the address (e.g., In Menlo Mall, 3rd Floor). It may also be used in conjunction with a hidden address (address.visible = false) e.g., Servicing the New York Area
     */
    displayAddress?: string;
    /**
     * Defaults to true. When present and false, the street address (address, address2,address3) should not be shown to consumers, but displayAddress should still appear, along with sublocality, city, state, and postalCode.
     */
    visible?: boolean;
    /**
     * The sublocality (e.g., neighborhood or district) of the location
     */
    sublocality?: string;
    /**
     * City
     */
    city: string;
    /**
     * The location's state, region, or province. Abbreviations are used for some countries.
     */
    state: string;
    /**
     * The ZIP or postal code of the location
     */
    postalCode: string;
    /**
     * The ISO 3166-1 alpha-2 code of the location
     */
    countryCode: string;
};

/**
 * A list of the location’s phone numbers. There will be exactly one MAIN phone number and optionally some non-MAIN phone numbers (at most one of each type).
 */
export type Phones = Array<{
    /**
     * A description of the phone number, e.g. “Main”.
     */
    description?: string;
    number: {
        /**
         * The ITU-T code of the location’s phone number. Note that this may not match the country code of the location address.
         */
        countryCode?: string;
        /**
         * Area code and phone number. How the number would be dialed in the location’s country (with no punctuation or country code).
         */
        number?: string;
    };
    type?: 'MAIN' | 'ALTERNATE' | 'FAX' | 'LOCAL' | 'MOBILE' | 'TOLL_FREE' | 'TTY';
}>;

/**
 * List of business categories, in your taxonomy, ordered by importance / relevance
 */
export type PartnerCategories = Array<{
    /**
     * Category ID in your taxonomy
     */
    id: string;
    /**
     * Category name
     */
    name: string;
}>;

/**
 * A list of contact email addresses that consumers can use to reach someone who represents the location
 */
export type Emails = Array<{
    /**
     * Email Address
     */
    address?: string;
    /**
     * Description
     */
    description?: string;
}>;

/**
 * Geolocation data describing where exactly the physical location is
 */
export type GeoData = {
    /**
     * The latitude coordinate of the location's center
     */
    displayLatitude: number;
    /**
     * The longitude coordinate of the location's center
     */
    displayLongitude: number;
    /**
     * The latitude coordinate of where consumers should go to first when visiting the location (e.g., a parking lot, a particular building entrance)
     */
    routableLatitude?: number;
    /**
     * The longitude coordinate of where consumers should go to first when visiting the location (e.g., a parking lot, a particular building entrance)
     */
    routableLongitude?: number;
    /**
     * The latitude coordinate of where consumers should go in order reach the location on foot (e.g., a particular entrance, the start of a walkway)
     */
    walkingLatitude?: number;
    /**
     * The latitude coordinate of where consumers should go in order reach the location on foot (e.g., a particular entrance, the start of a walkway)
     */
    walkingLongitude?: number;
};

/**
 * A set of opening and closing times for the specified date.
 */
export type Interval = Array<{
    /**
     * The starting time of the interval on the 24-hour clock (e.g., 09:00:00)
     */
    start?: string;
    /**
     * The end time of the interval on the 24-hour clock (e.g., 14:00:00)
     */
    end?: string;
}>;

/**
 * A set of special hours that may vary from the standard business hours.
 */
export type HolidayHour = Array<{
    /**
     * Date on which the holiday hours will be in effect. Will only provide future dates.
     */
    date?: string;
    /**
     * Indicates if the hours of operation are "closed" on the given date.
     */
    isClosed?: boolean;
    /**
     * Indicates whether the holiday hours are the same as the regular business hours for the given date.
     */
    isRegularHours?: boolean;
    /**
     * A set of opening and closing times for the specified date
     */
    intervals?: Array<Interval>;
}>;

/**
 * Contains structured business-hours information. Any days that are unspecified should be interpreted as 'we don't have information for this day'. Days that are specified, but contain no times, should be interpreted as 'Closed'. This is not a required field in Yext but should be displayed if provided.
 */
export type Hours = {
    day?: 'MONDAY' | 'TUESDAY' | 'WEDNESDAY' | 'THURSDAY' | 'FRIDAY' | 'SATURDAY' | 'SUNDAY';
    /**
     * A set of opening and closing times for the specified day
     */
    intervals?: Array<Interval>;
    /**
     * If populated, indicates that the business is temporarily closed and provides the date that the business is planning to reopen.
     */
    reopenDate?: string;
    /**
     * A set of special hours that may vary from the standard business hours.
     */
    holidayHours?: Array<HolidayHour>;
};

/**
 * Contains the daily access hours, holiday access hours, and reopen date for the Entity.
 *
 * Each day is represented by a sub-field of `accessHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
 * Similarly, holiday access hours are represented by the `holidayHours` sub-field.
 * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
 * SPECIAL CASES:
 * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
 * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
 * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
 *
 * Filtering Type: `hours`
 *
 * ```
 * Eligible For:
 * * atm
 * * healthcareFacility
 * * hotel
 * * location
 * * restaurant
 * ```
 */
export type AccessHours = {
    /**
     * Filtering Type: `object`
     */
    friday?: {
        /**
         * Indicates if the access hours are "closed" on Friday.
         *
         * Filtering Type: `boolean`
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is open on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         *
         * Filtering Type: `list of object`
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             *
             * Filtering Type: `time`
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             *
             * Filtering Type: `time`
             */
            start?: string;
        }>;
    };
    /**
     *
     * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
     *
     *
     * Array must be ordered.
     *
     *
     * Filtering Type: `list of object`
     */
    holidayHours?: Array<{
        /**
         * Date on which the holiday hours will be in effect. Cannot be in the past.
         *
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         *
         * Filtering Type: `date`
         */
        date: string;
        /**
         * Indicates if the access hours are "closed" on on the given date.
         *
         * Filtering Type: `boolean`
         */
        isClosed?: boolean;
        /**
         * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
         *
         * Filtering Type: `boolean`
         */
        isRegularHours?: boolean;
        /**
         * Contains the time intervals for which the Entity is open on the specified date.
         *
         * Filtering Type: `list of object`
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             *
             * Filtering Type: `time`
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             *
             * Filtering Type: `time`
             */
            start?: string;
        }>;
    }>;
    /**
     * Filtering Type: `object`
     */
    monday?: {
        /**
         * Indicates if the access hours are "closed" on Monday.
         *
         * Filtering Type: `boolean`
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is open on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         *
         * Filtering Type: `list of object`
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             *
             * Filtering Type: `time`
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             *
             * Filtering Type: `time`
             */
            start?: string;
        }>;
    };
    /**
     *
     * Date must be on or after 1970-01-01
     * Date must be before or on 2038-01-01
     *
     * Filtering Type: `date`
     */
    reopenDate?: string;
    /**
     * Filtering Type: `object`
     */
    saturday?: {
        /**
         * Indicates if the access hours are "closed" on Saturday.
         *
         * Filtering Type: `boolean`
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is open on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         *
         * Filtering Type: `list of object`
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             *
             * Filtering Type: `time`
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             *
             * Filtering Type: `time`
             */
            start?: string;
        }>;
    };
    /**
     * Filtering Type: `object`
     */
    sunday?: {
        /**
         * Indicates if the access hours are "closed" on Sunday.
         *
         * Filtering Type: `boolean`
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is open on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         *
         * Filtering Type: `list of object`
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             *
             * Filtering Type: `time`
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             *
             * Filtering Type: `time`
             */
            start?: string;
        }>;
    };
    /**
     * Filtering Type: `object`
     */
    thursday?: {
        /**
         * Indicates if the access hours are "closed" on Thursday.
         *
         * Filtering Type: `boolean`
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is open on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         *
         * Filtering Type: `list of object`
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             *
             * Filtering Type: `time`
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             *
             * Filtering Type: `time`
             */
            start?: string;
        }>;
    };
    /**
     * Filtering Type: `object`
     */
    tuesday?: {
        /**
         * Indicates if the access hours are "closed" on Tuesday.
         *
         * Filtering Type: `boolean`
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is open on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         *
         * Filtering Type: `list of object`
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             *
             * Filtering Type: `time`
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             *
             * Filtering Type: `time`
             */
            start?: string;
        }>;
    };
    /**
     * Filtering Type: `object`
     */
    wednesday?: {
        /**
         * Indicates if the access hours are "closed" on Wednesday.
         *
         * Filtering Type: `boolean`
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is open on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         *
         * Filtering Type: `list of object`
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             *
             * Filtering Type: `time`
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             *
             * Filtering Type: `time`
             */
            start?: string;
        }>;
    };
};

/**
 * Contains the daily brunch hours, holiday brunch hours, and reopen date for the Entity.
 *
 * Each day is represented by a sub-field of `brunchHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
 * Similarly, holiday brunch hours are represented by the `holidayHours` sub-field.
 * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
 * SPECIAL CASES:
 * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
 * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
 * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
 */
export type BrunchHours = {
    friday?: {
        /**
         * Indicates if the brunch hours are "closed" on Friday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is open for brunch on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    /**
     *
     * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
     *
     *
     * Array must be ordered.
     *
     */
    holidayHours?: Array<{
        /**
         * Date on which the holiday hours will be in effect. Cannot be in the past.
         *
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        date: string;
        /**
         * Indicates if the brunch hours are "closed" on on the given date.
         */
        isClosed?: boolean;
        /**
         * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
         */
        isRegularHours?: boolean;
        /**
         * Contains the time intervals for which the Entity is open on the specified date.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    }>;
    monday?: {
        /**
         * Indicates if the brunch hours are "closed" on Monday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is open for brunch on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    /**
     *
     * Date must be on or after 1970-01-01
     * Date must be before or on 2038-01-01
     */
    reopenDate?: string;
    saturday?: {
        /**
         * Indicates if the brunch hours are "closed" on Saturday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is open for brunch on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    sunday?: {
        /**
         * Indicates if the brunch hours are "closed" on Sunday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is open for brunch on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    thursday?: {
        /**
         * Indicates if the brunch hours are "closed" on Thursday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is open for brunch on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    tuesday?: {
        /**
         * Indicates if the brunch hours are "closed" on Tuesday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is open for brunch on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    wednesday?: {
        /**
         * Indicates if the brunch hours are "closed" on Wednesday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is open for brunch on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
};

/**
 * Contains the daily delivery hours, holiday delivery hours, and reopen date for the Entity.
 *
 * Each day is represented by a sub-field of `deliveryHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
 * Similarly, holiday delivery hours are represented by the `holidayHours` sub-field.
 * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
 * SPECIAL CASES:
 * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
 * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
 * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
 */
export type DeliveryHours = {
    friday?: {
        /**
         * Indicates if the delivery hours are "closed" on Friday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is delivering on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    /**
     *
     * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
     *
     *
     * Array must be ordered.
     *
     */
    holidayHours?: Array<{
        /**
         * Date on which the holiday hours will be in effect. Cannot be in the past.
         *
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        date: string;
        /**
         * Indicates if the delivery hours are "closed" on on the given date.
         */
        isClosed?: boolean;
        /**
         * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
         */
        isRegularHours?: boolean;
        /**
         * Contains the time intervals for which the Entity is delivering on the specified date.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    }>;
    monday?: {
        /**
         * Indicates if the delivery hours are "closed" on Monday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is delivering on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    /**
     *
     * Date must be on or after 1970-01-01
     * Date must be before or on 2038-01-01
     */
    reopenDate?: string;
    saturday?: {
        /**
         * Indicates if the delivery hours are "closed" on Saturday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is delivering on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    sunday?: {
        /**
         * Indicates if the delivery hours are "closed" on Sunday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is delivering on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    thursday?: {
        /**
         * Indicates if the delivery hours are "closed" on Thursday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is delivering on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    tuesday?: {
        /**
         * Indicates if the delivery hours are "closed" on Tuesday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is delivering on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    wednesday?: {
        /**
         * Indicates if the delivery hours are "closed" on Wednesday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is delivering on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
};

/**
 * Contains the daily drive-through hours, holiday drive-through hours, and reopen date for the Entity.
 *
 * Each day is represented by a sub-field of `driveThroughHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
 * Similarly, holiday drive-through hours are represented by the `holidayHours` sub-field.
 * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
 * SPECIAL CASES:
 * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
 * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
 * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
 */
export type DriveThroughHours = {
    friday?: {
        /**
         * Indicates if the drive-through hours are "closed" on Friday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity's drive-through is open on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    /**
     *
     * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
     *
     *
     * Array must be ordered.
     *
     */
    holidayHours?: Array<{
        /**
         * Date on which the holiday hours will be in effect. Cannot be in the past.
         *
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        date: string;
        /**
         * Indicates if the drive-through hours are "closed" on on the given date.
         */
        isClosed?: boolean;
        /**
         * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
         */
        isRegularHours?: boolean;
        /**
         * Contains the time intervals for which the Entity's drive-through is open on the specified date.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    }>;
    monday?: {
        /**
         * Indicates if the drive-through hours are "closed" on Monday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity's drive-through is open on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    /**
     *
     * Date must be on or after 1970-01-01
     * Date must be before or on 2038-01-01
     */
    reopenDate?: string;
    saturday?: {
        /**
         * Indicates if the drive-through hours are "closed" on Saturday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity's drive-through is open on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    sunday?: {
        /**
         * Indicates if the drive-through hours are "closed" on Sunday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity's drive-through is open on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    thursday?: {
        /**
         * Indicates if the drive-through hours are "closed" on Thursday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity's drive-through is open on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    tuesday?: {
        /**
         * Indicates if the drive-through hours are "closed" on Tuesday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity's drive-through is open on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    wednesday?: {
        /**
         * Indicates if the drive-through hours are "closed" on Wednesday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity's drive-through is open on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
};

/**
 * Contains the daily happy hours, holiday happy hours, and reopen date for the Entity.
 *
 * Each day is represented by a sub-field of `happyHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
 * Similarly, holiday happy hours are represented by the `holidayHours` sub-field.
 * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
 * SPECIAL CASES:
 * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
 * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
 * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
 */
export type HappyHours = {
    friday?: {
        /**
         * Indicates if the happy hours are "closed" on Friday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity's happy hours on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    /**
     *
     * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
     *
     *
     * Array must be ordered.
     *
     */
    holidayHours?: Array<{
        /**
         * Date on which the holiday hours will be in effect. Cannot be in the past.
         *
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        date: string;
        /**
         * Indicates if the happy hours are "closed" on on the given date.
         */
        isClosed?: boolean;
        /**
         * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
         */
        isRegularHours?: boolean;
        /**
         * Contains the time intervals for the Entity's happy hours on the specified date.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    }>;
    monday?: {
        /**
         * Indicates if the happy hours are "closed" on Monday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for the Entity's happy hours on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    /**
     *
     * Date must be on or after 1970-01-01
     * Date must be before or on 2038-01-01
     */
    reopenDate?: string;
    saturday?: {
        /**
         * Indicates if the happy hours are "closed" on Saturday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for the Entity's happy hours on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    sunday?: {
        /**
         * Indicates if the happy hours are "closed" on Sunday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for the Entity's happy hours on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    thursday?: {
        /**
         * Indicates if the happy hours are "closed" on Thursday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for the Entity's happy hours on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    tuesday?: {
        /**
         * Indicates if the happy hours are "closed" on Tuesday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for the Entity's happy hours on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    wednesday?: {
        /**
         * Indicates if the happy hours are "closed" on Wednesday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for the Entity's happy hours on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
};

/**
 * Contains the daily kitchen hours, holiday kitchen hours, and reopen date for the Entity.
 *
 * Each day is represented by a sub-field of `kitchenHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
 * Similarly, holiday kitchen hours are represented by the `holidayHours` sub-field.
 * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
 * SPECIAL CASES:
 * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
 * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
 * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
 */
export type KitchenHours = {
    friday?: {
        /**
         * Indicates if the kitchen hours are "closed" on Friday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity's kitchen is open on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    /**
     *
     * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
     *
     *
     * Array must be ordered.
     *
     */
    holidayHours?: Array<{
        /**
         * Date on which the holiday hours will be in effect. Cannot be in the past.
         *
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        date: string;
        /**
         * Indicates if the kitchen hours are "closed" on on the given date.
         */
        isClosed?: boolean;
        /**
         * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
         */
        isRegularHours?: boolean;
        /**
         * Contains the time intervals for which the Entity's kitchen is open on the specified date.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    }>;
    monday?: {
        /**
         * Indicates if the kitchen hours are "closed" on Monday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity's kitchen is open on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    /**
     *
     * Date must be on or after 1970-01-01
     * Date must be before or on 2038-01-01
     */
    reopenDate?: string;
    saturday?: {
        /**
         * Indicates if the kitchen hours are "closed" on Saturday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity's kitchen is open on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    sunday?: {
        /**
         * Indicates if the kitchen hours are "closed" on Sunday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity's kitchen is open on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    thursday?: {
        /**
         * Indicates if the kitchen hours are "closed" on Thursday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity's kitchen is open on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    tuesday?: {
        /**
         * Indicates if the kitchen hours are "closed" on Tuesday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity's kitchen open on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    wednesday?: {
        /**
         * Indicates if the kitchen hours are "closed" on Wednesday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity's kitchen is open on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
};

/**
 * Contains the daily online service hours, holiday online service hours, and reopen date for the Entity.
 *
 * Each day is represented by a sub-field of `onlineServiceHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
 * Similarly, holiday online service hours are represented by the `holidayHours` sub-field.
 * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
 * SPECIAL CASES:
 * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
 * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
 * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
 */
export type OnlineServiceHours = {
    friday?: {
        /**
         * Indicates if the online service hours are "closed" on Friday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for the Entity's online service hours on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    /**
     *
     * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
     *
     *
     * Array must be ordered.
     *
     */
    holidayHours?: Array<{
        /**
         * Date on which the holiday hours will be in effect. Cannot be in the past.
         *
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        date: string;
        /**
         * Indicates if the online service hours are "closed" on on the given date.
         */
        isClosed?: boolean;
        /**
         * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
         */
        isRegularHours?: boolean;
        /**
         * Contains the time intervals for the Entity's online service hours on the specified date.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    }>;
    monday?: {
        /**
         * Indicates if the online service hours are "closed" on Monday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for the Entity's online service hours on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    /**
     *
     * Date must be on or after 1970-01-01
     * Date must be before or on 2038-01-01
     */
    reopenDate?: string;
    saturday?: {
        /**
         * Indicates if the online service hours are "closed" on Saturday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for the Entity's online service hours on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    sunday?: {
        /**
         * Indicates if the online service hours are "closed" on Sunday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for the Entity's online service hours on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    thursday?: {
        /**
         * Indicates if the online service hours are "closed" on Thursday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for the Entity's online service hours on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    tuesday?: {
        /**
         * Indicates if the online service hours are "closed" on Tuesday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for the Entity's online service hours on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    wednesday?: {
        /**
         * Indicates if the online service hours are "closed" on Wednesday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for the Entity's online service hours on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
};

/**
 * Contains the daily pickup hours, holiday pickup hours, and reopen date for the Entity.
 *
 * Each day is represented by a sub-field of `pickupHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
 * Similarly, holiday pickup hours are represented by the `holidayHours` sub-field.
 * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
 * SPECIAL CASES:
 * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
 * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
 * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
 */
export type PickupHours = {
    friday?: {
        /**
         * Indicates if the pickup hours are "closed" on Friday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is open for pickup on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    /**
     *
     * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
     *
     *
     * Array must be ordered.
     *
     */
    holidayHours?: Array<{
        /**
         * Date on which the holiday hours will be in effect. Cannot be in the past.
         *
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        date: string;
        /**
         * Indicates if the pickup hours are "closed" on on the given date.
         */
        isClosed?: boolean;
        /**
         * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
         */
        isRegularHours?: boolean;
        /**
         * Contains the time intervals for which the Entity is open for pickup on the specified date.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    }>;
    monday?: {
        /**
         * Indicates if the pickup hours are "closed" on Monday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is open for pickup on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    /**
     *
     * Date must be on or after 1970-01-01
     * Date must be before or on 2038-01-01
     */
    reopenDate?: string;
    saturday?: {
        /**
         * Indicates if the pickup hours are "closed" on Saturday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is open for pickup on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    sunday?: {
        /**
         * Indicates if the pickup hours are "closed" on Sunday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is open for pickup on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    thursday?: {
        /**
         * Indicates if the pickup hours are "closed" on Thursday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is open for pickup on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    tuesday?: {
        /**
         * Indicates if the pickup hours are "closed" on Tuesday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is open for pickup on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    wednesday?: {
        /**
         * Indicates if the pickup hours are "closed" on Wednesday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is open for pickup on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
};

/**
 * Contains the daily senior hours, holiday senior hours, and reopen date for the Entity.
 *
 * Each day is represented by a sub-field of `seniorHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
 * Similarly, holiday senior hours are represented by the `holidayHours` sub-field.
 * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
 * SPECIAL CASES:
 * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
 * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
 * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
 */
export type SeniorHours = {
    friday?: {
        /**
         * Indicates if the senior hours are "closed" on Friday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for the Entity's senior hours on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    /**
     *
     * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
     *
     *
     * Array must be ordered.
     *
     */
    holidayHours?: Array<{
        /**
         * Date on which the holiday hours will be in effect. Cannot be in the past.
         *
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        date: string;
        /**
         * Indicates if the senior hours are "closed" on on the given date.
         */
        isClosed?: boolean;
        /**
         * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
         */
        isRegularHours?: boolean;
        /**
         * Contains the time intervals for the Entity's senior hours on the specified date.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    }>;
    monday?: {
        /**
         * Indicates if the senior hours are "closed" on Monday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for the Entity's senior hours on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    /**
     *
     * Date must be on or after 1970-01-01
     * Date must be before or on 2038-01-01
     */
    reopenDate?: string;
    saturday?: {
        /**
         * Indicates if the senior hours are "closed" on Saturday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for the Entity's senior hours on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    sunday?: {
        /**
         * Indicates if the senior hours are "closed" on Sunday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for the Entity's senior hours on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    thursday?: {
        /**
         * Indicates if the senior hours are "closed" on Thursday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for the Entity's senior hours on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    tuesday?: {
        /**
         * Indicates if the senior hours are "closed" on Tuesday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for the Entity's senior hours on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    wednesday?: {
        /**
         * Indicates if the senior hours are "closed" on Wednesday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for the Entity's senior hours on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
};

/**
 * Contains the daily takeout hours, holiday takeout hours, and reopen date for the Entity.
 *
 * Each day is represented by a sub-field of `takeoutHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
 * Similarly, holiday takeout hours are represented by the `holidayHours` sub-field.
 * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
 * SPECIAL CASES:
 * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
 * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
 * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
 */
export type TakeoutHours = {
    friday?: {
        /**
         * Indicates if the takeout hours are "closed" on Friday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is open for takeout on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    /**
     *
     * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
     *
     *
     * Array must be ordered.
     *
     */
    holidayHours?: Array<{
        /**
         * Date on which the holiday hours will be in effect. Cannot be in the past.
         *
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        date: string;
        /**
         * Indicates if the takeout hours are "closed" on on the given date.
         */
        isClosed?: boolean;
        /**
         * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
         */
        isRegularHours?: boolean;
        /**
         * Contains the time intervals for which the Entity is open for takeout on the specified date.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    }>;
    monday?: {
        /**
         * Indicates if the takeout hours are "closed" on Monday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is open for takeout on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    /**
     *
     * Date must be on or after 1970-01-01
     * Date must be before or on 2038-01-01
     */
    reopenDate?: string;
    saturday?: {
        /**
         * Indicates if the takeout hours are "closed" on Saturday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is open for takeout on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    sunday?: {
        /**
         * Indicates if the takeout hours are "closed" on Sunday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is open for takeout on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    thursday?: {
        /**
         * Indicates if the takeout hours are "closed" on Thursday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is open for takeout on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    tuesday?: {
        /**
         * Indicates if the takeout hours are "closed" on Tuesday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is open for takeout on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
    wednesday?: {
        /**
         * Indicates if the takeout hours are "closed" on Wednesday.
         */
        isClosed?: boolean;
        /**
         * Contains the time intervals for which the Entity is open for takeout on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
         */
        openIntervals?: Array<{
            /**
             * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            end?: string;
            /**
             * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
             */
            start?: string;
        }>;
    };
};

/**
 * You should be able to support and maintain the image size of the photo passed by Yext. We will not send you photos greater than 5MB.
 */
export type Image = {
    /**
     * URL where the image can be viewed. We encourage you to use this URL directly on your sites.
     */
    url?: string;
    type?: 'LOGO' | 'STOREFRONT' | 'GALLERY' | 'HEADSHOT';
    /**
     * The width of the image, in pixels.
     */
    width?: number;
    /**
     * The height of the image, in pixels.
     */
    height?: number;
    /**
     * A caption for the image.
     */
    description?: string;
};

/**
 * YouTube videos specified by the business
 */
export type Video = {
    /**
     * Youtube URL for the video
     */
    url?: string;
    /**
     * A caption for the video.
     */
    description?: string;
};

/**
 * The featured message of the location
 */
export type FeaturedMessage = {
    /**
     * The featured message text to be displayed.
     */
    message?: string;
    /**
     * The featured message should be clickable when the featured message URL is provided
     */
    url?: string;
};

export type Url = {
    /**
     * URL where the image can be viewed. We encourage you to use this URL directly on your sites.
     */
    url?: string;
    /**
     * A key describing what the URL references.
     */
    type?: 'WEBSITE' | 'RESERVATION' | 'MENU' | 'ORDER';
    /**
     * If provided, the URL shown in the link text for url. Since consumers are still taken to the URL specified in url, businesses are still able to track click-throughs.
     */
    displayUrl?: string;
};

/**
 * Listing attribution to whomever the business bought Listings from (i.e., Yext or one of Yext's partners)
 */
export type Attribution = {
    /**
     * Details for the attribution image.
     */
    image?: {
        /**
         * URL where the image can be viewed.
         */
        url?: string;
        /**
         * A brief description of the attribution image.
         */
        description?: string;
        /**
         * The width of the image, in pixels.
         */
        width?: number;
        /**
         * The height of the image, in pixels.
         */
        height?: number;
    };
};

export type Eclid = {
    /**
     * The list’s ID (numeric, Yext-assigned)
     */
    id?: number;
    type?: 'MENU' | 'PRODUCTS' | 'EVENTS' | 'BIOS';
    /**
     * The text of the link for the List’s type within the listing. When consumers click the link, they see a set of tabs, one for each List of that type.
     */
    description?: string;
    /**
     * Name of the List, which is shown on the List’s tab.
     */
    name?: string;
};

export type FrequentlyAskedQuestion = {
    /**
     * The question of the FAQ
     */
    question?: string;
    /**
     * The answer to the question
     */
    answer?: string;
};

export type ListingDelivery = {
    /**
     * Yext’s ID for the location
     */
    yextId?: string;
    /**
     * The ID of this location, as assigned by you. This value will be present in all instances except when Yext is issuing an ORDER request (e.g., it does not yet exist in your system)
     */
    partnerId?: string;
    /**
     * The name to be displayed on the listing
     */
    name?: string;
    address?: Address;
    /**
     * Provides additional information on where the entity can be found (e.g. Times Square, Global Center Mall)
     */
    geomodifier?: string;
    /**
     * A list of the location’s phone numbers. There will be exactly one MAIN phone number and optionally some non-MAIN phone numbers (at most one of each type)
     */
    phones?: Array<Phones>;
    /**
     * List of business categories, in your taxonomy, ordered by importance / relevance
     */
    categories?: Array<PartnerCategories>;
    /**
     * A description of the location. The description might be quite long (i.e., thousands of characters). It is the publisher’s responsibility to HTML-escape it. As part of display, the publisher should convert newline characters to break tags in order to maintain the structure of paragraphs and lists
     */
    description?: string;
    /**
     * List of emails associated with the listing
     */
    emails?: Array<Emails>;
    geodata?: GeoData;
    /**
     * Contains structured business-hours information. Any days that are unspecified should be interpreted as 'we don't have information for this day'. Days that are specified, but contain no times, should be interpreted as 'Closed'. This is not a required field in Yext but should be displayed if provided.
     */
    hours?: Array<Hours>;
    accessHours?: AccessHours;
    brunchHours?: BrunchHours;
    deliveryHours?: DeliveryHours;
    driveThroughHours?: DriveThroughHours;
    happyHours?: HappyHours;
    kitchenHours?: KitchenHours;
    onlineServiceHours?: OnlineServiceHours;
    pickupHours?: PickupHours;
    seniorHours?: SeniorHours;
    takeoutHours?: TakeoutHours;
    /**
     * Photos of the location (e.g., storefront, logo). You should be able to support and maintain the image size of the photo passed by Yext. We will not send you photos greater than 5MB.
     */
    images?: Array<Image>;
    /**
     * YouTube videos specified by the business
     */
    videos?: Array<Video>;
    featuredMessage?: FeaturedMessage;
    /**
     * A list of payment methods accepted at the location
     */
    paymentOptions?: Array<string>;
    /**
     * A list of URLs (e.g., the location's website and reservation page). There will be at most one URL of each type.
     */
    urls?: Array<Url>;
    /**
     * The location’s Twitter username, without the leading '@'
     */
    twitterHandle?: string;
    /**
     * The location’s Facebook Page URL (e.g., http://www.facebook.com/YextInc)
     */
    facebookPageUrl?: string;
    /**
     * Valid Instagram username for the entity without the leading '@'
     */
    instagramHandle?: string;
    attribution?: Attribution;
    /**
     * Keywords by which the business wants to be searchable (e.g., Sears wants to be searchable by Craftsman, their brand of tools)
     */
    keywords?: Array<string>;
    /**
     * The Enhanced Content Lists (ECLs) for this listing. This field provides the information about the Lists that are associated with this location (if any), but it does not include the item data, since it may be quite large and change frequently. You can call Get List to retrieve that data / HTML.
     */
    lists?: Array<Eclid>;
    /**
     * The value is true if the business has indicated that this location is closed. They may still have an active PowerListing to direct customers to the nearest store in their chain. Defaults to false.
     */
    closed?: boolean;
    /**
     * If present, indicates the last day that the store was, or will be, open.
     * * Since the date may be in the future, the mere presence of closeDate does not imply that the location is already closed.
     * * (YYYY-MM-DD format)
     *
     */
    closeDate?: string;
    /**
     * If present, indicates the business is temporarily closed and the day that the store will open
     * * (YYYY-MM-DD format)
     *
     */
    reopenDate?: string;
    pickupAndDeliveryOptions?: Array<'INSTORE_PICKUP' | 'CURBSIDE_PICKUP' | 'DELIVERY' | 'SAME_DAY_DELIVERY' | 'NO_CONTACT_DELIVERY'>;
    /**
     * A list of the location's specialties (e.g., Pizza)
     */
    specialties?: Array<string>;
    /**
     * A list of brands that the location sells (e.g., Toshiba, Samsung, Sony)
     */
    brands?: Array<string>;
    /**
     * A list of products or product groups provided at this location (e.g., Optical, Hardware)
     */
    products?: Array<string>;
    /**
     * A list of services (e.g., Gift Registry, Store Pickup, Eye Exams, Spinal Decompression)
     */
    services?: Array<string>;
    /**
     * The year the location opened
     */
    yearEstablished?: string;
    /**
     * Any association memberships relevant to the location (e.g., New York Doctors Association)
     */
    associations?: Array<string>;
    /**
     * Languages spoken at this location
     */
    languages?: Array<string>;
    /**
     * A list of Frequently Asked Questions about the business.
     */
    frequentlyAskedQuestions?: Array<FrequentlyAskedQuestion>;
    /**
     * Used to indicate whether a business is black-owned or not
     */
    blackOwnedBusiness?: boolean;
    priceRange?: '$' | '$$' | '$$$' | '$$$$';
    /**
     * Used to indicate the neighborhood of a business
     */
    neighborhood?: string;
};

export type ResponseMeta = {
    /**
     * Unique ID for this request / response.
     */
    uuid?: string;
};

export type ResponseError = {
    /**
     * Code that uniquely identifies the error or warning.
     *
     */
    code?: number;
    type?: 'FATAL_ERROR' | 'NON_FATAL_ERROR' | 'WARNING';
    /**
     * Message explaining the problem.
     */
    message?: string;
};

export type ResponseMetaWithError = ResponseMeta & {
    /**
     * List of errors and warnings.
     */
    errors?: Array<ResponseError>;
};

export type SuppressionRequest = {
    /**
     * The partner listing ID that should be suppressed (or unsuppressed)
     */
    listingId?: string;
    /**
     * * true, if listing should be suppressed
     * * false, if listing should be unsupressed
     *
     */
    supress?: boolean;
    /**
     * A partner listing ID.
     * * When suppressing a listing, this value may be provided. If it is provided, the partner is expected to redirect the profile page specified in listingId to the one specified in canonicalListingId.
     *
     */
    canonicalListingId?: string;
};

/**
 * List of comments on the review
 */
export type PartnerReviewComments = Array<{
    /**
     * Id of the review comment
     */
    commentId: string;
    /**
     * Date and time that the review comment was posted in ISO-9601 format
     */
    timestamp: string;
    /**
     * The name of the user who posted the review comment
     */
    authorName: string;
    /**
     * The full text of the review comment
     */
    content: string;
    /**
     * Indicates whether the comment was made by the business owner
     */
    ownerResponse?: boolean;
}>;

export type PartnerReview = {
    /**
     * A unique ID for each review, as assinged by you
     */
    reviewId: string;
    /**
     * The status of the review
     */
    status: 'ACTIVE' | 'SUPPRESSED';
    /**
     * Date and time that the review was posted in ISO-9601 format
     */
    timestamp: string;
    /**
     * The name of the user who posted the review
     */
    authorName: string;
    /**
     * The title of the review
     */
    title?: string;
    /**
     * The full text of the review
     */
    content?: string;
    /**
     * The URL where consumers can read the review
     */
    url: string;
    /**
     * The rating given by the review
     */
    rating?: string;
    /**
     * The maximum possible rating of a review on your site
     */
    maxRating?: string;
    /**
     * If present, the business owner has flagged the review a specific reason
     */
    flagReason?: 'INAPPROPRIATE_CONTENT' | 'SPAM' | 'NOT_LOCATION_RELATED';
    comments?: PartnerReviewComments;
    /**
     * Indicates whether the review was written in response to a request from a review_generation program
     */
    generated?: boolean;
};

export type PartnerReviewList = {
    /**
     * Total number of reviews on the listing
     */
    total: number;
    /**
     * The average rating across all reviews for this listing
     */
    rating: number;
    /**
     * The maximum possible rating (e.g., 5)
     */
    maxRating: number;
    /**
     * A list of num Review objects, respecting the filter specified in before and sorted in reverse chronological order
     */
    reviews?: Array<PartnerReview>;
};

export type PartnerReviewResponse = {
    /**
     * The review's ID on your site
     *
     */
    reviewId?: string;
    /**
     * If present, the business owner is flagging the review for the specified reason.
     *
     */
    flagReason?: 'INAPPROPRIATE_CONTENT' | 'SPAM' | 'NOT_LOCATION_RELATED';
    /**
     * If present, the business owner's response to the review
     *
     */
    comment?: string;
};

export type PartnerReviewResponseUpdate = {
    /**
     * The review's ID on your site
     *
     */
    reviewId?: string;
    /**
     * If present, the business owner is flaggint the review for the specified reason.
     *
     */
    flagReason?: 'INAPPROPRIATE_CONTENT' | 'SPAM' | 'NOT_LOCATION_RELATED';
    /**
     * The review comment's Id on your site
     *
     */
    commentId?: string;
    /**
     * If present, the business owner's response to the review
     *
     */
    comment?: string;
};

export type PartnerSuggestion = {
    /**
     * A unique ID for each suggestion, as assigned by you
     */
    id: string;
    /**
     * The name of the listing field associated with the suggestion (e.g. main_phone, website_url)
     */
    field: string;
    /**
     * * ACCEPTED (the suggestion has been applied to the listing)
     * * PENDING (the suggestion is valid but has not been actioned on)
     * * REJECTED (the suggestion has been rejected)
     * * EXPIRED (the suggestion is no longer valid)'
     *
     */
    status: 'ACCEPTED' | 'PENDING' | 'REJECTED' | 'EXPIRED';
    /**
     * Date and time that the suggestion was created in ISO-9601 format
     */
    timestamp: string;
    /**
     * The existing field data
     */
    currentValue?: string;
    /**
     * The content of the suggestion. The type of this value should match the type structure of the field
     */
    proposedValue: string;
};

export type PartnerSuggestionList = {
    /**
     * Total number of suggestions on the listing
     */
    total: number;
    /**
     * A list of num Suggestion objects. Suggestions returned should only have status PENDING
     */
    suggestions: Array<PartnerSuggestion>;
};

/**
 * The request body of a suggestion accept/reject
 */
export type SuggestionAcceptBody = {
    /**
     * The unique suggestion Id
     */
    suggestionId: string;
    accept: 'ACTIVE' | 'SUPPRESSED';
};

/**
 * Business name.
 *
 * e.g. `Yext`
 *
 */
export type Name = string;

/**
 * Street address, including house numbers, to search.
 *
 * e.g `75 9th Ave`, `Bismarckstraße 35`
 *
 */
export type Address2 = string;

/**
 * Address line 2 to search.
 *
 * e.g. `7th floor`
 *
 */
export type Address22 = string;

/**
 * Sublocality (e.g., neighborhood, ward or district) in which to search.
 *
 * e.g. `pos. Lesnoe` (Russia), `Saiwai-ku` (Japan)
 *
 */
export type Sublocality = string;

/**
 * City in which to search
 *
 * e.g. `New York`
 *
 */
export type Locality = string;

/**
 * State, region, or province in which to search. Abbreviations are used for some countries.
 *
 * e.g. `NY`, `Kanagawa`
 *
 */
export type Region = string;

/**
 * ZIP or postal code in which to search.
 *
 * e.g. `10011`, `212-0011`, `4841 EP`
 *
 */
export type PostalCode = string;

/**
 * The ISO 3166-1 alpha-2 code of the country in which to search.
 *
 * e.g. `GB`, `NL`, `GB`
 *
 */
export type Country = string;

/**
 * Location's phone number.
 * For international numbers, this number reflects how the number is dialed within the country (i.e., no country code or punctuation).
 *
 * e.g. `2126518966`
 *
 */
export type Phone = string;

/**
 * latitude and longitude coordinates, separated by a comma.
 *
 * e.g. `-27.1259105,-109.4789002`
 *
 */
export type Latlng = string;

/**
 * The first name of the healthcare professional whose records should be returned.
 *
 * NOTE: You only need to support this parameter if your site supports first/last name search.
 *
 */
export type FirstName = string;

/**
 * The last name of the healthcare professional whose records should be returned.
 *
 * NOTE: You only need to support this parameter if your site supports first/last name search.
 *
 */
export type LastName = string;

/**
 * National Provider Identifier (NPI) of the healthcare professional or facility whose records should be returned.
 *
 * NOTE: This parameter will only be used if you have NPI data.
 *
 */
export type Npi = string;

/**
 * The kind of records that should be returned.
 *
 * NOTE: This parameter will only be used if you support more than just the `Location` type.
 *
 */
export type Type = 'Location' | 'HealthcareFacility' | 'HealthcareProfessional';

/**
 * The unique identifier of a listing on your site is known as the listingId.
 *
 */
export type ListingId = string;

/**
 * The number of reviews to return
 *
 */
export type Num = string;

/**
 * An ISO-8601 date-time that filters results to those reviews posted before this time (exclusive). If not specified, no filter should be applied.
 * NOTE: This parameter is used to page through results. For example, Yext may fetch the newest 10 reviews and make a subsequent request with before set to the last review's timestamp in order to fetch the second page.
 *
 */
export type Before = string;

/**
 * ID of this Review.
 */
export type ReviewId = number;

/**
 * The review comment’s ID on your site
 *
 */
export type CommentId = string;

/**
 * The unique identifier of a suggestion on your site is known as the suggestionId.
 *
 */
export type SuggestionId = string;

export type SearchData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * Business name.
         *
         * e.g. `Yext`
         *
         */
        name?: string;
        /**
         * Street address, including house numbers, to search.
         *
         * e.g `75 9th Ave`, `Bismarckstraße 35`
         *
         */
        address?: string;
        /**
         * Address line 2 to search.
         *
         * e.g. `7th floor`
         *
         */
        address2?: string;
        /**
         * Sublocality (e.g., neighborhood, ward or district) in which to search.
         *
         * e.g. `pos. Lesnoe` (Russia), `Saiwai-ku` (Japan)
         *
         */
        sublocality?: string;
        /**
         * City in which to search
         *
         * e.g. `New York`
         *
         */
        city?: string;
        /**
         * State, region, or province in which to search. Abbreviations are used for some countries.
         *
         * e.g. `NY`, `Kanagawa`
         *
         */
        state?: string;
        /**
         * ZIP or postal code in which to search.
         *
         * e.g. `10011`, `212-0011`, `4841 EP`
         *
         */
        zip?: string;
        /**
         * The ISO 3166-1 alpha-2 code of the country in which to search.
         *
         * e.g. `GB`, `NL`, `GB`
         *
         */
        countryCode?: string;
        /**
         * Location's phone number.
         * For international numbers, this number reflects how the number is dialed within the country (i.e., no country code or punctuation).
         *
         * e.g. `2126518966`
         *
         */
        phone?: string;
        /**
         * latitude and longitude coordinates, separated by a comma.
         *
         * e.g. `-27.1259105,-109.4789002`
         *
         */
        latlng?: string;
        /**
         * The first name of the healthcare professional whose records should be returned.
         *
         * NOTE: You only need to support this parameter if your site supports first/last name search.
         *
         */
        firstName?: string;
        /**
         * The last name of the healthcare professional whose records should be returned.
         *
         * NOTE: You only need to support this parameter if your site supports first/last name search.
         *
         */
        lastName?: string;
        /**
         * National Provider Identifier (NPI) of the healthcare professional or facility whose records should be returned.
         *
         * NOTE: This parameter will only be used if you have NPI data.
         *
         */
        npi?: string;
        /**
         * The kind of records that should be returned.
         *
         * NOTE: This parameter will only be used if you support more than just the `Location` type.
         *
         */
        type?: 'Location' | 'HealthcareFacility' | 'HealthcareProfessional';
    };
    url: '/listings/search';
};

export type SearchErrors = {
    /**
     * ErrorResponseNew
     *
     * Bad Request
     */
    400: {
        error?: ResponseErrorNew;
    };
    /**
     * ServerError
     *
     * Server Error
     */
    500: {
        error?: ResponseErrorNew;
    };
};

export type SearchError = SearchErrors[keyof SearchErrors];

export type SearchResponses = {
    /**
     * SearchSuccess
     *
     * A list of search results; empty if no matching listings could be found.
     */
    200: Array<SearchResult>;
};

export type SearchResponse = SearchResponses[keyof SearchResponses];

export type DetailsData = {
    body?: never;
    path?: never;
    query: {
        /**
         * The unique identifier of a listing on your site.
         *
         */
        id: string;
    };
    url: '/listings/details';
};

export type DetailsErrors = {
    /**
     * ErrorResponseNew
     *
     * Bad Request
     */
    400: {
        error?: ResponseErrorNew;
    };
    /**
     * 404NotFoundResponse
     *
     * Not Found
     */
    404: {
        error?: {
            /**
             * Not Found
             */
            message?: string;
        };
    };
    /**
     * ServerError
     *
     * Server Error
     */
    500: {
        error?: ResponseErrorNew;
    };
};

export type DetailsError = DetailsErrors[keyof DetailsErrors];

export type DetailsResponses = {
    /**
     * DetailsResponse
     *
     * OK
     */
    200: Details | HealthcareProfessional | HealthcareFacility;
};

export type DetailsResponse = DetailsResponses[keyof DetailsResponses];

export type OrderData = {
    body: ListingDelivery;
    path?: never;
    query?: never;
    url: '/listings/order';
};

export type OrderErrors = {
    /**
     * BlockedResponse
     *
     * Blocked
     */
    409: {
        /**
         * The current status of the Listing.
         */
        status?: 'REJECTED' | 'BLOCKED';
        /**
         * List of issues that are causing the status to be REJECTED or BLOCKED
         */
        issues?: Array<{
            /**
             * Describes the issue
             */
            description?: string;
            /**
             * (Optional) Specifies which field (as named in the JSON Listing Format body) is causing the issue. If the field is part of an array, its array index should be included. This value should be omitted if the issue does not apply to a specific field.
             * * If we send more than one location in a request, this value should identify which location the field is part of by the location's yextId (e.g., if the postalCode field of the location with yextId 3524 is causing the issue: location[3524].address.postalCode)
             *
             */
            field?: string;
            /**
             * (Optional) contains a value for the field that you would accept
             */
            suggestedValue?: string;
        }>;
    };
    /**
     * ServerError
     *
     * Server Error
     */
    500: {
        error?: ResponseErrorNew;
    };
};

export type OrderError = OrderErrors[keyof OrderErrors];

export type OrderResponses = {
    /**
     * LiveResponse
     *
     * OK
     */
    200: {
        /**
         * In the case of a 200 response, should always be 'LIVE'
         */
        status?: string;
        /**
         * Your ID for the Listing. If the listing already exists on your site, id must match that listing's ID in your system.
         */
        id?: string;
        /**
         * The URL where the listing can be seen on your site
         */
        url?: string;
    };
};

export type OrderResponse = OrderResponses[keyof OrderResponses];

export type CancelData = {
    body?: never;
    path: {
        /**
         * The unique identifier of a listing on your site is known as the listingId.
         *
         */
        listingId: string;
    };
    query?: never;
    url: '/listings/{listingId}';
};

export type CancelErrors = {
    /**
     * ErrorResponse
     *
     * Error Response
     */
    400: {
        meta?: ResponseMetaWithError;
        response?: {
            [key: string]: unknown;
        };
    };
};

export type CancelError = CancelErrors[keyof CancelErrors];

export type CancelResponses = {
    /**
     * OK
     */
    200: unknown;
};

export type UpdateData = {
    body: ListingDelivery;
    path: {
        /**
         * The unique identifier of a listing on your site is known as the listingId.
         *
         */
        listingId: string;
    };
    query?: never;
    url: '/listings/{listingId}';
};

export type UpdateErrors = {
    /**
     * 404NotFoundResponse
     *
     * Not Found
     */
    404: {
        error?: {
            /**
             * Not Found
             */
            message?: string;
        };
    };
    /**
     * BlockedResponse
     *
     * Blocked
     */
    409: {
        /**
         * The current status of the Listing.
         */
        status?: 'REJECTED' | 'BLOCKED';
        /**
         * List of issues that are causing the status to be REJECTED or BLOCKED
         */
        issues?: Array<{
            /**
             * Describes the issue
             */
            description?: string;
            /**
             * (Optional) Specifies which field (as named in the JSON Listing Format body) is causing the issue. If the field is part of an array, its array index should be included. This value should be omitted if the issue does not apply to a specific field.
             * * If we send more than one location in a request, this value should identify which location the field is part of by the location's yextId (e.g., if the postalCode field of the location with yextId 3524 is causing the issue: location[3524].address.postalCode)
             *
             */
            field?: string;
            /**
             * (Optional) contains a value for the field that you would accept
             */
            suggestedValue?: string;
        }>;
    };
    /**
     * ServerError
     *
     * Server Error
     */
    500: {
        error?: ResponseErrorNew;
    };
};

export type UpdateError = UpdateErrors[keyof UpdateErrors];

export type UpdateResponses = {
    /**
     * LiveResponse
     *
     * OK
     */
    200: {
        /**
         * In the case of a 200 response, should always be 'LIVE'
         */
        status?: string;
        /**
         * Your ID for the Listing. If the listing already exists on your site, id must match that listing's ID in your system.
         */
        id?: string;
        /**
         * The URL where the listing can be seen on your site
         */
        url?: string;
    };
};

export type UpdateResponse = UpdateResponses[keyof UpdateResponses];

export type SuppressData = {
    body?: SuppressionRequest;
    path?: never;
    query?: never;
    url: '/listings/suppress';
};

export type SuppressErrors = {
    /**
     * 404NotFoundResponse
     *
     * Not Found
     */
    404: {
        error?: {
            /**
             * Not Found
             */
            message?: string;
        };
    };
    /**
     * BlockedSuppression
     *
     * Blocked
     */
    409: {
        /**
         * Issue that is causing the suppression to be blocked
         */
        issue?: string;
    };
    /**
     * ServerError
     *
     * Server Error
     */
    500: {
        error?: ResponseErrorNew;
    };
};

export type SuppressError = SuppressErrors[keyof SuppressErrors];

export type SuppressResponses = {
    /**
     * OK
     */
    200: unknown;
};

export type GetReviewListData = {
    body?: never;
    path?: never;
    query: {
        /**
         * The unique identifier of a listing on your site is known as the listingId.
         *
         */
        listingId: string;
        /**
         * The number of reviews to return
         *
         */
        num: string;
        /**
         * An ISO-8601 date-time that filters results to those reviews posted before this time (exclusive). If not specified, no filter should be applied.
         * NOTE: This parameter is used to page through results. For example, Yext may fetch the newest 10 reviews and make a subsequent request with before set to the last review's timestamp in order to fetch the second page.
         *
         */
        before?: string;
    };
    url: '/listings/reviews';
};

export type GetReviewListErrors = {
    /**
     * ErrorResponseNew
     *
     * Bad Request
     */
    400: {
        error?: ResponseErrorNew;
    };
    /**
     * 404NotFoundResponse
     *
     * Not Found
     */
    404: {
        error?: {
            /**
             * Not Found
             */
            message?: string;
        };
    };
    /**
     * ServerError
     *
     * Server Error
     */
    500: {
        error?: ResponseErrorNew;
    };
};

export type GetReviewListError = GetReviewListErrors[keyof GetReviewListErrors];

export type GetReviewListResponses = {
    /**
     * OK
     */
    200: PartnerReviewList;
};

export type GetReviewListResponse = GetReviewListResponses[keyof GetReviewListResponses];

export type GetReviewData = {
    body?: never;
    path: {
        /**
         * ID of this Review.
         */
        reviewId: number;
    };
    query?: never;
    url: '/listings/reviews/{reviewId}';
};

export type GetReviewErrors = {
    /**
     * ErrorResponseNew
     *
     * Bad Request
     */
    400: {
        error?: ResponseErrorNew;
    };
    /**
     * 404NotFoundResponse
     *
     * Not Found
     */
    404: {
        error?: {
            /**
             * Not Found
             */
            message?: string;
        };
    };
    /**
     * ServerError
     *
     * Server Error
     */
    500: {
        error?: ResponseErrorNew;
    };
};

export type GetReviewError = GetReviewErrors[keyof GetReviewErrors];

export type GetReviewResponses = {
    /**
     * OK
     */
    200: PartnerReview;
};

export type GetReviewResponse = GetReviewResponses[keyof GetReviewResponses];

export type ReviewRespondData = {
    body?: PartnerReviewResponse;
    path: {
        /**
         * ID of this Review.
         */
        reviewId: number;
    };
    query?: never;
    url: '/listings/reviews/{reviewId}';
};

export type ReviewRespondErrors = {
    /**
     * ErrorResponseNew
     *
     * Bad Request
     */
    400: {
        error?: ResponseErrorNew;
    };
    /**
     * 404NotFoundResponse
     *
     * Not Found
     */
    404: {
        error?: {
            /**
             * Not Found
             */
            message?: string;
        };
    };
    /**
     * ServerError
     *
     * Server Error
     */
    500: {
        error?: ResponseErrorNew;
    };
};

export type ReviewRespondError = ReviewRespondErrors[keyof ReviewRespondErrors];

export type ReviewRespondResponses = {
    /**
     * NewCommentSuccess
     *
     * OK
     */
    200: {
        /**
         * The ID of the comment that was successfully created
         */
        commentId?: string;
    };
};

export type ReviewRespondResponse = ReviewRespondResponses[keyof ReviewRespondResponses];

export type DeleteReviewCommentData = {
    body?: never;
    path: {
        /**
         * ID of this Review.
         */
        reviewId: number;
        /**
         * The review comment’s ID on your site
         *
         */
        commentId: string;
    };
    query?: never;
    url: '/listings/reviews/{reviewId}/comment/{commentId}';
};

export type DeleteReviewCommentErrors = {
    /**
     * ErrorResponseNew
     *
     * Bad Request
     */
    400: {
        error?: ResponseErrorNew;
    };
    /**
     * 404NotFoundResponse
     *
     * Not Found
     */
    404: {
        error?: {
            /**
             * Not Found
             */
            message?: string;
        };
    };
    /**
     * ServerError
     *
     * Server Error
     */
    500: {
        error?: ResponseErrorNew;
    };
};

export type DeleteReviewCommentError = DeleteReviewCommentErrors[keyof DeleteReviewCommentErrors];

export type DeleteReviewCommentResponses = {
    /**
     * OK
     */
    200: unknown;
};

export type UpdateReviewCommentData = {
    body?: PartnerReviewResponseUpdate;
    path: {
        /**
         * ID of this Review.
         */
        reviewId: number;
        /**
         * The review comment’s ID on your site
         *
         */
        commentId: string;
    };
    query?: never;
    url: '/listings/reviews/{reviewId}/comment/{commentId}';
};

export type UpdateReviewCommentErrors = {
    /**
     * ErrorResponseNew
     *
     * Bad Request
     */
    400: {
        error?: ResponseErrorNew;
    };
    /**
     * 404NotFoundResponse
     *
     * Not Found
     */
    404: {
        error?: {
            /**
             * Not Found
             */
            message?: string;
        };
    };
    /**
     * ServerError
     *
     * Server Error
     */
    500: {
        error?: ResponseErrorNew;
    };
};

export type UpdateReviewCommentError = UpdateReviewCommentErrors[keyof UpdateReviewCommentErrors];

export type UpdateReviewCommentResponses = {
    /**
     * OK
     */
    200: unknown;
};

export type GetSuggestionsListData = {
    body?: never;
    path?: never;
    query: {
        /**
         * The unique identifier of a listing on your site is known as the listingId.
         *
         */
        listingId: string;
    };
    url: '/listings/suggestions';
};

export type GetSuggestionsListErrors = {
    /**
     * ErrorResponseNew
     *
     * Bad Request
     */
    400: {
        error?: ResponseErrorNew;
    };
    /**
     * 404NotFoundResponse
     *
     * Not Found
     */
    404: {
        error?: {
            /**
             * Not Found
             */
            message?: string;
        };
    };
    /**
     * ServerError
     *
     * Server Error
     */
    500: {
        error?: ResponseErrorNew;
    };
};

export type GetSuggestionsListError = GetSuggestionsListErrors[keyof GetSuggestionsListErrors];

export type GetSuggestionsListResponses = {
    /**
     * OK
     */
    200: PartnerSuggestionList;
};

export type GetSuggestionsListResponse = GetSuggestionsListResponses[keyof GetSuggestionsListResponses];

export type GetSuggestionData = {
    body?: never;
    path: {
        /**
         * The unique identifier of a suggestion on your site is known as the suggestionId.
         *
         */
        suggestionId: string;
    };
    query?: never;
    url: '/listings/suggestions/{suggestionId}';
};

export type GetSuggestionErrors = {
    /**
     * ErrorResponseNew
     *
     * Bad Request
     */
    400: {
        error?: ResponseErrorNew;
    };
    /**
     * 404NotFoundResponse
     *
     * Not Found
     */
    404: {
        error?: {
            /**
             * Not Found
             */
            message?: string;
        };
    };
    /**
     * ServerError
     *
     * Server Error
     */
    500: {
        error?: ResponseErrorNew;
    };
};

export type GetSuggestionError = GetSuggestionErrors[keyof GetSuggestionErrors];

export type GetSuggestionResponses = {
    /**
     * OK
     */
    200: PartnerSuggestion;
};

export type GetSuggestionResponse = GetSuggestionResponses[keyof GetSuggestionResponses];

export type AcceptSuggestionData = {
    body?: SuggestionAcceptBody;
    path: {
        /**
         * The unique identifier of a suggestion on your site is known as the suggestionId.
         *
         */
        suggestionId: string;
    };
    query?: never;
    url: '/listings/suggestions/{suggestionId}';
};

export type AcceptSuggestionErrors = {
    /**
     * ErrorResponseNew
     *
     * Bad Request
     */
    400: {
        error?: ResponseErrorNew;
    };
    /**
     * 404NotFoundResponse
     *
     * Not Found
     */
    404: {
        error?: {
            /**
             * Not Found
             */
            message?: string;
        };
    };
    /**
     * ServerError
     *
     * Server Error
     */
    500: {
        error?: ResponseErrorNew;
    };
};

export type AcceptSuggestionError = AcceptSuggestionErrors[keyof AcceptSuggestionErrors];

export type AcceptSuggestionResponses = {
    /**
     * OK
     */
    200: unknown;
};
