// This file is auto-generated by @hey-api/openapi-ts

export type ClientOptions = {
    baseUrl: 'https://api.yextapis.com/v2' | (string & {});
};

export type ResponseMeta = {
    /**
     * Unique ID for this request / response.
     */
    uuid?: string;
};

export type ResponseError = {
    name?: string;
    /**
     * Code that uniquely identifies the error or warning.
     *
     */
    code?: number;
    type?: 'FATAL_ERROR' | 'NON_FATAL_ERROR' | 'WARNING';
    /**
     * Message explaining the problem.
     */
    message?: string;
};

export type ResponseMetaWithError = ResponseMeta & {
    /**
     * List of errors and warnings.
     */
    errors?: Array<ResponseError>;
};

/**
 * Available Service
 */
export type AvailableService = {
    sku?: string;
    /**
     * The ID for the agreement that this service is offered under.
     */
    agreementId?: number;
    /**
     * Information about this service.
     */
    serviceDescription?: string;
    /**
     * Recipient type of the service. Only shown with `v` parameters `20210318` and later.
     */
    recipientType?: 'Location' | 'Sub-Account';
    /**
     * Optional. The minimum amount of time a service has to be active before it can be canceled. In the units specified by *minimumUsageUnit*.
     */
    minimumUsage?: number;
    /**
     * Optional. The unit for *minimumUsage*.
     */
    minimumUsageUnit?: 'DAYS' | 'MONTHS';
    /**
     * Optional. List of SKUs that this is an add-on to.
     *
     * When adding this service for a location by creating an *addRequest*, you must either supply one of these SKUs along with this one, or the location must already have one of these services.
     *
     */
    addOnTo?: Array<string>;
};

export type NewLocationAddRequest = {
    /**
     * Your ID for the location to be added.
     */
    newLocationId: string;
    /**
     * *(Partner Portal mode)* Supply your ID for the account that the new location should be created in. If this account is your main account or an existing sub-account, the location will be placed there. Otherwise, a new account will be created.
     *
     */
    newLocationAccountId?: string;
    /**
     * *(Partner Portal mode)* If a new account is created, this field's value will set the account's name. If a new account is created, and a value for this field is not provided, the new account's name will default to the location's name.
     *
     */
    newLocationAccountName?: string;
    /**
     * *(Partner Portal mode, advanced use only)* If you have a multi-layer account structure and want the new account created for this request to be under one of your sub-accounts, rather than directly under your main account, specify that sub-account's ID in this field.
     *
     */
    newAccountParentAccountId?: string;
    /**
     * If you set `newLocationId`, you must specify either `newLocationData` or `newEntityData`.
     *
     * This field allows you to set the profile data for a location, using our legacy format.
     * Profile content is specified in the same format as when creating a location with the Locations: Create endpoint.
     *
     * If the Add Request is processed successfully, this data will be used to create a new location in your account.
     *
     */
    newLocationData: Location;
    /**
     * If you set `newEntityData`, this field specifies the type of entity to be created.
     *
     */
    newEntityType?: 'atm' | 'healthcareFacility' | 'healthcareProfessional' | 'hotel' | 'location' | 'restaurant';
    /**
     * If you set `newLocationId`, you must specify either `newLocationData` or `newEntityData`.
     *
     * This field allows you to set the profile data for an entity.
     * Profile content is specified in the same format as when creating an entity with the Entities: Create endpoint.
     * You can only create entities of a type specified by `newEntityType`.
     *
     * If the Add Request is processed successfully, this data will be used to create a new entity in your account.
     *
     * **The dropdown will specify the profile content for `newEntityData`. Specify the entity type with `newEntityType` described above.**
     *
     */
    newEntityData: unknown;
    /**
     * List of SKUs that you would like to sign the location up for, from among those listed in the **Available Services: List** response with Location recipientType.
     *
     */
    skus: Array<string>;
    /**
     * *(Advanced field)* The Agreement ID of the agreement that services will be added under. This value is set automatically by Yext when you create the Add request. (You can specify it yourself, but should not do so unless you have intentionally set up multiple active agreements with Yext, since it could cause your integration to break when you renew or upgrade your agreement.)
     *
     */
    agreementId?: number;
    /**
     * *(Sandbox API only)* Forces this Add request into the `REVIEW` status if it otherwise would have completed successfully.
     *
     */
    forceReview?: boolean;
};

export type AtmWrite = Omit<EntityWrite, 'EntityType'> & {
    /**
     * Contains the metadata about the entity.
     */
    meta?: {
        /**
         * Country code of this Entity's Language Profile (defaults to the country of the account)
         */
        countryCode?: string;
        /**
         * The ID of the folder containing this Entity
         */
        folderId?: string;
        /**
         * ID of this Entity
         */
        id?: string;
        /**
         * This Entity's labels. If the **`v`** parameter is before `20211215`, this will be an integer.
         */
        labels?: Array<string>;
        /**
         * Language code of this Entity's Language Profile (defaults to the language code of the account)
         */
        language?: string;
    };
    /**
     *
     *
     *
     * Cannot Include:
     * * HTML markup
     */
    name?: string;
    /**
     * Contains the address of the entity (or where the entity is located)
     *
     * Must be a valid address
     * Cannot be a P.O. Box
     *
     * If the entity is an `event`, either an **`address`** value or a **`linkedLocation`** value can be provided.
     */
    address?: {
        /**
         * The city the entity (or the entity's location) is in
         *
         *
         * Cannot Include:
         * * a URL or domain name
         */
        city?: string;
        countryCode?: string;
        /**
         * Provides additional information to help consumers get to the entity. This string appears along with the entity's address (e.g., `In Menlo Mall, 3rd Floor`).
         * It may also be used in conjunction with a hidden address (i.e., when **`addressHidden`** is `true`) to give consumers information about where the entity can be found (e.g., `Servicing the New York area`).
         */
        extraDescription?: string;
        /**
         *
         * Cannot Include:
         * * a URL or domain name
         */
        line1?: string;
        /**
         *
         * Cannot Include:
         * * a URL or domain name
         */
        line2?: string;
        /**
         * The entity's postal code. The postal code must be valid for the entity's country. Cannot include a URL or domain name.
         *
         *
         * Cannot Include:
         * * a URL or domain name
         */
        postalCode?: string;
        /**
         * The name of the entity's region or state.
         *
         *
         * Cannot Include:
         * * a URL or domain name
         */
        region?: string;
        /**
         * The name of the entity's sublocality
         *
         *
         * Cannot Include:
         * * a URL or domain name
         */
        sublocality?: string;
    };
    /**
     * Contains the daily access hours, holiday access hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `accessHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday access hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    accessHours?: {
        friday?: {
            /**
             * Indicates if the access hours are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the access hours are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the access hours are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the access hours are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the access hours are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the access hours are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the access hours are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the access hours are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * Additional information about hours that does not fit in **`hours`** (e.g., `"Closed during the winter"`)
     */
    additionalHoursText?: string;
    /**
     * Other names for your business that you would like us to use when tracking your search performance
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 3 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    alternateNames?: Array<string>;
    /**
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    alternatePhone?: string;
    /**
     * Other websites for your business that we should search for when tracking your search performance
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 3 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* common domain names, e.g., google.com, youtube.com, etc.
     */
    alternateWebsites?: Array<string>;
    /**
     * Use this field to add action links to your Apple Listings.
     *
     * The call to action category will be displayed on the action link button.
     *
     * The App Store URL should contain a valid link to the landing page of an App in the Apple App Store.
     *
     * The Quick Link URL is where a user is taken when an action link is clicked by a user.
     *
     * The App Name sub-field is not displayed on Apple Listings and is only used to distinguish the call-to-action type when utilizing action links in Apple posts.
     *
     *
     * Array must be ordered.
     *
     */
    appleActionLinks?: Array<{
        appName: string;
        appStoreUrl?: string;
        category: 'BOOK_TRAVEL' | 'CHECK_IN' | 'FEES_POLICIES' | 'FLIGHT_STATUS' | 'TICKETS' | 'TICKETING' | 'AMENITIES' | 'FRONT_DESK' | 'PARKING' | 'GIFT_CARD' | 'WAITLIST' | 'DELIVERY' | 'ORDER' | 'TAKEOUT' | 'PICKUP' | 'RESERVE' | 'MENU' | 'APPOINTMENT' | 'PORTFOLIO' | 'QUOTE' | 'SERVICES' | 'STORE_ORDERS' | 'STORE_SHOP' | 'STORE_SUPPORT' | 'SCHEDULE' | 'SHOWTIMES' | 'AVAILABILITY' | 'PRICING' | 'ACTIVITIES' | 'BOOK' | 'BOOK_(HOTEL)' | 'BOOK_(RIDE)' | 'BOOK_(TOUR)' | 'CAREERS' | 'CHARGE' | 'COUPONS' | 'DELIVERY_(RETAIL)' | 'DONATE' | 'EVENTS' | 'ORDER_(RETAIL)' | 'OTHER_MENU' | 'PICKUP_(RETAIL)' | 'RESERVE_(PARKING)' | 'SHOWS' | 'SPORTS' | 'SUPPORT' | 'TEE_TIME' | 'GIFT_CARD_(RESTAURANT)';
        quickLinkUrl: string;
    }>;
    /**
     * The business description to be sent to Apple
     */
    appleBusinessDescription?: string;
    /**
     * The ID associated with an individual Business Folder in your Apple account
     */
    appleBusinessId?: string;
    /**
     * The ID associated with your Apple account. Numerical values only
     */
    appleCompanyId?: string;
    /**
     * The cover photo for the entity''s Apple profile
     *
     *
     * Image must be at least 1600 x 1040 pixels
     *
     * Image may be no more than 4864 x 3163 pixels
     *
     * Supported Aspect Ratios:
     * * 154 x 100
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     */
    appleCoverPhoto?: {
        alternateText?: string;
        url: string;
    };
    /**
     * The name to be displayed on Apple for the entity.
     * NOTE: The names of Brands and their respective Locations within an Apple Business Connect Account must match identically.
     * Cannot Include:
     * HTML markup
     *
     *
     * Cannot Include:
     * * HTML markup
     */
    appleDisplayName?: string;
    /**
     * Yext Category IDs
     *
     * IDs must be valid and selectable (i.e., cannot be parent categories).
     *
     * NOTE: The list of category IDs that you send us must be comprehensive. For example, if you send us a list of IDs that does not include IDs that you sent in your last update, Yext considers the missing categories to be deleted, and we remove them from your listings.
     *
     */
    categoryIds?: Array<string>;
    /**
     * Indicates whether the entity is closed
     */
    closed?: boolean;
    /**
     * Additional keywords you would like us to use when tracking your search performance
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 5 elements.
     *
     */
    customKeywords?: Array<string>;
    /**
     * A description of the entity
     *
     *
     * Cannot Include:
     * * HTML markup
     */
    description?: string;
    /**
     * Coordinates where the map pin for the entity should be displayed, as provided by you
     */
    displayCoordinate?: {
        latitude?: number;
        longitude?: number;
    };
    /**
     * Contains the daily drive-through hours, holiday drive-through hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `driveThroughHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday drive-through hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    driveThroughHours?: {
        friday?: {
            /**
             * Indicates if the drive-through hours are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the drive-through hours are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the drive-through hours are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the drive-through hours are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the drive-through hours are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the drive-through hours are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the drive-through hours are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the drive-through hours are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * Coordinates of the drop-off area for the entity, as provided by you
     */
    dropoffCoordinate?: {
        latitude?: number;
        longitude?: number;
    };
    /**
     * A description of the entity to be used in the "About You" section on Facebook
     */
    facebookAbout?: string;
    /**
     * Designates the Facebook Call-to-Action button text and value
     *
     * Valid contents of **`value`** depends on the Call-to-Action's **`type`**:
     * * `NONE`: (optional)
     * * `BOOK_NOW`: URL
     * * `CALL_NOW`: Phone number
     * * `CONTACT_US`: URL
     * * `SEND_MESSAGE`: Any string
     * * `USE_APP`: URL
     * * `PLAY_GAME`: URL
     * * `SHOP_NOW`: URL
     * * `SIGN_UP`: URL
     * * `WATCH_VIDEO`: URL
     * * `SEND_EMAIL`: Email address
     * * `LEARN_MORE`: URL
     * * `PURCHASE_GIFT_CARDS`: URL
     * * `ORDER_NOW`: URL
     * * `FOLLOW_PAGE`: Any string
     */
    facebookCallToAction?: {
        /**
         * The action the consumer is being prompted to take by the button's text
         */
        type: 'NONE' | 'BOOK_NOW' | 'CALL_NOW' | 'CONTACT_US' | 'SEND_MESSAGE' | 'USE_APP' | 'PLAY_GAME' | 'SHOP_NOW' | 'SIGN_UP' | 'WATCH_VIDEO' | 'SEND_EMAIL' | 'LEARN_MORE' | 'PURCHASE_GIFT_CARDS' | 'ORDER_NOW' | 'FOLLOW_PAGE';
        /**
         * Indicates where consumers will be directed to upon clicking the Call-to-Action button (e.g., a URL). It can be a free-form string or an embedded value, depending on what the user specifies.
         *
         * For example, if the user sets the Facebook Call-to-Action as " 'Sign Up' using 'Website URL' " in the Yext platform, **`type`** will be `SIGN_UP` and **`value`** will be `[[websiteUrl]]`.
         *
         * The Call-to-Action will have the same behavior if the user sets the value to "Custom Value" in the platform and embeds a field.
         */
        value?: string;
    };
    /**
     * The cover photo for the entity's Facebook profile
     *
     * Displayed as a 851 x 315 pixel image
     *
     * You may need a cover photo in order for your listing to appear on Facebook. Please check your listings tab to learn more.
     *
     *
     * Image must be at least 400 x 150 pixels
     *
     * Image area (width x height) may be no more than 41000000 pixels
     *
     * Image may be no more than 30000 x 30000 pixels
     *
     * Supported Aspect Ratios:
     * * 1 x 1
     * * 4 x 3
     * * 3 x 2
     * * 5 x 3
     * * 16 x 9
     * * 3 x 1
     * * 2 x 3
     * * 5 x 7
     * * 4 x 5
     * * 4 x 1
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     */
    facebookCoverPhoto?: {
        alternateText?: string;
        url: string;
    };
    /**
     * Location Descriptors are used for Enterprise businesses that sync Facebook listings using brand page location structure. The Location Descriptor is typically an additional geographic description (e.g. geomodifier) that will appear in parentheses after the name on the Facebook listing.
     *
     *
     * Cannot Include:
     * * HTML markup
     */
    facebookDescriptor?: string;
    /**
     * The name for this entity's Facebook profile.  A separate name may be specified to send only to Facebook in order to comply with any specific Facebook rules or naming conventions.
     */
    facebookName?: string;
    /**
     * The city to be displayed on this entity's Facebook profile
     */
    facebookOverrideCity?: string;
    /**
     * URL for the entity's Facebook Page.
     *
     * Valid formats:
     *
     * - facebook.com/profile.php?id=[numId]
     * - facebook.com/group.php?gid=[numId]
     * - facebook.com/groups/[numId]
     * - facebook.com/[Name]
     * - facebook.com/pages/[Name]/[numId]
     * - facebook.com/people/[Name]/[numId]
     *
     * where [Name] is a String and [numId] is an Integer
     * The success response will contain a warning message explaining why the URL wasn't stored in the system.
     */
    facebookPageUrl?: string;
    /**
     * The Facebook Page ID of this entity's brand page if in a brand page location structure
     */
    facebookParentPageId?: string;
    /**
     * The profile picture for the entity's Facebook profile
     *
     * You must have a profile picture in order for your listing to appear on Facebook.
     *
     *
     * Image must be at least 180 x 180 pixels
     *
     * Image area (width x height) may be no more than 41000000 pixels
     *
     * Image may be no more than 30000 x 30000 pixels
     *
     * Supported Aspect Ratios:
     * * 1 x 1
     * * 4 x 3
     * * 3 x 2
     * * 5 x 3
     * * 16 x 9
     * * 3 x 1
     * * 2 x 3
     * * 5 x 7
     * * 4 x 5
     * * 4 x 1
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     */
    facebookProfilePhoto?: {
        alternateText?: string;
        url: string;
    };
    /**
     * The Store ID used for this entity in a brand page location structure
     */
    facebookStoreId?: string;
    /**
     * The username that appear's in the Facebook listing URL to help customers find and remember a brand’s Facebook page.  The username is also be used for tagging the Facebook page in other users’ posts, and searching for the Facebook page.
     */
    facebookVanityUrl?: string;
    /**
     * The URL you would like to submit to Facebook in place of the one given in **`websiteUrl`** (if applicable).
     */
    facebookWebsiteOverride?: string;
    /**
     * Must be a valid fax number.
     *
     * If the fax number's calling code is for a country other than the one given in the entity's **`countryCode`**, the fax number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    fax?: string;
    /**
     * Information about the entity's Featured Message
     */
    featuredMessage?: {
        /**
         * The text of Featured Message. Default: `Call today!`
         *
         * Cannot include:
         *
         * - inappropriate language
         * - HTML markup
         * - a URL or domain name
         * - a phone number
         * - control characters ([\x00-\x1F\x7F])
         * - insufficient spacing
         *
         * If you submit a Featured Message that contains profanity or more than 50 characters, it will be ignored. The success response will contain a warning message explaining why your Featured Message wasn't stored in the system.
         *
         *
         * Cannot Include:
         * * HTML markup
         */
        description?: string;
        /**
         * Valid URL linked to the Featured Message text
         */
        url?: string;
    };
    /**
     * A list of questions that are frequently asked about this entity
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     */
    frequentlyAskedQuestions?: Array<{
        answer?: string;
        question: string;
    }>;
    /**
     * Provides additional information on where the entity can be found (e.g., `Times Square`, `Global Center Mall`)
     */
    geomodifier?: string;
    /**
     * The unique IDs of the entity's Google Business Profile keywords, as well as the unique IDs of any values selected for each keyword.
     *
     * Valid keywords (e.g., `has_drive_through`, `has_fitting_room`, `kitchen_in_room`) are determined by the entity's primary category. A full list of keywords can be retrieved with the Google Fields: List endpoint.
     *
     * Keyword values provide more details on how the keyword applies to the entity (e.g., if the keyword is `has_drive_through`, its values may be `true` or `false`).
     *
     * * If the **`v`** parameter is before `20181204`: **`googleAttributes`** is formatted as a map of key-value pairs (e.g., `[{ "id": "has_wheelchair_accessible_entrance", "values": [ "true" ] }]`)
     * * If the **`v`** parameter is on or after `20181204`: the contents are formatted as a list of objects (e.g., `{ "has_wheelchair_accessible_entrance": [ "true" ]}`)
     *
     * **NOTE:** The latest Google Attributes are available via the Google Fields: List endpoint. Google Attributes are managed by Google and are subject to change without notice. To prevent errors, make sure your API implementation is not dependent on the presence of specific attributes.
     */
    googleAttributes?: {};
    /**
     * The cover photo for the entity's Google profile
     *
     *
     * Image must be at least 250 x 250 pixels
     *
     */
    googleCoverPhoto?: {
        alternateText?: string;
        url: string;
    };
    /**
     * Information about Google Messaging, WhatsApp and SMS, for the entity’s point of contact for messaging/chat functionality.
     *
     * NOTE: Only one, either WhatsApp or SMS is displayed on the Google listing. If both SMS Number and WhatsApp URL are provided only SMS Number will be displayed on the listing.
     */
    googleMessaging?: {
        /**
         * The SMS phone number of the entity's point of contact for messaging/ chat functionality.
         * Must be a valid phone number.
         * If the phone number's calling code is for a country other than the one given in the entity's countryCode, the phone number provided must contain the calling code (e.g., +44 in +442038083831). Otherwise, the calling code is optional.
         */
        smsNumber?: string;
        /**
         * A valid URL for this entity's WhatsApp account.
         * Must be a valid URL
         *
         */
        whatsappMessagingUrl?: string;
    };
    /**
     * Google Business Profile Labels help users organize their locations into groups within GBP.
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 10 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    googleMyBusinessLabels?: Array<string>;
    /**
     * The unique identifier of this entity on Google Maps.
     */
    googlePlaceId?: string;
    /**
     * The profile photo for the entity's Google profile
     *
     *
     * Image must be at least 250 x 250 pixels
     *
     * Image may be no more than 5000 x 5000 pixels
     *
     * Supported Aspect Ratios:
     * * 1 x 1
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     */
    googleProfilePhoto?: {
        alternateText?: string;
        url: string;
    };
    /**
     * The URL you would like to submit to Google Business Profile in place of the one given in **`websiteUrl`** (if applicable).
     *
     * For example, if you want to analyze the traffic driven by your Google listings separately from other traffic, enter the alternate URL that you will use for tracking in this field.
     */
    googleWebsiteOverride?: string;
    /**
     * Indicates whether holiday-hour confirmation alerts are enabled for the Yext Knowledge Assistant for this entity
     */
    holidayHoursConversationEnabled?: boolean;
    /**
     * Contains the daily hours, holiday hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `hours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    hours?: {
        friday?: {
            /**
             * Indicates if the hours of operation are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the hours of operation are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the hours of operation are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the hours of operation are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the hours of operation are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the hours of operation are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the hours of operation are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the hours of operation are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * A statement of the ownership and authorship of a document. Individuals or organizations based in many German-speaking countries are required by law to include an Impressum in published media.
     */
    impressum?: string;
    /**
     * The ISO 3166-2 region code for the entity
     *
     * Yext will determine the entity's code and update **`isoRegionCode`** with that value. If Yext is unable to determine the code for the entity, the entity'ss ISO 3166-1 alpha-2 country code will be used.
     */
    isoRegionCode?: string;
    /**
     * Keywords that describe the entity.
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    keywords?: Array<string>;
    /**
     * The IDs of the entity labels that have been added to this entity. Entity labels help you identify entities that share a certain characteristic; they do not appear on your entity's listings.
     *
     * **NOTE:** You can only add labels that have already been created via our web interface. Currently, it is not possible to create new labels via the API.
     */
    labels?: Array<string>;
    /**
     * The URL of this entity's Landing Page that was created with Yext Pages
     */
    landingPageUrl?: string;
    /**
     * Must be a valid, non-toll-free phone number, based on the country specified in **`address.region`**. Phone numbers for US entities must contain 10 digits.
     */
    localPhone?: string;
    /**
     * For atms, the external ID of the entity that the atm is installed in. The entity must be in the same business account as the atm.
     */
    locatedIn?: string;
    /**
     * Indicates the entity's type, if it is not an event
     */
    locationType?: 'LOCATION' | 'HEALTHCARE_FACILITY' | 'HEALTHCARE_PROFESSIONAL' | 'ATM' | 'RESTAURANT' | 'HOTEL';
    /**
     * An image of the entity's logo
     *
     *
     * Supported Aspect Ratios:
     * * 1 x 1
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     */
    logo?: {
        clickthroughUrl?: string;
        description?: string;
        details?: string;
        /**
         *
         * Supported Aspect Ratios:
         * * 1 x 1
         *
         * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
         *
         */
        image: {
            alternateText?: string;
            url: string;
        };
    };
    /**
     * The main phone number of the entity's point of contact
     *
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    mainPhone?: string;
    /**
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    mobilePhone?: string;
    /**
     * Indicates whether Knowledge Nudge is enabled for the Yext Knowledge Assistant for this entity
     */
    nudgeEnabled?: boolean;
    /**
     * The date that the entity is set to open for the first time.
     * Must be formatted in YYYY-MM-DD format.
     *
     * Date must be on or after 1970-01-01 Date must be before or on 2038-01-01
     *
     */
    openDate?: string;
    /**
     *
     * **NOTE:** The list of photos that you send us must be comprehensive. For example, if you send us a list of photos that does not include photos that you sent in your last update, Yext considers the missing photos to be deleted, and we remove them from your listings.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 500 elements.
     *
     * Array item description:
     *
     * >Supported Aspect Ratios:
     * >* 1 x 1
     * >* 4 x 3
     * >* 3 x 2
     * >* 5 x 3
     * >* 16 x 9
     * >* 3 x 1
     * >* 2 x 3
     * >* 5 x 7
     * >* 4 x 5
     * >* 4 x 1
     * >
     * >**NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     * >
     */
    photoGallery?: Array<{
        clickthroughUrl?: string;
        description?: string;
        details?: string;
        /**
         *
         * Supported Aspect Ratios:
         * * 1 x 1
         * * 4 x 3
         * * 3 x 2
         * * 5 x 3
         * * 16 x 9
         * * 3 x 1
         * * 2 x 3
         * * 5 x 7
         * * 4 x 5
         * * 4 x 1
         *
         * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
         *
         */
        image: {
            alternateText?: string;
            url: string;
        };
    }>;
    /**
     * Coordinates of where consumers can be picked up at the entity, as provided by you
     */
    pickupCoordinate?: {
        latitude?: number;
        longitude?: number;
    };
    /**
     * he typical price of products sold by this location, on a scale of 1 (low) to 4 (high)
     */
    priceRange?: 'UNSPECIFIED' | 'ONE' | 'TWO' | 'THREE' | 'FOUR';
    /**
     * ID of the user who is the primary Knowledge Assistant contact for the entity
     */
    primaryConversationContact?: string;
    /**
     * Indicates whether Yext Knowledge Assistant question-and-answer conversations are enabled for this entity
     */
    questionsAndAnswers?: boolean;
    /**
     * Information about the competitors whose search performance you would like to compare to your own
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 5 elements.
     *
     */
    rankTrackingCompetitors?: Array<{
        /**
         * A name of a competitor
         *
         *
         * Cannot Include:
         * * HTML markup
         */
        name: string;
        /**
         * The business website of a competitor
         *
         *
         * Cannot Include:
         * * common domain names, e.g., google.com, youtube.com, etc.
         */
        website: string;
    }>;
    /**
     * Indicates whether Rank Tracking is enabled
     */
    rankTrackingEnabled?: boolean;
    /**
     * How often we send search queries to track your search performance
     */
    rankTrackingFrequency?: 'WEEKLY' | 'MONTHLY' | 'QUARTERLY';
    /**
     * The keywords for which you would like to track your search performance
     *
     */
    rankTrackingKeywords?: Array<'NAME' | 'PRIMARY_CATEGORY' | 'SECONDARY_CATEGORY'>;
    /**
     * The ways in which your keywords will be arranged in the search queries we use to track your performance
     *
     *
     * Array must have a minimum of 2 elements.
     *
     * Array may have a maximum of 4 elements.
     *
     */
    rankTrackingQueryTemplates?: Array<'KEYWORD' | 'KEYWORD_ZIP' | 'KEYWORD_CITY' | 'KEYWORD_IN_CITY' | 'KEYWORD_NEAR_ME' | 'KEYWORD_CITY_STATE'>;
    /**
     * The search engines that we will use to track your performance
     */
    rankTrackingSites?: Array<'GOOGLE_DESKTOP' | 'GOOGLE_MOBILE' | 'BING_DESKTOP' | 'BING_MOBILE' | 'YAHOO_DESKTOP' | 'YAHOO_MOBILE'>;
    /**
     * Indicates whether Yext Knowledge Assistant review-response conversations are enabled for this entity
     */
    reviewResponseConversationEnabled?: boolean;
    /**
     * Destination coordinates to use for driving directions to the entity, as provided by you
     */
    routableCoordinate?: {
        latitude?: number;
        longitude?: number;
    };
    /**
     * The timezone of the entity, in the standard `IANA time zone database` format (tz database). e.g. `"America/New_York"`
     */
    timezone?: string;
    /**
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    tollFreePhone?: string;
    /**
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    ttyPhone?: string;
    /**
     * Destination coordinates to use for walking directions to the entity, as provided by you
     */
    walkableCoordinate?: {
        latitude?: number;
        longitude?: number;
    };
    /**
     * Information about the website for this entity
     */
    websiteUrl?: {
        /**
         * The URL that is shown on your listings in place of **`websiteUrl.url`**. You can use **`websiteUrl.displayUrl`** to display a short, memorable web address that redirects consumers to the URL given in **`websiteUrl.url`**.
         *
         * Must be a valid URL and be specified along with **`websiteUrl.url`**.
         */
        displayUrl?: string;
        /**
         * If set to true, only the display URL will be sent to those publishers who do not support separate display and tracking URLs for this field.
         */
        preferDisplayUrl?: boolean;
        /**
         * A valid URL for this entity's website
         */
        url?: string;
    };
    EntityType?: 'atm';
};

export type EntityWrite = {
    /**
     * **This is used only to filter the fields below and should NOT be included in any API calls.
     * If create, specify the entity type in the query parameter. If update, specify the entity type
     * in the request body in the `meta` object.**
     *
     */
    EntityType?: string;
};

export type HealthcareFacilityWrite = Omit<EntityWrite, 'EntityType'> & {
    /**
     * Contains the metadata about the entity.
     */
    meta?: {
        /**
         * Country code of this Entity's Language Profile (defaults to the country of the account)
         */
        countryCode?: string;
        /**
         * The ID of the folder containing this Entity
         */
        folderId?: string;
        /**
         * ID of this Entity
         */
        id?: string;
        /**
         * This Entity's labels. If the **`v`** parameter is before `20211215`, this will be an integer.
         */
        labels?: Array<string>;
        /**
         * Language code of this Entity's Language Profile (defaults to the language code of the account)
         */
        language?: string;
    };
    /**
     *
     *
     *
     * Cannot Include:
     * * HTML markup
     */
    name?: string;
    /**
     * Contains the address of the entity (or where the entity is located)
     *
     * Must be a valid address
     * Cannot be a P.O. Box
     *
     * If the entity is an `event`, either an **`address`** value or a **`linkedLocation`** value can be provided.
     */
    address?: {
        /**
         * The city the entity (or the entity's location) is in
         *
         *
         * Cannot Include:
         * * a URL or domain name
         */
        city?: string;
        countryCode?: string;
        /**
         * Provides additional information to help consumers get to the entity. This string appears along with the entity's address (e.g., `In Menlo Mall, 3rd Floor`).
         * It may also be used in conjunction with a hidden address (i.e., when **`addressHidden`** is `true`) to give consumers information about where the entity can be found (e.g., `Servicing the New York area`).
         */
        extraDescription?: string;
        /**
         *
         * Cannot Include:
         * * a URL or domain name
         */
        line1?: string;
        /**
         *
         * Cannot Include:
         * * a URL or domain name
         */
        line2?: string;
        /**
         * The entity's postal code. The postal code must be valid for the entity's country. Cannot include a URL or domain name.
         *
         *
         * Cannot Include:
         * * a URL or domain name
         */
        postalCode?: string;
        /**
         * The name of the entity's region or state.
         *
         *
         * Cannot Include:
         * * a URL or domain name
         */
        region?: string;
        /**
         * The name of the entity's sublocality
         *
         *
         * Cannot Include:
         * * a URL or domain name
         */
        sublocality?: string;
    };
    /**
     * Indicates whether the healthcare provider is accepting new patients.
     */
    acceptingNewPatients?: boolean;
    /**
     * Contains the daily access hours, holiday access hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `accessHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday access hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    accessHours?: {
        friday?: {
            /**
             * Indicates if the access hours are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the access hours are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the access hours are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the access hours are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the access hours are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the access hours are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the access hours are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the access hours are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * Additional information about hours that does not fit in **`hours`** (e.g., `"Closed during the winter"`)
     */
    additionalHoursText?: string;
    /**
     * If `true`, the entity's street address will not be shown on listings. Defaults to `false`.
     */
    addressHidden?: boolean;
    /**
     * Other names for your business that you would like us to use when tracking your search performance
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 3 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    alternateNames?: Array<string>;
    /**
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    alternatePhone?: string;
    /**
     * Other websites for your business that we should search for when tracking your search performance
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 3 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* common domain names, e.g., google.com, youtube.com, etc.
     */
    alternateWebsites?: Array<string>;
    /**
     * Use this field to add action links to your Apple Listings.
     *
     * The call to action category will be displayed on the action link button.
     *
     * The App Store URL should contain a valid link to the landing page of an App in the Apple App Store.
     *
     * The Quick Link URL is where a user is taken when an action link is clicked by a user.
     *
     * The App Name sub-field is not displayed on Apple Listings and is only used to distinguish the call-to-action type when utilizing action links in Apple posts.
     *
     *
     * Array must be ordered.
     *
     */
    appleActionLinks?: Array<{
        appName: string;
        appStoreUrl?: string;
        category: 'BOOK_TRAVEL' | 'CHECK_IN' | 'FEES_POLICIES' | 'FLIGHT_STATUS' | 'TICKETS' | 'TICKETING' | 'AMENITIES' | 'FRONT_DESK' | 'PARKING' | 'GIFT_CARD' | 'WAITLIST' | 'DELIVERY' | 'ORDER' | 'TAKEOUT' | 'PICKUP' | 'RESERVE' | 'MENU' | 'APPOINTMENT' | 'PORTFOLIO' | 'QUOTE' | 'SERVICES' | 'STORE_ORDERS' | 'STORE_SHOP' | 'STORE_SUPPORT' | 'SCHEDULE' | 'SHOWTIMES' | 'AVAILABILITY' | 'PRICING' | 'ACTIVITIES' | 'BOOK' | 'BOOK_(HOTEL)' | 'BOOK_(RIDE)' | 'BOOK_(TOUR)' | 'CAREERS' | 'CHARGE' | 'COUPONS' | 'DELIVERY_(RETAIL)' | 'DONATE' | 'EVENTS' | 'ORDER_(RETAIL)' | 'OTHER_MENU' | 'PICKUP_(RETAIL)' | 'RESERVE_(PARKING)' | 'SHOWS' | 'SPORTS' | 'SUPPORT' | 'TEE_TIME' | 'GIFT_CARD_(RESTAURANT)';
        quickLinkUrl: string;
    }>;
    /**
     * The business description to be sent to Apple
     */
    appleBusinessDescription?: string;
    /**
     * The ID associated with an individual Business Folder in your Apple account
     */
    appleBusinessId?: string;
    /**
     * The ID associated with your Apple account. Numerical values only
     */
    appleCompanyId?: string;
    /**
     * The cover photo for the entity''s Apple profile
     *
     *
     * Image must be at least 1600 x 1040 pixels
     *
     * Image may be no more than 4864 x 3163 pixels
     *
     * Supported Aspect Ratios:
     * * 154 x 100
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     */
    appleCoverPhoto?: {
        alternateText?: string;
        url: string;
    };
    /**
     * The name to be displayed on Apple for the entity.
     * NOTE: The names of Brands and their respective Locations within an Apple Business Connect Account must match identically.
     * Cannot Include:
     * HTML markup
     *
     *
     * Cannot Include:
     * * HTML markup
     */
    appleDisplayName?: string;
    /**
     * Association memberships relevant to the entity (e.g., `"New York Doctors Association"`)
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    associations?: Array<string>;
    /**
     * Information about the Bio Content Lists associated with this entity
     */
    bios?: {
        /**
         * IDs of the Bio Lists associated with this entity
         *
         *
         * Array must be ordered.
         *
         * Array may have a maximum of 40 elements.
         *
         */
        ids?: Array<string>;
        /**
         * Label to be used for this entity's Bio Lists. This label will appear on your entity's listings.
         */
        label?: string;
    };
    /**
     * Brands sold by this entity
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    brands?: Array<string>;
    /**
     * Information about the events Content Lists (Calendars) associated with this entity
     */
    calendars?: {
        /**
         * IDs of the Calendars associated with this entity
         *
         *
         * Array must be ordered.
         *
         * Array may have a maximum of 40 elements.
         *
         */
        ids?: Array<string>;
        /**
         * Label to be used for this entity's Calendars. This label will appear on your entity's listings.
         */
        label?: string;
    };
    /**
     * Yext Category IDs
     *
     * IDs must be valid and selectable (i.e., cannot be parent categories).
     *
     * NOTE: The list of category IDs that you send us must be comprehensive. For example, if you send us a list of IDs that does not include IDs that you sent in your last update, Yext considers the missing categories to be deleted, and we remove them from your listings.
     *
     */
    categoryIds?: Array<string>;
    /**
     * Indicates whether the entity is closed
     */
    closed?: boolean;
    /**
     * A list of the conditions treated by the healthcare provider
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    conditionsTreated?: Array<string>;
    /**
     * Information or messaging related to COVID-19.
     */
    covidMessaging?: string;
    /**
     * An appointment URL for scheduling a COVID-19 test.
     */
    covidTestAppointmentUrl?: string;
    /**
     * Indicates whether an appointment is required for a COVID-19 test.
     */
    covidTestingAppointmentRequired?: boolean;
    /**
     * Indicates whether location is a drive-through site for COVID-19 tests.
     */
    covidTestingDriveThroughSite?: boolean;
    /**
     * Indicates whether location offers free COVID-19 testing.
     */
    covidTestingIsFree?: boolean;
    /**
     * Indicates whether there are patient restrictions for COVID-19 testing.
     */
    covidTestingPatientRestrictions?: boolean;
    /**
     * Indicates whether a referral is required for COVID-19 testing.
     */
    covidTestingReferralRequired?: boolean;
    /**
     * Information or instructions for the COVID-19 testing site.
     */
    covidTestingSiteInstructions?: string;
    /**
     * Indicates whether an appointment is required for a COVID-19 vaccine.
     */
    covidVaccineAppointmentRequired?: boolean;
    /**
     * Indicates whether location is a drive-through site for COVID-19 vaccines.
     */
    covidVaccineDriveThroughSite?: boolean;
    /**
     * An information URL for more information about COVID-19 vaccines.
     */
    covidVaccineInformationUrl?: string;
    /**
     * Indicates whether there are patient restrictions for a COVID-19 vaccine.
     */
    covidVaccinePatientRestrictions?: boolean;
    /**
     * Indicates whether a referral is required for a COVID-19 vaccine.
     */
    covidVaccineReferralRequired?: boolean;
    /**
     * Information or instructions for the COVID-19 vaccination site.
     */
    covidVaccineSiteInstructions?: string;
    /**
     * Indicates which COVID-19 vaccines the location offers.
     */
    covidVaccinesOffered?: Array<'PFIZER' | 'MODERNA' | 'JOHNSON_&_JOHNSON'>;
    /**
     * Additional keywords you would like us to use when tracking your search performance
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 5 elements.
     *
     */
    customKeywords?: Array<string>;
    /**
     * A description of the entity
     *
     *
     * Cannot Include:
     * * HTML markup
     */
    description?: string;
    /**
     * Coordinates where the map pin for the entity should be displayed, as provided by you
     */
    displayCoordinate?: {
        latitude?: number;
        longitude?: number;
    };
    /**
     * Coordinates of the drop-off area for the entity, as provided by you
     */
    dropoffCoordinate?: {
        latitude?: number;
        longitude?: number;
    };
    /**
     * Emails addresses for this entity's point of contact
     *
     * Must be valid email addresses
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 5 elements.
     *
     */
    emails?: Array<string>;
    /**
     * A description of the entity to be used in the "About You" section on Facebook
     */
    facebookAbout?: string;
    /**
     * Designates the Facebook Call-to-Action button text and value
     *
     * Valid contents of **`value`** depends on the Call-to-Action's **`type`**:
     * * `NONE`: (optional)
     * * `BOOK_NOW`: URL
     * * `CALL_NOW`: Phone number
     * * `CONTACT_US`: URL
     * * `SEND_MESSAGE`: Any string
     * * `USE_APP`: URL
     * * `PLAY_GAME`: URL
     * * `SHOP_NOW`: URL
     * * `SIGN_UP`: URL
     * * `WATCH_VIDEO`: URL
     * * `SEND_EMAIL`: Email address
     * * `LEARN_MORE`: URL
     * * `PURCHASE_GIFT_CARDS`: URL
     * * `ORDER_NOW`: URL
     * * `FOLLOW_PAGE`: Any string
     */
    facebookCallToAction?: {
        /**
         * The action the consumer is being prompted to take by the button's text
         */
        type: 'NONE' | 'BOOK_NOW' | 'CALL_NOW' | 'CONTACT_US' | 'SEND_MESSAGE' | 'USE_APP' | 'PLAY_GAME' | 'SHOP_NOW' | 'SIGN_UP' | 'WATCH_VIDEO' | 'SEND_EMAIL' | 'LEARN_MORE' | 'PURCHASE_GIFT_CARDS' | 'ORDER_NOW' | 'FOLLOW_PAGE';
        /**
         * Indicates where consumers will be directed to upon clicking the Call-to-Action button (e.g., a URL). It can be a free-form string or an embedded value, depending on what the user specifies.
         *
         * For example, if the user sets the Facebook Call-to-Action as " 'Sign Up' using 'Website URL' " in the Yext platform, **`type`** will be `SIGN_UP` and **`value`** will be `[[websiteUrl]]`.
         *
         * The Call-to-Action will have the same behavior if the user sets the value to "Custom Value" in the platform and embeds a field.
         */
        value?: string;
    };
    /**
     * The cover photo for the entity's Facebook profile
     *
     * Displayed as a 851 x 315 pixel image
     *
     * You may need a cover photo in order for your listing to appear on Facebook. Please check your listings tab to learn more.
     *
     *
     * Image must be at least 400 x 150 pixels
     *
     * Image area (width x height) may be no more than 41000000 pixels
     *
     * Image may be no more than 30000 x 30000 pixels
     *
     * Supported Aspect Ratios:
     * * 1 x 1
     * * 4 x 3
     * * 3 x 2
     * * 5 x 3
     * * 16 x 9
     * * 3 x 1
     * * 2 x 3
     * * 5 x 7
     * * 4 x 5
     * * 4 x 1
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     */
    facebookCoverPhoto?: {
        alternateText?: string;
        url: string;
    };
    /**
     * Location Descriptors are used for Enterprise businesses that sync Facebook listings using brand page location structure. The Location Descriptor is typically an additional geographic description (e.g. geomodifier) that will appear in parentheses after the name on the Facebook listing.
     *
     *
     * Cannot Include:
     * * HTML markup
     */
    facebookDescriptor?: string;
    /**
     * The name for this entity's Facebook profile.  A separate name may be specified to send only to Facebook in order to comply with any specific Facebook rules or naming conventions.
     */
    facebookName?: string;
    /**
     * The city to be displayed on this entity's Facebook profile
     */
    facebookOverrideCity?: string;
    /**
     * URL for the entity's Facebook Page.
     *
     * Valid formats:
     *
     * - facebook.com/profile.php?id=[numId]
     * - facebook.com/group.php?gid=[numId]
     * - facebook.com/groups/[numId]
     * - facebook.com/[Name]
     * - facebook.com/pages/[Name]/[numId]
     * - facebook.com/people/[Name]/[numId]
     *
     * where [Name] is a String and [numId] is an Integer
     * The success response will contain a warning message explaining why the URL wasn't stored in the system.
     */
    facebookPageUrl?: string;
    /**
     * The Facebook Page ID of this entity's brand page if in a brand page location structure
     */
    facebookParentPageId?: string;
    /**
     * The profile picture for the entity's Facebook profile
     *
     * You must have a profile picture in order for your listing to appear on Facebook.
     *
     *
     * Image must be at least 180 x 180 pixels
     *
     * Image area (width x height) may be no more than 41000000 pixels
     *
     * Image may be no more than 30000 x 30000 pixels
     *
     * Supported Aspect Ratios:
     * * 1 x 1
     * * 4 x 3
     * * 3 x 2
     * * 5 x 3
     * * 16 x 9
     * * 3 x 1
     * * 2 x 3
     * * 5 x 7
     * * 4 x 5
     * * 4 x 1
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     */
    facebookProfilePhoto?: {
        alternateText?: string;
        url: string;
    };
    /**
     * The Store ID used for this entity in a brand page location structure
     */
    facebookStoreId?: string;
    /**
     * The username that appear's in the Facebook listing URL to help customers find and remember a brand’s Facebook page.  The username is also be used for tagging the Facebook page in other users’ posts, and searching for the Facebook page.
     */
    facebookVanityUrl?: string;
    /**
     * The URL you would like to submit to Facebook in place of the one given in **`websiteUrl`** (if applicable).
     */
    facebookWebsiteOverride?: string;
    /**
     * Must be a valid fax number.
     *
     * If the fax number's calling code is for a country other than the one given in the entity's **`countryCode`**, the fax number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    fax?: string;
    /**
     * Information about the entity's Featured Message
     */
    featuredMessage?: {
        /**
         * The text of Featured Message. Default: `Call today!`
         *
         * Cannot include:
         *
         * - inappropriate language
         * - HTML markup
         * - a URL or domain name
         * - a phone number
         * - control characters ([\x00-\x1F\x7F])
         * - insufficient spacing
         *
         * If you submit a Featured Message that contains profanity or more than 50 characters, it will be ignored. The success response will contain a warning message explaining why your Featured Message wasn't stored in the system.
         *
         *
         * Cannot Include:
         * * HTML markup
         */
        description?: string;
        /**
         * Valid URL linked to the Featured Message text
         */
        url?: string;
    };
    /**
     * Link to the review-collection page, where consumers can leave first-party reviews
     */
    firstPartyReviewPage?: string;
    /**
     * A list of questions that are frequently asked about this entity
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     */
    frequentlyAskedQuestions?: Array<{
        answer?: string;
        question: string;
    }>;
    /**
     * Indicates whether the staff is vaccinated against COVID-19.
     */
    fullyVaccinatedStaff?: boolean;
    /**
     * Provides additional information on where the entity can be found (e.g., `Times Square`, `Global Center Mall`)
     */
    geomodifier?: string;
    /**
     * The unique IDs of the entity's Google Business Profile keywords, as well as the unique IDs of any values selected for each keyword.
     *
     * Valid keywords (e.g., `has_drive_through`, `has_fitting_room`, `kitchen_in_room`) are determined by the entity's primary category. A full list of keywords can be retrieved with the Google Fields: List endpoint.
     *
     * Keyword values provide more details on how the keyword applies to the entity (e.g., if the keyword is `has_drive_through`, its values may be `true` or `false`).
     *
     * * If the **`v`** parameter is before `20181204`: **`googleAttributes`** is formatted as a map of key-value pairs (e.g., `[{ "id": "has_wheelchair_accessible_entrance", "values": [ "true" ] }]`)
     * * If the **`v`** parameter is on or after `20181204`: the contents are formatted as a list of objects (e.g., `{ "has_wheelchair_accessible_entrance": [ "true" ]}`)
     *
     * **NOTE:** The latest Google Attributes are available via the Google Fields: List endpoint. Google Attributes are managed by Google and are subject to change without notice. To prevent errors, make sure your API implementation is not dependent on the presence of specific attributes.
     */
    googleAttributes?: {};
    /**
     * The cover photo for the entity's Google profile
     *
     *
     * Image must be at least 250 x 250 pixels
     *
     */
    googleCoverPhoto?: {
        alternateText?: string;
        url: string;
    };
    /**
     * Information about Google Messaging, WhatsApp and SMS, for the entity’s point of contact for messaging/chat functionality.
     *
     * NOTE: Only one, either WhatsApp or SMS is displayed on the Google listing. If both SMS Number and WhatsApp URL are provided only SMS Number will be displayed on the listing.
     */
    googleMessaging?: {
        /**
         * The SMS phone number of the entity's point of contact for messaging/ chat functionality.
         * Must be a valid phone number.
         * If the phone number's calling code is for a country other than the one given in the entity's countryCode, the phone number provided must contain the calling code (e.g., +44 in +442038083831). Otherwise, the calling code is optional.
         */
        smsNumber?: string;
        /**
         * A valid URL for this entity's WhatsApp account.
         * Must be a valid URL
         *
         */
        whatsappMessagingUrl?: string;
    };
    /**
     * Google Business Profile Labels help users organize their locations into groups within GBP.
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 10 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    googleMyBusinessLabels?: Array<string>;
    /**
     * The unique identifier of this entity on Google Maps.
     */
    googlePlaceId?: string;
    /**
     * The profile photo for the entity's Google profile
     *
     *
     * Image must be at least 250 x 250 pixels
     *
     * Image may be no more than 5000 x 5000 pixels
     *
     * Supported Aspect Ratios:
     * * 1 x 1
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     */
    googleProfilePhoto?: {
        alternateText?: string;
        url: string;
    };
    /**
     * The URL you would like to submit to Google Business Profile in place of the one given in **`websiteUrl`** (if applicable).
     *
     * For example, if you want to analyze the traffic driven by your Google listings separately from other traffic, enter the alternate URL that you will use for tracking in this field.
     */
    googleWebsiteOverride?: string;
    /**
     * Indicates whether holiday-hour confirmation alerts are enabled for the Yext Knowledge Assistant for this entity
     */
    holidayHoursConversationEnabled?: boolean;
    /**
     * Contains the daily hours, holiday hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `hours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    hours?: {
        friday?: {
            /**
             * Indicates if the hours of operation are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the hours of operation are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the hours of operation are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the hours of operation are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the hours of operation are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the hours of operation are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the hours of operation are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the hours of operation are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * A statement of the ownership and authorship of a document. Individuals or organizations based in many German-speaking countries are required by law to include an Impressum in published media.
     */
    impressum?: string;
    /**
     * Valid Instagram username for the entity without the leading "@" (e.g., `NewCityAuto`)
     */
    instagramHandle?: string;
    /**
     * A list of insurance policies accepted by the healthcare provider
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    insuranceAccepted?: Array<string>;
    /**
     * The ISO 3166-2 region code for the entity
     *
     * Yext will determine the entity's code and update **`isoRegionCode`** with that value. If Yext is unable to determine the code for the entity, the entity'ss ISO 3166-1 alpha-2 country code will be used.
     */
    isoRegionCode?: string;
    /**
     * Keywords that describe the entity.
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    keywords?: Array<string>;
    /**
     * The IDs of the entity labels that have been added to this entity. Entity labels help you identify entities that share a certain characteristic; they do not appear on your entity's listings.
     *
     * **NOTE:** You can only add labels that have already been created via our web interface. Currently, it is not possible to create new labels via the API.
     */
    labels?: Array<string>;
    /**
     * The URL of this entity's Landing Page that was created with Yext Pages
     */
    landingPageUrl?: string;
    /**
     * The langauges in which consumers can commicate with this entity or its staff members
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    languages?: Array<string>;
    /**
     * URL for your LinkedIn account, format should be https://www.linkedin.com/in/yourUsername
     */
    linkedInUrl?: string;
    /**
     * Must be a valid, non-toll-free phone number, based on the country specified in **`address.region`**. Phone numbers for US entities must contain 10 digits.
     */
    localPhone?: string;
    /**
     * Indicates the entity's type, if it is not an event
     */
    locationType?: 'LOCATION' | 'HEALTHCARE_FACILITY' | 'HEALTHCARE_PROFESSIONAL' | 'ATM' | 'RESTAURANT' | 'HOTEL';
    /**
     * An image of the entity's logo
     *
     *
     * Supported Aspect Ratios:
     * * 1 x 1
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     */
    logo?: {
        clickthroughUrl?: string;
        description?: string;
        details?: string;
        /**
         *
         * Supported Aspect Ratios:
         * * 1 x 1
         *
         * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
         *
         */
        image: {
            alternateText?: string;
            url: string;
        };
    };
    /**
     * The main phone number of the entity's point of contact
     *
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    mainPhone?: string;
    /**
     * Information about the URL where consumers can view the entity's menu
     */
    menuUrl?: {
        /**
         * The URL that is shown on your listings in place of **`menuUrl.url`**. You can use **`menuUrl.displayUrl`** to display a short, memorable web address that redirects consumers to the URL given in **`menuUrl.url`**.
         */
        displayUrl?: string;
        /**
         * If set to true, only the display URL will be sent to those publishers who do not support separate display and tracking URLs for this field.
         */
        preferDisplayUrl?: boolean;
        /**
         * A valid URL where consumers can view the entity's menu
         */
        url?: string;
    };
    /**
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    mobilePhone?: string;
    /**
     * The National Provider Identifier (NPI) of the healthcare provider
     */
    npi?: string;
    /**
     * Indicates whether Knowledge Nudge is enabled for the Yext Knowledge Assistant for this entity
     */
    nudgeEnabled?: boolean;
    /**
     * Contains the daily online service hours, holiday online service hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `onlineServiceHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday online service hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    onlineServiceHours?: {
        friday?: {
            /**
             * Indicates if the online service hours are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the online service hours are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the online service hours are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the online service hours are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the online service hours are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the online service hours are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the online service hours are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the online service hours are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * The date that the entity is set to open for the first time.
     * Must be formatted in YYYY-MM-DD format.
     *
     * Date must be on or after 1970-01-01 Date must be before or on 2038-01-01
     *
     */
    openDate?: string;
    /**
     * Information about the URL used to place orders that will be fulfilled by the entity
     */
    orderUrl?: {
        /**
         * The URL that is shown on your listings in place of **`orderUrl.url`**. You can use **`orderUrl.displayUrl`** to display a short, memorable web address that redirects consumers to the URL given in **`orderUrl.url`**.
         */
        displayUrl?: string;
        /**
         * If set to true, only the display URL will be sent to those publishers who do not support separate display and tracking URLs for this field.
         */
        preferDisplayUrl?: boolean;
        /**
         * A valid URL used to place an order at this entity
         */
        url?: string;
    };
    /**
     * The payment methods accepted by this entity
     *
     * Valid elements depend on the entity's country.
     *
     */
    paymentOptions?: Array<'AFTERPAY' | 'ALIPAY' | 'AMERICANEXPRESS' | 'ANDROIDPAY' | 'APPLEPAY' | 'ATM' | 'ATMQUICK' | 'BACS' | 'BANCONTACT' | 'BANKDEPOSIT' | 'BANKPAY' | 'BGO' | 'BITCOIN' | 'Bar' | 'CARTASI' | 'CASH' | 'CCS' | 'CHECK' | 'CHEQUESVACANCES' | 'CONB' | 'CONTACTLESSPAYME' | 'CVVV' | 'DEBITCARD' | 'DEBITNOTE' | 'DINERSCLUB' | 'DIRECTDEBIT' | 'DISCOVER' | 'ECKARTE' | 'ECOCHEQUE' | 'EKENA' | 'EMV' | 'FINANCING' | 'GIFTCARD' | 'GOPAY' | 'HAYAKAKEN' | 'HEBAG' | 'IBOD' | 'ICCARDS' | 'ICOCA' | 'ID' | 'IDEAL' | 'INCA' | 'INVOICE' | 'JCB' | 'JCoinPay' | 'JKOPAY' | 'KITACA' | 'KLA' | 'KLARNA' | 'LINEPAY' | 'MAESTRO' | 'MANACA' | 'MASTERCARD' | 'MIPAY' | 'MONIZZE' | 'MPAY' | 'Manuelle Lastsch' | 'Merpay' | 'NANACO' | 'NEXI' | 'NIMOCA' | 'OREM' | 'PASMO' | 'PAYBACKPAY' | 'PAYBOX' | 'PAYCONIQ' | 'PAYPAL' | 'PAYPAY' | 'PAYSEC' | 'PIN' | 'POSTEPAY' | 'QRCODE' | 'QUICPAY' | 'RAKUTENEDY' | 'RAKUTENPAY' | 'SAMSUNGPAY' | 'SODEXO' | 'SUGOCA' | 'SUICA' | 'SWISH' | 'TICKETRESTAURANT' | 'TOICA' | 'TRAVELERSCHECK' | 'TSCUBIC' | 'TWINT' | 'UNIONPAY' | 'VEV' | 'VISA' | 'VISAELECTRON' | 'VOB' | 'VOUCHER' | 'VPAY' | 'WAON' | 'WECHATPAY' | 'WIRETRANSFER' | 'Yucho Pay' | 'ZELLE' | 'auPay' | 'dBarai' | 'Überweisung'>;
    /**
     *
     * **NOTE:** The list of photos that you send us must be comprehensive. For example, if you send us a list of photos that does not include photos that you sent in your last update, Yext considers the missing photos to be deleted, and we remove them from your listings.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 500 elements.
     *
     * Array item description:
     *
     * >Supported Aspect Ratios:
     * >* 1 x 1
     * >* 4 x 3
     * >* 3 x 2
     * >* 5 x 3
     * >* 16 x 9
     * >* 3 x 1
     * >* 2 x 3
     * >* 5 x 7
     * >* 4 x 5
     * >* 4 x 1
     * >
     * >**NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     * >
     */
    photoGallery?: Array<{
        clickthroughUrl?: string;
        description?: string;
        details?: string;
        /**
         *
         * Supported Aspect Ratios:
         * * 1 x 1
         * * 4 x 3
         * * 3 x 2
         * * 5 x 3
         * * 16 x 9
         * * 3 x 1
         * * 2 x 3
         * * 5 x 7
         * * 4 x 5
         * * 4 x 1
         *
         * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
         *
         */
        image: {
            alternateText?: string;
            url: string;
        };
    }>;
    /**
     * Coordinates of where consumers can be picked up at the entity, as provided by you
     */
    pickupCoordinate?: {
        latitude?: number;
        longitude?: number;
    };
    /**
     * Contains the daily pickup hours, holiday pickup hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `pickupHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday pickup hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    pickupHours?: {
        friday?: {
            /**
             * Indicates if the pickup hours are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the pickup hours are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the pickup hours are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the pickup hours are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the pickup hours are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the pickup hours are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the pickup hours are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the pickup hours are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * URL for your Pinterest account, format should be https://www.pinterest.com/yourUsername
     */
    pinterestUrl?: string;
    /**
     * he typical price of products sold by this location, on a scale of 1 (low) to 4 (high)
     */
    priceRange?: 'UNSPECIFIED' | 'ONE' | 'TWO' | 'THREE' | 'FOUR';
    /**
     * ID of the user who is the primary Knowledge Assistant contact for the entity
     */
    primaryConversationContact?: string;
    /**
     * Information about the Products & Services Content Lists associated with this entity
     */
    productLists?: {
        /**
         * IDs of the Products & Services Lists associated with this entity
         *
         *
         * Array must be ordered.
         *
         * Array may have a maximum of 40 elements.
         *
         */
        ids?: Array<string>;
        /**
         * Label to be used for this entity's Products & Services Lists. This label will appear on your entity's listings.
         */
        label?: string;
    };
    /**
     * Indicates whether Yext Knowledge Assistant question-and-answer conversations are enabled for this entity
     */
    questionsAndAnswers?: boolean;
    /**
     * Information about the competitors whose search performance you would like to compare to your own
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 5 elements.
     *
     */
    rankTrackingCompetitors?: Array<{
        /**
         * A name of a competitor
         *
         *
         * Cannot Include:
         * * HTML markup
         */
        name: string;
        /**
         * The business website of a competitor
         *
         *
         * Cannot Include:
         * * common domain names, e.g., google.com, youtube.com, etc.
         */
        website: string;
    }>;
    /**
     * Indicates whether Rank Tracking is enabled
     */
    rankTrackingEnabled?: boolean;
    /**
     * How often we send search queries to track your search performance
     */
    rankTrackingFrequency?: 'WEEKLY' | 'MONTHLY' | 'QUARTERLY';
    /**
     * The keywords for which you would like to track your search performance
     *
     */
    rankTrackingKeywords?: Array<'NAME' | 'PRIMARY_CATEGORY' | 'SECONDARY_CATEGORY'>;
    /**
     * The ways in which your keywords will be arranged in the search queries we use to track your performance
     *
     *
     * Array must have a minimum of 2 elements.
     *
     * Array may have a maximum of 4 elements.
     *
     */
    rankTrackingQueryTemplates?: Array<'KEYWORD' | 'KEYWORD_ZIP' | 'KEYWORD_CITY' | 'KEYWORD_IN_CITY' | 'KEYWORD_NEAR_ME' | 'KEYWORD_CITY_STATE'>;
    /**
     * The search engines that we will use to track your performance
     */
    rankTrackingSites?: Array<'GOOGLE_DESKTOP' | 'GOOGLE_MOBILE' | 'BING_DESKTOP' | 'BING_MOBILE' | 'YAHOO_DESKTOP' | 'YAHOO_MOBILE'>;
    /**
     * Information about the URL consumers can visit to make reservations at this entity
     */
    reservationUrl?: {
        /**
         * The URL that is shown on your listings in place of **`reservationUrl.url`**. You can use **`reservationUrl.displayUrl`** to display a short, memorable web address that redirects consumers to the URL given in **`reservationUrl.url`**.
         *
         * Must be a valid URL and be specified along with **`reservationUrl.url`**.
         */
        displayUrl?: string;
        /**
         * If set to true, only the display URL will be sent to those publishers who do not support separate display and tracking URLs for this field.
         */
        preferDisplayUrl?: boolean;
        /**
         * A valid URL used to make reservations at this entity
         */
        url?: string;
    };
    /**
     * The URL given Review Invitation emails where consumers can leave a review about the entity
     */
    reviewGenerationUrl?: string;
    /**
     * Indicates whether Yext Knowledge Assistant review-response conversations are enabled for this entity
     */
    reviewResponseConversationEnabled?: boolean;
    /**
     * Destination coordinates to use for driving directions to the entity, as provided by you
     */
    routableCoordinate?: {
        latitude?: number;
        longitude?: number;
    };
    /**
     * Information about the area that is served by this entity. It is specified as a list of cities and/or postal codes.
     *
     * **Only for Google Business Profile and Bing:** Currently, **serviceArea** is only supported by Google Business Profile and Bing and will not affect your listings on other sites.
     */
    serviceArea?: {
        /**
         * A list of places served by the entity, where each place is either:
         * - a postal code, or
         * - the name of a city.
         *
         *
         *
         * Array must be ordered.
         *
         * Array may have a maximum of 200 elements.
         *
         */
        places?: Array<string>;
    };
    /**
     * Information about the area that is served by this entity. It is specified as a list of service area names, their associated types and google place ids.
     * **Only for Google Business Profile and Bing:** Currently, **serviceArea** is only supported by Google Business Profile and Bing and will not affect your listings on other sites.
     *
     *
     * Array may have a maximum of 200 elements.
     *
     */
    serviceAreaPlaces?: Array<{
        name?: string;
        googlePlaceId?: string;
        type?: 'POSTAL_CODE' | 'REGION' | 'COUNTY' | 'CITY' | 'SUBLOCALITY';
    }>;
    /**
     * Services offered by this entity
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    services?: Array<string>;
    /**
     * URL for your TikTok profile, format should be https://www.tiktok.com/yourUsername
     */
    tikTokUrl?: string;
    /**
     * The timezone of the entity, in the standard `IANA time zone database` format (tz database). e.g. `"America/New_York"`
     */
    timezone?: string;
    /**
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    tollFreePhone?: string;
    /**
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    ttyPhone?: string;
    /**
     * Valid Twitter handle for the entity without the leading "@" (e.g., `JohnSmith`)
     *
     * If you submit an invalid Twitter handle, it will be ignored. The success response will contain a warning message explaining why your Twitter handle wasn't stored in the system.
     */
    twitterHandle?: string;
    /**
     * Information about the Yext-powered link that can be copied and pasted into the markup of Yext Pages where the embedded Uber link should appear
     */
    uberLink?: {
        /**
         * Indicates whether the embedded Uber link for this entity appears as text or a button
         *
         * When consumers click on this link on a mobile device, the Uber app (if installed) will open with your entity set as the trip destination. If the Uber app is not installed, the consumer will be prompted to download it.
         */
        presentation: 'BUTTON' | 'LINK';
        /**
         * The text of the embedded Uber link
         *
         * Default is `Ride there with Uber`.
         *
         * **NOTE:** This field is only available if **`uberLink.presentation`** is `LINK`.
         */
        text?: string;
    };
    /**
     * Information about the call-to-action consumers will see in the Uber app during a trip to your entity
     */
    uberTripBranding?: {
        /**
         * A longer description that will appear near the call-to-action in the Uber app during a trip to your entity.
         *
         * **NOTE:** If a value for **`uberTripBranding.description`** is provided, values must also be provided for **`uberTripBranding.text`** and **`uberTripBranding.url`**.
         */
        description: string;
        /**
         * The text of the call-to-action that will appear in the Uber app during a trip to your entity (e.g., `Check out our menu!`)
         *
         * **NOTE:** If a value for **`uberTripBranding.text`** is provided, values must also be provided for **`uberTripBranding.url`** and **`uberTripBranding.description`**.
         */
        text: string;
        /**
         * The URL that the consumer will be redirected to when tapping on the call-to-action in the Uber app during a trip to your entity.
         *
         * **NOTE:** If a value for **`uberTripBranding.url`** is provided, values must also be provided for **`uberTripBranding.text`** and **`uberTripBranding.description`**.
         */
        url: string;
    };
    /**
     * Valid YouTube URLs for embedding a video on some publisher sites
     *
     * **NOTE:** Currently, only the first URL in the Array appears in your listings.
     *
     *
     *
     * Array must be ordered.
     *
     */
    videos?: Array<{
        /**
         *
         * Cannot Include:
         * * HTML markup
         */
        description?: string;
        video: {
            url: string;
        };
    }>;
    /**
     * Destination coordinates to use for walking directions to the entity, as provided by you
     */
    walkableCoordinate?: {
        latitude?: number;
        longitude?: number;
    };
    /**
     * Information about the website for this entity
     */
    websiteUrl?: {
        /**
         * The URL that is shown on your listings in place of **`websiteUrl.url`**. You can use **`websiteUrl.displayUrl`** to display a short, memorable web address that redirects consumers to the URL given in **`websiteUrl.url`**.
         *
         * Must be a valid URL and be specified along with **`websiteUrl.url`**.
         */
        displayUrl?: string;
        /**
         * If set to true, only the display URL will be sent to those publishers who do not support separate display and tracking URLs for this field.
         */
        preferDisplayUrl?: boolean;
        /**
         * A valid URL for this entity's website
         */
        url?: string;
    };
    /**
     * The year the entity was established.
     */
    yearEstablished?: number;
    /**
     * URL for your YouTube channel, format should be https://www.youtube.com/c/yourUsername
     */
    youTubeChannelUrl?: string;
    EntityType?: 'healthcareFacility';
};

export type HealthcareProfessionalWrite = Omit<EntityWrite, 'EntityType'> & {
    /**
     * Contains the metadata about the entity.
     */
    meta?: {
        /**
         * Country code of this Entity's Language Profile (defaults to the country of the account)
         */
        countryCode?: string;
        /**
         * The ID of the folder containing this Entity
         */
        folderId?: string;
        /**
         * ID of this Entity
         */
        id?: string;
        /**
         * This Entity's labels. If the **`v`** parameter is before `20211215`, this will be an integer.
         */
        labels?: Array<string>;
        /**
         * Language code of this Entity's Language Profile (defaults to the language code of the account)
         */
        language?: string;
    };
    /**
     *
     *
     *
     * Cannot Include:
     * * HTML markup
     */
    name?: string;
    /**
     * Contains the address of the entity (or where the entity is located)
     *
     * Must be a valid address
     * Cannot be a P.O. Box
     *
     * If the entity is an `event`, either an **`address`** value or a **`linkedLocation`** value can be provided.
     */
    address?: {
        /**
         * The city the entity (or the entity's location) is in
         *
         *
         * Cannot Include:
         * * a URL or domain name
         */
        city?: string;
        countryCode?: string;
        /**
         * Provides additional information to help consumers get to the entity. This string appears along with the entity's address (e.g., `In Menlo Mall, 3rd Floor`).
         * It may also be used in conjunction with a hidden address (i.e., when **`addressHidden`** is `true`) to give consumers information about where the entity can be found (e.g., `Servicing the New York area`).
         */
        extraDescription?: string;
        /**
         *
         * Cannot Include:
         * * a URL or domain name
         */
        line1?: string;
        /**
         *
         * Cannot Include:
         * * a URL or domain name
         */
        line2?: string;
        /**
         * The entity's postal code. The postal code must be valid for the entity's country. Cannot include a URL or domain name.
         *
         *
         * Cannot Include:
         * * a URL or domain name
         */
        postalCode?: string;
        /**
         * The name of the entity's region or state.
         *
         *
         * Cannot Include:
         * * a URL or domain name
         */
        region?: string;
        /**
         * The name of the entity's sublocality
         *
         *
         * Cannot Include:
         * * a URL or domain name
         */
        sublocality?: string;
    };
    /**
     * Indicates whether the healthcare provider is accepting new patients.
     */
    acceptingNewPatients?: boolean;
    /**
     * Additional information about hours that does not fit in **`hours`** (e.g., `"Closed during the winter"`)
     */
    additionalHoursText?: string;
    /**
     * If `true`, the entity's street address will not be shown on listings. Defaults to `false`.
     */
    addressHidden?: boolean;
    /**
     * A list of hospitals where the healthcare professional admits patients
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    admittingHospitals?: Array<string>;
    /**
     * Other names for your business that you would like us to use when tracking your search performance
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 3 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    alternateNames?: Array<string>;
    /**
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    alternatePhone?: string;
    /**
     * Other websites for your business that we should search for when tracking your search performance
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 3 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* common domain names, e.g., google.com, youtube.com, etc.
     */
    alternateWebsites?: Array<string>;
    /**
     * Use this field to add action links to your Apple Listings.
     *
     * The call to action category will be displayed on the action link button.
     *
     * The App Store URL should contain a valid link to the landing page of an App in the Apple App Store.
     *
     * The Quick Link URL is where a user is taken when an action link is clicked by a user.
     *
     * The App Name sub-field is not displayed on Apple Listings and is only used to distinguish the call-to-action type when utilizing action links in Apple posts.
     *
     *
     * Array must be ordered.
     *
     */
    appleActionLinks?: Array<{
        appName: string;
        appStoreUrl?: string;
        category: 'BOOK_TRAVEL' | 'CHECK_IN' | 'FEES_POLICIES' | 'FLIGHT_STATUS' | 'TICKETS' | 'TICKETING' | 'AMENITIES' | 'FRONT_DESK' | 'PARKING' | 'GIFT_CARD' | 'WAITLIST' | 'DELIVERY' | 'ORDER' | 'TAKEOUT' | 'PICKUP' | 'RESERVE' | 'MENU' | 'APPOINTMENT' | 'PORTFOLIO' | 'QUOTE' | 'SERVICES' | 'STORE_ORDERS' | 'STORE_SHOP' | 'STORE_SUPPORT' | 'SCHEDULE' | 'SHOWTIMES' | 'AVAILABILITY' | 'PRICING' | 'ACTIVITIES' | 'BOOK' | 'BOOK_(HOTEL)' | 'BOOK_(RIDE)' | 'BOOK_(TOUR)' | 'CAREERS' | 'CHARGE' | 'COUPONS' | 'DELIVERY_(RETAIL)' | 'DONATE' | 'EVENTS' | 'ORDER_(RETAIL)' | 'OTHER_MENU' | 'PICKUP_(RETAIL)' | 'RESERVE_(PARKING)' | 'SHOWS' | 'SPORTS' | 'SUPPORT' | 'TEE_TIME' | 'GIFT_CARD_(RESTAURANT)';
        quickLinkUrl: string;
    }>;
    /**
     * The business description to be sent to Apple
     */
    appleBusinessDescription?: string;
    /**
     * The ID associated with an individual Business Folder in your Apple account
     */
    appleBusinessId?: string;
    /**
     * The ID associated with your Apple account. Numerical values only
     */
    appleCompanyId?: string;
    /**
     * The cover photo for the entity''s Apple profile
     *
     *
     * Image must be at least 1600 x 1040 pixels
     *
     * Image may be no more than 4864 x 3163 pixels
     *
     * Supported Aspect Ratios:
     * * 154 x 100
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     */
    appleCoverPhoto?: {
        alternateText?: string;
        url: string;
    };
    /**
     * The name to be displayed on Apple for the entity.
     * NOTE: The names of Brands and their respective Locations within an Apple Business Connect Account must match identically.
     * Cannot Include:
     * HTML markup
     *
     *
     * Cannot Include:
     * * HTML markup
     */
    appleDisplayName?: string;
    /**
     * Association memberships relevant to the entity (e.g., `"New York Doctors Association"`)
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    associations?: Array<string>;
    /**
     * Information about the Bio Content Lists associated with this entity
     */
    bios?: {
        /**
         * IDs of the Bio Lists associated with this entity
         *
         *
         * Array must be ordered.
         *
         * Array may have a maximum of 40 elements.
         *
         */
        ids?: Array<string>;
        /**
         * Label to be used for this entity's Bio Lists. This label will appear on your entity's listings.
         */
        label?: string;
    };
    /**
     * Brands sold by this entity
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    brands?: Array<string>;
    /**
     * Yext Category IDs
     *
     * IDs must be valid and selectable (i.e., cannot be parent categories).
     *
     * NOTE: The list of category IDs that you send us must be comprehensive. For example, if you send us a list of IDs that does not include IDs that you sent in your last update, Yext considers the missing categories to be deleted, and we remove them from your listings.
     *
     */
    categoryIds?: Array<string>;
    /**
     * A list of the certifications held by the healthcare professional
     * **NOTE:** This field is only available to locations whose **`entityType`** is `healthcareProfessional`.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    certifications?: Array<string>;
    /**
     * Indicates whether the entity is closed
     */
    closed?: boolean;
    /**
     * A list of the conditions treated by the healthcare provider
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    conditionsTreated?: Array<string>;
    /**
     * Information or messaging related to COVID-19.
     */
    covidMessaging?: string;
    /**
     * Additional keywords you would like us to use when tracking your search performance
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 5 elements.
     *
     */
    customKeywords?: Array<string>;
    /**
     * A list of the degrees earned by the healthcare professional
     *
     *
     * Array must be ordered.
     *
     */
    degrees?: Array<'ANP' | 'APN' | 'APRN' | 'ARNP' | 'AUD' | 'BSW' | 'CCCA' | 'CNM' | 'CNP' | 'CNS' | 'CPNP' | 'CRNA' | 'CRNP' | 'DC' | 'DDS' | 'DMD' | 'DNP' | 'DO' | 'DPM' | 'DPT' | 'DSW' | 'DVM' | 'FNP' | 'GNP' | 'LAC' | 'LCSW' | 'LPN' | 'MBA' | 'MBBS' | 'MD' | 'MPAS' | 'MPH' | 'MSW' | 'ND' | 'NNP' | 'NP' | 'OD' | 'PA' | 'PAC' | 'PHARMD' | 'PHD' | 'PNP' | 'PSYD' | 'RD' | 'RSW' | 'VMD' | 'WHNP'>;
    /**
     * A description of the entity
     *
     *
     * Cannot Include:
     * * HTML markup
     */
    description?: string;
    /**
     * Coordinates where the map pin for the entity should be displayed, as provided by you
     */
    displayCoordinate?: {
        latitude?: number;
        longitude?: number;
    };
    /**
     * Coordinates of the drop-off area for the entity, as provided by you
     */
    dropoffCoordinate?: {
        latitude?: number;
        longitude?: number;
    };
    /**
     * Information about the education or training completed by the healthcare professional
     *
     *
     * Array must be ordered.
     *
     */
    educationList?: Array<{
        institutionName: string;
        type: 'FELLOWSHIP' | 'RESIDENCY' | 'INTERNSHIP' | 'MEDICAL_SCHOOL';
        yearCompleted: number;
    }>;
    /**
     * Emails addresses for this entity's point of contact
     *
     * Must be valid email addresses
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 5 elements.
     *
     */
    emails?: Array<string>;
    /**
     * A description of the entity to be used in the "About You" section on Facebook
     */
    facebookAbout?: string;
    /**
     * Designates the Facebook Call-to-Action button text and value
     *
     * Valid contents of **`value`** depends on the Call-to-Action's **`type`**:
     * * `NONE`: (optional)
     * * `BOOK_NOW`: URL
     * * `CALL_NOW`: Phone number
     * * `CONTACT_US`: URL
     * * `SEND_MESSAGE`: Any string
     * * `USE_APP`: URL
     * * `PLAY_GAME`: URL
     * * `SHOP_NOW`: URL
     * * `SIGN_UP`: URL
     * * `WATCH_VIDEO`: URL
     * * `SEND_EMAIL`: Email address
     * * `LEARN_MORE`: URL
     * * `PURCHASE_GIFT_CARDS`: URL
     * * `ORDER_NOW`: URL
     * * `FOLLOW_PAGE`: Any string
     */
    facebookCallToAction?: {
        /**
         * The action the consumer is being prompted to take by the button's text
         */
        type: 'NONE' | 'BOOK_NOW' | 'CALL_NOW' | 'CONTACT_US' | 'SEND_MESSAGE' | 'USE_APP' | 'PLAY_GAME' | 'SHOP_NOW' | 'SIGN_UP' | 'WATCH_VIDEO' | 'SEND_EMAIL' | 'LEARN_MORE' | 'PURCHASE_GIFT_CARDS' | 'ORDER_NOW' | 'FOLLOW_PAGE';
        /**
         * Indicates where consumers will be directed to upon clicking the Call-to-Action button (e.g., a URL). It can be a free-form string or an embedded value, depending on what the user specifies.
         *
         * For example, if the user sets the Facebook Call-to-Action as " 'Sign Up' using 'Website URL' " in the Yext platform, **`type`** will be `SIGN_UP` and **`value`** will be `[[websiteUrl]]`.
         *
         * The Call-to-Action will have the same behavior if the user sets the value to "Custom Value" in the platform and embeds a field.
         */
        value?: string;
    };
    /**
     * The cover photo for the entity's Facebook profile
     *
     * Displayed as a 851 x 315 pixel image
     *
     * You may need a cover photo in order for your listing to appear on Facebook. Please check your listings tab to learn more.
     *
     *
     * Image must be at least 400 x 150 pixels
     *
     * Image area (width x height) may be no more than 41000000 pixels
     *
     * Image may be no more than 30000 x 30000 pixels
     *
     * Supported Aspect Ratios:
     * * 1 x 1
     * * 4 x 3
     * * 3 x 2
     * * 5 x 3
     * * 16 x 9
     * * 3 x 1
     * * 2 x 3
     * * 5 x 7
     * * 4 x 5
     * * 4 x 1
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     */
    facebookCoverPhoto?: {
        alternateText?: string;
        url: string;
    };
    /**
     * Location Descriptors are used for Enterprise businesses that sync Facebook listings using brand page location structure. The Location Descriptor is typically an additional geographic description (e.g. geomodifier) that will appear in parentheses after the name on the Facebook listing.
     *
     *
     * Cannot Include:
     * * HTML markup
     */
    facebookDescriptor?: string;
    /**
     * The name for this entity's Facebook profile.  A separate name may be specified to send only to Facebook in order to comply with any specific Facebook rules or naming conventions.
     */
    facebookName?: string;
    /**
     * The city to be displayed on this entity's Facebook profile
     */
    facebookOverrideCity?: string;
    /**
     * URL for the entity's Facebook Page.
     *
     * Valid formats:
     *
     * - facebook.com/profile.php?id=[numId]
     * - facebook.com/group.php?gid=[numId]
     * - facebook.com/groups/[numId]
     * - facebook.com/[Name]
     * - facebook.com/pages/[Name]/[numId]
     * - facebook.com/people/[Name]/[numId]
     *
     * where [Name] is a String and [numId] is an Integer
     * The success response will contain a warning message explaining why the URL wasn't stored in the system.
     */
    facebookPageUrl?: string;
    /**
     * The Facebook Page ID of this entity's brand page if in a brand page location structure
     */
    facebookParentPageId?: string;
    /**
     * The profile picture for the entity's Facebook profile
     *
     * You must have a profile picture in order for your listing to appear on Facebook.
     *
     *
     * Image must be at least 180 x 180 pixels
     *
     * Image area (width x height) may be no more than 41000000 pixels
     *
     * Image may be no more than 30000 x 30000 pixels
     *
     * Supported Aspect Ratios:
     * * 1 x 1
     * * 4 x 3
     * * 3 x 2
     * * 5 x 3
     * * 16 x 9
     * * 3 x 1
     * * 2 x 3
     * * 5 x 7
     * * 4 x 5
     * * 4 x 1
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     */
    facebookProfilePhoto?: {
        alternateText?: string;
        url: string;
    };
    /**
     * The Store ID used for this entity in a brand page location structure
     */
    facebookStoreId?: string;
    /**
     * The username that appear's in the Facebook listing URL to help customers find and remember a brand’s Facebook page.  The username is also be used for tagging the Facebook page in other users’ posts, and searching for the Facebook page.
     */
    facebookVanityUrl?: string;
    /**
     * The URL you would like to submit to Facebook in place of the one given in **`websiteUrl`** (if applicable).
     */
    facebookWebsiteOverride?: string;
    /**
     * Must be a valid fax number.
     *
     * If the fax number's calling code is for a country other than the one given in the entity's **`countryCode`**, the fax number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    fax?: string;
    /**
     * Information about the entity's Featured Message
     */
    featuredMessage?: {
        /**
         * The text of Featured Message. Default: `Call today!`
         *
         * Cannot include:
         *
         * - inappropriate language
         * - HTML markup
         * - a URL or domain name
         * - a phone number
         * - control characters ([\x00-\x1F\x7F])
         * - insufficient spacing
         *
         * If you submit a Featured Message that contains profanity or more than 50 characters, it will be ignored. The success response will contain a warning message explaining why your Featured Message wasn't stored in the system.
         *
         *
         * Cannot Include:
         * * HTML markup
         */
        description?: string;
        /**
         * Valid URL linked to the Featured Message text
         */
        url?: string;
    };
    /**
     * The first name of the healthcare professional
     *
     *
     * Cannot Include:
     * * a URL or domain name
     * * HTML markup
     */
    firstName?: string;
    /**
     * Link to the review-collection page, where consumers can leave first-party reviews
     */
    firstPartyReviewPage?: string;
    /**
     * A list of questions that are frequently asked about this entity
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     */
    frequentlyAskedQuestions?: Array<{
        answer?: string;
        question: string;
    }>;
    /**
     * The gender of the healthcare professional
     */
    gender?: 'UNSPECIFIED' | 'FEMALE' | 'MALE' | 'NONBINARY' | 'TRANSGENDER_FEMALE' | 'TRANSGENDER_MALE' | 'OTHER' | 'PREFER_NOT_TO_DISCLOSE';
    /**
     * Provides additional information on where the entity can be found (e.g., `Times Square`, `Global Center Mall`)
     */
    geomodifier?: string;
    /**
     * The unique IDs of the entity's Google Business Profile keywords, as well as the unique IDs of any values selected for each keyword.
     *
     * Valid keywords (e.g., `has_drive_through`, `has_fitting_room`, `kitchen_in_room`) are determined by the entity's primary category. A full list of keywords can be retrieved with the Google Fields: List endpoint.
     *
     * Keyword values provide more details on how the keyword applies to the entity (e.g., if the keyword is `has_drive_through`, its values may be `true` or `false`).
     *
     * * If the **`v`** parameter is before `20181204`: **`googleAttributes`** is formatted as a map of key-value pairs (e.g., `[{ "id": "has_wheelchair_accessible_entrance", "values": [ "true" ] }]`)
     * * If the **`v`** parameter is on or after `20181204`: the contents are formatted as a list of objects (e.g., `{ "has_wheelchair_accessible_entrance": [ "true" ]}`)
     *
     * **NOTE:** The latest Google Attributes are available via the Google Fields: List endpoint. Google Attributes are managed by Google and are subject to change without notice. To prevent errors, make sure your API implementation is not dependent on the presence of specific attributes.
     */
    googleAttributes?: {};
    /**
     * The cover photo for the entity's Google profile
     *
     *
     * Image must be at least 250 x 250 pixels
     *
     */
    googleCoverPhoto?: {
        alternateText?: string;
        url: string;
    };
    /**
     * Information about Google Messaging, WhatsApp and SMS, for the entity’s point of contact for messaging/chat functionality.
     *
     * NOTE: Only one, either WhatsApp or SMS is displayed on the Google listing. If both SMS Number and WhatsApp URL are provided only SMS Number will be displayed on the listing.
     */
    googleMessaging?: {
        /**
         * The SMS phone number of the entity's point of contact for messaging/ chat functionality.
         * Must be a valid phone number.
         * If the phone number's calling code is for a country other than the one given in the entity's countryCode, the phone number provided must contain the calling code (e.g., +44 in +442038083831). Otherwise, the calling code is optional.
         */
        smsNumber?: string;
        /**
         * A valid URL for this entity's WhatsApp account.
         * Must be a valid URL
         *
         */
        whatsappMessagingUrl?: string;
    };
    /**
     * Google Business Profile Labels help users organize their locations into groups within GBP.
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 10 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    googleMyBusinessLabels?: Array<string>;
    /**
     * The unique identifier of this entity on Google Maps.
     */
    googlePlaceId?: string;
    /**
     * The profile photo for the entity's Google profile
     *
     *
     * Image must be at least 250 x 250 pixels
     *
     * Image may be no more than 5000 x 5000 pixels
     *
     * Supported Aspect Ratios:
     * * 1 x 1
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     */
    googleProfilePhoto?: {
        alternateText?: string;
        url: string;
    };
    /**
     * The URL you would like to submit to Google Business Profile in place of the one given in **`websiteUrl`** (if applicable).
     *
     * For example, if you want to analyze the traffic driven by your Google listings separately from other traffic, enter the alternate URL that you will use for tracking in this field.
     */
    googleWebsiteOverride?: string;
    /**
     * A portrait of the healthcare professional
     */
    headshot?: {
        alternateText?: string;
        url: string;
    };
    /**
     * Indicates whether holiday-hour confirmation alerts are enabled for the Yext Knowledge Assistant for this entity
     */
    holidayHoursConversationEnabled?: boolean;
    /**
     * Contains the daily hours, holiday hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `hours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    hours?: {
        friday?: {
            /**
             * Indicates if the hours of operation are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the hours of operation are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the hours of operation are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the hours of operation are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the hours of operation are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the hours of operation are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the hours of operation are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the hours of operation are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * A statement of the ownership and authorship of a document. Individuals or organizations based in many German-speaking countries are required by law to include an Impressum in published media.
     */
    impressum?: string;
    /**
     * Valid Instagram username for the entity without the leading "@" (e.g., `NewCityAuto`)
     */
    instagramHandle?: string;
    /**
     * A list of insurance policies accepted by the healthcare provider
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    insuranceAccepted?: Array<string>;
    /**
     * Indicates whether the healthcare entity is the primary entity in its group
     */
    isClusterPrimary?: boolean;
    /**
     * The ISO 3166-2 region code for the entity
     *
     * Yext will determine the entity's code and update **`isoRegionCode`** with that value. If Yext is unable to determine the code for the entity, the entity'ss ISO 3166-1 alpha-2 country code will be used.
     */
    isoRegionCode?: string;
    /**
     * Keywords that describe the entity.
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    keywords?: Array<string>;
    /**
     * The IDs of the entity labels that have been added to this entity. Entity labels help you identify entities that share a certain characteristic; they do not appear on your entity's listings.
     *
     * **NOTE:** You can only add labels that have already been created via our web interface. Currently, it is not possible to create new labels via the API.
     */
    labels?: Array<string>;
    /**
     * The URL of this entity's Landing Page that was created with Yext Pages
     */
    landingPageUrl?: string;
    /**
     * The langauges in which consumers can commicate with this entity or its staff members
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    languages?: Array<string>;
    /**
     * The last name of the healthcare professional
     *
     *
     * Cannot Include:
     * * a URL or domain name
     * * HTML markup
     */
    lastName?: string;
    /**
     * URL for your LinkedIn account, format should be https://www.linkedin.com/in/yourUsername
     */
    linkedInUrl?: string;
    /**
     * Must be a valid, non-toll-free phone number, based on the country specified in **`address.region`**. Phone numbers for US entities must contain 10 digits.
     */
    localPhone?: string;
    /**
     * Indicates the entity's type, if it is not an event
     */
    locationType?: 'LOCATION' | 'HEALTHCARE_FACILITY' | 'HEALTHCARE_PROFESSIONAL' | 'ATM' | 'RESTAURANT' | 'HOTEL';
    /**
     * An image of the entity's logo
     *
     *
     * Supported Aspect Ratios:
     * * 1 x 1
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     */
    logo?: {
        clickthroughUrl?: string;
        description?: string;
        details?: string;
        /**
         *
         * Supported Aspect Ratios:
         * * 1 x 1
         *
         * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
         *
         */
        image: {
            alternateText?: string;
            url: string;
        };
    };
    /**
     * The main phone number of the entity's point of contact
     *
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    mainPhone?: string;
    /**
     * Information about the URL where consumers can view the entity's menu
     */
    menuUrl?: {
        /**
         * The URL that is shown on your listings in place of **`menuUrl.url`**. You can use **`menuUrl.displayUrl`** to display a short, memorable web address that redirects consumers to the URL given in **`menuUrl.url`**.
         */
        displayUrl?: string;
        /**
         * If set to true, only the display URL will be sent to those publishers who do not support separate display and tracking URLs for this field.
         */
        preferDisplayUrl?: boolean;
        /**
         * A valid URL where consumers can view the entity's menu
         */
        url?: string;
    };
    /**
     * The middle name of the healthcare professional
     *
     *
     * Cannot Include:
     * * a URL or domain name
     * * HTML markup
     */
    middleName?: string;
    /**
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    mobilePhone?: string;
    /**
     * The National Provider Identifier (NPI) of the healthcare provider
     */
    npi?: string;
    /**
     * Indicates whether Knowledge Nudge is enabled for the Yext Knowledge Assistant for this entity
     */
    nudgeEnabled?: boolean;
    /**
     * The name of the office where the healthcare professional works, if different from **`name`**
     */
    officeName?: string;
    /**
     * Contains the daily online service hours, holiday online service hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `onlineServiceHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday online service hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    onlineServiceHours?: {
        friday?: {
            /**
             * Indicates if the online service hours are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the online service hours are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the online service hours are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the online service hours are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the online service hours are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the online service hours are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the online service hours are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the online service hours are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * The date that the entity is set to open for the first time.
     * Must be formatted in YYYY-MM-DD format.
     *
     * Date must be on or after 1970-01-01 Date must be before or on 2038-01-01
     *
     */
    openDate?: string;
    /**
     * Information about the URL used to place orders that will be fulfilled by the entity
     */
    orderUrl?: {
        /**
         * The URL that is shown on your listings in place of **`orderUrl.url`**. You can use **`orderUrl.displayUrl`** to display a short, memorable web address that redirects consumers to the URL given in **`orderUrl.url`**.
         */
        displayUrl?: string;
        /**
         * If set to true, only the display URL will be sent to those publishers who do not support separate display and tracking URLs for this field.
         */
        preferDisplayUrl?: boolean;
        /**
         * A valid URL used to place an order at this entity
         */
        url?: string;
    };
    /**
     * The payment methods accepted by this entity
     *
     * Valid elements depend on the entity's country.
     *
     */
    paymentOptions?: Array<'AFTERPAY' | 'ALIPAY' | 'AMERICANEXPRESS' | 'ANDROIDPAY' | 'APPLEPAY' | 'ATM' | 'ATMQUICK' | 'BACS' | 'BANCONTACT' | 'BANKDEPOSIT' | 'BANKPAY' | 'BGO' | 'BITCOIN' | 'Bar' | 'CARTASI' | 'CASH' | 'CCS' | 'CHECK' | 'CHEQUESVACANCES' | 'CONB' | 'CONTACTLESSPAYME' | 'CVVV' | 'DEBITCARD' | 'DEBITNOTE' | 'DINERSCLUB' | 'DIRECTDEBIT' | 'DISCOVER' | 'ECKARTE' | 'ECOCHEQUE' | 'EKENA' | 'EMV' | 'FINANCING' | 'GIFTCARD' | 'GOPAY' | 'HAYAKAKEN' | 'HEBAG' | 'IBOD' | 'ICCARDS' | 'ICOCA' | 'ID' | 'IDEAL' | 'INCA' | 'INVOICE' | 'JCB' | 'JCoinPay' | 'JKOPAY' | 'KITACA' | 'KLA' | 'KLARNA' | 'LINEPAY' | 'MAESTRO' | 'MANACA' | 'MASTERCARD' | 'MIPAY' | 'MONIZZE' | 'MPAY' | 'Manuelle Lastsch' | 'Merpay' | 'NANACO' | 'NEXI' | 'NIMOCA' | 'OREM' | 'PASMO' | 'PAYBACKPAY' | 'PAYBOX' | 'PAYCONIQ' | 'PAYPAL' | 'PAYPAY' | 'PAYSEC' | 'PIN' | 'POSTEPAY' | 'QRCODE' | 'QUICPAY' | 'RAKUTENEDY' | 'RAKUTENPAY' | 'SAMSUNGPAY' | 'SODEXO' | 'SUGOCA' | 'SUICA' | 'SWISH' | 'TICKETRESTAURANT' | 'TOICA' | 'TRAVELERSCHECK' | 'TSCUBIC' | 'TWINT' | 'UNIONPAY' | 'VEV' | 'VISA' | 'VISAELECTRON' | 'VOB' | 'VOUCHER' | 'VPAY' | 'WAON' | 'WECHATPAY' | 'WIRETRANSFER' | 'Yucho Pay' | 'ZELLE' | 'auPay' | 'dBarai' | 'Überweisung'>;
    /**
     *
     * **NOTE:** The list of photos that you send us must be comprehensive. For example, if you send us a list of photos that does not include photos that you sent in your last update, Yext considers the missing photos to be deleted, and we remove them from your listings.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 500 elements.
     *
     * Array item description:
     *
     * >Supported Aspect Ratios:
     * >* 1 x 1
     * >* 4 x 3
     * >* 3 x 2
     * >* 5 x 3
     * >* 16 x 9
     * >* 3 x 1
     * >* 2 x 3
     * >* 5 x 7
     * >* 4 x 5
     * >* 4 x 1
     * >
     * >**NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     * >
     */
    photoGallery?: Array<{
        clickthroughUrl?: string;
        description?: string;
        details?: string;
        /**
         *
         * Supported Aspect Ratios:
         * * 1 x 1
         * * 4 x 3
         * * 3 x 2
         * * 5 x 3
         * * 16 x 9
         * * 3 x 1
         * * 2 x 3
         * * 5 x 7
         * * 4 x 5
         * * 4 x 1
         *
         * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
         *
         */
        image: {
            alternateText?: string;
            url: string;
        };
    }>;
    /**
     * Coordinates of where consumers can be picked up at the entity, as provided by you
     */
    pickupCoordinate?: {
        latitude?: number;
        longitude?: number;
    };
    /**
     * URL for your Pinterest account, format should be https://www.pinterest.com/yourUsername
     */
    pinterestUrl?: string;
    /**
     * he typical price of products sold by this location, on a scale of 1 (low) to 4 (high)
     */
    priceRange?: 'UNSPECIFIED' | 'ONE' | 'TWO' | 'THREE' | 'FOUR';
    /**
     * ID of the user who is the primary Knowledge Assistant contact for the entity
     */
    primaryConversationContact?: string;
    /**
     * Information about the Products & Services Content Lists associated with this entity
     */
    productLists?: {
        /**
         * IDs of the Products & Services Lists associated with this entity
         *
         *
         * Array must be ordered.
         *
         * Array may have a maximum of 40 elements.
         *
         */
        ids?: Array<string>;
        /**
         * Label to be used for this entity's Products & Services Lists. This label will appear on your entity's listings.
         */
        label?: string;
    };
    /**
     * Indicates whether Yext Knowledge Assistant question-and-answer conversations are enabled for this entity
     */
    questionsAndAnswers?: boolean;
    /**
     * Information about the competitors whose search performance you would like to compare to your own
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 5 elements.
     *
     */
    rankTrackingCompetitors?: Array<{
        /**
         * A name of a competitor
         *
         *
         * Cannot Include:
         * * HTML markup
         */
        name: string;
        /**
         * The business website of a competitor
         *
         *
         * Cannot Include:
         * * common domain names, e.g., google.com, youtube.com, etc.
         */
        website: string;
    }>;
    /**
     * Indicates whether Rank Tracking is enabled
     */
    rankTrackingEnabled?: boolean;
    /**
     * How often we send search queries to track your search performance
     */
    rankTrackingFrequency?: 'WEEKLY' | 'MONTHLY' | 'QUARTERLY';
    /**
     * The keywords for which you would like to track your search performance
     *
     */
    rankTrackingKeywords?: Array<'NAME' | 'PRIMARY_CATEGORY' | 'SECONDARY_CATEGORY'>;
    /**
     * The ways in which your keywords will be arranged in the search queries we use to track your performance
     *
     *
     * Array must have a minimum of 2 elements.
     *
     * Array may have a maximum of 4 elements.
     *
     */
    rankTrackingQueryTemplates?: Array<'KEYWORD' | 'KEYWORD_ZIP' | 'KEYWORD_CITY' | 'KEYWORD_IN_CITY' | 'KEYWORD_NEAR_ME' | 'KEYWORD_CITY_STATE'>;
    /**
     * The search engines that we will use to track your performance
     */
    rankTrackingSites?: Array<'GOOGLE_DESKTOP' | 'GOOGLE_MOBILE' | 'BING_DESKTOP' | 'BING_MOBILE' | 'YAHOO_DESKTOP' | 'YAHOO_MOBILE'>;
    /**
     * Information about the URL consumers can visit to make reservations at this entity
     */
    reservationUrl?: {
        /**
         * The URL that is shown on your listings in place of **`reservationUrl.url`**. You can use **`reservationUrl.displayUrl`** to display a short, memorable web address that redirects consumers to the URL given in **`reservationUrl.url`**.
         *
         * Must be a valid URL and be specified along with **`reservationUrl.url`**.
         */
        displayUrl?: string;
        /**
         * If set to true, only the display URL will be sent to those publishers who do not support separate display and tracking URLs for this field.
         */
        preferDisplayUrl?: boolean;
        /**
         * A valid URL used to make reservations at this entity
         */
        url?: string;
    };
    /**
     * The URL given Review Invitation emails where consumers can leave a review about the entity
     */
    reviewGenerationUrl?: string;
    /**
     * Indicates whether Yext Knowledge Assistant review-response conversations are enabled for this entity
     */
    reviewResponseConversationEnabled?: boolean;
    /**
     * Destination coordinates to use for driving directions to the entity, as provided by you
     */
    routableCoordinate?: {
        latitude?: number;
        longitude?: number;
    };
    /**
     * Information about the area that is served by this entity. It is specified as a list of cities and/or postal codes.
     *
     * **Only for Google Business Profile and Bing:** Currently, **serviceArea** is only supported by Google Business Profile and Bing and will not affect your listings on other sites.
     */
    serviceArea?: {
        /**
         * A list of places served by the entity, where each place is either:
         * - a postal code, or
         * - the name of a city.
         *
         *
         *
         * Array must be ordered.
         *
         * Array may have a maximum of 200 elements.
         *
         */
        places?: Array<string>;
    };
    /**
     * Information about the area that is served by this entity. It is specified as a list of service area names, their associated types and google place ids.
     * **Only for Google Business Profile and Bing:** Currently, **serviceArea** is only supported by Google Business Profile and Bing and will not affect your listings on other sites.
     *
     *
     * Array may have a maximum of 200 elements.
     *
     */
    serviceAreaPlaces?: Array<{
        name?: string;
        googlePlaceId?: string;
        type?: 'POSTAL_CODE' | 'REGION' | 'COUNTY' | 'CITY' | 'SUBLOCALITY';
    }>;
    /**
     * Services offered by this entity
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    services?: Array<string>;
    /**
     * URL for your TikTok profile, format should be https://www.tiktok.com/yourUsername
     */
    tikTokUrl?: string;
    /**
     * The timezone of the entity, in the standard `IANA time zone database` format (tz database). e.g. `"America/New_York"`
     */
    timezone?: string;
    /**
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    tollFreePhone?: string;
    /**
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    ttyPhone?: string;
    /**
     * Valid Twitter handle for the entity without the leading "@" (e.g., `JohnSmith`)
     *
     * If you submit an invalid Twitter handle, it will be ignored. The success response will contain a warning message explaining why your Twitter handle wasn't stored in the system.
     */
    twitterHandle?: string;
    /**
     * Information about the Yext-powered link that can be copied and pasted into the markup of Yext Pages where the embedded Uber link should appear
     */
    uberLink?: {
        /**
         * Indicates whether the embedded Uber link for this entity appears as text or a button
         *
         * When consumers click on this link on a mobile device, the Uber app (if installed) will open with your entity set as the trip destination. If the Uber app is not installed, the consumer will be prompted to download it.
         */
        presentation: 'BUTTON' | 'LINK';
        /**
         * The text of the embedded Uber link
         *
         * Default is `Ride there with Uber`.
         *
         * **NOTE:** This field is only available if **`uberLink.presentation`** is `LINK`.
         */
        text?: string;
    };
    /**
     * Information about the call-to-action consumers will see in the Uber app during a trip to your entity
     */
    uberTripBranding?: {
        /**
         * A longer description that will appear near the call-to-action in the Uber app during a trip to your entity.
         *
         * **NOTE:** If a value for **`uberTripBranding.description`** is provided, values must also be provided for **`uberTripBranding.text`** and **`uberTripBranding.url`**.
         */
        description: string;
        /**
         * The text of the call-to-action that will appear in the Uber app during a trip to your entity (e.g., `Check out our menu!`)
         *
         * **NOTE:** If a value for **`uberTripBranding.text`** is provided, values must also be provided for **`uberTripBranding.url`** and **`uberTripBranding.description`**.
         */
        text: string;
        /**
         * The URL that the consumer will be redirected to when tapping on the call-to-action in the Uber app during a trip to your entity.
         *
         * **NOTE:** If a value for **`uberTripBranding.url`** is provided, values must also be provided for **`uberTripBranding.text`** and **`uberTripBranding.description`**.
         */
        url: string;
    };
    /**
     * Valid YouTube URLs for embedding a video on some publisher sites
     *
     * **NOTE:** Currently, only the first URL in the Array appears in your listings.
     *
     *
     *
     * Array must be ordered.
     *
     */
    videos?: Array<{
        /**
         *
         * Cannot Include:
         * * HTML markup
         */
        description?: string;
        video: {
            url: string;
        };
    }>;
    /**
     * Destination coordinates to use for walking directions to the entity, as provided by you
     */
    walkableCoordinate?: {
        latitude?: number;
        longitude?: number;
    };
    /**
     * Information about the website for this entity
     */
    websiteUrl?: {
        /**
         * The URL that is shown on your listings in place of **`websiteUrl.url`**. You can use **`websiteUrl.displayUrl`** to display a short, memorable web address that redirects consumers to the URL given in **`websiteUrl.url`**.
         *
         * Must be a valid URL and be specified along with **`websiteUrl.url`**.
         */
        displayUrl?: string;
        /**
         * If set to true, only the display URL will be sent to those publishers who do not support separate display and tracking URLs for this field.
         */
        preferDisplayUrl?: boolean;
        /**
         * A valid URL for this entity's website
         */
        url?: string;
    };
    /**
     * The year the entity was established.
     */
    yearEstablished?: number;
    /**
     * URL for your YouTube channel, format should be https://www.youtube.com/c/yourUsername
     */
    youTubeChannelUrl?: string;
    EntityType?: 'healthcareProfessional';
};

export type HotelWrite = Omit<EntityWrite, 'EntityType'> & {
    /**
     * Contains the metadata about the entity.
     */
    meta?: {
        /**
         * Country code of this Entity's Language Profile (defaults to the country of the account)
         */
        countryCode?: string;
        /**
         * The ID of the folder containing this Entity
         */
        folderId?: string;
        /**
         * ID of this Entity
         */
        id?: string;
        /**
         * This Entity's labels. If the **`v`** parameter is before `20211215`, this will be an integer.
         */
        labels?: Array<string>;
        /**
         * Language code of this Entity's Language Profile (defaults to the language code of the account)
         */
        language?: string;
    };
    /**
     *
     *
     *
     * Cannot Include:
     * * HTML markup
     */
    name?: string;
    /**
     * Contains the address of the entity (or where the entity is located)
     *
     * Must be a valid address
     * Cannot be a P.O. Box
     *
     * If the entity is an `event`, either an **`address`** value or a **`linkedLocation`** value can be provided.
     */
    address?: {
        /**
         * The city the entity (or the entity's location) is in
         *
         *
         * Cannot Include:
         * * a URL or domain name
         */
        city?: string;
        countryCode?: string;
        /**
         * Provides additional information to help consumers get to the entity. This string appears along with the entity's address (e.g., `In Menlo Mall, 3rd Floor`).
         * It may also be used in conjunction with a hidden address (i.e., when **`addressHidden`** is `true`) to give consumers information about where the entity can be found (e.g., `Servicing the New York area`).
         */
        extraDescription?: string;
        /**
         *
         * Cannot Include:
         * * a URL or domain name
         */
        line1?: string;
        /**
         *
         * Cannot Include:
         * * a URL or domain name
         */
        line2?: string;
        /**
         * The entity's postal code. The postal code must be valid for the entity's country. Cannot include a URL or domain name.
         *
         *
         * Cannot Include:
         * * a URL or domain name
         */
        postalCode?: string;
        /**
         * The name of the entity's region or state.
         *
         *
         * Cannot Include:
         * * a URL or domain name
         */
        region?: string;
        /**
         * The name of the entity's sublocality
         *
         *
         * Cannot Include:
         * * a URL or domain name
         */
        sublocality?: string;
    };
    /**
     * Contains the daily access hours, holiday access hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `accessHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday access hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    accessHours?: {
        friday?: {
            /**
             * Indicates if the access hours are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the access hours are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the access hours are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the access hours are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the access hours are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the access hours are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the access hours are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the access hours are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * Additional information about hours that does not fit in **`hours`** (e.g., `"Closed during the winter"`)
     */
    additionalHoursText?: string;
    /**
     * If `true`, the entity's street address will not be shown on listings. Defaults to `false`.
     */
    addressHidden?: boolean;
    /**
     * Indicates whether the entity has a pool for adults only.
     */
    adultPool?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity offers a shuttle to/from the airport.
     */
    airportShuttle?: 'AIRPORT_SHUTTLE_AVAILABLE' | 'AIRPORT_SHUTTLE_AVAILABLE_FOR_FREE' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity offers a shuttle service of car service to/from nearby airports or train stations.
     */
    airportTransfer?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity offers all-inclusive rates.
     */
    allInclusive?: 'ALL_INCLUSIVE_RATES_AVAILABLE' | 'ALL_INCLUSIVE_RATES_ONLY' | 'NOT_APPLICABLE';
    /**
     * Other names for your business that you would like us to use when tracking your search performance
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 3 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    alternateNames?: Array<string>;
    /**
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    alternatePhone?: string;
    /**
     * Other websites for your business that we should search for when tracking your search performance
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 3 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* common domain names, e.g., google.com, youtube.com, etc.
     */
    alternateWebsites?: Array<string>;
    /**
     * The URL where consumers can download the entity's Android app
     */
    androidAppUrl?: string;
    /**
     * Use this field to add action links to your Apple Listings.
     *
     * The call to action category will be displayed on the action link button.
     *
     * The App Store URL should contain a valid link to the landing page of an App in the Apple App Store.
     *
     * The Quick Link URL is where a user is taken when an action link is clicked by a user.
     *
     * The App Name sub-field is not displayed on Apple Listings and is only used to distinguish the call-to-action type when utilizing action links in Apple posts.
     *
     *
     * Array must be ordered.
     *
     */
    appleActionLinks?: Array<{
        appName: string;
        appStoreUrl?: string;
        category: 'BOOK_TRAVEL' | 'CHECK_IN' | 'FEES_POLICIES' | 'FLIGHT_STATUS' | 'TICKETS' | 'TICKETING' | 'AMENITIES' | 'FRONT_DESK' | 'PARKING' | 'GIFT_CARD' | 'WAITLIST' | 'DELIVERY' | 'ORDER' | 'TAKEOUT' | 'PICKUP' | 'RESERVE' | 'MENU' | 'APPOINTMENT' | 'PORTFOLIO' | 'QUOTE' | 'SERVICES' | 'STORE_ORDERS' | 'STORE_SHOP' | 'STORE_SUPPORT' | 'SCHEDULE' | 'SHOWTIMES' | 'AVAILABILITY' | 'PRICING' | 'ACTIVITIES' | 'BOOK' | 'BOOK_(HOTEL)' | 'BOOK_(RIDE)' | 'BOOK_(TOUR)' | 'CAREERS' | 'CHARGE' | 'COUPONS' | 'DELIVERY_(RETAIL)' | 'DONATE' | 'EVENTS' | 'ORDER_(RETAIL)' | 'OTHER_MENU' | 'PICKUP_(RETAIL)' | 'RESERVE_(PARKING)' | 'SHOWS' | 'SPORTS' | 'SUPPORT' | 'TEE_TIME' | 'GIFT_CARD_(RESTAURANT)';
        quickLinkUrl: string;
    }>;
    /**
     * The business description to be sent to Apple
     */
    appleBusinessDescription?: string;
    /**
     * The ID associated with an individual Business Folder in your Apple account
     */
    appleBusinessId?: string;
    /**
     * The ID associated with your Apple account. Numerical values only
     */
    appleCompanyId?: string;
    /**
     * The cover photo for the entity''s Apple profile
     *
     *
     * Image must be at least 1600 x 1040 pixels
     *
     * Image may be no more than 4864 x 3163 pixels
     *
     * Supported Aspect Ratios:
     * * 154 x 100
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     */
    appleCoverPhoto?: {
        alternateText?: string;
        url: string;
    };
    /**
     * The name to be displayed on Apple for the entity.
     * NOTE: The names of Brands and their respective Locations within an Apple Business Connect Account must match identically.
     * Cannot Include:
     * HTML markup
     *
     *
     * Cannot Include:
     * * HTML markup
     */
    appleDisplayName?: string;
    /**
     * Association memberships relevant to the entity (e.g., `"New York Doctors Association"`)
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    associations?: Array<string>;
    /**
     * Indicates whether the entity offers babysitting.
     */
    babysittingOffered?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity offers baggage storage pre check-in and post check-out.
     */
    baggageStorage?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has an indoor or outdoor bar onsite.
     */
    bar?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has access to a beach.
     */
    beachAccess?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity is physically located next to a beach.
     */
    beachFrontProperty?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity offers bicycles for rent or for free.
     */
    bicycles?: 'BICYCLE_RENTALS' | 'BICYCLE_RENTALS_FOR_FREE' | 'NOT_APPLICABLE';
    /**
     * Information about the Bio Content Lists associated with this entity
     */
    bios?: {
        /**
         * IDs of the Bio Lists associated with this entity
         *
         *
         * Array must be ordered.
         *
         * Array may have a maximum of 40 elements.
         *
         */
        ids?: Array<string>;
        /**
         * Label to be used for this entity's Bio Lists. This label will appear on your entity's listings.
         */
        label?: string;
    };
    /**
     * Indicates whether the entity has a boutique store. Gift shop or convenience store are not eligible.
     */
    boutiqueStores?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Brands sold by this entity
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    brands?: Array<string>;
    /**
     * Indicates whether the entity offers breakfast.
     */
    breakfast?: 'BREAKFAST_AVAILABLE' | 'BREAKFAST_AVAILABLE_FOR_FREE' | 'NOT_APPLICABLE';
    /**
     * Contains the daily brunch hours, holiday brunch hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `brunchHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday brunch hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    brunchHours?: {
        friday?: {
            /**
             * Indicates if the brunch hours are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for brunch on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the brunch hours are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the brunch hours are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for brunch on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the brunch hours are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for brunch on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the brunch hours are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for brunch on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the brunch hours are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for brunch on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the brunch hours are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for brunch on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the brunch hours are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for brunch on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * Indicates whether the entity has a business center.
     */
    businessCenter?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Information about the events Content Lists (Calendars) associated with this entity
     */
    calendars?: {
        /**
         * IDs of the Calendars associated with this entity
         *
         *
         * Array must be ordered.
         *
         * Array may have a maximum of 40 elements.
         *
         */
        ids?: Array<string>;
        /**
         * Label to be used for this entity's Calendars. This label will appear on your entity's listings.
         */
        label?: string;
    };
    /**
     * Indicates whether the entity offers car rental.
     */
    carRental?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has a casino on premise or nearby.
     */
    casino?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Yext Category IDs
     *
     * IDs must be valid and selectable (i.e., cannot be parent categories).
     *
     * NOTE: The list of category IDs that you send us must be comprehensive. For example, if you send us a list of IDs that does not include IDs that you sent in your last update, Yext considers the missing categories to be deleted, and we remove them from your listings.
     *
     */
    categoryIds?: Array<string>;
    /**
     * Indicates if the entity is cat friendly.
     */
    catsAllowed?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * The check-in time
     */
    checkInTime?: string;
    /**
     * The check-out time
     */
    checkOutTime?: string;
    /**
     * The 1 to 5 star rating of the entitiy based on its services and facilities.
     */
    classificationRating?: string;
    /**
     * Indicates whether the entity is closed
     */
    closed?: boolean;
    /**
     * Indicates whether the entity offers concierge service.
     */
    concierge?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has a convenience store.
     */
    convenienceStore?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity offers currency exchange services.
     */
    currencyExchange?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Additional keywords you would like us to use when tracking your search performance
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 5 elements.
     *
     */
    customKeywords?: Array<string>;
    /**
     * A description of the entity
     *
     *
     * Cannot Include:
     * * HTML markup
     */
    description?: string;
    /**
     * Coordinates where the map pin for the entity should be displayed, as provided by you
     */
    displayCoordinate?: {
        latitude?: number;
        longitude?: number;
    };
    /**
     * Indicates whether the entity has a doctor on premise or on call.
     */
    doctorOnCall?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates if the entity is dog friendly.
     */
    dogsAllowed?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Coordinates of the drop-off area for the entity, as provided by you
     */
    dropoffCoordinate?: {
        latitude?: number;
        longitude?: number;
    };
    /**
     * Indicates whether the entity has electric car chargine stations on premise.
     */
    electricChargingStation?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has an elevator.
     */
    elevator?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has an elliptical machine.
     */
    ellipticalMachine?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Emails addresses for this entity's point of contact
     *
     * Must be valid email addresses
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 5 elements.
     *
     */
    emails?: Array<string>;
    /**
     * A description of the entity to be used in the "About You" section on Facebook
     */
    facebookAbout?: string;
    /**
     * Designates the Facebook Call-to-Action button text and value
     *
     * Valid contents of **`value`** depends on the Call-to-Action's **`type`**:
     * * `NONE`: (optional)
     * * `BOOK_NOW`: URL
     * * `CALL_NOW`: Phone number
     * * `CONTACT_US`: URL
     * * `SEND_MESSAGE`: Any string
     * * `USE_APP`: URL
     * * `PLAY_GAME`: URL
     * * `SHOP_NOW`: URL
     * * `SIGN_UP`: URL
     * * `WATCH_VIDEO`: URL
     * * `SEND_EMAIL`: Email address
     * * `LEARN_MORE`: URL
     * * `PURCHASE_GIFT_CARDS`: URL
     * * `ORDER_NOW`: URL
     * * `FOLLOW_PAGE`: Any string
     */
    facebookCallToAction?: {
        /**
         * The action the consumer is being prompted to take by the button's text
         */
        type: 'NONE' | 'BOOK_NOW' | 'CALL_NOW' | 'CONTACT_US' | 'SEND_MESSAGE' | 'USE_APP' | 'PLAY_GAME' | 'SHOP_NOW' | 'SIGN_UP' | 'WATCH_VIDEO' | 'SEND_EMAIL' | 'LEARN_MORE' | 'PURCHASE_GIFT_CARDS' | 'ORDER_NOW' | 'FOLLOW_PAGE';
        /**
         * Indicates where consumers will be directed to upon clicking the Call-to-Action button (e.g., a URL). It can be a free-form string or an embedded value, depending on what the user specifies.
         *
         * For example, if the user sets the Facebook Call-to-Action as " 'Sign Up' using 'Website URL' " in the Yext platform, **`type`** will be `SIGN_UP` and **`value`** will be `[[websiteUrl]]`.
         *
         * The Call-to-Action will have the same behavior if the user sets the value to "Custom Value" in the platform and embeds a field.
         */
        value?: string;
    };
    /**
     * The cover photo for the entity's Facebook profile
     *
     * Displayed as a 851 x 315 pixel image
     *
     * You may need a cover photo in order for your listing to appear on Facebook. Please check your listings tab to learn more.
     *
     *
     * Image must be at least 400 x 150 pixels
     *
     * Image area (width x height) may be no more than 41000000 pixels
     *
     * Image may be no more than 30000 x 30000 pixels
     *
     * Supported Aspect Ratios:
     * * 1 x 1
     * * 4 x 3
     * * 3 x 2
     * * 5 x 3
     * * 16 x 9
     * * 3 x 1
     * * 2 x 3
     * * 5 x 7
     * * 4 x 5
     * * 4 x 1
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     */
    facebookCoverPhoto?: {
        alternateText?: string;
        url: string;
    };
    /**
     * Location Descriptors are used for Enterprise businesses that sync Facebook listings using brand page location structure. The Location Descriptor is typically an additional geographic description (e.g. geomodifier) that will appear in parentheses after the name on the Facebook listing.
     *
     *
     * Cannot Include:
     * * HTML markup
     */
    facebookDescriptor?: string;
    /**
     * The name for this entity's Facebook profile.  A separate name may be specified to send only to Facebook in order to comply with any specific Facebook rules or naming conventions.
     */
    facebookName?: string;
    /**
     * The city to be displayed on this entity's Facebook profile
     */
    facebookOverrideCity?: string;
    /**
     * URL for the entity's Facebook Page.
     *
     * Valid formats:
     *
     * - facebook.com/profile.php?id=[numId]
     * - facebook.com/group.php?gid=[numId]
     * - facebook.com/groups/[numId]
     * - facebook.com/[Name]
     * - facebook.com/pages/[Name]/[numId]
     * - facebook.com/people/[Name]/[numId]
     *
     * where [Name] is a String and [numId] is an Integer
     * The success response will contain a warning message explaining why the URL wasn't stored in the system.
     */
    facebookPageUrl?: string;
    /**
     * The Facebook Page ID of this entity's brand page if in a brand page location structure
     */
    facebookParentPageId?: string;
    /**
     * The profile picture for the entity's Facebook profile
     *
     * You must have a profile picture in order for your listing to appear on Facebook.
     *
     *
     * Image must be at least 180 x 180 pixels
     *
     * Image area (width x height) may be no more than 41000000 pixels
     *
     * Image may be no more than 30000 x 30000 pixels
     *
     * Supported Aspect Ratios:
     * * 1 x 1
     * * 4 x 3
     * * 3 x 2
     * * 5 x 3
     * * 16 x 9
     * * 3 x 1
     * * 2 x 3
     * * 5 x 7
     * * 4 x 5
     * * 4 x 1
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     */
    facebookProfilePhoto?: {
        alternateText?: string;
        url: string;
    };
    /**
     * The Store ID used for this entity in a brand page location structure
     */
    facebookStoreId?: string;
    /**
     * The username that appear's in the Facebook listing URL to help customers find and remember a brand’s Facebook page.  The username is also be used for tagging the Facebook page in other users’ posts, and searching for the Facebook page.
     */
    facebookVanityUrl?: string;
    /**
     * The URL you would like to submit to Facebook in place of the one given in **`websiteUrl`** (if applicable).
     */
    facebookWebsiteOverride?: string;
    /**
     * Must be a valid fax number.
     *
     * If the fax number's calling code is for a country other than the one given in the entity's **`countryCode`**, the fax number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    fax?: string;
    /**
     * Information about the entity's Featured Message
     */
    featuredMessage?: {
        /**
         * The text of Featured Message. Default: `Call today!`
         *
         * Cannot include:
         *
         * - inappropriate language
         * - HTML markup
         * - a URL or domain name
         * - a phone number
         * - control characters ([\x00-\x1F\x7F])
         * - insufficient spacing
         *
         * If you submit a Featured Message that contains profanity or more than 50 characters, it will be ignored. The success response will contain a warning message explaining why your Featured Message wasn't stored in the system.
         *
         *
         * Cannot Include:
         * * HTML markup
         */
        description?: string;
        /**
         * Valid URL linked to the Featured Message text
         */
        url?: string;
    };
    /**
     * Link to the review-collection page, where consumers can leave first-party reviews
     */
    firstPartyReviewPage?: string;
    /**
     * Indicates whether the entity has a fitness center.
     */
    fitnessCenter?: 'FITNESS_CENTER_AVAILABLE' | 'FITNESS_CENTER_AVAILABLE_FOR_FREE' | 'NOT_APPLICABLE';
    /**
     * The number of floors the entity has from ground floor to top floor.
     */
    floorCount?: number;
    /**
     * Indicates whether the entity has free weights.
     */
    freeWeights?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * A list of questions that are frequently asked about this entity
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     */
    frequentlyAskedQuestions?: Array<{
        answer?: string;
        question: string;
    }>;
    /**
     * Indicates whether the entity has a front desk.
     */
    frontDesk?: 'FRONT_DESK_AVAILABLE' | 'FRONT_DESK_AVAILABLE_24_HOURS' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the staff is vaccinated against COVID-19.
     */
    fullyVaccinatedStaff?: boolean;
    /**
     * Indicates whether the entity has a game room.
     */
    gameRoom?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Provides additional information on where the entity can be found (e.g., `Times Square`, `Global Center Mall`)
     */
    geomodifier?: string;
    /**
     * Indicates whether the entity has a gift shop.
     */
    giftShop?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has a golf couse on premise or nearby. The golf course may be independently run.
     */
    golf?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * The unique IDs of the entity's Google Business Profile keywords, as well as the unique IDs of any values selected for each keyword.
     *
     * Valid keywords (e.g., `has_drive_through`, `has_fitting_room`, `kitchen_in_room`) are determined by the entity's primary category. A full list of keywords can be retrieved with the Google Fields: List endpoint.
     *
     * Keyword values provide more details on how the keyword applies to the entity (e.g., if the keyword is `has_drive_through`, its values may be `true` or `false`).
     *
     * * If the **`v`** parameter is before `20181204`: **`googleAttributes`** is formatted as a map of key-value pairs (e.g., `[{ "id": "has_wheelchair_accessible_entrance", "values": [ "true" ] }]`)
     * * If the **`v`** parameter is on or after `20181204`: the contents are formatted as a list of objects (e.g., `{ "has_wheelchair_accessible_entrance": [ "true" ]}`)
     *
     * **NOTE:** The latest Google Attributes are available via the Google Fields: List endpoint. Google Attributes are managed by Google and are subject to change without notice. To prevent errors, make sure your API implementation is not dependent on the presence of specific attributes.
     */
    googleAttributes?: {};
    /**
     * The cover photo for the entity's Google profile
     *
     *
     * Image must be at least 250 x 250 pixels
     *
     */
    googleCoverPhoto?: {
        alternateText?: string;
        url: string;
    };
    /**
     * Information about Google Messaging, WhatsApp and SMS, for the entity’s point of contact for messaging/chat functionality.
     *
     * NOTE: Only one, either WhatsApp or SMS is displayed on the Google listing. If both SMS Number and WhatsApp URL are provided only SMS Number will be displayed on the listing.
     */
    googleMessaging?: {
        /**
         * The SMS phone number of the entity's point of contact for messaging/ chat functionality.
         * Must be a valid phone number.
         * If the phone number's calling code is for a country other than the one given in the entity's countryCode, the phone number provided must contain the calling code (e.g., +44 in +442038083831). Otherwise, the calling code is optional.
         */
        smsNumber?: string;
        /**
         * A valid URL for this entity's WhatsApp account.
         * Must be a valid URL
         *
         */
        whatsappMessagingUrl?: string;
    };
    /**
     * Google Business Profile Labels help users organize their locations into groups within GBP.
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 10 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    googleMyBusinessLabels?: Array<string>;
    /**
     * The unique identifier of this entity on Google Maps.
     */
    googlePlaceId?: string;
    /**
     * The profile photo for the entity's Google profile
     *
     *
     * Image must be at least 250 x 250 pixels
     *
     * Image may be no more than 5000 x 5000 pixels
     *
     * Supported Aspect Ratios:
     * * 1 x 1
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     */
    googleProfilePhoto?: {
        alternateText?: string;
        url: string;
    };
    /**
     * The URL you would like to submit to Google Business Profile in place of the one given in **`websiteUrl`** (if applicable).
     *
     * For example, if you want to analyze the traffic driven by your Google listings separately from other traffic, enter the alternate URL that you will use for tracking in this field.
     */
    googleWebsiteOverride?: string;
    /**
     * Contains the daily happy hours, holiday happy hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `happyHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday happy hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    happyHours?: {
        friday?: {
            /**
             * Indicates if the happy hours are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's happy hours on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the happy hours are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for the Entity's happy hours on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the happy hours are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's happy hours on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the happy hours are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's happy hours on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the happy hours are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's happy hours on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the happy hours are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's happy hours on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the happy hours are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's happy hours on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the happy hours are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's happy hours on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * Indicates whether holiday-hour confirmation alerts are enabled for the Yext Knowledge Assistant for this entity
     */
    holidayHoursConversationEnabled?: boolean;
    /**
     * Indicates whether the entity offers horseback riding.
     */
    horsebackRiding?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has a hot tub.
     */
    hotTub?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Contains the daily hours, holiday hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `hours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    hours?: {
        friday?: {
            /**
             * Indicates if the hours of operation are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the hours of operation are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the hours of operation are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the hours of operation are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the hours of operation are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the hours of operation are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the hours of operation are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the hours of operation are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * Indicates whether the entity offers housekeeping services.
     */
    housekeeping?: 'HOUSEKEEPING_AVAILABLE' | 'HOUSEKEEPING_AVAILABLE_DAILY' | 'NOT_APPLICABLE';
    /**
     * A statement of the ownership and authorship of a document. Individuals or organizations based in many German-speaking countries are required by law to include an Impressum in published media.
     */
    impressum?: string;
    /**
     * A count of the number of indoor pools
     */
    indoorPoolCount?: number;
    /**
     * Valid Instagram username for the entity without the leading "@" (e.g., `NewCityAuto`)
     */
    instagramHandle?: string;
    /**
     * The URL where consumers can download the entity's app to their iPhone or iPad
     */
    iosAppUrl?: string;
    /**
     * The ISO 3166-2 region code for the entity
     *
     * Yext will determine the entity's code and update **`isoRegionCode`** with that value. If Yext is unable to determine the code for the entity, the entity'ss ISO 3166-1 alpha-2 country code will be used.
     */
    isoRegionCode?: string;
    /**
     * Keywords that describe the entity.
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    keywords?: Array<string>;
    /**
     * Indicates if the entity is kid friendly.
     */
    kidFriendly?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates if the property has a Kids Club.
     */
    kidsClub?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity allows kids to stay free.
     */
    kidsStayFree?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Contains the daily kitchen hours, holiday kitchen hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `kitchenHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday kitchen hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    kitchenHours?: {
        friday?: {
            /**
             * Indicates if the kitchen hours are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen is open on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the kitchen hours are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen is open on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the kitchen hours are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen is open on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the kitchen hours are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen is open on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the kitchen hours are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen is open on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the kitchen hours are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen is open on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the kitchen hours are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen open on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the kitchen hours are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen is open on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * The IDs of the entity labels that have been added to this entity. Entity labels help you identify entities that share a certain characteristic; they do not appear on your entity's listings.
     *
     * **NOTE:** You can only add labels that have already been created via our web interface. Currently, it is not possible to create new labels via the API.
     */
    labels?: Array<string>;
    /**
     * The URL of this entity's Landing Page that was created with Yext Pages
     */
    landingPageUrl?: string;
    /**
     * The langauges in which consumers can commicate with this entity or its staff members
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    languages?: Array<string>;
    /**
     * Indicates whether the entity offers laundry services.
     */
    laundry?: 'FULL_SERVICE' | 'SELF_SERVICE' | 'NOT_APPLICABLE';
    /**
     * Indicates if the property has a lazy river
     */
    lazyRiver?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates if the property has a lifeguard on duty
     */
    lifeguard?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * URL for your LinkedIn account, format should be https://www.linkedin.com/in/yourUsername
     */
    linkedInUrl?: string;
    /**
     * Must be a valid, non-toll-free phone number, based on the country specified in **`address.region`**. Phone numbers for US entities must contain 10 digits.
     */
    localPhone?: string;
    /**
     * Indicates whether the entity offers local shuttle services.
     */
    localShuttle?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates the entity's type, if it is not an event
     */
    locationType?: 'LOCATION' | 'HEALTHCARE_FACILITY' | 'HEALTHCARE_PROFESSIONAL' | 'ATM' | 'RESTAURANT' | 'HOTEL';
    /**
     * An image of the entity's logo
     *
     *
     * Supported Aspect Ratios:
     * * 1 x 1
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     */
    logo?: {
        clickthroughUrl?: string;
        description?: string;
        details?: string;
        /**
         *
         * Supported Aspect Ratios:
         * * 1 x 1
         *
         * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
         *
         */
        image: {
            alternateText?: string;
            url: string;
        };
    };
    /**
     * The main phone number of the entity's point of contact
     *
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    mainPhone?: string;
    /**
     * Indicates whether the entity offers massage services.
     */
    massage?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * The maximum age specified by the property for children to stay in the room/suite of a parent or adult without an additional fee
     */
    maxAgeOfKidsStayFree?: number;
    /**
     * The maximum number of children who can stay in the room/suite of a parent or adult without an additional fee
     */
    maxNumberOfKidsStayFree?: number;
    /**
     * The number of meeting rooms the entity has.
     */
    meetingRoomCount?: number;
    /**
     * Information about the URL where consumers can view the entity's menu
     */
    menuUrl?: {
        /**
         * The URL that is shown on your listings in place of **`menuUrl.url`**. You can use **`menuUrl.displayUrl`** to display a short, memorable web address that redirects consumers to the URL given in **`menuUrl.url`**.
         */
        displayUrl?: string;
        /**
         * If set to true, only the display URL will be sent to those publishers who do not support separate display and tracking URLs for this field.
         */
        preferDisplayUrl?: boolean;
        /**
         * A valid URL where consumers can view the entity's menu
         */
        url?: string;
    };
    /**
     * Information about the Menu Content Lists associated with this entity
     */
    menus?: {
        /**
         * IDs of the Menu Lists associated with this entity
         *
         *
         * Array must be ordered.
         *
         * Array may have a maximum of 40 elements.
         *
         */
        ids?: Array<string>;
        /**
         * Label to be used for this entity's Menu Lists. This label will appear on your entity's listings.
         */
        label?: string;
    };
    /**
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    mobilePhone?: string;
    /**
     * Indicates whether the entity is mobility/wheelchair accessible
     */
    mobilityAccessible?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has a nightclub.
     */
    nightclub?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether Knowledge Nudge is enabled for the Yext Knowledge Assistant for this entity
     */
    nudgeEnabled?: boolean;
    /**
     * The date that the entity is set to open for the first time.
     * Must be formatted in YYYY-MM-DD format.
     *
     * Date must be on or after 1970-01-01 Date must be before or on 2038-01-01
     *
     */
    openDate?: string;
    /**
     * Information about the URL used to place orders that will be fulfilled by the entity
     */
    orderUrl?: {
        /**
         * The URL that is shown on your listings in place of **`orderUrl.url`**. You can use **`orderUrl.displayUrl`** to display a short, memorable web address that redirects consumers to the URL given in **`orderUrl.url`**.
         */
        displayUrl?: string;
        /**
         * If set to true, only the display URL will be sent to those publishers who do not support separate display and tracking URLs for this field.
         */
        preferDisplayUrl?: boolean;
        /**
         * A valid URL used to place an order at this entity
         */
        url?: string;
    };
    /**
     * The number of outdoor pools the entity has.
     */
    outdoorPoolCount?: number;
    /**
     * Indicates whether the entity offers parking services.
     */
    parking?: 'PARKING_AVAILABLE' | 'PARKING_AVAILABLE_FOR_FREE' | 'NOT_APPLICABLE';
    /**
     * The payment methods accepted by this entity
     *
     * Valid elements depend on the entity's country.
     *
     */
    paymentOptions?: Array<'AFTERPAY' | 'ALIPAY' | 'AMERICANEXPRESS' | 'ANDROIDPAY' | 'APPLEPAY' | 'ATM' | 'ATMQUICK' | 'BACS' | 'BANCONTACT' | 'BANKDEPOSIT' | 'BANKPAY' | 'BGO' | 'BITCOIN' | 'Bar' | 'CARTASI' | 'CASH' | 'CCS' | 'CHECK' | 'CHEQUESVACANCES' | 'CONB' | 'CONTACTLESSPAYME' | 'CVVV' | 'DEBITCARD' | 'DEBITNOTE' | 'DINERSCLUB' | 'DIRECTDEBIT' | 'DISCOVER' | 'ECKARTE' | 'ECOCHEQUE' | 'EKENA' | 'EMV' | 'FINANCING' | 'GIFTCARD' | 'GOPAY' | 'HAYAKAKEN' | 'HEBAG' | 'IBOD' | 'ICCARDS' | 'ICOCA' | 'ID' | 'IDEAL' | 'INCA' | 'INVOICE' | 'JCB' | 'JCoinPay' | 'JKOPAY' | 'KITACA' | 'KLA' | 'KLARNA' | 'LINEPAY' | 'MAESTRO' | 'MANACA' | 'MASTERCARD' | 'MIPAY' | 'MONIZZE' | 'MPAY' | 'Manuelle Lastsch' | 'Merpay' | 'NANACO' | 'NEXI' | 'NIMOCA' | 'OREM' | 'PASMO' | 'PAYBACKPAY' | 'PAYBOX' | 'PAYCONIQ' | 'PAYPAL' | 'PAYPAY' | 'PAYSEC' | 'PIN' | 'POSTEPAY' | 'QRCODE' | 'QUICPAY' | 'RAKUTENEDY' | 'RAKUTENPAY' | 'SAMSUNGPAY' | 'SODEXO' | 'SUGOCA' | 'SUICA' | 'SWISH' | 'TICKETRESTAURANT' | 'TOICA' | 'TRAVELERSCHECK' | 'TSCUBIC' | 'TWINT' | 'UNIONPAY' | 'VEV' | 'VISA' | 'VISAELECTRON' | 'VOB' | 'VOUCHER' | 'VPAY' | 'WAON' | 'WECHATPAY' | 'WIRETRANSFER' | 'Yucho Pay' | 'ZELLE' | 'auPay' | 'dBarai' | 'Überweisung'>;
    /**
     * Indicates if the entity is pet friendly.
     */
    petsAllowed?: 'PETS_WELCOME' | 'PETS_WELCOME_FOR_FREE' | 'NOT_APPLICABLE' | 'NOT_ALLOWED';
    /**
     *
     * **NOTE:** The list of photos that you send us must be comprehensive. For example, if you send us a list of photos that does not include photos that you sent in your last update, Yext considers the missing photos to be deleted, and we remove them from your listings.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 500 elements.
     *
     * Array item description:
     *
     * >Supported Aspect Ratios:
     * >* 1 x 1
     * >* 4 x 3
     * >* 3 x 2
     * >* 5 x 3
     * >* 16 x 9
     * >* 3 x 1
     * >* 2 x 3
     * >* 5 x 7
     * >* 4 x 5
     * >* 4 x 1
     * >
     * >**NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     * >
     */
    photoGallery?: Array<{
        clickthroughUrl?: string;
        description?: string;
        details?: string;
        /**
         *
         * Supported Aspect Ratios:
         * * 1 x 1
         * * 4 x 3
         * * 3 x 2
         * * 5 x 3
         * * 16 x 9
         * * 3 x 1
         * * 2 x 3
         * * 5 x 7
         * * 4 x 5
         * * 4 x 1
         *
         * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
         *
         */
        image: {
            alternateText?: string;
            url: string;
        };
    }>;
    /**
     * Coordinates of where consumers can be picked up at the entity, as provided by you
     */
    pickupCoordinate?: {
        latitude?: number;
        longitude?: number;
    };
    /**
     * URL for your Pinterest account, format should be https://www.pinterest.com/yourUsername
     */
    pinterestUrl?: string;
    /**
     * ID of the user who is the primary Knowledge Assistant contact for the entity
     */
    primaryConversationContact?: string;
    /**
     * Indicates whether the entity has access to a private beach.
     */
    privateBeach?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity offers private car services.
     */
    privateCarService?: 'PRIVATE_CAR_SERVICE' | 'PRIVATE_CAR_SERVICE_FOR_FREE' | 'NOT_APPLICABLE';
    /**
     * Information about the Products & Services Content Lists associated with this entity
     */
    productLists?: {
        /**
         * IDs of the Products & Services Lists associated with this entity
         *
         *
         * Array must be ordered.
         *
         * Array may have a maximum of 40 elements.
         *
         */
        ids?: Array<string>;
        /**
         * Label to be used for this entity's Products & Services Lists. This label will appear on your entity's listings.
         */
        label?: string;
    };
    /**
     * Indicates whether Yext Knowledge Assistant question-and-answer conversations are enabled for this entity
     */
    questionsAndAnswers?: boolean;
    /**
     * Information about the competitors whose search performance you would like to compare to your own
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 5 elements.
     *
     */
    rankTrackingCompetitors?: Array<{
        /**
         * A name of a competitor
         *
         *
         * Cannot Include:
         * * HTML markup
         */
        name: string;
        /**
         * The business website of a competitor
         *
         *
         * Cannot Include:
         * * common domain names, e.g., google.com, youtube.com, etc.
         */
        website: string;
    }>;
    /**
     * Indicates whether Rank Tracking is enabled
     */
    rankTrackingEnabled?: boolean;
    /**
     * How often we send search queries to track your search performance
     */
    rankTrackingFrequency?: 'WEEKLY' | 'MONTHLY' | 'QUARTERLY';
    /**
     * The keywords for which you would like to track your search performance
     *
     */
    rankTrackingKeywords?: Array<'NAME' | 'PRIMARY_CATEGORY' | 'SECONDARY_CATEGORY'>;
    /**
     * The ways in which your keywords will be arranged in the search queries we use to track your performance
     *
     *
     * Array must have a minimum of 2 elements.
     *
     * Array may have a maximum of 4 elements.
     *
     */
    rankTrackingQueryTemplates?: Array<'KEYWORD' | 'KEYWORD_ZIP' | 'KEYWORD_CITY' | 'KEYWORD_IN_CITY' | 'KEYWORD_NEAR_ME' | 'KEYWORD_CITY_STATE'>;
    /**
     * The search engines that we will use to track your performance
     */
    rankTrackingSites?: Array<'GOOGLE_DESKTOP' | 'GOOGLE_MOBILE' | 'BING_DESKTOP' | 'BING_MOBILE' | 'YAHOO_DESKTOP' | 'YAHOO_MOBILE'>;
    /**
     * Information about the URL consumers can visit to make reservations at this entity
     */
    reservationUrl?: {
        /**
         * The URL that is shown on your listings in place of **`reservationUrl.url`**. You can use **`reservationUrl.displayUrl`** to display a short, memorable web address that redirects consumers to the URL given in **`reservationUrl.url`**.
         *
         * Must be a valid URL and be specified along with **`reservationUrl.url`**.
         */
        displayUrl?: string;
        /**
         * If set to true, only the display URL will be sent to those publishers who do not support separate display and tracking URLs for this field.
         */
        preferDisplayUrl?: boolean;
        /**
         * A valid URL used to make reservations at this entity
         */
        url?: string;
    };
    /**
     * The number of restaurants the entity has.
     */
    restaurantCount?: number;
    /**
     * The URL given Review Invitation emails where consumers can leave a review about the entity
     */
    reviewGenerationUrl?: string;
    /**
     * Indicates whether Yext Knowledge Assistant review-response conversations are enabled for this entity
     */
    reviewResponseConversationEnabled?: boolean;
    /**
     * The number of rooms the entity has.
     */
    roomCount?: number;
    /**
     * Indicates whether the entity offers room service.
     */
    roomService?: 'ROOM_SERVICE_AVAILABLE' | 'ROOM_SERVICE_AVAILABLE_24_HOURS' | 'NOT_APPLICABLE';
    /**
     * Destination coordinates to use for driving directions to the entity, as provided by you
     */
    routableCoordinate?: {
        latitude?: number;
        longitude?: number;
    };
    /**
     * Indicates whether the entity has a salon.
     */
    salon?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has a sauna.
     */
    sauna?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity offers scuba diving.
     */
    scuba?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity offers self parking services.
     */
    selfParking?: 'SELF_PARKING_AVAILABLE' | 'SELF_PARKING_AVAILABLE_FOR_FREE' | 'NOT_APPLICABLE';
    /**
     * Information about the area that is served by this entity. It is specified as a list of cities and/or postal codes.
     *
     * **Only for Google Business Profile and Bing:** Currently, **serviceArea** is only supported by Google Business Profile and Bing and will not affect your listings on other sites.
     */
    serviceArea?: {
        /**
         * A list of places served by the entity, where each place is either:
         * - a postal code, or
         * - the name of a city.
         *
         *
         *
         * Array must be ordered.
         *
         * Array may have a maximum of 200 elements.
         *
         */
        places?: Array<string>;
    };
    /**
     * Information about the area that is served by this entity. It is specified as a list of service area names, their associated types and google place ids.
     * **Only for Google Business Profile and Bing:** Currently, **serviceArea** is only supported by Google Business Profile and Bing and will not affect your listings on other sites.
     *
     *
     * Array may have a maximum of 200 elements.
     *
     */
    serviceAreaPlaces?: Array<{
        name?: string;
        googlePlaceId?: string;
        type?: 'POSTAL_CODE' | 'REGION' | 'COUNTY' | 'CITY' | 'SUBLOCALITY';
    }>;
    /**
     * Services offered by this entity
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    services?: Array<string>;
    /**
     * Indicates if the entity is smoke free.
     */
    smokeFreeProperty?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity offers snorkeling.
     */
    snorkeling?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity offers a social hour.
     */
    socialHour?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has a spa.
     */
    spa?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has a sit-down restaurant.
     */
    tableService?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has tennis courts.
     */
    tennis?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has a thermal pool.
     */
    thermalPool?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * URL for your TikTok profile, format should be https://www.tiktok.com/yourUsername
     */
    tikTokUrl?: string;
    /**
     * The timezone of the entity, in the standard `IANA time zone database` format (tz database). e.g. `"America/New_York"`
     */
    timezone?: string;
    /**
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    tollFreePhone?: string;
    /**
     * Indicates whether the entity has a treadmill.
     */
    treadmill?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    ttyPhone?: string;
    /**
     * Indicates whether the entity offers turndown service.
     */
    turndownService?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Valid Twitter handle for the entity without the leading "@" (e.g., `JohnSmith`)
     *
     * If you submit an invalid Twitter handle, it will be ignored. The success response will contain a warning message explaining why your Twitter handle wasn't stored in the system.
     */
    twitterHandle?: string;
    /**
     * Information about the Yext-powered link that can be copied and pasted into the markup of Yext Pages where the embedded Uber link should appear
     */
    uberLink?: {
        /**
         * Indicates whether the embedded Uber link for this entity appears as text or a button
         *
         * When consumers click on this link on a mobile device, the Uber app (if installed) will open with your entity set as the trip destination. If the Uber app is not installed, the consumer will be prompted to download it.
         */
        presentation: 'BUTTON' | 'LINK';
        /**
         * The text of the embedded Uber link
         *
         * Default is `Ride there with Uber`.
         *
         * **NOTE:** This field is only available if **`uberLink.presentation`** is `LINK`.
         */
        text?: string;
    };
    /**
     * Information about the call-to-action consumers will see in the Uber app during a trip to your entity
     */
    uberTripBranding?: {
        /**
         * A longer description that will appear near the call-to-action in the Uber app during a trip to your entity.
         *
         * **NOTE:** If a value for **`uberTripBranding.description`** is provided, values must also be provided for **`uberTripBranding.text`** and **`uberTripBranding.url`**.
         */
        description: string;
        /**
         * The text of the call-to-action that will appear in the Uber app during a trip to your entity (e.g., `Check out our menu!`)
         *
         * **NOTE:** If a value for **`uberTripBranding.text`** is provided, values must also be provided for **`uberTripBranding.url`** and **`uberTripBranding.description`**.
         */
        text: string;
        /**
         * The URL that the consumer will be redirected to when tapping on the call-to-action in the Uber app during a trip to your entity.
         *
         * **NOTE:** If a value for **`uberTripBranding.url`** is provided, values must also be provided for **`uberTripBranding.text`** and **`uberTripBranding.description`**.
         */
        url: string;
    };
    /**
     * Indicates whether the entity offers valet parking services.
     */
    valetParking?: 'VALET_PARKING_AVAILABLE' | 'VALET_PARKING_AVAILABLE_FOR_FREE' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has a vending machine.
     */
    vendingMachine?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Valid YouTube URLs for embedding a video on some publisher sites
     *
     * **NOTE:** Currently, only the first URL in the Array appears in your listings.
     *
     *
     *
     * Array must be ordered.
     *
     */
    videos?: Array<{
        /**
         *
         * Cannot Include:
         * * HTML markup
         */
        description?: string;
        video: {
            url: string;
        };
    }>;
    /**
     * Indicates whether the entity has a wading pool.
     */
    wadingPool?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity offers wake up call services.
     */
    wakeUpCalls?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Destination coordinates to use for walking directions to the entity, as provided by you
     */
    walkableCoordinate?: {
        latitude?: number;
        longitude?: number;
    };
    /**
     * Indicates whether the entity has a water park.
     */
    waterPark?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity offers water skiing.
     */
    waterSkiing?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity offers any kind of watercrafts.
     */
    watercraft?: 'WATERCRAFT_RENTALS' | 'WATERCRAFT_RENTALS_FOR_FREE' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has a water slide.
     */
    waterslide?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has a wave pool.
     */
    wavePool?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Information about the website for this entity
     */
    websiteUrl?: {
        /**
         * The URL that is shown on your listings in place of **`websiteUrl.url`**. You can use **`websiteUrl.displayUrl`** to display a short, memorable web address that redirects consumers to the URL given in **`websiteUrl.url`**.
         *
         * Must be a valid URL and be specified along with **`websiteUrl.url`**.
         */
        displayUrl?: string;
        /**
         * If set to true, only the display URL will be sent to those publishers who do not support separate display and tracking URLs for this field.
         */
        preferDisplayUrl?: boolean;
        /**
         * A valid URL for this entity's website
         */
        url?: string;
    };
    /**
     * Indicates whether the entity has a weight machine.
     */
    weightMachine?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates if the entity is wheelchair accessible.
     */
    wheelchairAccessible?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has WiFi available
     */
    wifiAvailable?: 'WIFI_AVAILABLE' | 'WIFI_AVAILABLE_FOR_FREE' | 'NOT_APPLICABLE';
    /**
     * The year the entity was established.
     */
    yearEstablished?: number;
    /**
     * The most recent year the entity was partially or completely renovated.
     */
    yearLastRenovated?: number;
    /**
     * URL for your YouTube channel, format should be https://www.youtube.com/c/yourUsername
     */
    youTubeChannelUrl?: string;
    EntityType?: 'hotel';
};

export type LocationWrite = Omit<EntityWrite, 'EntityType'> & {
    /**
     * Contains the metadata about the entity.
     */
    meta?: {
        /**
         * Country code of this Entity's Language Profile (defaults to the country of the account)
         */
        countryCode?: string;
        /**
         * The ID of the folder containing this Entity
         */
        folderId?: string;
        /**
         * ID of this Entity
         */
        id?: string;
        /**
         * This Entity's labels. If the **`v`** parameter is before `20211215`, this will be an integer.
         */
        labels?: Array<string>;
        /**
         * Language code of this Entity's Language Profile (defaults to the language code of the account)
         */
        language?: string;
    };
    /**
     *
     *
     *
     * Cannot Include:
     * * HTML markup
     */
    name?: string;
    /**
     * Contains the address of the entity (or where the entity is located)
     *
     * Must be a valid address
     * Cannot be a P.O. Box
     *
     * If the entity is an `event`, either an **`address`** value or a **`linkedLocation`** value can be provided.
     */
    address?: {
        /**
         * The city the entity (or the entity's location) is in
         *
         *
         * Cannot Include:
         * * a URL or domain name
         */
        city?: string;
        countryCode?: string;
        /**
         * Provides additional information to help consumers get to the entity. This string appears along with the entity's address (e.g., `In Menlo Mall, 3rd Floor`).
         * It may also be used in conjunction with a hidden address (i.e., when **`addressHidden`** is `true`) to give consumers information about where the entity can be found (e.g., `Servicing the New York area`).
         */
        extraDescription?: string;
        /**
         *
         * Cannot Include:
         * * a URL or domain name
         */
        line1?: string;
        /**
         *
         * Cannot Include:
         * * a URL or domain name
         */
        line2?: string;
        /**
         * The entity's postal code. The postal code must be valid for the entity's country. Cannot include a URL or domain name.
         *
         *
         * Cannot Include:
         * * a URL or domain name
         */
        postalCode?: string;
        /**
         * The name of the entity's region or state.
         *
         *
         * Cannot Include:
         * * a URL or domain name
         */
        region?: string;
        /**
         * The name of the entity's sublocality
         *
         *
         * Cannot Include:
         * * a URL or domain name
         */
        sublocality?: string;
    };
    /**
     * Contains the daily access hours, holiday access hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `accessHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday access hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    accessHours?: {
        friday?: {
            /**
             * Indicates if the access hours are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the access hours are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the access hours are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the access hours are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the access hours are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the access hours are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the access hours are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the access hours are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * Additional information about hours that does not fit in **`hours`** (e.g., `"Closed during the winter"`)
     */
    additionalHoursText?: string;
    /**
     * If `true`, the entity's street address will not be shown on listings. Defaults to `false`.
     */
    addressHidden?: boolean;
    /**
     * Other names for your business that you would like us to use when tracking your search performance
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 3 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    alternateNames?: Array<string>;
    /**
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    alternatePhone?: string;
    /**
     * Other websites for your business that we should search for when tracking your search performance
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 3 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* common domain names, e.g., google.com, youtube.com, etc.
     */
    alternateWebsites?: Array<string>;
    /**
     * The URL where consumers can download the entity's Android app
     */
    androidAppUrl?: string;
    /**
     * Use this field to add action links to your Apple Listings.
     *
     * The call to action category will be displayed on the action link button.
     *
     * The App Store URL should contain a valid link to the landing page of an App in the Apple App Store.
     *
     * The Quick Link URL is where a user is taken when an action link is clicked by a user.
     *
     * The App Name sub-field is not displayed on Apple Listings and is only used to distinguish the call-to-action type when utilizing action links in Apple posts.
     *
     *
     * Array must be ordered.
     *
     */
    appleActionLinks?: Array<{
        appName: string;
        appStoreUrl?: string;
        category: 'BOOK_TRAVEL' | 'CHECK_IN' | 'FEES_POLICIES' | 'FLIGHT_STATUS' | 'TICKETS' | 'TICKETING' | 'AMENITIES' | 'FRONT_DESK' | 'PARKING' | 'GIFT_CARD' | 'WAITLIST' | 'DELIVERY' | 'ORDER' | 'TAKEOUT' | 'PICKUP' | 'RESERVE' | 'MENU' | 'APPOINTMENT' | 'PORTFOLIO' | 'QUOTE' | 'SERVICES' | 'STORE_ORDERS' | 'STORE_SHOP' | 'STORE_SUPPORT' | 'SCHEDULE' | 'SHOWTIMES' | 'AVAILABILITY' | 'PRICING' | 'ACTIVITIES' | 'BOOK' | 'BOOK_(HOTEL)' | 'BOOK_(RIDE)' | 'BOOK_(TOUR)' | 'CAREERS' | 'CHARGE' | 'COUPONS' | 'DELIVERY_(RETAIL)' | 'DONATE' | 'EVENTS' | 'ORDER_(RETAIL)' | 'OTHER_MENU' | 'PICKUP_(RETAIL)' | 'RESERVE_(PARKING)' | 'SHOWS' | 'SPORTS' | 'SUPPORT' | 'TEE_TIME' | 'GIFT_CARD_(RESTAURANT)';
        quickLinkUrl: string;
    }>;
    /**
     * The business description to be sent to Apple
     */
    appleBusinessDescription?: string;
    /**
     * The ID associated with an individual Business Folder in your Apple account
     */
    appleBusinessId?: string;
    /**
     * The ID associated with your Apple account. Numerical values only
     */
    appleCompanyId?: string;
    /**
     * The cover photo for the entity''s Apple profile
     *
     *
     * Image must be at least 1600 x 1040 pixels
     *
     * Image may be no more than 4864 x 3163 pixels
     *
     * Supported Aspect Ratios:
     * * 154 x 100
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     */
    appleCoverPhoto?: {
        alternateText?: string;
        url: string;
    };
    /**
     * The name to be displayed on Apple for the entity.
     * NOTE: The names of Brands and their respective Locations within an Apple Business Connect Account must match identically.
     * Cannot Include:
     * HTML markup
     *
     *
     * Cannot Include:
     * * HTML markup
     */
    appleDisplayName?: string;
    /**
     * Association memberships relevant to the entity (e.g., `"New York Doctors Association"`)
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    associations?: Array<string>;
    /**
     * Information about the Bio Content Lists associated with this entity
     */
    bios?: {
        /**
         * IDs of the Bio Lists associated with this entity
         *
         *
         * Array must be ordered.
         *
         * Array may have a maximum of 40 elements.
         *
         */
        ids?: Array<string>;
        /**
         * Label to be used for this entity's Bio Lists. This label will appear on your entity's listings.
         */
        label?: string;
    };
    /**
     * Brands sold by this entity
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    brands?: Array<string>;
    /**
     * Contains the daily brunch hours, holiday brunch hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `brunchHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday brunch hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    brunchHours?: {
        friday?: {
            /**
             * Indicates if the brunch hours are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for brunch on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the brunch hours are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the brunch hours are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for brunch on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the brunch hours are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for brunch on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the brunch hours are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for brunch on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the brunch hours are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for brunch on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the brunch hours are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for brunch on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the brunch hours are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for brunch on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * Information about the events Content Lists (Calendars) associated with this entity
     */
    calendars?: {
        /**
         * IDs of the Calendars associated with this entity
         *
         *
         * Array must be ordered.
         *
         * Array may have a maximum of 40 elements.
         *
         */
        ids?: Array<string>;
        /**
         * Label to be used for this entity's Calendars. This label will appear on your entity's listings.
         */
        label?: string;
    };
    /**
     * Yext Category IDs
     *
     * IDs must be valid and selectable (i.e., cannot be parent categories).
     *
     * NOTE: The list of category IDs that you send us must be comprehensive. For example, if you send us a list of IDs that does not include IDs that you sent in your last update, Yext considers the missing categories to be deleted, and we remove them from your listings.
     *
     */
    categoryIds?: Array<string>;
    /**
     * Indicates whether the entity is closed
     */
    closed?: boolean;
    /**
     * Information or messaging related to COVID-19.
     */
    covidMessaging?: string;
    /**
     * An appointment URL for scheduling a COVID-19 test.
     */
    covidTestAppointmentUrl?: string;
    /**
     * Indicates whether an appointment is required for a COVID-19 test.
     */
    covidTestingAppointmentRequired?: boolean;
    /**
     * Indicates whether location is a drive-through site for COVID-19 tests.
     */
    covidTestingDriveThroughSite?: boolean;
    /**
     * Indicates whether location offers free COVID-19 testing.
     */
    covidTestingIsFree?: boolean;
    /**
     * Indicates whether there are patient restrictions for COVID-19 testing.
     */
    covidTestingPatientRestrictions?: boolean;
    /**
     * Indicates whether a referral is required for COVID-19 testing.
     */
    covidTestingReferralRequired?: boolean;
    /**
     * Information or instructions for the COVID-19 testing site.
     */
    covidTestingSiteInstructions?: string;
    /**
     * Indicates whether an appointment is required for a COVID-19 vaccine.
     */
    covidVaccineAppointmentRequired?: boolean;
    /**
     * Indicates whether location is a drive-through site for COVID-19 vaccines.
     */
    covidVaccineDriveThroughSite?: boolean;
    /**
     * An information URL for more information about COVID-19 vaccines.
     */
    covidVaccineInformationUrl?: string;
    /**
     * Indicates whether there are patient restrictions for a COVID-19 vaccine.
     */
    covidVaccinePatientRestrictions?: boolean;
    /**
     * Indicates whether a referral is required for a COVID-19 vaccine.
     */
    covidVaccineReferralRequired?: boolean;
    /**
     * Information or instructions for the COVID-19 vaccination site.
     */
    covidVaccineSiteInstructions?: string;
    /**
     * Indicates which COVID-19 vaccines the location offers.
     */
    covidVaccinesOffered?: Array<'PFIZER' | 'MODERNA' | 'JOHNSON_&_JOHNSON'>;
    /**
     * Additional keywords you would like us to use when tracking your search performance
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 5 elements.
     *
     */
    customKeywords?: Array<string>;
    /**
     * Contains the daily delivery hours, holiday delivery hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `deliveryHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday delivery hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    deliveryHours?: {
        friday?: {
            /**
             * Indicates if the delivery hours are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is delivering on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the delivery hours are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity is delivering on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the delivery hours are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is delivering on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the delivery hours are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is delivering on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the delivery hours are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is delivering on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the delivery hours are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is delivering on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the delivery hours are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is delivering on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the delivery hours are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is delivering on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * A description of the entity
     *
     *
     * Cannot Include:
     * * HTML markup
     */
    description?: string;
    /**
     * Coordinates where the map pin for the entity should be displayed, as provided by you
     */
    displayCoordinate?: {
        latitude?: number;
        longitude?: number;
    };
    /**
     * Contains the daily drive-through hours, holiday drive-through hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `driveThroughHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday drive-through hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    driveThroughHours?: {
        friday?: {
            /**
             * Indicates if the drive-through hours are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the drive-through hours are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the drive-through hours are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the drive-through hours are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the drive-through hours are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the drive-through hours are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the drive-through hours are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the drive-through hours are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * Coordinates of the drop-off area for the entity, as provided by you
     */
    dropoffCoordinate?: {
        latitude?: number;
        longitude?: number;
    };
    /**
     * Emails addresses for this entity's point of contact
     *
     * Must be valid email addresses
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 5 elements.
     *
     */
    emails?: Array<string>;
    /**
     * A description of the entity to be used in the "About You" section on Facebook
     */
    facebookAbout?: string;
    /**
     * Designates the Facebook Call-to-Action button text and value
     *
     * Valid contents of **`value`** depends on the Call-to-Action's **`type`**:
     * * `NONE`: (optional)
     * * `BOOK_NOW`: URL
     * * `CALL_NOW`: Phone number
     * * `CONTACT_US`: URL
     * * `SEND_MESSAGE`: Any string
     * * `USE_APP`: URL
     * * `PLAY_GAME`: URL
     * * `SHOP_NOW`: URL
     * * `SIGN_UP`: URL
     * * `WATCH_VIDEO`: URL
     * * `SEND_EMAIL`: Email address
     * * `LEARN_MORE`: URL
     * * `PURCHASE_GIFT_CARDS`: URL
     * * `ORDER_NOW`: URL
     * * `FOLLOW_PAGE`: Any string
     */
    facebookCallToAction?: {
        /**
         * The action the consumer is being prompted to take by the button's text
         */
        type: 'NONE' | 'BOOK_NOW' | 'CALL_NOW' | 'CONTACT_US' | 'SEND_MESSAGE' | 'USE_APP' | 'PLAY_GAME' | 'SHOP_NOW' | 'SIGN_UP' | 'WATCH_VIDEO' | 'SEND_EMAIL' | 'LEARN_MORE' | 'PURCHASE_GIFT_CARDS' | 'ORDER_NOW' | 'FOLLOW_PAGE';
        /**
         * Indicates where consumers will be directed to upon clicking the Call-to-Action button (e.g., a URL). It can be a free-form string or an embedded value, depending on what the user specifies.
         *
         * For example, if the user sets the Facebook Call-to-Action as " 'Sign Up' using 'Website URL' " in the Yext platform, **`type`** will be `SIGN_UP` and **`value`** will be `[[websiteUrl]]`.
         *
         * The Call-to-Action will have the same behavior if the user sets the value to "Custom Value" in the platform and embeds a field.
         */
        value?: string;
    };
    /**
     * The cover photo for the entity's Facebook profile
     *
     * Displayed as a 851 x 315 pixel image
     *
     * You may need a cover photo in order for your listing to appear on Facebook. Please check your listings tab to learn more.
     *
     *
     * Image must be at least 400 x 150 pixels
     *
     * Image area (width x height) may be no more than 41000000 pixels
     *
     * Image may be no more than 30000 x 30000 pixels
     *
     * Supported Aspect Ratios:
     * * 1 x 1
     * * 4 x 3
     * * 3 x 2
     * * 5 x 3
     * * 16 x 9
     * * 3 x 1
     * * 2 x 3
     * * 5 x 7
     * * 4 x 5
     * * 4 x 1
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     */
    facebookCoverPhoto?: {
        alternateText?: string;
        url: string;
    };
    /**
     * Location Descriptors are used for Enterprise businesses that sync Facebook listings using brand page location structure. The Location Descriptor is typically an additional geographic description (e.g. geomodifier) that will appear in parentheses after the name on the Facebook listing.
     *
     *
     * Cannot Include:
     * * HTML markup
     */
    facebookDescriptor?: string;
    /**
     * The name for this entity's Facebook profile.  A separate name may be specified to send only to Facebook in order to comply with any specific Facebook rules or naming conventions.
     */
    facebookName?: string;
    /**
     * The city to be displayed on this entity's Facebook profile
     */
    facebookOverrideCity?: string;
    /**
     * URL for the entity's Facebook Page.
     *
     * Valid formats:
     *
     * - facebook.com/profile.php?id=[numId]
     * - facebook.com/group.php?gid=[numId]
     * - facebook.com/groups/[numId]
     * - facebook.com/[Name]
     * - facebook.com/pages/[Name]/[numId]
     * - facebook.com/people/[Name]/[numId]
     *
     * where [Name] is a String and [numId] is an Integer
     * The success response will contain a warning message explaining why the URL wasn't stored in the system.
     */
    facebookPageUrl?: string;
    /**
     * The Facebook Page ID of this entity's brand page if in a brand page location structure
     */
    facebookParentPageId?: string;
    /**
     * The profile picture for the entity's Facebook profile
     *
     * You must have a profile picture in order for your listing to appear on Facebook.
     *
     *
     * Image must be at least 180 x 180 pixels
     *
     * Image area (width x height) may be no more than 41000000 pixels
     *
     * Image may be no more than 30000 x 30000 pixels
     *
     * Supported Aspect Ratios:
     * * 1 x 1
     * * 4 x 3
     * * 3 x 2
     * * 5 x 3
     * * 16 x 9
     * * 3 x 1
     * * 2 x 3
     * * 5 x 7
     * * 4 x 5
     * * 4 x 1
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     */
    facebookProfilePhoto?: {
        alternateText?: string;
        url: string;
    };
    /**
     * The Store ID used for this entity in a brand page location structure
     */
    facebookStoreId?: string;
    /**
     * The username that appear's in the Facebook listing URL to help customers find and remember a brand’s Facebook page.  The username is also be used for tagging the Facebook page in other users’ posts, and searching for the Facebook page.
     */
    facebookVanityUrl?: string;
    /**
     * The URL you would like to submit to Facebook in place of the one given in **`websiteUrl`** (if applicable).
     */
    facebookWebsiteOverride?: string;
    /**
     * Must be a valid fax number.
     *
     * If the fax number's calling code is for a country other than the one given in the entity's **`countryCode`**, the fax number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    fax?: string;
    /**
     * Information about the entity's Featured Message
     */
    featuredMessage?: {
        /**
         * The text of Featured Message. Default: `Call today!`
         *
         * Cannot include:
         *
         * - inappropriate language
         * - HTML markup
         * - a URL or domain name
         * - a phone number
         * - control characters ([\x00-\x1F\x7F])
         * - insufficient spacing
         *
         * If you submit a Featured Message that contains profanity or more than 50 characters, it will be ignored. The success response will contain a warning message explaining why your Featured Message wasn't stored in the system.
         *
         *
         * Cannot Include:
         * * HTML markup
         */
        description?: string;
        /**
         * Valid URL linked to the Featured Message text
         */
        url?: string;
    };
    /**
     * Link to the review-collection page, where consumers can leave first-party reviews
     */
    firstPartyReviewPage?: string;
    /**
     * A list of questions that are frequently asked about this entity
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     */
    frequentlyAskedQuestions?: Array<{
        answer?: string;
        question: string;
    }>;
    /**
     * Indicates whether the staff is vaccinated against COVID-19.
     */
    fullyVaccinatedStaff?: boolean;
    /**
     * Provides additional information on where the entity can be found (e.g., `Times Square`, `Global Center Mall`)
     */
    geomodifier?: string;
    /**
     * The unique IDs of the entity's Google Business Profile keywords, as well as the unique IDs of any values selected for each keyword.
     *
     * Valid keywords (e.g., `has_drive_through`, `has_fitting_room`, `kitchen_in_room`) are determined by the entity's primary category. A full list of keywords can be retrieved with the Google Fields: List endpoint.
     *
     * Keyword values provide more details on how the keyword applies to the entity (e.g., if the keyword is `has_drive_through`, its values may be `true` or `false`).
     *
     * * If the **`v`** parameter is before `20181204`: **`googleAttributes`** is formatted as a map of key-value pairs (e.g., `[{ "id": "has_wheelchair_accessible_entrance", "values": [ "true" ] }]`)
     * * If the **`v`** parameter is on or after `20181204`: the contents are formatted as a list of objects (e.g., `{ "has_wheelchair_accessible_entrance": [ "true" ]}`)
     *
     * **NOTE:** The latest Google Attributes are available via the Google Fields: List endpoint. Google Attributes are managed by Google and are subject to change without notice. To prevent errors, make sure your API implementation is not dependent on the presence of specific attributes.
     */
    googleAttributes?: {};
    /**
     * The cover photo for the entity's Google profile
     *
     *
     * Image must be at least 250 x 250 pixels
     *
     */
    googleCoverPhoto?: {
        alternateText?: string;
        url: string;
    };
    /**
     * Information about Google Messaging, WhatsApp and SMS, for the entity’s point of contact for messaging/chat functionality.
     *
     * NOTE: Only one, either WhatsApp or SMS is displayed on the Google listing. If both SMS Number and WhatsApp URL are provided only SMS Number will be displayed on the listing.
     */
    googleMessaging?: {
        /**
         * The SMS phone number of the entity's point of contact for messaging/ chat functionality.
         * Must be a valid phone number.
         * If the phone number's calling code is for a country other than the one given in the entity's countryCode, the phone number provided must contain the calling code (e.g., +44 in +442038083831). Otherwise, the calling code is optional.
         */
        smsNumber?: string;
        /**
         * A valid URL for this entity's WhatsApp account.
         * Must be a valid URL
         *
         */
        whatsappMessagingUrl?: string;
    };
    /**
     * Google Business Profile Labels help users organize their locations into groups within GBP.
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 10 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    googleMyBusinessLabels?: Array<string>;
    /**
     * The unique identifier of this entity on Google Maps.
     */
    googlePlaceId?: string;
    /**
     * The profile photo for the entity's Google profile
     *
     *
     * Image must be at least 250 x 250 pixels
     *
     * Image may be no more than 5000 x 5000 pixels
     *
     * Supported Aspect Ratios:
     * * 1 x 1
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     */
    googleProfilePhoto?: {
        alternateText?: string;
        url: string;
    };
    /**
     * The URL you would like to submit to Google Business Profile in place of the one given in **`websiteUrl`** (if applicable).
     *
     * For example, if you want to analyze the traffic driven by your Google listings separately from other traffic, enter the alternate URL that you will use for tracking in this field.
     */
    googleWebsiteOverride?: string;
    /**
     * Contains the daily happy hours, holiday happy hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `happyHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday happy hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    happyHours?: {
        friday?: {
            /**
             * Indicates if the happy hours are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's happy hours on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the happy hours are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for the Entity's happy hours on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the happy hours are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's happy hours on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the happy hours are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's happy hours on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the happy hours are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's happy hours on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the happy hours are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's happy hours on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the happy hours are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's happy hours on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the happy hours are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's happy hours on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * Indicates whether holiday-hour confirmation alerts are enabled for the Yext Knowledge Assistant for this entity
     */
    holidayHoursConversationEnabled?: boolean;
    /**
     * Contains the daily hours, holiday hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `hours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    hours?: {
        friday?: {
            /**
             * Indicates if the hours of operation are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the hours of operation are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the hours of operation are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the hours of operation are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the hours of operation are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the hours of operation are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the hours of operation are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the hours of operation are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * A statement of the ownership and authorship of a document. Individuals or organizations based in many German-speaking countries are required by law to include an Impressum in published media.
     */
    impressum?: string;
    /**
     * Valid Instagram username for the entity without the leading "@" (e.g., `NewCityAuto`)
     */
    instagramHandle?: string;
    /**
     * The URL where consumers can download the entity's app to their iPhone or iPad
     */
    iosAppUrl?: string;
    /**
     * The ISO 3166-2 region code for the entity
     *
     * Yext will determine the entity's code and update **`isoRegionCode`** with that value. If Yext is unable to determine the code for the entity, the entity'ss ISO 3166-1 alpha-2 country code will be used.
     */
    isoRegionCode?: string;
    /**
     * Keywords that describe the entity.
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    keywords?: Array<string>;
    /**
     * Contains the daily kitchen hours, holiday kitchen hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `kitchenHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday kitchen hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    kitchenHours?: {
        friday?: {
            /**
             * Indicates if the kitchen hours are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen is open on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the kitchen hours are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen is open on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the kitchen hours are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen is open on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the kitchen hours are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen is open on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the kitchen hours are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen is open on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the kitchen hours are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen is open on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the kitchen hours are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen open on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the kitchen hours are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen is open on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * The IDs of the entity labels that have been added to this entity. Entity labels help you identify entities that share a certain characteristic; they do not appear on your entity's listings.
     *
     * **NOTE:** You can only add labels that have already been created via our web interface. Currently, it is not possible to create new labels via the API.
     */
    labels?: Array<string>;
    /**
     * The URL of this entity's Landing Page that was created with Yext Pages
     */
    landingPageUrl?: string;
    /**
     * The langauges in which consumers can commicate with this entity or its staff members
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    languages?: Array<string>;
    /**
     * URL for your LinkedIn account, format should be https://www.linkedin.com/in/yourUsername
     */
    linkedInUrl?: string;
    /**
     * Must be a valid, non-toll-free phone number, based on the country specified in **`address.region`**. Phone numbers for US entities must contain 10 digits.
     */
    localPhone?: string;
    /**
     * Indicates the entity's type, if it is not an event
     */
    locationType?: 'LOCATION' | 'HEALTHCARE_FACILITY' | 'HEALTHCARE_PROFESSIONAL' | 'ATM' | 'RESTAURANT' | 'HOTEL';
    /**
     * An image of the entity's logo
     *
     *
     * Supported Aspect Ratios:
     * * 1 x 1
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     */
    logo?: {
        clickthroughUrl?: string;
        description?: string;
        details?: string;
        /**
         *
         * Supported Aspect Ratios:
         * * 1 x 1
         *
         * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
         *
         */
        image: {
            alternateText?: string;
            url: string;
        };
    };
    /**
     * The main phone number of the entity's point of contact
     *
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    mainPhone?: string;
    /**
     * Information about the URL where consumers can view the entity's menu
     */
    menuUrl?: {
        /**
         * The URL that is shown on your listings in place of **`menuUrl.url`**. You can use **`menuUrl.displayUrl`** to display a short, memorable web address that redirects consumers to the URL given in **`menuUrl.url`**.
         */
        displayUrl?: string;
        /**
         * If set to true, only the display URL will be sent to those publishers who do not support separate display and tracking URLs for this field.
         */
        preferDisplayUrl?: boolean;
        /**
         * A valid URL where consumers can view the entity's menu
         */
        url?: string;
    };
    /**
     * Information about the Menu Content Lists associated with this entity
     */
    menus?: {
        /**
         * IDs of the Menu Lists associated with this entity
         *
         *
         * Array must be ordered.
         *
         * Array may have a maximum of 40 elements.
         *
         */
        ids?: Array<string>;
        /**
         * Label to be used for this entity's Menu Lists. This label will appear on your entity's listings.
         */
        label?: string;
    };
    /**
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    mobilePhone?: string;
    /**
     * Indicates whether Knowledge Nudge is enabled for the Yext Knowledge Assistant for this entity
     */
    nudgeEnabled?: boolean;
    /**
     * Contains the daily online service hours, holiday online service hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `onlineServiceHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday online service hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    onlineServiceHours?: {
        friday?: {
            /**
             * Indicates if the online service hours are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the online service hours are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the online service hours are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the online service hours are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the online service hours are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the online service hours are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the online service hours are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the online service hours are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * The date that the entity is set to open for the first time.
     * Must be formatted in YYYY-MM-DD format.
     *
     * Date must be on or after 1970-01-01 Date must be before or on 2038-01-01
     *
     */
    openDate?: string;
    /**
     * Information about the URL used to place orders that will be fulfilled by the entity
     */
    orderUrl?: {
        /**
         * The URL that is shown on your listings in place of **`orderUrl.url`**. You can use **`orderUrl.displayUrl`** to display a short, memorable web address that redirects consumers to the URL given in **`orderUrl.url`**.
         */
        displayUrl?: string;
        /**
         * If set to true, only the display URL will be sent to those publishers who do not support separate display and tracking URLs for this field.
         */
        preferDisplayUrl?: boolean;
        /**
         * A valid URL used to place an order at this entity
         */
        url?: string;
    };
    /**
     * The payment methods accepted by this entity
     *
     * Valid elements depend on the entity's country.
     *
     */
    paymentOptions?: Array<'AFTERPAY' | 'ALIPAY' | 'AMERICANEXPRESS' | 'ANDROIDPAY' | 'APPLEPAY' | 'ATM' | 'ATMQUICK' | 'BACS' | 'BANCONTACT' | 'BANKDEPOSIT' | 'BANKPAY' | 'BGO' | 'BITCOIN' | 'Bar' | 'CARTASI' | 'CASH' | 'CCS' | 'CHECK' | 'CHEQUESVACANCES' | 'CONB' | 'CONTACTLESSPAYME' | 'CVVV' | 'DEBITCARD' | 'DEBITNOTE' | 'DINERSCLUB' | 'DIRECTDEBIT' | 'DISCOVER' | 'ECKARTE' | 'ECOCHEQUE' | 'EKENA' | 'EMV' | 'FINANCING' | 'GIFTCARD' | 'GOPAY' | 'HAYAKAKEN' | 'HEBAG' | 'IBOD' | 'ICCARDS' | 'ICOCA' | 'ID' | 'IDEAL' | 'INCA' | 'INVOICE' | 'JCB' | 'JCoinPay' | 'JKOPAY' | 'KITACA' | 'KLA' | 'KLARNA' | 'LINEPAY' | 'MAESTRO' | 'MANACA' | 'MASTERCARD' | 'MIPAY' | 'MONIZZE' | 'MPAY' | 'Manuelle Lastsch' | 'Merpay' | 'NANACO' | 'NEXI' | 'NIMOCA' | 'OREM' | 'PASMO' | 'PAYBACKPAY' | 'PAYBOX' | 'PAYCONIQ' | 'PAYPAL' | 'PAYPAY' | 'PAYSEC' | 'PIN' | 'POSTEPAY' | 'QRCODE' | 'QUICPAY' | 'RAKUTENEDY' | 'RAKUTENPAY' | 'SAMSUNGPAY' | 'SODEXO' | 'SUGOCA' | 'SUICA' | 'SWISH' | 'TICKETRESTAURANT' | 'TOICA' | 'TRAVELERSCHECK' | 'TSCUBIC' | 'TWINT' | 'UNIONPAY' | 'VEV' | 'VISA' | 'VISAELECTRON' | 'VOB' | 'VOUCHER' | 'VPAY' | 'WAON' | 'WECHATPAY' | 'WIRETRANSFER' | 'Yucho Pay' | 'ZELLE' | 'auPay' | 'dBarai' | 'Überweisung'>;
    /**
     *
     * **NOTE:** The list of photos that you send us must be comprehensive. For example, if you send us a list of photos that does not include photos that you sent in your last update, Yext considers the missing photos to be deleted, and we remove them from your listings.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 500 elements.
     *
     * Array item description:
     *
     * >Supported Aspect Ratios:
     * >* 1 x 1
     * >* 4 x 3
     * >* 3 x 2
     * >* 5 x 3
     * >* 16 x 9
     * >* 3 x 1
     * >* 2 x 3
     * >* 5 x 7
     * >* 4 x 5
     * >* 4 x 1
     * >
     * >**NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     * >
     */
    photoGallery?: Array<{
        clickthroughUrl?: string;
        description?: string;
        details?: string;
        /**
         *
         * Supported Aspect Ratios:
         * * 1 x 1
         * * 4 x 3
         * * 3 x 2
         * * 5 x 3
         * * 16 x 9
         * * 3 x 1
         * * 2 x 3
         * * 5 x 7
         * * 4 x 5
         * * 4 x 1
         *
         * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
         *
         */
        image: {
            alternateText?: string;
            url: string;
        };
    }>;
    /**
     * Coordinates of where consumers can be picked up at the entity, as provided by you
     */
    pickupCoordinate?: {
        latitude?: number;
        longitude?: number;
    };
    /**
     * Contains the daily pickup hours, holiday pickup hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `pickupHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday pickup hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    pickupHours?: {
        friday?: {
            /**
             * Indicates if the pickup hours are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the pickup hours are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the pickup hours are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the pickup hours are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the pickup hours are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the pickup hours are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the pickup hours are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the pickup hours are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * URL for your Pinterest account, format should be https://www.pinterest.com/yourUsername
     */
    pinterestUrl?: string;
    /**
     * he typical price of products sold by this location, on a scale of 1 (low) to 4 (high)
     */
    priceRange?: 'UNSPECIFIED' | 'ONE' | 'TWO' | 'THREE' | 'FOUR';
    /**
     * ID of the user who is the primary Knowledge Assistant contact for the entity
     */
    primaryConversationContact?: string;
    /**
     * Information about the Products & Services Content Lists associated with this entity
     */
    productLists?: {
        /**
         * IDs of the Products & Services Lists associated with this entity
         *
         *
         * Array must be ordered.
         *
         * Array may have a maximum of 40 elements.
         *
         */
        ids?: Array<string>;
        /**
         * Label to be used for this entity's Products & Services Lists. This label will appear on your entity's listings.
         */
        label?: string;
    };
    /**
     * Products sold by this entity
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    products?: Array<string>;
    /**
     * Indicates whether Yext Knowledge Assistant question-and-answer conversations are enabled for this entity
     */
    questionsAndAnswers?: boolean;
    /**
     * Information about the competitors whose search performance you would like to compare to your own
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 5 elements.
     *
     */
    rankTrackingCompetitors?: Array<{
        /**
         * A name of a competitor
         *
         *
         * Cannot Include:
         * * HTML markup
         */
        name: string;
        /**
         * The business website of a competitor
         *
         *
         * Cannot Include:
         * * common domain names, e.g., google.com, youtube.com, etc.
         */
        website: string;
    }>;
    /**
     * Indicates whether Rank Tracking is enabled
     */
    rankTrackingEnabled?: boolean;
    /**
     * How often we send search queries to track your search performance
     */
    rankTrackingFrequency?: 'WEEKLY' | 'MONTHLY' | 'QUARTERLY';
    /**
     * The keywords for which you would like to track your search performance
     *
     */
    rankTrackingKeywords?: Array<'NAME' | 'PRIMARY_CATEGORY' | 'SECONDARY_CATEGORY'>;
    /**
     * The ways in which your keywords will be arranged in the search queries we use to track your performance
     *
     *
     * Array must have a minimum of 2 elements.
     *
     * Array may have a maximum of 4 elements.
     *
     */
    rankTrackingQueryTemplates?: Array<'KEYWORD' | 'KEYWORD_ZIP' | 'KEYWORD_CITY' | 'KEYWORD_IN_CITY' | 'KEYWORD_NEAR_ME' | 'KEYWORD_CITY_STATE'>;
    /**
     * The search engines that we will use to track your performance
     */
    rankTrackingSites?: Array<'GOOGLE_DESKTOP' | 'GOOGLE_MOBILE' | 'BING_DESKTOP' | 'BING_MOBILE' | 'YAHOO_DESKTOP' | 'YAHOO_MOBILE'>;
    /**
     * Information about the URL consumers can visit to make reservations at this entity
     */
    reservationUrl?: {
        /**
         * The URL that is shown on your listings in place of **`reservationUrl.url`**. You can use **`reservationUrl.displayUrl`** to display a short, memorable web address that redirects consumers to the URL given in **`reservationUrl.url`**.
         *
         * Must be a valid URL and be specified along with **`reservationUrl.url`**.
         */
        displayUrl?: string;
        /**
         * If set to true, only the display URL will be sent to those publishers who do not support separate display and tracking URLs for this field.
         */
        preferDisplayUrl?: boolean;
        /**
         * A valid URL used to make reservations at this entity
         */
        url?: string;
    };
    /**
     * The URL given Review Invitation emails where consumers can leave a review about the entity
     */
    reviewGenerationUrl?: string;
    /**
     * Indicates whether Yext Knowledge Assistant review-response conversations are enabled for this entity
     */
    reviewResponseConversationEnabled?: boolean;
    /**
     * Destination coordinates to use for driving directions to the entity, as provided by you
     */
    routableCoordinate?: {
        latitude?: number;
        longitude?: number;
    };
    /**
     * Contains the daily senior hours, holiday senior hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `seniorHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday senior hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    seniorHours?: {
        friday?: {
            /**
             * Indicates if the senior hours are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's senior hours on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the senior hours are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for the Entity's senior hours on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the senior hours are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's senior hours on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the senior hours are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's senior hours on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the senior hours are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's senior hours on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the senior hours are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's senior hours on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the senior hours are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's senior hours on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the senior hours are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's senior hours on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * Information about the area that is served by this entity. It is specified as a list of cities and/or postal codes.
     *
     * **Only for Google Business Profile and Bing:** Currently, **serviceArea** is only supported by Google Business Profile and Bing and will not affect your listings on other sites.
     */
    serviceArea?: {
        /**
         * A list of places served by the entity, where each place is either:
         * - a postal code, or
         * - the name of a city.
         *
         *
         *
         * Array must be ordered.
         *
         * Array may have a maximum of 200 elements.
         *
         */
        places?: Array<string>;
    };
    /**
     * Information about the area that is served by this entity. It is specified as a list of service area names, their associated types and google place ids.
     * **Only for Google Business Profile and Bing:** Currently, **serviceArea** is only supported by Google Business Profile and Bing and will not affect your listings on other sites.
     *
     *
     * Array may have a maximum of 200 elements.
     *
     */
    serviceAreaPlaces?: Array<{
        name?: string;
        googlePlaceId?: string;
        type?: 'POSTAL_CODE' | 'REGION' | 'COUNTY' | 'CITY' | 'SUBLOCALITY';
    }>;
    /**
     * Services offered by this entity
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    services?: Array<string>;
    /**
     * Up to 100 of this entity's specialities (e.g., for food and dining: `Chicago style`)
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    specialities?: Array<string>;
    /**
     * Contains the daily takeout hours, holiday takeout hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `takeoutHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday takeout hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    takeoutHours?: {
        friday?: {
            /**
             * Indicates if the takeout hours are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for takeout on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the takeout hours are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for takeout on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the takeout hours are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for takeout on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the takeout hours are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for takeout on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the takeout hours are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for takeout on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the takeout hours are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for takeout on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the takeout hours are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for takeout on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the takeout hours are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for takeout on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * URL for your TikTok profile, format should be https://www.tiktok.com/yourUsername
     */
    tikTokUrl?: string;
    /**
     * The timezone of the entity, in the standard `IANA time zone database` format (tz database). e.g. `"America/New_York"`
     */
    timezone?: string;
    /**
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    tollFreePhone?: string;
    /**
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    ttyPhone?: string;
    /**
     * Valid Twitter handle for the entity without the leading "@" (e.g., `JohnSmith`)
     *
     * If you submit an invalid Twitter handle, it will be ignored. The success response will contain a warning message explaining why your Twitter handle wasn't stored in the system.
     */
    twitterHandle?: string;
    /**
     * Information about the Yext-powered link that can be copied and pasted into the markup of Yext Pages where the embedded Uber link should appear
     */
    uberLink?: {
        /**
         * Indicates whether the embedded Uber link for this entity appears as text or a button
         *
         * When consumers click on this link on a mobile device, the Uber app (if installed) will open with your entity set as the trip destination. If the Uber app is not installed, the consumer will be prompted to download it.
         */
        presentation: 'BUTTON' | 'LINK';
        /**
         * The text of the embedded Uber link
         *
         * Default is `Ride there with Uber`.
         *
         * **NOTE:** This field is only available if **`uberLink.presentation`** is `LINK`.
         */
        text?: string;
    };
    /**
     * Information about the call-to-action consumers will see in the Uber app during a trip to your entity
     */
    uberTripBranding?: {
        /**
         * A longer description that will appear near the call-to-action in the Uber app during a trip to your entity.
         *
         * **NOTE:** If a value for **`uberTripBranding.description`** is provided, values must also be provided for **`uberTripBranding.text`** and **`uberTripBranding.url`**.
         */
        description: string;
        /**
         * The text of the call-to-action that will appear in the Uber app during a trip to your entity (e.g., `Check out our menu!`)
         *
         * **NOTE:** If a value for **`uberTripBranding.text`** is provided, values must also be provided for **`uberTripBranding.url`** and **`uberTripBranding.description`**.
         */
        text: string;
        /**
         * The URL that the consumer will be redirected to when tapping on the call-to-action in the Uber app during a trip to your entity.
         *
         * **NOTE:** If a value for **`uberTripBranding.url`** is provided, values must also be provided for **`uberTripBranding.text`** and **`uberTripBranding.description`**.
         */
        url: string;
    };
    /**
     * Valid YouTube URLs for embedding a video on some publisher sites
     *
     * **NOTE:** Currently, only the first URL in the Array appears in your listings.
     *
     *
     *
     * Array must be ordered.
     *
     */
    videos?: Array<{
        /**
         *
         * Cannot Include:
         * * HTML markup
         */
        description?: string;
        video: {
            url: string;
        };
    }>;
    /**
     * Destination coordinates to use for walking directions to the entity, as provided by you
     */
    walkableCoordinate?: {
        latitude?: number;
        longitude?: number;
    };
    /**
     * Information about the website for this entity
     */
    websiteUrl?: {
        /**
         * The URL that is shown on your listings in place of **`websiteUrl.url`**. You can use **`websiteUrl.displayUrl`** to display a short, memorable web address that redirects consumers to the URL given in **`websiteUrl.url`**.
         *
         * Must be a valid URL and be specified along with **`websiteUrl.url`**.
         */
        displayUrl?: string;
        /**
         * If set to true, only the display URL will be sent to those publishers who do not support separate display and tracking URLs for this field.
         */
        preferDisplayUrl?: boolean;
        /**
         * A valid URL for this entity's website
         */
        url?: string;
    };
    /**
     * The year the entity was established.
     */
    yearEstablished?: number;
    /**
     * URL for your YouTube channel, format should be https://www.youtube.com/c/yourUsername
     */
    youTubeChannelUrl?: string;
    EntityType?: 'location';
};

export type RestaurantWrite = Omit<EntityWrite, 'EntityType'> & {
    /**
     * Contains the metadata about the entity.
     */
    meta?: {
        /**
         * Country code of this Entity's Language Profile (defaults to the country of the account)
         */
        countryCode?: string;
        /**
         * The ID of the folder containing this Entity
         */
        folderId?: string;
        /**
         * ID of this Entity
         */
        id?: string;
        /**
         * This Entity's labels. If the **`v`** parameter is before `20211215`, this will be an integer.
         */
        labels?: Array<string>;
        /**
         * Language code of this Entity's Language Profile (defaults to the language code of the account)
         */
        language?: string;
    };
    /**
     *
     *
     *
     * Cannot Include:
     * * HTML markup
     */
    name?: string;
    /**
     * Contains the address of the entity (or where the entity is located)
     *
     * Must be a valid address
     * Cannot be a P.O. Box
     *
     * If the entity is an `event`, either an **`address`** value or a **`linkedLocation`** value can be provided.
     */
    address?: {
        /**
         * The city the entity (or the entity's location) is in
         *
         *
         * Cannot Include:
         * * a URL or domain name
         */
        city?: string;
        countryCode?: string;
        /**
         * Provides additional information to help consumers get to the entity. This string appears along with the entity's address (e.g., `In Menlo Mall, 3rd Floor`).
         * It may also be used in conjunction with a hidden address (i.e., when **`addressHidden`** is `true`) to give consumers information about where the entity can be found (e.g., `Servicing the New York area`).
         */
        extraDescription?: string;
        /**
         *
         * Cannot Include:
         * * a URL or domain name
         */
        line1?: string;
        /**
         *
         * Cannot Include:
         * * a URL or domain name
         */
        line2?: string;
        /**
         * The entity's postal code. The postal code must be valid for the entity's country. Cannot include a URL or domain name.
         *
         *
         * Cannot Include:
         * * a URL or domain name
         */
        postalCode?: string;
        /**
         * The name of the entity's region or state.
         *
         *
         * Cannot Include:
         * * a URL or domain name
         */
        region?: string;
        /**
         * The name of the entity's sublocality
         *
         *
         * Cannot Include:
         * * a URL or domain name
         */
        sublocality?: string;
    };
    /**
     * Indicates whether the entity accepts reservations.
     */
    acceptsReservations?: boolean;
    /**
     * Contains the daily access hours, holiday access hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `accessHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday access hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    accessHours?: {
        friday?: {
            /**
             * Indicates if the access hours are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the access hours are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the access hours are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the access hours are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the access hours are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the access hours are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the access hours are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the access hours are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * Additional information about hours that does not fit in **`hours`** (e.g., `"Closed during the winter"`)
     */
    additionalHoursText?: string;
    /**
     * If `true`, the entity's street address will not be shown on listings. Defaults to `false`.
     */
    addressHidden?: boolean;
    /**
     * Other names for your business that you would like us to use when tracking your search performance
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 3 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    alternateNames?: Array<string>;
    /**
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    alternatePhone?: string;
    /**
     * Other websites for your business that we should search for when tracking your search performance
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 3 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* common domain names, e.g., google.com, youtube.com, etc.
     */
    alternateWebsites?: Array<string>;
    /**
     * The URL where consumers can download the entity's Android app
     */
    androidAppUrl?: string;
    /**
     * Use this field to add action links to your Apple Listings.
     *
     * The call to action category will be displayed on the action link button.
     *
     * The App Store URL should contain a valid link to the landing page of an App in the Apple App Store.
     *
     * The Quick Link URL is where a user is taken when an action link is clicked by a user.
     *
     * The App Name sub-field is not displayed on Apple Listings and is only used to distinguish the call-to-action type when utilizing action links in Apple posts.
     *
     *
     * Array must be ordered.
     *
     */
    appleActionLinks?: Array<{
        appName: string;
        appStoreUrl?: string;
        category: 'BOOK_TRAVEL' | 'CHECK_IN' | 'FEES_POLICIES' | 'FLIGHT_STATUS' | 'TICKETS' | 'TICKETING' | 'AMENITIES' | 'FRONT_DESK' | 'PARKING' | 'GIFT_CARD' | 'WAITLIST' | 'DELIVERY' | 'ORDER' | 'TAKEOUT' | 'PICKUP' | 'RESERVE' | 'MENU' | 'APPOINTMENT' | 'PORTFOLIO' | 'QUOTE' | 'SERVICES' | 'STORE_ORDERS' | 'STORE_SHOP' | 'STORE_SUPPORT' | 'SCHEDULE' | 'SHOWTIMES' | 'AVAILABILITY' | 'PRICING' | 'ACTIVITIES' | 'BOOK' | 'BOOK_(HOTEL)' | 'BOOK_(RIDE)' | 'BOOK_(TOUR)' | 'CAREERS' | 'CHARGE' | 'COUPONS' | 'DELIVERY_(RETAIL)' | 'DONATE' | 'EVENTS' | 'ORDER_(RETAIL)' | 'OTHER_MENU' | 'PICKUP_(RETAIL)' | 'RESERVE_(PARKING)' | 'SHOWS' | 'SPORTS' | 'SUPPORT' | 'TEE_TIME' | 'GIFT_CARD_(RESTAURANT)';
        quickLinkUrl: string;
    }>;
    /**
     * The business description to be sent to Apple
     */
    appleBusinessDescription?: string;
    /**
     * The ID associated with an individual Business Folder in your Apple account
     */
    appleBusinessId?: string;
    /**
     * The ID associated with your Apple account. Numerical values only
     */
    appleCompanyId?: string;
    /**
     * The cover photo for the entity''s Apple profile
     *
     *
     * Image must be at least 1600 x 1040 pixels
     *
     * Image may be no more than 4864 x 3163 pixels
     *
     * Supported Aspect Ratios:
     * * 154 x 100
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     */
    appleCoverPhoto?: {
        alternateText?: string;
        url: string;
    };
    /**
     * The name to be displayed on Apple for the entity.
     * NOTE: The names of Brands and their respective Locations within an Apple Business Connect Account must match identically.
     * Cannot Include:
     * HTML markup
     *
     *
     * Cannot Include:
     * * HTML markup
     */
    appleDisplayName?: string;
    /**
     * Association memberships relevant to the entity (e.g., `"New York Doctors Association"`)
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    associations?: Array<string>;
    /**
     * The formality of clothing typically worn at this restaurant
     */
    attire?: 'UNSPECIFIED' | 'DRESSY' | 'CASUAL' | 'FORMAL';
    /**
     * Information about the Bio Content Lists associated with this entity
     */
    bios?: {
        /**
         * IDs of the Bio Lists associated with this entity
         *
         *
         * Array must be ordered.
         *
         * Array may have a maximum of 40 elements.
         *
         */
        ids?: Array<string>;
        /**
         * Label to be used for this entity's Bio Lists. This label will appear on your entity's listings.
         */
        label?: string;
    };
    /**
     * Brands sold by this entity
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    brands?: Array<string>;
    /**
     * Contains the daily brunch hours, holiday brunch hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `brunchHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday brunch hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    brunchHours?: {
        friday?: {
            /**
             * Indicates if the brunch hours are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for brunch on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the brunch hours are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the brunch hours are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for brunch on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the brunch hours are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for brunch on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the brunch hours are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for brunch on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the brunch hours are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for brunch on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the brunch hours are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for brunch on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the brunch hours are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for brunch on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * Information about the events Content Lists (Calendars) associated with this entity
     */
    calendars?: {
        /**
         * IDs of the Calendars associated with this entity
         *
         *
         * Array must be ordered.
         *
         * Array may have a maximum of 40 elements.
         *
         */
        ids?: Array<string>;
        /**
         * Label to be used for this entity's Calendars. This label will appear on your entity's listings.
         */
        label?: string;
    };
    /**
     * Yext Category IDs
     *
     * IDs must be valid and selectable (i.e., cannot be parent categories).
     *
     * NOTE: The list of category IDs that you send us must be comprehensive. For example, if you send us a list of IDs that does not include IDs that you sent in your last update, Yext considers the missing categories to be deleted, and we remove them from your listings.
     *
     */
    categoryIds?: Array<string>;
    /**
     * Indicates whether the entity is closed
     */
    closed?: boolean;
    /**
     * Additional keywords you would like us to use when tracking your search performance
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 5 elements.
     *
     */
    customKeywords?: Array<string>;
    /**
     * Contains the daily delivery hours, holiday delivery hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `deliveryHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday delivery hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    deliveryHours?: {
        friday?: {
            /**
             * Indicates if the delivery hours are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is delivering on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the delivery hours are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity is delivering on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the delivery hours are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is delivering on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the delivery hours are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is delivering on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the delivery hours are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is delivering on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the delivery hours are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is delivering on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the delivery hours are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is delivering on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the delivery hours are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is delivering on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * A description of the entity
     *
     *
     * Cannot Include:
     * * HTML markup
     */
    description?: string;
    /**
     * Coordinates where the map pin for the entity should be displayed, as provided by you
     */
    displayCoordinate?: {
        latitude?: number;
        longitude?: number;
    };
    /**
     * Contains the daily drive-through hours, holiday drive-through hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `driveThroughHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday drive-through hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    driveThroughHours?: {
        friday?: {
            /**
             * Indicates if the drive-through hours are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the drive-through hours are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the drive-through hours are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the drive-through hours are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the drive-through hours are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the drive-through hours are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the drive-through hours are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the drive-through hours are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * Coordinates of the drop-off area for the entity, as provided by you
     */
    dropoffCoordinate?: {
        latitude?: number;
        longitude?: number;
    };
    /**
     * Emails addresses for this entity's point of contact
     *
     * Must be valid email addresses
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 5 elements.
     *
     */
    emails?: Array<string>;
    /**
     * A description of the entity to be used in the "About You" section on Facebook
     */
    facebookAbout?: string;
    /**
     * Designates the Facebook Call-to-Action button text and value
     *
     * Valid contents of **`value`** depends on the Call-to-Action's **`type`**:
     * * `NONE`: (optional)
     * * `BOOK_NOW`: URL
     * * `CALL_NOW`: Phone number
     * * `CONTACT_US`: URL
     * * `SEND_MESSAGE`: Any string
     * * `USE_APP`: URL
     * * `PLAY_GAME`: URL
     * * `SHOP_NOW`: URL
     * * `SIGN_UP`: URL
     * * `WATCH_VIDEO`: URL
     * * `SEND_EMAIL`: Email address
     * * `LEARN_MORE`: URL
     * * `PURCHASE_GIFT_CARDS`: URL
     * * `ORDER_NOW`: URL
     * * `FOLLOW_PAGE`: Any string
     */
    facebookCallToAction?: {
        /**
         * The action the consumer is being prompted to take by the button's text
         */
        type: 'NONE' | 'BOOK_NOW' | 'CALL_NOW' | 'CONTACT_US' | 'SEND_MESSAGE' | 'USE_APP' | 'PLAY_GAME' | 'SHOP_NOW' | 'SIGN_UP' | 'WATCH_VIDEO' | 'SEND_EMAIL' | 'LEARN_MORE' | 'PURCHASE_GIFT_CARDS' | 'ORDER_NOW' | 'FOLLOW_PAGE';
        /**
         * Indicates where consumers will be directed to upon clicking the Call-to-Action button (e.g., a URL). It can be a free-form string or an embedded value, depending on what the user specifies.
         *
         * For example, if the user sets the Facebook Call-to-Action as " 'Sign Up' using 'Website URL' " in the Yext platform, **`type`** will be `SIGN_UP` and **`value`** will be `[[websiteUrl]]`.
         *
         * The Call-to-Action will have the same behavior if the user sets the value to "Custom Value" in the platform and embeds a field.
         */
        value?: string;
    };
    /**
     * The cover photo for the entity's Facebook profile
     *
     * Displayed as a 851 x 315 pixel image
     *
     * You may need a cover photo in order for your listing to appear on Facebook. Please check your listings tab to learn more.
     *
     *
     * Image must be at least 400 x 150 pixels
     *
     * Image area (width x height) may be no more than 41000000 pixels
     *
     * Image may be no more than 30000 x 30000 pixels
     *
     * Supported Aspect Ratios:
     * * 1 x 1
     * * 4 x 3
     * * 3 x 2
     * * 5 x 3
     * * 16 x 9
     * * 3 x 1
     * * 2 x 3
     * * 5 x 7
     * * 4 x 5
     * * 4 x 1
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     */
    facebookCoverPhoto?: {
        alternateText?: string;
        url: string;
    };
    /**
     * Location Descriptors are used for Enterprise businesses that sync Facebook listings using brand page location structure. The Location Descriptor is typically an additional geographic description (e.g. geomodifier) that will appear in parentheses after the name on the Facebook listing.
     *
     *
     * Cannot Include:
     * * HTML markup
     */
    facebookDescriptor?: string;
    /**
     * The name for this entity's Facebook profile.  A separate name may be specified to send only to Facebook in order to comply with any specific Facebook rules or naming conventions.
     */
    facebookName?: string;
    /**
     * The city to be displayed on this entity's Facebook profile
     */
    facebookOverrideCity?: string;
    /**
     * URL for the entity's Facebook Page.
     *
     * Valid formats:
     *
     * - facebook.com/profile.php?id=[numId]
     * - facebook.com/group.php?gid=[numId]
     * - facebook.com/groups/[numId]
     * - facebook.com/[Name]
     * - facebook.com/pages/[Name]/[numId]
     * - facebook.com/people/[Name]/[numId]
     *
     * where [Name] is a String and [numId] is an Integer
     * The success response will contain a warning message explaining why the URL wasn't stored in the system.
     */
    facebookPageUrl?: string;
    /**
     * The Facebook Page ID of this entity's brand page if in a brand page location structure
     */
    facebookParentPageId?: string;
    /**
     * The profile picture for the entity's Facebook profile
     *
     * You must have a profile picture in order for your listing to appear on Facebook.
     *
     *
     * Image must be at least 180 x 180 pixels
     *
     * Image area (width x height) may be no more than 41000000 pixels
     *
     * Image may be no more than 30000 x 30000 pixels
     *
     * Supported Aspect Ratios:
     * * 1 x 1
     * * 4 x 3
     * * 3 x 2
     * * 5 x 3
     * * 16 x 9
     * * 3 x 1
     * * 2 x 3
     * * 5 x 7
     * * 4 x 5
     * * 4 x 1
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     */
    facebookProfilePhoto?: {
        alternateText?: string;
        url: string;
    };
    /**
     * The Store ID used for this entity in a brand page location structure
     */
    facebookStoreId?: string;
    /**
     * The username that appear's in the Facebook listing URL to help customers find and remember a brand’s Facebook page.  The username is also be used for tagging the Facebook page in other users’ posts, and searching for the Facebook page.
     */
    facebookVanityUrl?: string;
    /**
     * The URL you would like to submit to Facebook in place of the one given in **`websiteUrl`** (if applicable).
     */
    facebookWebsiteOverride?: string;
    /**
     * Must be a valid fax number.
     *
     * If the fax number's calling code is for a country other than the one given in the entity's **`countryCode`**, the fax number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    fax?: string;
    /**
     * Information about the entity's Featured Message
     */
    featuredMessage?: {
        /**
         * The text of Featured Message. Default: `Call today!`
         *
         * Cannot include:
         *
         * - inappropriate language
         * - HTML markup
         * - a URL or domain name
         * - a phone number
         * - control characters ([\x00-\x1F\x7F])
         * - insufficient spacing
         *
         * If you submit a Featured Message that contains profanity or more than 50 characters, it will be ignored. The success response will contain a warning message explaining why your Featured Message wasn't stored in the system.
         *
         *
         * Cannot Include:
         * * HTML markup
         */
        description?: string;
        /**
         * Valid URL linked to the Featured Message text
         */
        url?: string;
    };
    /**
     * Link to the review-collection page, where consumers can leave first-party reviews
     */
    firstPartyReviewPage?: string;
    /**
     * A list of questions that are frequently asked about this entity
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     */
    frequentlyAskedQuestions?: Array<{
        answer?: string;
        question: string;
    }>;
    /**
     * Indicates whether the staff is vaccinated against COVID-19.
     */
    fullyVaccinatedStaff?: boolean;
    /**
     * Provides additional information on where the entity can be found (e.g., `Times Square`, `Global Center Mall`)
     */
    geomodifier?: string;
    /**
     * The unique IDs of the entity's Google Business Profile keywords, as well as the unique IDs of any values selected for each keyword.
     *
     * Valid keywords (e.g., `has_drive_through`, `has_fitting_room`, `kitchen_in_room`) are determined by the entity's primary category. A full list of keywords can be retrieved with the Google Fields: List endpoint.
     *
     * Keyword values provide more details on how the keyword applies to the entity (e.g., if the keyword is `has_drive_through`, its values may be `true` or `false`).
     *
     * * If the **`v`** parameter is before `20181204`: **`googleAttributes`** is formatted as a map of key-value pairs (e.g., `[{ "id": "has_wheelchair_accessible_entrance", "values": [ "true" ] }]`)
     * * If the **`v`** parameter is on or after `20181204`: the contents are formatted as a list of objects (e.g., `{ "has_wheelchair_accessible_entrance": [ "true" ]}`)
     *
     * **NOTE:** The latest Google Attributes are available via the Google Fields: List endpoint. Google Attributes are managed by Google and are subject to change without notice. To prevent errors, make sure your API implementation is not dependent on the presence of specific attributes.
     */
    googleAttributes?: {};
    /**
     * The cover photo for the entity's Google profile
     *
     *
     * Image must be at least 250 x 250 pixels
     *
     */
    googleCoverPhoto?: {
        alternateText?: string;
        url: string;
    };
    /**
     * Information about Google Messaging, WhatsApp and SMS, for the entity’s point of contact for messaging/chat functionality.
     *
     * NOTE: Only one, either WhatsApp or SMS is displayed on the Google listing. If both SMS Number and WhatsApp URL are provided only SMS Number will be displayed on the listing.
     */
    googleMessaging?: {
        /**
         * The SMS phone number of the entity's point of contact for messaging/ chat functionality.
         * Must be a valid phone number.
         * If the phone number's calling code is for a country other than the one given in the entity's countryCode, the phone number provided must contain the calling code (e.g., +44 in +442038083831). Otherwise, the calling code is optional.
         */
        smsNumber?: string;
        /**
         * A valid URL for this entity's WhatsApp account.
         * Must be a valid URL
         *
         */
        whatsappMessagingUrl?: string;
    };
    /**
     * Google Business Profile Labels help users organize their locations into groups within GBP.
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 10 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    googleMyBusinessLabels?: Array<string>;
    /**
     * The unique identifier of this entity on Google Maps.
     */
    googlePlaceId?: string;
    /**
     * The profile photo for the entity's Google profile
     *
     *
     * Image must be at least 250 x 250 pixels
     *
     * Image may be no more than 5000 x 5000 pixels
     *
     * Supported Aspect Ratios:
     * * 1 x 1
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     */
    googleProfilePhoto?: {
        alternateText?: string;
        url: string;
    };
    /**
     * The URL you would like to submit to Google Business Profile in place of the one given in **`websiteUrl`** (if applicable).
     *
     * For example, if you want to analyze the traffic driven by your Google listings separately from other traffic, enter the alternate URL that you will use for tracking in this field.
     */
    googleWebsiteOverride?: string;
    /**
     * Contains the daily happy hours, holiday happy hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `happyHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday happy hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    happyHours?: {
        friday?: {
            /**
             * Indicates if the happy hours are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's happy hours on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the happy hours are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for the Entity's happy hours on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the happy hours are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's happy hours on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the happy hours are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's happy hours on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the happy hours are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's happy hours on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the happy hours are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's happy hours on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the happy hours are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's happy hours on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the happy hours are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's happy hours on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * Indicates whether holiday-hour confirmation alerts are enabled for the Yext Knowledge Assistant for this entity
     */
    holidayHoursConversationEnabled?: boolean;
    /**
     * Contains the daily hours, holiday hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `hours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    hours?: {
        friday?: {
            /**
             * Indicates if the hours of operation are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the hours of operation are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the hours of operation are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the hours of operation are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the hours of operation are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the hours of operation are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the hours of operation are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the hours of operation are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * A statement of the ownership and authorship of a document. Individuals or organizations based in many German-speaking countries are required by law to include an Impressum in published media.
     */
    impressum?: string;
    /**
     * Valid Instagram username for the entity without the leading "@" (e.g., `NewCityAuto`)
     */
    instagramHandle?: string;
    /**
     * The URL where consumers can download the entity's app to their iPhone or iPad
     */
    iosAppUrl?: string;
    /**
     * The ISO 3166-2 region code for the entity
     *
     * Yext will determine the entity's code and update **`isoRegionCode`** with that value. If Yext is unable to determine the code for the entity, the entity'ss ISO 3166-1 alpha-2 country code will be used.
     */
    isoRegionCode?: string;
    /**
     * Keywords that describe the entity.
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    keywords?: Array<string>;
    /**
     * Contains the daily kitchen hours, holiday kitchen hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `kitchenHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday kitchen hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    kitchenHours?: {
        friday?: {
            /**
             * Indicates if the kitchen hours are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen is open on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the kitchen hours are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen is open on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the kitchen hours are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen is open on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the kitchen hours are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen is open on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the kitchen hours are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen is open on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the kitchen hours are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen is open on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the kitchen hours are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen open on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the kitchen hours are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen is open on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * The IDs of the entity labels that have been added to this entity. Entity labels help you identify entities that share a certain characteristic; they do not appear on your entity's listings.
     *
     * **NOTE:** You can only add labels that have already been created via our web interface. Currently, it is not possible to create new labels via the API.
     */
    labels?: Array<string>;
    /**
     * The URL of this entity's Landing Page that was created with Yext Pages
     */
    landingPageUrl?: string;
    /**
     * The langauges in which consumers can commicate with this entity or its staff members
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    languages?: Array<string>;
    /**
     * URL for your LinkedIn account, format should be https://www.linkedin.com/in/yourUsername
     */
    linkedInUrl?: string;
    /**
     * Must be a valid, non-toll-free phone number, based on the country specified in **`address.region`**. Phone numbers for US entities must contain 10 digits.
     */
    localPhone?: string;
    /**
     * Indicates the entity's type, if it is not an event
     */
    locationType?: 'LOCATION' | 'HEALTHCARE_FACILITY' | 'HEALTHCARE_PROFESSIONAL' | 'ATM' | 'RESTAURANT' | 'HOTEL';
    /**
     * An image of the entity's logo
     *
     *
     * Supported Aspect Ratios:
     * * 1 x 1
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     */
    logo?: {
        clickthroughUrl?: string;
        description?: string;
        details?: string;
        /**
         *
         * Supported Aspect Ratios:
         * * 1 x 1
         *
         * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
         *
         */
        image: {
            alternateText?: string;
            url: string;
        };
    };
    /**
     * The main phone number of the entity's point of contact
     *
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    mainPhone?: string;
    /**
     * Types of meals served at this restaurant
     */
    mealsServed?: Array<'BREAKFAST' | 'LUNCH' | 'BRUNCH' | 'DINNER' | 'HAPPY_HOUR' | 'LATE_NIGHT'>;
    /**
     * Information about the URL where consumers can view the entity's menu
     */
    menuUrl?: {
        /**
         * The URL that is shown on your listings in place of **`menuUrl.url`**. You can use **`menuUrl.displayUrl`** to display a short, memorable web address that redirects consumers to the URL given in **`menuUrl.url`**.
         */
        displayUrl?: string;
        /**
         * If set to true, only the display URL will be sent to those publishers who do not support separate display and tracking URLs for this field.
         */
        preferDisplayUrl?: boolean;
        /**
         * A valid URL where consumers can view the entity's menu
         */
        url?: string;
    };
    /**
     * Information about the Menu Content Lists associated with this entity
     */
    menus?: {
        /**
         * IDs of the Menu Lists associated with this entity
         *
         *
         * Array must be ordered.
         *
         * Array may have a maximum of 40 elements.
         *
         */
        ids?: Array<string>;
        /**
         * Label to be used for this entity's Menu Lists. This label will appear on your entity's listings.
         */
        label?: string;
    };
    /**
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    mobilePhone?: string;
    /**
     * Indicates whether Knowledge Nudge is enabled for the Yext Knowledge Assistant for this entity
     */
    nudgeEnabled?: boolean;
    /**
     * Contains the daily online service hours, holiday online service hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `onlineServiceHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday online service hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    onlineServiceHours?: {
        friday?: {
            /**
             * Indicates if the online service hours are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the online service hours are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the online service hours are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the online service hours are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the online service hours are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the online service hours are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the online service hours are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the online service hours are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * The date that the entity is set to open for the first time.
     * Must be formatted in YYYY-MM-DD format.
     *
     * Date must be on or after 1970-01-01 Date must be before or on 2038-01-01
     *
     */
    openDate?: string;
    /**
     * Information about the URL used to place orders that will be fulfilled by the entity
     */
    orderUrl?: {
        /**
         * The URL that is shown on your listings in place of **`orderUrl.url`**. You can use **`orderUrl.displayUrl`** to display a short, memorable web address that redirects consumers to the URL given in **`orderUrl.url`**.
         */
        displayUrl?: string;
        /**
         * If set to true, only the display URL will be sent to those publishers who do not support separate display and tracking URLs for this field.
         */
        preferDisplayUrl?: boolean;
        /**
         * A valid URL used to place an order at this entity
         */
        url?: string;
    };
    /**
     * The payment methods accepted by this entity
     *
     * Valid elements depend on the entity's country.
     *
     */
    paymentOptions?: Array<'AFTERPAY' | 'ALIPAY' | 'AMERICANEXPRESS' | 'ANDROIDPAY' | 'APPLEPAY' | 'ATM' | 'ATMQUICK' | 'BACS' | 'BANCONTACT' | 'BANKDEPOSIT' | 'BANKPAY' | 'BGO' | 'BITCOIN' | 'Bar' | 'CARTASI' | 'CASH' | 'CCS' | 'CHECK' | 'CHEQUESVACANCES' | 'CONB' | 'CONTACTLESSPAYME' | 'CVVV' | 'DEBITCARD' | 'DEBITNOTE' | 'DINERSCLUB' | 'DIRECTDEBIT' | 'DISCOVER' | 'ECKARTE' | 'ECOCHEQUE' | 'EKENA' | 'EMV' | 'FINANCING' | 'GIFTCARD' | 'GOPAY' | 'HAYAKAKEN' | 'HEBAG' | 'IBOD' | 'ICCARDS' | 'ICOCA' | 'ID' | 'IDEAL' | 'INCA' | 'INVOICE' | 'JCB' | 'JCoinPay' | 'JKOPAY' | 'KITACA' | 'KLA' | 'KLARNA' | 'LINEPAY' | 'MAESTRO' | 'MANACA' | 'MASTERCARD' | 'MIPAY' | 'MONIZZE' | 'MPAY' | 'Manuelle Lastsch' | 'Merpay' | 'NANACO' | 'NEXI' | 'NIMOCA' | 'OREM' | 'PASMO' | 'PAYBACKPAY' | 'PAYBOX' | 'PAYCONIQ' | 'PAYPAL' | 'PAYPAY' | 'PAYSEC' | 'PIN' | 'POSTEPAY' | 'QRCODE' | 'QUICPAY' | 'RAKUTENEDY' | 'RAKUTENPAY' | 'SAMSUNGPAY' | 'SODEXO' | 'SUGOCA' | 'SUICA' | 'SWISH' | 'TICKETRESTAURANT' | 'TOICA' | 'TRAVELERSCHECK' | 'TSCUBIC' | 'TWINT' | 'UNIONPAY' | 'VEV' | 'VISA' | 'VISAELECTRON' | 'VOB' | 'VOUCHER' | 'VPAY' | 'WAON' | 'WECHATPAY' | 'WIRETRANSFER' | 'Yucho Pay' | 'ZELLE' | 'auPay' | 'dBarai' | 'Überweisung'>;
    /**
     *
     * **NOTE:** The list of photos that you send us must be comprehensive. For example, if you send us a list of photos that does not include photos that you sent in your last update, Yext considers the missing photos to be deleted, and we remove them from your listings.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 500 elements.
     *
     * Array item description:
     *
     * >Supported Aspect Ratios:
     * >* 1 x 1
     * >* 4 x 3
     * >* 3 x 2
     * >* 5 x 3
     * >* 16 x 9
     * >* 3 x 1
     * >* 2 x 3
     * >* 5 x 7
     * >* 4 x 5
     * >* 4 x 1
     * >
     * >**NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     * >
     */
    photoGallery?: Array<{
        clickthroughUrl?: string;
        description?: string;
        details?: string;
        /**
         *
         * Supported Aspect Ratios:
         * * 1 x 1
         * * 4 x 3
         * * 3 x 2
         * * 5 x 3
         * * 16 x 9
         * * 3 x 1
         * * 2 x 3
         * * 5 x 7
         * * 4 x 5
         * * 4 x 1
         *
         * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
         *
         */
        image: {
            alternateText?: string;
            url: string;
        };
    }>;
    /**
     * Coordinates of where consumers can be picked up at the entity, as provided by you
     */
    pickupCoordinate?: {
        latitude?: number;
        longitude?: number;
    };
    /**
     * Contains the daily pickup hours, holiday pickup hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `pickupHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday pickup hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    pickupHours?: {
        friday?: {
            /**
             * Indicates if the pickup hours are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the pickup hours are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the pickup hours are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the pickup hours are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the pickup hours are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the pickup hours are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the pickup hours are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the pickup hours are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * URL for your Pinterest account, format should be https://www.pinterest.com/yourUsername
     */
    pinterestUrl?: string;
    /**
     * he typical price of products sold by this location, on a scale of 1 (low) to 4 (high)
     */
    priceRange?: 'UNSPECIFIED' | 'ONE' | 'TWO' | 'THREE' | 'FOUR';
    /**
     * ID of the user who is the primary Knowledge Assistant contact for the entity
     */
    primaryConversationContact?: string;
    /**
     * Information about the Products & Services Content Lists associated with this entity
     */
    productLists?: {
        /**
         * IDs of the Products & Services Lists associated with this entity
         *
         *
         * Array must be ordered.
         *
         * Array may have a maximum of 40 elements.
         *
         */
        ids?: Array<string>;
        /**
         * Label to be used for this entity's Products & Services Lists. This label will appear on your entity's listings.
         */
        label?: string;
    };
    /**
     * Indicates whether Yext Knowledge Assistant question-and-answer conversations are enabled for this entity
     */
    questionsAndAnswers?: boolean;
    /**
     * Information about the competitors whose search performance you would like to compare to your own
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 5 elements.
     *
     */
    rankTrackingCompetitors?: Array<{
        /**
         * A name of a competitor
         *
         *
         * Cannot Include:
         * * HTML markup
         */
        name: string;
        /**
         * The business website of a competitor
         *
         *
         * Cannot Include:
         * * common domain names, e.g., google.com, youtube.com, etc.
         */
        website: string;
    }>;
    /**
     * Indicates whether Rank Tracking is enabled
     */
    rankTrackingEnabled?: boolean;
    /**
     * How often we send search queries to track your search performance
     */
    rankTrackingFrequency?: 'WEEKLY' | 'MONTHLY' | 'QUARTERLY';
    /**
     * The keywords for which you would like to track your search performance
     *
     */
    rankTrackingKeywords?: Array<'NAME' | 'PRIMARY_CATEGORY' | 'SECONDARY_CATEGORY'>;
    /**
     * The ways in which your keywords will be arranged in the search queries we use to track your performance
     *
     *
     * Array must have a minimum of 2 elements.
     *
     * Array may have a maximum of 4 elements.
     *
     */
    rankTrackingQueryTemplates?: Array<'KEYWORD' | 'KEYWORD_ZIP' | 'KEYWORD_CITY' | 'KEYWORD_IN_CITY' | 'KEYWORD_NEAR_ME' | 'KEYWORD_CITY_STATE'>;
    /**
     * The search engines that we will use to track your performance
     */
    rankTrackingSites?: Array<'GOOGLE_DESKTOP' | 'GOOGLE_MOBILE' | 'BING_DESKTOP' | 'BING_MOBILE' | 'YAHOO_DESKTOP' | 'YAHOO_MOBILE'>;
    /**
     * Information about the URL consumers can visit to make reservations at this entity
     */
    reservationUrl?: {
        /**
         * The URL that is shown on your listings in place of **`reservationUrl.url`**. You can use **`reservationUrl.displayUrl`** to display a short, memorable web address that redirects consumers to the URL given in **`reservationUrl.url`**.
         *
         * Must be a valid URL and be specified along with **`reservationUrl.url`**.
         */
        displayUrl?: string;
        /**
         * If set to true, only the display URL will be sent to those publishers who do not support separate display and tracking URLs for this field.
         */
        preferDisplayUrl?: boolean;
        /**
         * A valid URL used to make reservations at this entity
         */
        url?: string;
    };
    /**
     * The URL given Review Invitation emails where consumers can leave a review about the entity
     */
    reviewGenerationUrl?: string;
    /**
     * Indicates whether Yext Knowledge Assistant review-response conversations are enabled for this entity
     */
    reviewResponseConversationEnabled?: boolean;
    /**
     * Destination coordinates to use for driving directions to the entity, as provided by you
     */
    routableCoordinate?: {
        latitude?: number;
        longitude?: number;
    };
    /**
     * Contains the daily senior hours, holiday senior hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `seniorHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday senior hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    seniorHours?: {
        friday?: {
            /**
             * Indicates if the senior hours are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's senior hours on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the senior hours are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for the Entity's senior hours on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the senior hours are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's senior hours on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the senior hours are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's senior hours on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the senior hours are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's senior hours on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the senior hours are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's senior hours on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the senior hours are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's senior hours on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the senior hours are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's senior hours on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * Information about the area that is served by this entity. It is specified as a list of cities and/or postal codes.
     *
     * **Only for Google Business Profile and Bing:** Currently, **serviceArea** is only supported by Google Business Profile and Bing and will not affect your listings on other sites.
     */
    serviceArea?: {
        /**
         * A list of places served by the entity, where each place is either:
         * - a postal code, or
         * - the name of a city.
         *
         *
         *
         * Array must be ordered.
         *
         * Array may have a maximum of 200 elements.
         *
         */
        places?: Array<string>;
    };
    /**
     * Information about the area that is served by this entity. It is specified as a list of service area names, their associated types and google place ids.
     * **Only for Google Business Profile and Bing:** Currently, **serviceArea** is only supported by Google Business Profile and Bing and will not affect your listings on other sites.
     *
     *
     * Array may have a maximum of 200 elements.
     *
     */
    serviceAreaPlaces?: Array<{
        name?: string;
        googlePlaceId?: string;
        type?: 'POSTAL_CODE' | 'REGION' | 'COUNTY' | 'CITY' | 'SUBLOCALITY';
    }>;
    /**
     * Services offered by this entity
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    services?: Array<string>;
    /**
     * Up to 100 of this entity's specialities (e.g., for food and dining: `Chicago style`)
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     */
    specialities?: Array<string>;
    /**
     * Contains the daily takeout hours, holiday takeout hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `takeoutHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday takeout hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     */
    takeoutHours?: {
        friday?: {
            /**
             * Indicates if the takeout hours are "closed" on Friday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for takeout on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             */
            date: string;
            /**
             * Indicates if the takeout hours are "closed" on on the given date.
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for takeout on the specified date.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        }>;
        monday?: {
            /**
             * Indicates if the takeout hours are "closed" on Monday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for takeout on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         */
        reopenDate?: string;
        saturday?: {
            /**
             * Indicates if the takeout hours are "closed" on Saturday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for takeout on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        sunday?: {
            /**
             * Indicates if the takeout hours are "closed" on Sunday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for takeout on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        thursday?: {
            /**
             * Indicates if the takeout hours are "closed" on Thursday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for takeout on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        tuesday?: {
            /**
             * Indicates if the takeout hours are "closed" on Tuesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for takeout on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
        wednesday?: {
            /**
             * Indicates if the takeout hours are "closed" on Wednesday.
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for takeout on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 */
                start: string;
            }>;
        };
    };
    /**
     * URL for your TikTok profile, format should be https://www.tiktok.com/yourUsername
     */
    tikTokUrl?: string;
    /**
     * The timezone of the entity, in the standard `IANA time zone database` format (tz database). e.g. `"America/New_York"`
     */
    timezone?: string;
    /**
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    tollFreePhone?: string;
    /**
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     */
    ttyPhone?: string;
    /**
     * Valid Twitter handle for the entity without the leading "@" (e.g., `JohnSmith`)
     *
     * If you submit an invalid Twitter handle, it will be ignored. The success response will contain a warning message explaining why your Twitter handle wasn't stored in the system.
     */
    twitterHandle?: string;
    /**
     * Information about the Yext-powered link that can be copied and pasted into the markup of Yext Pages where the embedded Uber link should appear
     */
    uberLink?: {
        /**
         * Indicates whether the embedded Uber link for this entity appears as text or a button
         *
         * When consumers click on this link on a mobile device, the Uber app (if installed) will open with your entity set as the trip destination. If the Uber app is not installed, the consumer will be prompted to download it.
         */
        presentation: 'BUTTON' | 'LINK';
        /**
         * The text of the embedded Uber link
         *
         * Default is `Ride there with Uber`.
         *
         * **NOTE:** This field is only available if **`uberLink.presentation`** is `LINK`.
         */
        text?: string;
    };
    /**
     * Information about the call-to-action consumers will see in the Uber app during a trip to your entity
     */
    uberTripBranding?: {
        /**
         * A longer description that will appear near the call-to-action in the Uber app during a trip to your entity.
         *
         * **NOTE:** If a value for **`uberTripBranding.description`** is provided, values must also be provided for **`uberTripBranding.text`** and **`uberTripBranding.url`**.
         */
        description: string;
        /**
         * The text of the call-to-action that will appear in the Uber app during a trip to your entity (e.g., `Check out our menu!`)
         *
         * **NOTE:** If a value for **`uberTripBranding.text`** is provided, values must also be provided for **`uberTripBranding.url`** and **`uberTripBranding.description`**.
         */
        text: string;
        /**
         * The URL that the consumer will be redirected to when tapping on the call-to-action in the Uber app during a trip to your entity.
         *
         * **NOTE:** If a value for **`uberTripBranding.url`** is provided, values must also be provided for **`uberTripBranding.text`** and **`uberTripBranding.description`**.
         */
        url: string;
    };
    /**
     * Valid YouTube URLs for embedding a video on some publisher sites
     *
     * **NOTE:** Currently, only the first URL in the Array appears in your listings.
     *
     *
     *
     * Array must be ordered.
     *
     */
    videos?: Array<{
        /**
         *
         * Cannot Include:
         * * HTML markup
         */
        description?: string;
        video: {
            url: string;
        };
    }>;
    /**
     * Destination coordinates to use for walking directions to the entity, as provided by you
     */
    walkableCoordinate?: {
        latitude?: number;
        longitude?: number;
    };
    /**
     * Information about the website for this entity
     */
    websiteUrl?: {
        /**
         * The URL that is shown on your listings in place of **`websiteUrl.url`**. You can use **`websiteUrl.displayUrl`** to display a short, memorable web address that redirects consumers to the URL given in **`websiteUrl.url`**.
         *
         * Must be a valid URL and be specified along with **`websiteUrl.url`**.
         */
        displayUrl?: string;
        /**
         * If set to true, only the display URL will be sent to those publishers who do not support separate display and tracking URLs for this field.
         */
        preferDisplayUrl?: boolean;
        /**
         * A valid URL for this entity's website
         */
        url?: string;
    };
    /**
     * The year the entity was established.
     */
    yearEstablished?: number;
    /**
     * URL for your YouTube channel, format should be https://www.youtube.com/c/yourUsername
     */
    youTubeChannelUrl?: string;
    EntityType?: 'restaurant';
};

export type NewLocationAddRequestAtm = Omit<NewLocationAddRequest, 'newEntityType'> & {
    /**
     * If you set `newLocationId`, you must specify either `newLocationData` or `newEntityData`.
     *
     * This field allows you to set the profile data for an entity.
     * Profile content is specified in the same format as when creating an entity with the Entities: Create endpoint.
     * You can only create entities of a type specified by `newEntityType`.
     *
     * If the Add Request is processed successfully, this data will be used to create a new entity in your account.
     *
     * **The dropdown will specify the profile content for `newEntityData`. Specify the entity type with `newEntityType` described above.**
     *
     */
    newEntityData?: Omit<AtmWrite, 'EntityType'> & {
        EntityType?: 'NewLocationAddRequestATM';
    };
    newEntityType?: 'atm';
};

export type NewLocationAddRequestHealthcareFacility = Omit<NewLocationAddRequest, 'newEntityType'> & {
    /**
     * If you set `newLocationId`, you must specify either `newLocationData` or `newEntityData`.
     *
     * This field allows you to set the profile data for an entity.
     * Profile content is specified in the same format as when creating an entity with the Entities: Create endpoint.
     * You can only create entities of a type specified by `newEntityType`.
     *
     * If the Add Request is processed successfully, this data will be used to create a new entity in your account.
     *
     * **The dropdown will specify the profile content for `newEntityData`. Specify the entity type with `newEntityType` described above.**
     *
     */
    newEntityData?: Omit<HealthcareFacilityWrite, 'EntityType'> & {
        EntityType?: 'NewLocationAddRequestHealthcareFacility';
    };
    newEntityType?: 'healthcareFacility';
};

export type NewLocationAddRequestHealthcareProfessional = Omit<NewLocationAddRequest, 'newEntityType'> & {
    /**
     * If you set `newLocationId`, you must specify either `newLocationData` or `newEntityData`.
     *
     * This field allows you to set the profile data for an entity.
     * Profile content is specified in the same format as when creating an entity with the Entities: Create endpoint.
     * You can only create entities of a type specified by `newEntityType`.
     *
     * If the Add Request is processed successfully, this data will be used to create a new entity in your account.
     *
     * **The dropdown will specify the profile content for `newEntityData`. Specify the entity type with `newEntityType` described above.**
     *
     */
    newEntityData?: Omit<HealthcareProfessionalWrite, 'EntityType'> & {
        EntityType?: 'NewLocationAddRequestHealthcareProfessional';
    };
    newEntityType?: 'healthcareProfessional';
};

export type NewLocationAddRequestHotel = Omit<NewLocationAddRequest, 'newEntityType'> & {
    /**
     * If you set `newLocationId`, you must specify either `newLocationData` or `newEntityData`.
     *
     * This field allows you to set the profile data for an entity.
     * Profile content is specified in the same format as when creating an entity with the Entities: Create endpoint.
     * You can only create entities of a type specified by `newEntityType`.
     *
     * If the Add Request is processed successfully, this data will be used to create a new entity in your account.
     *
     * **The dropdown will specify the profile content for `newEntityData`. Specify the entity type with `newEntityType` described above.**
     *
     */
    newEntityData?: Omit<HotelWrite, 'EntityType'> & {
        EntityType?: 'NewLocationAddRequestHotel';
    };
    newEntityType?: 'hotel';
};

export type NewLocationAddRequestLocation = Omit<NewLocationAddRequest, 'newEntityType'> & {
    /**
     * If you set `newLocationId`, you must specify either `newLocationData` or `newEntityData`.
     *
     * This field allows you to set the profile data for an entity.
     * Profile content is specified in the same format as when creating an entity with the Entities: Create endpoint.
     * You can only create entities of a type specified by `newEntityType`.
     *
     * If the Add Request is processed successfully, this data will be used to create a new entity in your account.
     *
     * **The dropdown will specify the profile content for `newEntityData`. Specify the entity type with `newEntityType` described above.**
     *
     */
    newEntityData?: Omit<LocationWrite, 'EntityType'> & {
        EntityType?: 'NewLocationAddRequestLocation';
    };
    newEntityType?: 'location';
};

export type NewLocationAddRequestRestaurant = Omit<NewLocationAddRequest, 'newEntityType'> & {
    /**
     * If you set `newLocationId`, you must specify either `newLocationData` or `newEntityData`.
     *
     * This field allows you to set the profile data for an entity.
     * Profile content is specified in the same format as when creating an entity with the Entities: Create endpoint.
     * You can only create entities of a type specified by `newEntityType`.
     *
     * If the Add Request is processed successfully, this data will be used to create a new entity in your account.
     *
     * **The dropdown will specify the profile content for `newEntityData`. Specify the entity type with `newEntityType` described above.**
     *
     */
    newEntityData?: Omit<RestaurantWrite, 'EntityType'> & {
        EntityType?: 'NewLocationAddRequestRestaurant';
    };
    newEntityType?: 'restaurant';
};

export type LocationType = 'LOCATION' | 'HEALTHCARE_PROFESSIONAL' | 'HEALTHCARE_FACILITY' | 'RESTAURANT' | 'ATM';

export type Photo = {
    /**
     * Valid URL to image. Accepted formats: .jpg, .gif, .png.
     *
     * While updating this field, if the image could not be downloaded, or if its URL is invalid, the image will be ignored. The success response will contain a warning message explaining why the image was not stored in the system.
     *
     */
    url?: string;
    /**
     * The URL the image was uploaded from, if applicable. Note that this URL may not currently be valid.
     *
     */
    readonly sourceUrl?: string;
    /**
     * Image description.
     */
    description?: string;
    /**
     * Image details.
     */
    details?: string;
    /**
     * The alternate text to be used for accessibility purposes.
     */
    alternateText?: string;
    /**
     * Original photo width.
     */
    readonly width?: number;
    /**
     * Original photo height.
     */
    readonly height?: number;
    readonly derivatives?: Array<{
        /**
         * The URL to derivative image.
         * Derivative images are alternate versions of the original image (e.g., smaller versions used to improve page-load times on your site). They are primarily used with our Pages product.
         */
        readonly url?: string;
        /**
         * Derivative photo width.
         */
        readonly width?: number;
        /**
         * Derivative photo height.
         */
        readonly height?: number;
    }>;
};

export type Location = {
    /**
     * Primary key. Unique alphanumeric (Latin-1) ID assigned by the Customer.
     */
    id?: string;
    /**
     * A static globally unique id for the location. Note that this field cannot be used in place of the location id in API calls to get or update location information.
     */
    readonly uid?: string;
    /**
     * Must refer to an **account.id** that already exists.
     */
    accountId?: string;
    /**
     * The timestamp of the most recent change to this location record.  Will be ignored when the client is saving location data to Yext.
     *
     * **NOTE:** The timestamp may change even if observable fields stay the same.
     *
     */
    readonly timestamp?: number;
    /**
     * The timezone of the location
     *
     */
    readonly timezone?: string;
    locationType?: LocationType;
    /**
     * Cannot include:
     * * inappropriate language
     * * HTML markup or entities
     * * a URL or domain name
     * * a phone number
     * * control characters ([\x00-\x1F\x7F])
     *
     * Should be in appropriate letter case (e.g., not in all capital letters)
     *
     */
    locationName?: string;
    /**
     * The first name of the healthcare professional
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `HEALTHCARE_PROFESSIONAL`.
     *
     */
    firstName?: string;
    /**
     * The middle name of the healthcare professional
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `HEALTHCARE_PROFESSIONAL`.
     *
     */
    middleName?: string;
    /**
     * The last name of the healthcare professional
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `HEALTHCARE_PROFESSIONAL`.
     *
     */
    lastName?: string;
    /**
     * The name of the office where the healthcare professional works, if different from **locationName**
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `HEALTHCARE_PROFESSIONAL`.
     *
     */
    officeName?: string;
    /**
     * The gender of the healthcare professional
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `HEALTHCARE_PROFESSIONAL`.
     *
     */
    gender?: 'FEMALE' | 'F' | 'MALE' | 'M' | 'UNSPECIFIED';
    /**
     * The National Provider Identifier (NPI) of the healthcare provider
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `HEALTHCARE_PROFESSIONAL` or `HEALTHCARE_FACILITY`.
     *
     */
    npi?: string;
    /**
     * Must be a valid address
     *
     * Cannot be a P.O. Box
     *
     */
    address?: string;
    /**
     * Cannot be a P.O. Box
     */
    address2?: string;
    /**
     * If true, do not show street address on listings. Defaults to false.
     */
    suppressAddress?: boolean;
    /**
     * Provides additional information to help consumers get to the location. This string appears along with the location's address (e.g., In Menlo Mall, 3rd Floor).
     *
     * It may also be used in conjunction with a hidden address (i.e., when **suppressAddress** is true) to give consumers information about where the location is found (e.g., Servicing the New York area).
     *
     * Cannot be a P.O. Box
     *
     */
    displayAddress?: string;
    city?: string;
    /**
     * For US locations, the two-character code of the location’s state, or DC for the District of Columbia
     * For non-US locations, the name of the location’s province / region / state
     *
     */
    state?: string;
    /**
     * The name of the location's sublocality.
     */
    sublocality?: string;
    /**
     * The location's postal code. For US locations, this field contains the five- or nine-digit ZIP code (the hyphen is optional).
     */
    zip?: string;
    /**
     * The two-character ISO 3166-1 code of the location's country or region. If omitted, US is used.
     */
    countryCode?: string;
    /**
     * Area that is served by this location. It may be specified as a radius from the location's address or as a list of cities and/or postal codes.
     *
     * **Only for Google Business Profile:** Currently, **serviceArea** is only supported by Google Business Profile and will not affect your listings on other sites.
     *
     */
    serviceArea?: {
        /**
         * The distance around the location that the business serves
         *
         * **NOTE:** This field is no longer supported by Google Business Profile and is deprecated. We no longer accept or store values for **`radius`**.
         *
         */
        radius?: number;
        /**
         * The unit in which radius is measured.
         *
         * **NOTE:** This field is no longer supported by Google Business Profile and is deprecated. We no longer accept or store values for **`units`**.
         *
         */
        unit?: string;
        /**
         * A list of places served by the location, where each place is either:
         * * a postal code, or
         * * the name of a city.
         *
         */
        places?: Array<string>;
    };
    /**
     * Must be a valid phone number.
     */
    phone?: string;
    /**
     * Set to true if the number listed in **phone** is a tracked phone number.
     *
     * **NOTE:** When updating **isPhoneTracked**, you must provide a value for **phone** in the same request.
     *
     */
    isPhoneTracked?: boolean;
    /**
     * Must be a valid, non-toll-free phone number.
     *
     * Required if:
     * * **isPhoneTracked** is true and the non-tracked number is a toll-free number, **OR**
     * * **isPhoneTracked** is false and **phone** is a toll-free number
     *
     */
    localPhone?: string;
    /**
     * Must be a valid phone number, based on the country specified in `countryCode`. Phone numbers for US locations must contain 10 digits.
     */
    alternatePhone?: string;
    /**
     * Must be a valid phone number, based on the country specified in `countryCode`. Phone numbers for US locations must contain 10 digits.
     */
    faxPhone?: string;
    /**
     * Must be a valid phone number, based on the country specified in `countryCode`. Phone numbers for US locations must contain 10 digits.
     */
    mobilePhone?: string;
    /**
     * Must be a valid phone number, based on the country specified in `countryCode`. Phone numbers for US locations must contain 10 digits.
     */
    tollFreePhone?: string;
    /**
     * Must be a valid phone number, based on the country specified in `countryCode`. Phone numbers for US locations must contain 10 digits.
     */
    ttyPhone?: string;
    /**
     * Yext Category IDs. A Location must have at least one and at most 10 Categories.
     *
     * IDs must be valid and selectable (i.e., cannot be parent categories).
     *
     * **NOTE:** The list of category IDs that you send us must be comprehensive. For example, if you send us a list of IDs that does not include IDs that you sent in your last update, Yext considers the missing categories to be deleted, and we remove them from your listings.
     *
     */
    categoryIds?: Array<string>;
    /**
     * The Featured Message. Default: Call today!
     *
     * Cannot include:
     * * inappropriate language
     * * HTML markup
     * * a URL or domain name
     * * a phone number
     * * control characters ([\x00-\x1F\x7F])
     * * insufficient spacing
     *
     * If you submit a Featured Message that contains profanity or more than 50 characters, it will be ignored. The success response will contain a warning message explaining why your Featured Message wasn't stored in the system.
     *
     */
    featuredMessage?: string;
    /**
     * Valid URL to which the Featured Message is linked
     */
    featuredMessageUrl?: string;
    /**
     * The URL of the location's website. This URL will be shown on your listings unless you specify a value for `displayWebsiteUrl`.
     *
     * Must be a valid URL and is required whenever `displayWebsiteUrl` is specified.
     *
     */
    websiteUrl?: string;
    /**
     * The URL that is shown on your listings in place of `websiteUrl`. You can use `displayWebsiteUrl` to display a short, memorable web address that redirects consumers to the URL given in `websiteUrl`.
     *
     * Must be a valid URL and be specified along with `websiteUrl`.
     *
     */
    displayWebsiteUrl?: string;
    /**
     * A valid URL used for reservations at this location.
     */
    reservationUrl?: string;
    /**
     * The URL that is shown on your listings in place of `reservationUrl`. You can use `displayReservationUrl` to display a short, memorable web address that redirects consumers to the URL given in `reservationUrl`.
     *
     * Must be a valid URL and be specified along with `reservationUrl`.
     *
     */
    displayReservationUrl?: string;
    /**
     * The URL of the location's menu.
     */
    menuUrl?: string;
    /**
     * The URL that is shown on your listings in place of `menuUrl`. You can use `displayMenuUrl` to display a short, memorable web address that redirects consumers to the URL given in `menuUrl`.
     *
     * Must be a valid URL and be specified along with `menuUrl`.
     *
     */
    displayMenuUrl?: string;
    /**
     * The URL used to place orders that will be fulfilled at the location.
     */
    orderUrl?: string;
    /**
     * The URL that is shown on your listings in place of `orderUrl`. You can use `displayOrderUrl` to display a short, memorable web address that redirects consumers to the URL given in `orderUrl`.
     *
     * Must be a valid URL and be specified along with `orderUrl`.
     *
     */
    displayOrderUrl?: string;
    /**
     * Hours should be submitted as a comma-separated list of days, where each day's hours are specified as follows:
     *
     * d:oh:om:ch:cm
     * * d = day of the week –
     * * 1 – Sunday
     * * 2 – Monday
     * * 3 – Tuesday
     * * 4 – Wednesday
     * * 5 – Thursday
     * * 6 – Friday
     * * 7 – Saturday
     * * oh:om = opening time in 24-hour format
     * * ch:cm = closing time in 24-hour format
     *
     * Times with single-digit hours (e.g., 9 AM) can be submitted with or without a leading zero (9:00 or 09:00).
     *
     * **Example:** open 9 AM to 5 PM Monday and Tuesday, open 10 AM to 4 PM on Saturday – 2:9:00:17:00,3:9:00:17:00,7:10:00:16:00
     *
     * SPECIAL CASES:
     * * To indicate that a location is open 24 hours on a specific day, set 00:00 as both the opening and closing time for that day.
     * * **Example:** open all day on Saturdays – 7:00:00:00:00
     * * To indicate that a location is closed on a specific day, omit that day from the list or set it as closed ("closed" is not case sensitive).
     * * **Example:** closed on Sundays – 1:closed
     * * **NOTE:** If a location is closed seven days a week, set at least one day to closed. Otherwise, **hours** is an empty string, and we assume you are not submitting hours information for that location.
     * * To indicate that a location has split hours on a specific day, submit a set of hours for each block of time the location is open.
     * * **Example:** open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM on Mondays – 2:9:00:12:00,2:13:00:17:00
     *
     * **NOTE:** To set hours for specific days of the year rather than days of the week, use **holidayHours**.
     *
     */
    hours?: string;
    /**
     * Additional information about business hours that does not fit in **hours** (e.g., Closed during the winter)
     */
    additionalHoursText?: string;
    /**
     * Holiday hours for this location.
     *
     * **NOTE:** hours must be set in order for holidayHours to appear on your listings)
     *
     */
    holidayHours?: Array<{
        /**
         * Special opening and closing times in 24-hour format (OH:OM:CH:CM, where OH:OM is the opening time and CH:CM is the closing time).
         *
         * Times with single-digit hours (e.g., 9 AM) can be submitted with or without a leading zero (9:00 or 09:00).
         *
         * Examples:
         * * 9:00:15:00 — Opening at 9:00 AM, closing at 3:00 PM
         * * "" (empty string) — Closed all day
         * * 0:00:0:00 or 0:00:23:59 — Open 24 hours
         * * 9:00:15:00,17:00:19:00 — Split hours: open from 9:00 AM to 3:00 PM and again from 5:00 PM to 7:00 PM
         *
         * **NOTE:** If **isRegularHours** is set to true, we will ignore this field.
         *
         */
        hours?: string;
        /**
         * The date on which the holiday hours will be in effect
         */
        date?: string;
        /**
         * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
         */
        isRegularHours?: boolean;
    }>;
    description?: string;
    /**
     * A list of the conditions treated by the healthcare provider
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `HEALTHCARE_PROFESSIONAL` or `HEALTHCARE_FACILITY`.
     *
     */
    conditionsTreated?: Array<string>;
    /**
     * A list of the certifications held by the healthcare professional
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `HEALTHCARE_PROFESSIONAL`.
     *
     */
    certifications?: Array<string>;
    /**
     * A list of the types of education and training completed by the healthcare professional
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `HEALTHCARE_PROFESSIONAL`.
     *
     */
    educationList?: Array<{
        /**
         * The kind of education or training completed
         */
        type?: 'FELLOWSHIP' | 'INTERNSHIP' | 'MEDICAL_SCHOOL' | 'RESIDENCY';
        /**
         * The name of the institution where the healthcare professional received the education or training
         */
        institutionName?: string;
        /**
         * The year the healthcare professional completed the education or training
         */
        yearCompleted?: string;
    }>;
    /**
     * A list of the degrees earned by the healthcare professional
     *
     * **NOTE:**  This field is only available to locations whose **`locationType`** is `HEALTHCARE_PROFESSIONAL`.
     *
     * Valid values:
     *
     * * `ANP` (Adult Nurse Practitioner)
     * * `APN` (Advanced Practice Nurse)
     * * `APRN` (Advanced Practice Registered Nurse)
     * * `ARNP` (Advanced Registered Nurse Practitioner)
     * * `CNM` (Certified Nurse Midwife)
     * * `CNP` (Certified Nurse Practitioner)
     * * `CNS` (Clinical Nurse Specialist)
     * * `CPNP` (Certified Pediatric Nurse Practitioner)
     * * `CRNA` (Certified Registered Nurse Anesthetist)
     * * `CRNP` (Certified Registered Nurse Practitioner)
     * * `DC` (Doctor of Chiropractic)
     * * `DDS` (Doctor of Dental Surgery)
     * * `DMD` (Doctor of Dental Medicine)
     * * `DO` (Doctor of Osteopathy)
     * * `DPM` (Doctor of Podiatric Medicine)
     * * `DVM` (Doctor of Veterinary Medicine)
     * * `FNP` (Family Nurse Practitioner)
     * * `GNP` (Geriatric Nurse Practitioner)
     * * `LAC` (Licensed Acupuncturist)
     * * `LPN` (Licensed Practical Nurse)
     * * `MD` (Medical Doctor)
     * * `ND` (Naturopathic Doctor)
     * * `NP` (Nurse Practitioner)
     * * `OD` (Doctor of Optometry)
     * * `PA` (Physician Assistant)
     * * `PAC` (Physician Assistant Certified)
     * * `PHARMD` (Doctor of Pharmacy)
     * * `PHD` (Doctor of Philosophy)
     * * `PNP` (Pediatric Nurse Practitioner)
     * * `VMD` (Veterinary Medical Doctor)
     * * `WHNP` (Womens Health Nurse Practitioner)
     *
     */
    degrees?: Array<string>;
    /**
     * A list of hospitals where the healthcare professional admits patients
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `HEALTHCARE_PROFESSIONAL`.
     *
     */
    admittingHospitals?: Array<string>;
    /**
     * Indicates whether the healthcare provider is accepting new patients
     *
     * Default is true
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `HEALTHCARE_PROFESSIONAL` or `HEALTHCARE_FACILITY`.
     *
     */
    acceptingNewPatients?: boolean;
    /**
     * A set of field-value pairs indicating whether the location is closed and, if it is closed, the date of its closing.
     *
     * **NOTE:** This field does not appear in the GET response unless it has been explicitly set in a PUT request.
     *
     */
    closed?: {
        /**
         * Indicates whether the location is closed
         */
        isClosed?: boolean;
    };
    /**
     * The payment methods accepted at this location
     *
     * Valid elements depend on the location's country. For US locations, valid elements are:
     * * AMERICANEXPRESS
     * * CASH
     * * CHECK
     * * DINERSCLUB
     * * DISCOVER
     * * FINANCING
     * * INVOICE
     * * MASTERCARD
     * * TRAVELERSCHECK
     * * VISA
     * * ANDROIDPAY
     * * APPLEPAY
     * * SAMSUNGPAY
     * * BITCOIN
     * * PAYPAL
     *
     */
    paymentOptions?: Array<string>;
    /**
     * A list of insurance policies accepted by the healthcare provider
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `HEALTHCARE_PROFESSIONAL`.
     *
     */
    insuranceAccepted?: Array<string>;
    logo?: Photo;
    /**
     * Up to 50 Photos.
     *
     * **NOTE:** The list of photos that you send us must be comprehensive. For example, if you send us a list of photos that does not include photos that you sent in your last update, Yext considers the missing photos to be deleted, and we remove them from your listings.
     *
     */
    photos?: Array<Photo>;
    /**
     * A portrait of the healthcare professional
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `HEALTHCARE_PROFESSIONAL`.
     *
     */
    headshot?: Photo;
    /**
     * Valid YouTube URLs for embedding a video on some publisher sites.
     *
     * **NOTE:** Currently, only the first URL in the Array appears in your listings.
     *
     */
    videoUrls?: Array<string>;
    /**
     * Valid Instagram username for the location (e.g., NewCityFiat (without the leading "@"))
     */
    instagramHandle?: string;
    /**
     * Valid Twitter handle for the location (e.g., JohnSmith (without the leading '@')).
     * If you submit an invalid Twitter handle, it will be ignored. The success response will contain a warning message explaining why your Twitter handle wasn't stored in the system.
     */
    twitterHandle?: string;
    /**
     * The URL you would like to submit to Google Business Profile in place of the one given in **websiteUrl** (if applicable).
     *
     * For example, if you want to analyze the traffic driven by your Google listings separately from other traffic, enter the alternate URL that you will use for tracking in this field.
     *
     */
    googleWebsiteOverride?: string;
    /**
     * The cover photo for your business's Google profile
     *
     * NOTE: Your cover photo must meet all of the following requirements:
     * * have a 16:9 aspect ratio
     * * be at least 480 x 270 pixels
     * * be no more than 2120 x 1192 pixels
     *
     */
    googleCoverPhoto?: Photo;
    /**
     * The profile photo for your business's Google profile
     *
     * **NOTE:** Your profile picture must meet all of the following requirements:
     * * be a square
     * * be at least 250 x 250 pixels
     *
     */
    googleProfilePhoto?: Photo;
    /**
     * The Google Business Profile attributes for this location.
     *
     */
    googleAttributes?: Array<{
        /**
         * The unique ID Of the Google Business Profile keyword
         *
         * Keywords are determined by the location's primary category (e.g., `has_drive_through`, `has_fitting_room`, `kitchen_in_room`).
         *
         */
        id?: string;
        /**
         * The unique IDs of any options selected for the keyword.
         *
         * Keyword options provide more details on how the keyword applies to the location (e.g., if **`id`** is `has_drive_through`, **`optionIds`** may be `true` or `false`).
         *
         */
        optionIds?: Array<string>;
    }>;
    /**
     * URL for the location's Facebook Page.
     *
     * Valid formats:
     * * facebook.com/profile.php?id=[numId]
     * * facebook.com/group.php?gid=[numId]
     * * facebook.com/groups/[numId]
     * * facebook.com/[Name]
     * * facebook.com/pages/[Name]/[numId]
     *
     * where [Name] is a String and [numId] is an Integer
     *
     * If you submit a URL that is not in one of the valid formats, it will be ignored. The success response will contain a warning message explaining why the URL wasn't stored in the system.
     *
     * **NOTE:** This value is automatically set to the location's Facebook Page URL. You can only manually set **facebookPageUrl** if the location meets one of the following criteria:
     * * It is not subscribed to a Listings package that contains Facebook.
     * * It is opted out of Facebook.
     *
     */
    facebookPageUrl?: string;
    /**
     * Designates the Facebook Call-to-Action button text and value
     */
    facebookCallToAction?: {
        /**
         * The action the consumer is being prompted to take by the button's text
         */
        type?: 'NONE' | 'BOOK_NOW' | 'CALL_NOW' | 'CONTACT_US' | 'SEND_MESSAGE' | 'USE_APP' | 'PLAY_GAME' | 'SHOP_NOW' | 'SIGN_UP' | 'WATCH_VIDEO' | 'SEND_EMAIL' | 'LEARN_MORE' | 'PURCHASE_GIFT_CARDS' | 'ORDER_NOW' | 'FOLLOW_PAGE';
        /**
         * Indicates where consumers will be directed to upon clicking the Call-to-Action button (e.g., a URL). It can be a free-form string or an embedded value, depending on what the user specifies.
         *
         * For example, if the user sets the Facebook Call-to-Action as " 'Sign Up' using 'Website URL' " in the Yext platform, **`type`** will be `SIGN_UP` and **`value`** will be `[[websiteUrl]]`.
         *
         * The Call-to-Action will have the same behavior if the user sets the value to "Custom Value" in the platform and embeds a field.
         */
        value?: string;
    };
    /**
     * The cover photo for your business's Facebook profile
     *
     * Displayed as a 851 x 315 pixel image
     *
     * You must have a cover photo in order for your listing to appear on Facebook.
     *
     * **NOTE:** Your cover photo must be at least 400 pixels wide.
     *
     */
    facebookCoverPhoto?: Photo;
    /**
     * The profile picture for your business's Facebook profile
     *
     * You must have a profile picture in order for your listing to appear on Facebook.
     *
     * **NOTE:** Your profile picture must be larger than 180 x 180 pixels.
     *
     */
    facebookProfilePicture?: Photo;
    /**
     * Indicates whether the embedded Uber link for this location appears as text or a button
     *
     * When consumers click on this link on a mobile device, the Uber app (if installed) will open with your location set as the trip destination. If the Uber app is not installed, the consumer will be prompted to download it.
     *
     */
    uberLinkType?: 'LINK' | 'BUTTON';
    /**
     * The text of the embedded Uber link
     *
     * Default is "Ride there with Uber".
     *
     * **NOTE:** This field is only available if **uberLinkType** is LINK.
     *
     */
    uberLinkText?: string;
    /**
     * The text of the call-to-action that will appear in the Uber app during a trip to your location (e.g., Check out our menu!)
     *
     * **NOTE:** If a value for **uberTripBrandingText** is provided, values must also be provided for **uberTripBrandingUrl** and **uberTripBrandingDescription**.
     *
     */
    uberTripBrandingText?: string;
    /**
     * The URL that the consumer will be redirected to when tapping on the call-to-action in the Uber app during a trip to your location.
     *
     * **NOTE:** If a value for **uberTripBrandingUrl** is provided, values must also be provided for **uberTripBrandingText** and **uberTripBrandingDescription**.
     *
     */
    uberTripBrandingUrl?: string;
    /**
     * A longer description that will appear near the call-to-action in the Uber app during a trip to your location.
     *
     * **NOTE:** If a value for **uberTripBrandingDescription** is provided, values must also be provided for **uberTripBrandingText** and **uberTripBrandingUrl**.
     *
     */
    uberTripBrandingDescription?: string;
    /**
     * The Yext-powered code that can be copied and pasted into the markup of emails or web pages where the embedded Uber link should appear
     */
    readonly uberEmbedCode?: string;
    /**
     * The Yext-powered link that can be copied and pasted into the markup of Yext Pages where the embedded Uber link should appear
     */
    readonly uberLink?: string;
    /**
     * The Uber universal link for the location.
     *
     * For more information on universal links, see Uber's developer documentation.
     *
     * **NOTE**: This field is only available in the LiveAPI and only for US locations.
     *
     */
    readonly uberLinkRaw?: string;
    /**
     * The year that this location was opened, not the number of years it was open
     *
     * Minimum of 1000, maximum of current year + 10.
     *
     */
    yearEstablished?: string;
    /**
     * Latitude where the map pin should be displayed, as provided by you
     *
     * Between -90.0 and 90.0, inclusive
     *
     */
    displayLat?: number;
    /**
     * Longitude where the map pin should be displayed, as provided by you
     *
     * Between -180.0 and 180.0, inclusive
     *
     */
    displayLng?: number;
    /**
     * Latitude to use for driving directions to the location, as provided by you
     *
     * Between -90.0 and 90.0, inclusive
     *
     */
    routableLat?: number;
    /**
     * Longitude to use for driving directions to the location, as provided by you
     *
     * Between -180.0 and 180.0, inclusive
     *
     */
    routableLng?: number;
    /**
     * Latitude to use for walking directions to the location, as provided by you
     *
     * Between -90.0 and 90.0, inclusive
     *
     */
    walkableLat?: number;
    /**
     * Longitude to use for walking directions to the location, as provided by you
     *
     * Between -180.0 and 180.0, inclusive
     *
     */
    walkableLng?: number;
    /**
     * Latitude to use for pickup spot for the location, as provided by you
     *
     * Between -90.0 and 90.0, inclusive
     *
     */
    pickupLat?: number;
    /**
     * Longitude to use for pickup spot for the location, as provided by you
     *
     * Between -180.0 and 180.0, inclusive
     *
     */
    pickupLng?: number;
    /**
     * Latitude to use for drop off spot for the location, as provided by you
     *
     * Between -90.0 and 90.0, inclusive
     *
     */
    dropoffLat?: number;
    /**
     * Longitude to use for drop off spot for the location, as provided by you
     *
     * Between -180.0 and 180.0, inclusive
     *
     */
    dropoffLng?: number;
    /**
     * Latitude where the map pin should be displayed, as calculated by Yext
     *
     * Between -90.0 and 90.0, inclusive
     *
     */
    readonly yextDisplayLat?: number;
    /**
     * Longitude where the map pin should be displayed, as calculated by Yext
     *
     * Between -180.0 and 180.0, inclusive
     *
     */
    readonly yextDisplayLng?: number;
    /**
     * Latitude to use for driving directions to the location, as calculated by Yext
     *
     * Between -90.0 and 90.0, inclusive
     *
     */
    readonly yextRoutableLat?: number;
    /**
     * Longitude to use for driving directions to the location, as calculated by Yext
     *
     * Between -180.0 and 180.0, inclusive
     *
     */
    readonly yextRoutableLng?: number;
    /**
     * Latitude to use for walking directions to the location, as calculated by Yext
     *
     * Between -90.0 and 90.0, inclusive
     *
     */
    readonly yextWalkableLat?: number;
    /**
     * Longitude to use for walking directions to the location, as calculated by Yext
     *
     * Between -180.0 and 180.0, inclusive
     *
     */
    readonly yextWalkableLng?: number;
    /**
     * Latitude to use for pickup spot for the location, as calculated by Yext
     *
     * Between -90.0 and 90.0, inclusive
     *
     */
    readonly yextPickupLat?: number;
    /**
     * Longitude to use for pickup spot for the location, as calculated by Yext
     *
     * Between -180.0 and 180.0, inclusive
     *
     */
    readonly yextPickupLng?: number;
    /**
     * Latitude to use for drop off spot for the location, as calculated by Yext
     *
     * Between -90.0 and 90.0, inclusive
     *
     */
    readonly yextDropoffLat?: number;
    /**
     * Longitude to use for drop off spot for the location, as calculated by Yext
     *
     * Between -180.0 and 180.0, inclusive
     *
     */
    readonly yextDropoffLng?: number;
    /**
     * Up to five emails addresses for reaching this location
     *
     * Must be valid email addresses
     *
     */
    emails?: Array<string>;
    /**
     * Up to 100 specialities (e.g., for food and dining: Chicago style)
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     */
    specialities?: Array<string>;
    /**
     * Up to 100 association memberships relevant to the location (e.g., New York Doctors Association)
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     */
    associations?: Array<string>;
    /**
     * Up to 100 products sold at this location
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     */
    products?: Array<string>;
    /**
     * Up to 100 services offered at this location
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     */
    services?: Array<string>;
    /**
     * Up to 100 brands sold by this location
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     */
    brands?: Array<string>;
    /**
     * Language code of the language in which this location's information is provided. This language is considered the Location's primary language in our system.
     *
     *
     * If you would like to provide your Location data in more than one language, you can create a Language Profile for each of these additional (alternate) languages.
     *
     */
    language?: string;
    /**
     * Up to 100 languages spoken at this location.
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     */
    languages?: Array<string>;
    /**
     * Up to 100 keywords may be provided
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     */
    keywords?: Array<string>;
    /**
     * Label to be used for this location’s Menus. This label will appear on your location's listings.
     */
    menusLabel?: string;
    /**
     * IDs of Menus associated with this location.
     */
    menuIds?: Array<string>;
    /**
     * Label to be used for this location’s Bio lists. This label will appear on your location's listings.
     */
    bioListsLabel?: string;
    /**
     * IDs of Bio lists associated with this location.
     */
    bioListIds?: Array<string>;
    /**
     * Label to be used for this location’s Product & Services lists. This label will appear on your location's listings.
     */
    productListsLabel?: string;
    /**
     * IDs of Product lists associated with this location.
     */
    productListIds?: Array<string>;
    /**
     * Label to be used for this location’s Event lists. This label will appear on your location's listings.
     */
    eventListsLabel?: string;
    /**
     * IDs of Event lists associated with this location.
     */
    eventListIds?: Array<string>;
    /**
     * The folder that this location is in. Must be a valid, existing Yext Folder ID
     */
    folderId?: string;
    /**
     * The IDs of the location labels that have been added to this location. Location labels help you identify locations that share a certain characteristic; they do not appear on your location's listings.
     *
     * **NOTE:** You can only add labels that have already been created via our web interface. Currently, it is not possible to create new labels via the API.
     *
     * In Locations: Update requests:
     * * If the **`v`** parameter is before `20180223`: setting the value of **`labelIds`** to an empty array has no effect on the current value
     * * If the **`v`** parameter is `20180223` or after: setting the value of **`labelIds`** to an empty array deletes the current value
     *
     */
    labelIds?: Array<string>;
    /**
     * Indicates whether Intelligent Search Tracker is enabled.
     *
     * The Intelligent Search Tracker allows you to understand your performance in local search.
     *
     */
    intelligentSearchTrackingEnabled?: boolean;
    /**
     * How often we send search queries to track your search performance.
     *
     */
    intelligentSearchTrackingFrequency?: 'WEEKLY' | 'MONTHLY' | 'QUARTERLY';
    /**
     * Keywords that we will use to track your search performance. These keywords are based on the location information you've stored in our system.
     *
     */
    locationKeywords?: Array<'NAME' | 'PRIMARY_CATEGORY'>;
    /**
     * Additional keywords you would like us to use when tracking your search performance
     *
     */
    customKeywords?: Array<string>;
    /**
     * The ways in which your keywords will be arranged in the search queries we use to track your performance
     *
     */
    queryTemplates?: Array<'KEYWORD' | 'KEYWORD_ZIP' | 'KEYWORD_CITY' | 'KEYWORD_IN_CITY' | 'KEYWORD_NEAR_ME' | 'KEYWORD_CITY_STATE'>;
    /**
     * Other names for your business that you would like us to use when tracking your search performance
     *
     */
    alternateNames?: Array<string>;
    /**
     * Other websites for your business that we should look for when tracking your search performance
     *
     */
    alternateWebsites?: Array<string>;
    /**
     * The names and websites of the competitors whose search performance you would like to compare to your own
     *
     */
    competitors?: Array<{
        /**
         * The competitor's name
         */
        name?: string;
        /**
         * The competitor's website.
         */
        website?: string;
    }>;
    /**
     * The search engines that we will use to track your performance
     *
     */
    trackingSites?: Array<'GOOGLE_DESKTOP' | 'GOOGLE_MOBILE' | 'BING_DESKTOP' | 'BING_MOBILE' | 'YAHOO_DESKTOP' | 'YAHOO_MOBILE'>;
    /**
     * The ISO 3166-2 region code for the location.
     *
     * Yext will determine the location’s code and update isoRegionCode with that value. If Yext is unable to determine the code for the location, the location’s ISO 3166-1 alpha-2 country code will be used.
     *
     */
    readonly isoRegionCode?: string;
    /**
     * Link to the balancing URL that will auto-direct consumers to certain sites to leave reviews, based on review-generation settings
     *
     */
    readonly reviewBalancingURL?: string;
    /**
     * Link to the review-collection page, where consumers can leave first-party reviews
     *
     */
    readonly firstPartyReviewPage?: string;
    /**
     * Indicates whether the location is the primary location in its group
     *
     */
    isClusterPrimary?: boolean;
    /**
     * List of Schema Types for this location, based on its categories
     *
     */
    readonly schemaTypes?: Array<string>;
    /**
     * The formality of clothing typically worn at this location
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `RESTAURANT`.
     *
     */
    attire?: 'UNSPECIFIED' | 'DRESSY' | 'CASUAL' | 'FORMAL';
    /**
     * The typical price of products sold at this location, on a scale of 1 (low) to 4 (high)
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `RESTAURANT`.
     *
     */
    priceRange?: 'UNSPECIFIED' | 'ONE' | 'TWO' | 'THREE' | 'FOUR';
    /**
     * Types of meals served at this location
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `RESTAURANT`.
     *
     * Valid values:
     *
     * * `BREAKFAST`
     * * `LUNCH`
     * * `BRUNCH`
     * * `HAPPY_HOUR`
     * * `LATE_NIGHT`
     *
     */
    mealsServed?: Array<string>;
    /**
     * For ATMs, the external ID of the location that the ATM is installed in. The location must be in the same business account as the ATM.
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `ATM`.
     *
     */
    locatedIn?: string;
    /**
     * ID of the user who is the primary Knowledge Assistant contact for the entity
     *
     */
    primaryContact?: string;
    /**
     * Indicates whether or not review response conversations are enabled for the Yext Knowledge Assistant
     *
     */
    reviewResponseConversationEnabled?: boolean;
    /**
     * Indicates whether or not holiday hour confirmation alerts are enabled for the Yext Knowledge Assistant
     *
     */
    holidayHoursConfirmationEnabled?: boolean;
};

export type Entity = {
    /**
     * Contains the metadata about the entity.
     *
     * ```
     * Eligible For:
     * * atm
     * * event
     * * faq
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * job
     * * location
     * * restaurant
     * ```
     */
    meta?: {
        /**
         * ID of the account associated with this Entity
         */
        accountId?: string;
        /**
         * Country code of this Entity's Language Profile (defaults to the country of the account)
         *
         * Filtering Type: `text`
         */
        countryCode?: string;
        /**
         * The timestamp of when the entity record was created.
         */
        createdTimestamp?: string;
        /**
         * This Entity's type (e.g., location, event)
         *
         * Filtering Type: `text`
         */
        entityType?: string;
        /**
         * The ID of the folder containing this Entity
         *
         * Filtering Type: `text`
         */
        folderId?: string;
        /**
         * ID of this Entity
         *
         * Filtering Type: `text`
         */
        id?: string;
        /**
         * This Entity's labels. If the **`v`** parameter is before `20211215`, this will be an integer.
         *
         * Filtering Type: `list of text`
         */
        labels?: Array<string>;
        /**
         * Language code of this Entity's Language Profile (defaults to the language code of the account)
         *
         * Filtering Type: `text`
         */
        language?: string;
        /**
         * The timestamp of the most recent change to this entity record. Will be ignored when the client is saving entity data to Yext.
         *
         * **NOTE:** The timestamp may change even if observable fields stay the same.
         *
         */
        timestamp?: string;
        /**
         * The internal ID of the entity. This UID is a static, globally unique ID. Note that this value cannot be used in place of id in API calls to retrieve or edit Entity information.
         *
         * If the v param is before `20221206`, the returned value will be a hashed version of the entity UID (aka internal ID of the entity).
         *
         */
        uid?: string;
    };
    /**
     *
     *
     *
     * Cannot Include:
     * * HTML markup
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * atm
     * * board
     * * brand
     * * card
     * * contactCard
     * * event
     * * faq
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * helpArticle
     * * hotel
     * * hotelRoomType
     * * job
     * * location
     * * organization
     * * product
     * * restaurant
     * ```
     */
    name?: string;
    /**
     * Contains the address of the entity (or where the entity is located)
     *
     * Must be a valid address
     * Cannot be a P.O. Box
     *
     * If the entity is an `event`, either an **`address`** value or a **`linkedLocation`** value can be provided.
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * atm
     * * event
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    address?: {
        /**
         * The city the entity (or the entity's location) is in
         *
         *
         * Cannot Include:
         * * a URL or domain name
         *
         * Filtering Type: `text`
         */
        city?: string;
        /**
         * Filtering Type: `text`
         */
        countryCode?: string;
        /**
         * Provides additional information to help consumers get to the entity. This string appears along with the entity's address (e.g., `In Menlo Mall, 3rd Floor`).
         * It may also be used in conjunction with a hidden address (i.e., when **`addressHidden`** is `true`) to give consumers information about where the entity can be found (e.g., `Servicing the New York area`).
         *
         * Filtering Type: `text`
         */
        extraDescription?: string;
        /**
         *
         * Cannot Include:
         * * a URL or domain name
         *
         * Filtering Type: `text`
         */
        line1?: string;
        /**
         *
         * Cannot Include:
         * * a URL or domain name
         *
         * Filtering Type: `text`
         */
        line2?: string;
        /**
         * The entity's postal code. The postal code must be valid for the entity's country. Cannot include a URL or domain name.
         *
         *
         * Cannot Include:
         * * a URL or domain name
         *
         * Filtering Type: `text`
         */
        postalCode?: string;
        /**
         * The name of the entity's region or state.
         *
         *
         * Cannot Include:
         * * a URL or domain name
         *
         * Filtering Type: `text`
         */
        region?: string;
        /**
         * The name of the entity's sublocality
         *
         *
         * Cannot Include:
         * * a URL or domain name
         *
         * Filtering Type: `text`
         */
        sublocality?: string;
    };
    /**
     * Indicates whether the healthcare provider is accepting new patients.
     *
     * Filtering Type: `boolean`
     *
     * ```
     * Eligible For:
     * * healthcareFacility
     * * healthcareProfessional
     * ```
     */
    acceptingNewPatients?: boolean;
    /**
     * Indicates whether the entity accepts reservations.
     *
     * Filtering Type: `boolean`
     *
     * ```
     * Eligible For:
     * * restaurant
     * ```
     */
    acceptsReservations?: boolean;
    /**
     * Contains the daily access hours, holiday access hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `accessHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday access hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     *
     * Filtering Type: `hours`
     *
     * ```
     * Eligible For:
     * * atm
     * * healthcareFacility
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    accessHours?: {
        /**
         * Filtering Type: `object`
         */
        friday?: {
            /**
             * Indicates if the access hours are "closed" on Friday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         *
         * Filtering Type: `list of object`
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             *
             * Filtering Type: `date`
             */
            date: string;
            /**
             * Indicates if the access hours are "closed" on on the given date.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             *
             * Filtering Type: `boolean`
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on the specified date.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        }>;
        /**
         * Filtering Type: `object`
         */
        monday?: {
            /**
             * Indicates if the access hours are "closed" on Monday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         *
         * Filtering Type: `date`
         */
        reopenDate?: string;
        /**
         * Filtering Type: `object`
         */
        saturday?: {
            /**
             * Indicates if the access hours are "closed" on Saturday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        sunday?: {
            /**
             * Indicates if the access hours are "closed" on Sunday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        thursday?: {
            /**
             * Indicates if the access hours are "closed" on Thursday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        tuesday?: {
            /**
             * Indicates if the access hours are "closed" on Tuesday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        wednesday?: {
            /**
             * Indicates if the access hours are "closed" on Wednesday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
    };
    /**
     * Additional information about hours that does not fit in **`hours`** (e.g., `"Closed during the winter"`)
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * atm
     * * contactCard
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    additionalHoursText?: string;
    /**
     * If other locations are promoting this event, a list of those locations' **`id`**s in the Yext Knowledge Manager
     *
     *
     * Array must be ordered.
     *
     *
     * Filtering Type: `list of entityId`
     *
     * ```
     * Eligible For:
     * * event
     * ```
     */
    additionalPromotingLocations?: Array<string>;
    /**
     * If `true`, the entity's street address will not be shown on listings. Defaults to `false`.
     *
     * Filtering Type: `boolean`
     *
     * ```
     * Eligible For:
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    addressHidden?: boolean;
    /**
     * A list of hospitals where the healthcare professional admits patients
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     *
     * Filtering Type: `list of text`
     *
     * ```
     * Eligible For:
     * * healthcareProfessional
     * ```
     */
    admittingHospitals?: Array<string>;
    /**
     * Indicates whether the entity has a pool for adults only.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    adultPool?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Contains the age range for the event
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * event
     * ```
     */
    ageRange?: {
        /**
         * Maximum age for the event
         *
         * Filtering Type: `integer`
         */
        maxValue?: number;
        /**
         * Minimum age for the event
         *
         * Filtering Type: `integer`
         */
        minValue?: number;
    };
    /**
     * Indicates whether the entity offers a shuttle to/from the airport.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    airportShuttle?: 'AIRPORT_SHUTTLE_AVAILABLE' | 'AIRPORT_SHUTTLE_AVAILABLE_FOR_FREE' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity offers a shuttle service of car service to/from nearby airports or train stations.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    airportTransfer?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity offers all-inclusive rates.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    allInclusive?: 'ALL_INCLUSIVE_RATES_AVAILABLE' | 'ALL_INCLUSIVE_RATES_ONLY' | 'NOT_APPLICABLE';
    /**
     * Other names for your business that you would like us to use when tracking your search performance
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 3 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     *
     * Filtering Type: `list of text`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * organization
     * * restaurant
     * ```
     */
    alternateNames?: Array<string>;
    /**
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    alternatePhone?: string;
    /**
     * Other websites for your business that we should search for when tracking your search performance
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 3 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* common domain names, e.g., google.com, youtube.com, etc.
     *
     * Filtering Type: `list of text`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * organization
     * * restaurant
     * ```
     */
    alternateWebsites?: Array<string>;
    /**
     * The URL where consumers can download the entity's Android app
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * brand
     * * financialProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    androidAppUrl?: string;
    /**
     * The answer to the frequently asked question represented by this entity
     *
     *
     * Character limit: 0 .. 15000
     *
     * Supported formats include:
     * * BOLD
     * * ITALICS
     * * UNDERLINE
     * * BULLETED_LIST
     * * NUMBERED_LIST
     * * HYPERLINK
     * * IMAGE
     * * CODE_SPAN
     * * HEADINGS
     *
     * ```
     * Eligible For:
     * * faq
     * ```
     */
    answer?: string;
    /**
     * Use this field to add action links to your Apple Listings.
     *
     * The call to action category will be displayed on the action link button.
     *
     * The App Store URL should contain a valid link to the landing page of an App in the Apple App Store.
     *
     * The Quick Link URL is where a user is taken when an action link is clicked by a user.
     *
     * The App Name sub-field is not displayed on Apple Listings and is only used to distinguish the call-to-action type when utilizing action links in Apple posts.
     *
     *
     * Array must be ordered.
     *
     *
     * Filtering Type: `list of object`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    appleActionLinks?: Array<{
        /**
         * Filtering Type: `text`
         */
        appName: string;
        /**
         * Filtering Type: `text`
         */
        appStoreUrl?: string;
        /**
         * Filtering Type: `option`
         */
        category: 'BOOK_TRAVEL' | 'CHECK_IN' | 'FEES_POLICIES' | 'FLIGHT_STATUS' | 'TICKETS' | 'TICKETING' | 'AMENITIES' | 'FRONT_DESK' | 'PARKING' | 'GIFT_CARD' | 'WAITLIST' | 'DELIVERY' | 'ORDER' | 'TAKEOUT' | 'PICKUP' | 'RESERVE' | 'MENU' | 'APPOINTMENT' | 'PORTFOLIO' | 'QUOTE' | 'SERVICES' | 'STORE_ORDERS' | 'STORE_SHOP' | 'STORE_SUPPORT' | 'SCHEDULE' | 'SHOWTIMES' | 'AVAILABILITY' | 'PRICING' | 'ACTIVITIES' | 'BOOK' | 'BOOK_(HOTEL)' | 'BOOK_(RIDE)' | 'BOOK_(TOUR)' | 'CAREERS' | 'CHARGE' | 'COUPONS' | 'DELIVERY_(RETAIL)' | 'DONATE' | 'EVENTS' | 'ORDER_(RETAIL)' | 'OTHER_MENU' | 'PICKUP_(RETAIL)' | 'RESERVE_(PARKING)' | 'SHOWS' | 'SPORTS' | 'SUPPORT' | 'TEE_TIME' | 'GIFT_CARD_(RESTAURANT)';
        /**
         * Filtering Type: `text`
         */
        quickLinkUrl: string;
    }>;
    /**
     * The business description to be sent to Apple
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    appleBusinessDescription?: string;
    /**
     * The ID associated with an individual Business Folder in your Apple account
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    appleBusinessId?: string;
    /**
     * The ID associated with your Apple account. Numerical values only
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    appleCompanyId?: string;
    /**
     * The cover photo for the entity''s Apple profile
     *
     *
     * Image must be at least 1600 x 1040 pixels
     *
     * Image may be no more than 4864 x 3163 pixels
     *
     * Supported Aspect Ratios:
     * * 154 x 100
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    appleCoverPhoto?: {
        /**
         * Filtering Type: `text`
         */
        alternateText?: string;
        /**
         * Filtering Type: `text`
         */
        url: string;
    };
    /**
     * The name to be displayed on Apple for the entity.
     * NOTE: The names of Brands and their respective Locations within an Apple Business Connect Account must match identically.
     * Cannot Include:
     * HTML markup
     *
     *
     * Cannot Include:
     * * HTML markup
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    appleDisplayName?: string;
    /**
     * The application URL
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * job
     * ```
     */
    applicationUrl?: string;
    /**
     * Association memberships relevant to the entity (e.g., `"New York Doctors Association"`)
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     *
     * Filtering Type: `list of text`
     *
     * ```
     * Eligible For:
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    associations?: Array<string>;
    /**
     * Indicates whether the event is online, offline, or a mix.
     * A `virtualLocationUrl` must be specified for online and mixed events.
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * event
     * ```
     */
    attendance?: {
        /**
         * Filtering Type: `option`
         */
        attendanceMode: 'OFFLINE' | 'ONLINE' | 'MIXED';
        /**
         * Filtering Type: `text`
         */
        virtualLocationUrl?: string;
    };
    /**
     * The formality of clothing typically worn at this restaurant
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * restaurant
     * ```
     */
    attire?: 'UNSPECIFIED' | 'DRESSY' | 'CASUAL' | 'FORMAL';
    /**
     * Indicates whether the entity offers babysitting.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    babysittingOffered?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity offers baggage storage pre check-in and post check-out.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    baggageStorage?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has an indoor or outdoor bar onsite.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    bar?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has access to a beach.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    beachAccess?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity is physically located next to a beach.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    beachFrontProperty?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity offers bicycles for rent or for free.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    bicycles?: 'BICYCLE_RENTALS' | 'BICYCLE_RENTALS_FOR_FREE' | 'NOT_APPLICABLE';
    /**
     * Information about the Bio Content Lists associated with this entity
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * organization
     * * restaurant
     * ```
     */
    bios?: {
        /**
         * IDs of the Bio Lists associated with this entity
         *
         *
         * Array must be ordered.
         *
         * Array may have a maximum of 40 elements.
         *
         *
         * Filtering Type: `list of text`
         */
        ids?: Array<string>;
        /**
         * Label to be used for this entity's Bio Lists. This label will appear on your entity's listings.
         *
         * Filtering Type: `text`
         */
        label?: string;
    };
    /**
     * Indicates whether the entity has a boutique store. Gift shop or convenience store are not eligible.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    boutiqueStores?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Brands sold by this entity
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     *
     * Filtering Type: `list of text`
     *
     * ```
     * Eligible For:
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    brands?: Array<string>;
    /**
     * Indicates whether the entity offers breakfast.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    breakfast?: 'BREAKFAST_AVAILABLE' | 'BREAKFAST_AVAILABLE_FOR_FREE' | 'NOT_APPLICABLE';
    /**
     * Contains the daily brunch hours, holiday brunch hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `brunchHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday brunch hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     *
     * Filtering Type: `hours`
     *
     * ```
     * Eligible For:
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    brunchHours?: {
        /**
         * Filtering Type: `object`
         */
        friday?: {
            /**
             * Indicates if the brunch hours are "closed" on Friday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for brunch on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         *
         * Filtering Type: `list of object`
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             *
             * Filtering Type: `date`
             */
            date: string;
            /**
             * Indicates if the brunch hours are "closed" on on the given date.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             *
             * Filtering Type: `boolean`
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on the specified date.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        }>;
        /**
         * Filtering Type: `object`
         */
        monday?: {
            /**
             * Indicates if the brunch hours are "closed" on Monday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for brunch on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         *
         * Filtering Type: `date`
         */
        reopenDate?: string;
        /**
         * Filtering Type: `object`
         */
        saturday?: {
            /**
             * Indicates if the brunch hours are "closed" on Saturday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for brunch on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        sunday?: {
            /**
             * Indicates if the brunch hours are "closed" on Sunday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for brunch on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        thursday?: {
            /**
             * Indicates if the brunch hours are "closed" on Thursday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for brunch on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        tuesday?: {
            /**
             * Indicates if the brunch hours are "closed" on Tuesday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for brunch on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        wednesday?: {
            /**
             * Indicates if the brunch hours are "closed" on Wednesday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for brunch on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
    };
    /**
     * Indicates whether the entity has a business center.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    businessCenter?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Information about the events Content Lists (Calendars) associated with this entity
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * financialProfessional
     * * healthcareFacility
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    calendars?: {
        /**
         * IDs of the Calendars associated with this entity
         *
         *
         * Array must be ordered.
         *
         * Array may have a maximum of 40 elements.
         *
         *
         * Filtering Type: `list of text`
         */
        ids?: Array<string>;
        /**
         * Label to be used for this entity's Calendars. This label will appear on your entity's listings.
         *
         * Filtering Type: `text`
         */
        label?: string;
    };
    /**
     * Indicates whether the entity offers car rental.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    carRental?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has a casino on premise or nearby.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    casino?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Yext Categories. (Supported for versions > 20240220)
     *
     * A map of category list external IDs (i.e. "yext") to a list of category IDs.
     *
     * IDs must be valid and selectable (i.e., cannot be parent categories).
     *
     * Partial updates are accepted, meaning sending only the "yext" property will have no effect on any category list except the "yext" category.
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * atm
     * * event
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    categories?: {
        [key: string]: never;
    };
    /**
     * Yext Category IDs. (Deprecated: For versions > 20240220)
     *
     * IDs must be valid and selectable (i.e., cannot be parent categories).
     *
     * NOTE: The list of category IDs that you send us must be comprehensive. For example, if you send us a list of IDs that does not include IDs that you sent in your last update, Yext considers the missing categories to be deleted, and we remove them from your listings.
     *
     *
     * Filtering Type: `list of text`
     *
     * ```
     * Eligible For:
     * * atm
     * * event
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    categoryIds?: Array<string>;
    /**
     * Indicates if the entity is cat friendly.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    catsAllowed?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * A list of the certifications held by the healthcare professional
     * **NOTE:** This field is only available to locations whose **`entityType`** is `healthcareProfessional`.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     *
     * Filtering Type: `list of text`
     *
     * ```
     * Eligible For:
     * * financialProfessional
     * * healthcareProfessional
     * ```
     */
    certifications?: Array<string>;
    /**
     * The check-in time
     *
     * Filtering Type: `time`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    checkInTime?: string;
    /**
     * The check-out time
     *
     * Filtering Type: `time`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    checkOutTime?: string;
    /**
     * The 1 to 5 star rating of the entitiy based on its services and facilities.
     *
     * Filtering Type: `decimal`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    classificationRating?: string;
    /**
     * Indicates whether the entity is closed
     *
     * Filtering Type: `boolean`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    closed?: boolean;
    /**
     * Indicates whether the entity offers concierge service.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    concierge?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * A list of the conditions treated by the healthcare provider
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     *
     * Filtering Type: `list of text`
     *
     * ```
     * Eligible For:
     * * healthcareFacility
     * * healthcareProfessional
     * ```
     */
    conditionsTreated?: Array<string>;
    /**
     * Indicates whether the entity has a convenience store.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    convenienceStore?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Information or messaging related to COVID-19.
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * healthcareFacility
     * * healthcareProfessional
     * * location
     * ```
     */
    covidMessaging?: string;
    /**
     * An appointment URL for scheduling a COVID-19 test.
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * healthcareFacility
     * * location
     * ```
     */
    covidTestAppointmentUrl?: string;
    /**
     * Indicates whether an appointment is required for a COVID-19 test.
     *
     * Filtering Type: `boolean`
     *
     * ```
     * Eligible For:
     * * healthcareFacility
     * * location
     * ```
     */
    covidTestingAppointmentRequired?: boolean;
    /**
     * Indicates whether location is a drive-through site for COVID-19 tests.
     *
     * Filtering Type: `boolean`
     *
     * ```
     * Eligible For:
     * * healthcareFacility
     * * location
     * ```
     */
    covidTestingDriveThroughSite?: boolean;
    /**
     * Indicates whether location offers free COVID-19 testing.
     *
     * Filtering Type: `boolean`
     *
     * ```
     * Eligible For:
     * * healthcareFacility
     * * location
     * ```
     */
    covidTestingIsFree?: boolean;
    /**
     * Indicates whether there are patient restrictions for COVID-19 testing.
     *
     * Filtering Type: `boolean`
     *
     * ```
     * Eligible For:
     * * healthcareFacility
     * * location
     * ```
     */
    covidTestingPatientRestrictions?: boolean;
    /**
     * Indicates whether a referral is required for COVID-19 testing.
     *
     * Filtering Type: `boolean`
     *
     * ```
     * Eligible For:
     * * healthcareFacility
     * * location
     * ```
     */
    covidTestingReferralRequired?: boolean;
    /**
     * Information or instructions for the COVID-19 testing site.
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * healthcareFacility
     * * location
     * ```
     */
    covidTestingSiteInstructions?: string;
    /**
     * Indicates whether an appointment is required for a COVID-19 vaccine.
     *
     * Filtering Type: `boolean`
     *
     * ```
     * Eligible For:
     * * healthcareFacility
     * * location
     * ```
     */
    covidVaccineAppointmentRequired?: boolean;
    /**
     * Indicates whether location is a drive-through site for COVID-19 vaccines.
     *
     * Filtering Type: `boolean`
     *
     * ```
     * Eligible For:
     * * healthcareFacility
     * * location
     * ```
     */
    covidVaccineDriveThroughSite?: boolean;
    /**
     * An information URL for more information about COVID-19 vaccines.
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * healthcareFacility
     * * location
     * ```
     */
    covidVaccineInformationUrl?: string;
    /**
     * Indicates whether there are patient restrictions for a COVID-19 vaccine.
     *
     * Filtering Type: `boolean`
     *
     * ```
     * Eligible For:
     * * healthcareFacility
     * * location
     * ```
     */
    covidVaccinePatientRestrictions?: boolean;
    /**
     * Indicates whether a referral is required for a COVID-19 vaccine.
     *
     * Filtering Type: `boolean`
     *
     * ```
     * Eligible For:
     * * healthcareFacility
     * * location
     * ```
     */
    covidVaccineReferralRequired?: boolean;
    /**
     * Information or instructions for the COVID-19 vaccination site.
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * healthcareFacility
     * * location
     * ```
     */
    covidVaccineSiteInstructions?: string;
    /**
     * Indicates which COVID-19 vaccines the location offers.
     *
     * Filtering Type: `list of option`
     *
     * ```
     * Eligible For:
     * * healthcareFacility
     * * location
     * ```
     */
    covidVaccinesOffered?: Array<'PFIZER' | 'MODERNA' | 'JOHNSON_&_JOHNSON'>;
    /**
     * Indicates whether the entity offers currency exchange services.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    currencyExchange?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Additional keywords you would like us to use when tracking your search performance
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 5 elements.
     *
     *
     * Filtering Type: `list of text`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * organization
     * * restaurant
     * ```
     */
    customKeywords?: Array<string>;
    /**
     * The date this entity was posted
     *
     * Filtering Type: `date`
     *
     * ```
     * Eligible For:
     * * job
     * ```
     */
    datePosted?: string;
    /**
     * A list of the degrees earned by the healthcare professional
     *
     *
     * Array must be ordered.
     *
     *
     * Filtering Type: `list of option`
     *
     * ```
     * Eligible For:
     * * healthcareProfessional
     * ```
     */
    degrees?: Array<'ANP' | 'APN' | 'APRN' | 'ARNP' | 'AUD' | 'BSW' | 'CCCA' | 'CNM' | 'CNP' | 'CNS' | 'CPNP' | 'CRNA' | 'CRNP' | 'DC' | 'DDS' | 'DMD' | 'DNP' | 'DO' | 'DPM' | 'DPT' | 'DSW' | 'DVM' | 'FNP' | 'GNP' | 'LAC' | 'LCSW' | 'LPN' | 'MBA' | 'MBBS' | 'MD' | 'MPAS' | 'MPH' | 'MSW' | 'ND' | 'NNP' | 'NP' | 'OD' | 'PA' | 'PAC' | 'PHARMD' | 'PHD' | 'PNP' | 'PSYD' | 'RD' | 'RSW' | 'VMD' | 'WHNP'>;
    /**
     * Contains the daily delivery hours, holiday delivery hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `deliveryHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday delivery hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     *
     * Filtering Type: `hours`
     *
     * ```
     * Eligible For:
     * * location
     * * restaurant
     * ```
     */
    deliveryHours?: {
        /**
         * Filtering Type: `object`
         */
        friday?: {
            /**
             * Indicates if the delivery hours are "closed" on Friday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is delivering on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         *
         * Filtering Type: `list of object`
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             *
             * Filtering Type: `date`
             */
            date: string;
            /**
             * Indicates if the delivery hours are "closed" on on the given date.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             *
             * Filtering Type: `boolean`
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity is delivering on the specified date.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        }>;
        /**
         * Filtering Type: `object`
         */
        monday?: {
            /**
             * Indicates if the delivery hours are "closed" on Monday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is delivering on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         *
         * Filtering Type: `date`
         */
        reopenDate?: string;
        /**
         * Filtering Type: `object`
         */
        saturday?: {
            /**
             * Indicates if the delivery hours are "closed" on Saturday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is delivering on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        sunday?: {
            /**
             * Indicates if the delivery hours are "closed" on Sunday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is delivering on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        thursday?: {
            /**
             * Indicates if the delivery hours are "closed" on Thursday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is delivering on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        tuesday?: {
            /**
             * Indicates if the delivery hours are "closed" on Tuesday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is delivering on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        wednesday?: {
            /**
             * Indicates if the delivery hours are "closed" on Wednesday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is delivering on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
    };
    /**
     * A description of the entity
     *
     *
     * Cannot Include:
     * * HTML markup
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * atm
     * * contactCard
     * * event
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * job
     * * location
     * * organization
     * * restaurant
     * ```
     */
    description?: string;
    /**
     * Coordinates where the map pin for the entity should be displayed, as provided by you
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * atm
     * * event
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * job
     * * location
     * * restaurant
     * ```
     */
    displayCoordinate?: {
        /**
         * Filtering Type: `float`
         */
        latitude?: number;
        /**
         * Filtering Type: `float`
         */
        longitude?: number;
    };
    /**
     * Indicates whether the entity has a doctor on premise or on call.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    doctorOnCall?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates if the entity is dog friendly.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    dogsAllowed?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Contains the daily drive-through hours, holiday drive-through hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `driveThroughHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday drive-through hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     *
     * Filtering Type: `hours`
     *
     * ```
     * Eligible For:
     * * atm
     * * location
     * * restaurant
     * ```
     */
    driveThroughHours?: {
        /**
         * Filtering Type: `object`
         */
        friday?: {
            /**
             * Indicates if the drive-through hours are "closed" on Friday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         *
         * Filtering Type: `list of object`
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             *
             * Filtering Type: `date`
             */
            date: string;
            /**
             * Indicates if the drive-through hours are "closed" on on the given date.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             *
             * Filtering Type: `boolean`
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on the specified date.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        }>;
        /**
         * Filtering Type: `object`
         */
        monday?: {
            /**
             * Indicates if the drive-through hours are "closed" on Monday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         *
         * Filtering Type: `date`
         */
        reopenDate?: string;
        /**
         * Filtering Type: `object`
         */
        saturday?: {
            /**
             * Indicates if the drive-through hours are "closed" on Saturday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        sunday?: {
            /**
             * Indicates if the drive-through hours are "closed" on Sunday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        thursday?: {
            /**
             * Indicates if the drive-through hours are "closed" on Thursday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        tuesday?: {
            /**
             * Indicates if the drive-through hours are "closed" on Tuesday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        wednesday?: {
            /**
             * Indicates if the drive-through hours are "closed" on Wednesday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's drive-through is open on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
    };
    /**
     * Coordinates of the drop-off area for the entity, as provided by you
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * atm
     * * event
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    dropoffCoordinate?: {
        /**
         * Filtering Type: `float`
         */
        latitude?: number;
        /**
         * Filtering Type: `float`
         */
        longitude?: number;
    };
    /**
     * Information about the education or training completed by the healthcare professional
     *
     *
     * Array must be ordered.
     *
     *
     * Filtering Type: `list of object`
     *
     * ```
     * Eligible For:
     * * healthcareProfessional
     * ```
     */
    educationList?: Array<{
        /**
         * Filtering Type: `text`
         */
        institutionName: string;
        /**
         * Filtering Type: `option`
         */
        type: 'FELLOWSHIP' | 'RESIDENCY' | 'INTERNSHIP' | 'MEDICAL_SCHOOL';
        /**
         * Filtering Type: `integer`
         */
        yearCompleted: number;
    }>;
    /**
     * Indicates whether the entity has electric car chargine stations on premise.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    electricChargingStation?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has an elevator.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    elevator?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has an elliptical machine.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    ellipticalMachine?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Emails addresses for this entity's point of contact
     *
     * Must be valid email addresses
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 5 elements.
     *
     *
     * Filtering Type: `list of text`
     *
     * ```
     * Eligible For:
     * * contactCard
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * organization
     * * restaurant
     * ```
     */
    emails?: Array<string>;
    /**
     * The employment type for the open job. Indicates whether the job is full-time, part-time, temporary, etc.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * job
     * ```
     */
    employmentType?: 'FULL_TIME' | 'PART_TIME' | 'CONTRACTOR' | 'TEMPORARY' | 'INTERN' | 'VOLUNTEER' | 'PER_DIEM' | 'OTHER';
    /**
     * Information on whether the event will take place as scheduled
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * event
     * ```
     */
    eventStatus?: 'SCHEDULED' | 'RESCHEDULED' | 'POSTPONED' | 'CANCELED' | 'EVENT_MOVED_ONLINE';
    /**
     * A description of the entity to be used in the "About You" section on Facebook
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    facebookAbout?: string;
    /**
     * Designates the Facebook Call-to-Action button text and value
     *
     * Valid contents of **`value`** depends on the Call-to-Action's **`type`**:
     * * `NONE`: (optional)
     * * `BOOK_NOW`: URL
     * * `CALL_NOW`: Phone number
     * * `CONTACT_US`: URL
     * * `SEND_MESSAGE`: Any string
     * * `USE_APP`: URL
     * * `PLAY_GAME`: URL
     * * `SHOP_NOW`: URL
     * * `SIGN_UP`: URL
     * * `WATCH_VIDEO`: URL
     * * `SEND_EMAIL`: Email address
     * * `LEARN_MORE`: URL
     * * `PURCHASE_GIFT_CARDS`: URL
     * * `ORDER_NOW`: URL
     * * `FOLLOW_PAGE`: Any string
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    facebookCallToAction?: {
        /**
         * The action the consumer is being prompted to take by the button's text
         *
         * Filtering Type: `option`
         */
        type: 'NONE' | 'BOOK_NOW' | 'CALL_NOW' | 'CONTACT_US' | 'SEND_MESSAGE' | 'USE_APP' | 'PLAY_GAME' | 'SHOP_NOW' | 'SIGN_UP' | 'WATCH_VIDEO' | 'SEND_EMAIL' | 'LEARN_MORE' | 'PURCHASE_GIFT_CARDS' | 'ORDER_NOW' | 'FOLLOW_PAGE';
        /**
         * Indicates where consumers will be directed to upon clicking the Call-to-Action button (e.g., a URL). It can be a free-form string or an embedded value, depending on what the user specifies.
         *
         * For example, if the user sets the Facebook Call-to-Action as " 'Sign Up' using 'Website URL' " in the Yext platform, **`type`** will be `SIGN_UP` and **`value`** will be `[[websiteUrl]]`.
         *
         * The Call-to-Action will have the same behavior if the user sets the value to "Custom Value" in the platform and embeds a field.
         *
         * Filtering Type: `text`
         */
        value?: string;
    };
    /**
     * The cover photo for the entity's Facebook profile
     *
     * Displayed as a 851 x 315 pixel image
     *
     * You may need a cover photo in order for your listing to appear on Facebook. Please check your listings tab to learn more.
     *
     *
     * Image must be at least 400 x 150 pixels
     *
     * Image area (width x height) may be no more than 41000000 pixels
     *
     * Image may be no more than 30000 x 30000 pixels
     *
     * Supported Aspect Ratios:
     * * 1 x 1
     * * 4 x 3
     * * 3 x 2
     * * 5 x 3
     * * 16 x 9
     * * 3 x 1
     * * 2 x 3
     * * 5 x 7
     * * 4 x 5
     * * 4 x 1
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    facebookCoverPhoto?: {
        /**
         * Filtering Type: `text`
         */
        alternateText?: string;
        /**
         * Filtering Type: `text`
         */
        url: string;
    };
    /**
     * Location Descriptors are used for Enterprise businesses that sync Facebook listings using brand page location structure. The Location Descriptor is typically an additional geographic description (e.g. geomodifier) that will appear in parentheses after the name on the Facebook listing.
     *
     *
     * Cannot Include:
     * * HTML markup
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    facebookDescriptor?: string;
    /**
     * The name for this entity's Facebook profile.  A separate name may be specified to send only to Facebook in order to comply with any specific Facebook rules or naming conventions.
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    facebookName?: string;
    /**
     * The city to be displayed on this entity's Facebook profile
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    facebookOverrideCity?: string;
    /**
     * URL for the entity's Facebook Page.
     *
     * Valid formats:
     *
     * - facebook.com/profile.php?id=[numId]
     * - facebook.com/group.php?gid=[numId]
     * - facebook.com/groups/[numId]
     * - facebook.com/[Name]
     * - facebook.com/pages/[Name]/[numId]
     * - facebook.com/people/[Name]/[numId]
     *
     * where [Name] is a String and [numId] is an Integer
     * The success response will contain a warning message explaining why the URL wasn't stored in the system.
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * organization
     * * restaurant
     * ```
     */
    facebookPageUrl?: string;
    /**
     * The Facebook Page ID of this entity's brand page if in a brand page location structure
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * atm
     * * brand
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    facebookParentPageId?: string;
    /**
     * The profile picture for the entity's Facebook profile
     *
     * You must have a profile picture in order for your listing to appear on Facebook.
     *
     *
     * Image must be at least 180 x 180 pixels
     *
     * Image area (width x height) may be no more than 41000000 pixels
     *
     * Image may be no more than 30000 x 30000 pixels
     *
     * Supported Aspect Ratios:
     * * 1 x 1
     * * 4 x 3
     * * 3 x 2
     * * 5 x 3
     * * 16 x 9
     * * 3 x 1
     * * 2 x 3
     * * 5 x 7
     * * 4 x 5
     * * 4 x 1
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    facebookProfilePhoto?: {
        /**
         * Filtering Type: `text`
         */
        alternateText?: string;
        /**
         * Filtering Type: `text`
         */
        url: string;
    };
    /**
     * The Store ID used for this entity in a brand page location structure
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    facebookStoreId?: string;
    /**
     * The username that appear's in the Facebook listing URL to help customers find and remember a brand’s Facebook page.  The username is also be used for tagging the Facebook page in other users’ posts, and searching for the Facebook page.
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    facebookVanityUrl?: string;
    /**
     * The URL you would like to submit to Facebook in place of the one given in **`websiteUrl`** (if applicable).
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    facebookWebsiteOverride?: string;
    /**
     * Must be a valid fax number.
     *
     * If the fax number's calling code is for a country other than the one given in the entity's **`countryCode`**, the fax number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    fax?: string;
    /**
     * Information about the entity's Featured Message
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    featuredMessage?: {
        /**
         * The text of Featured Message. Default: `Call today!`
         *
         * Cannot include:
         *
         * - inappropriate language
         * - HTML markup
         * - a URL or domain name
         * - a phone number
         * - control characters ([\x00-\x1F\x7F])
         * - insufficient spacing
         *
         * If you submit a Featured Message that contains profanity or more than 50 characters, it will be ignored. The success response will contain a warning message explaining why your Featured Message wasn't stored in the system.
         *
         *
         * Cannot Include:
         * * HTML markup
         *
         * Filtering Type: `text`
         */
        description?: string;
        /**
         * Valid URL linked to the Featured Message text
         *
         * Filtering Type: `text`
         */
        url?: string;
    };
    /**
     * The first name of the healthcare professional
     *
     *
     * Cannot Include:
     * * a URL or domain name
     * * HTML markup
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * healthcareProfessional
     * ```
     */
    firstName?: string;
    /**
     * Link to the review-collection page, where consumers can leave first-party reviews
     *
     * ```
     * Eligible For:
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    firstPartyReviewPage?: string;
    /**
     * Indicates whether the entity has a fitness center.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    fitnessCenter?: 'FITNESS_CENTER_AVAILABLE' | 'FITNESS_CENTER_AVAILABLE_FOR_FREE' | 'NOT_APPLICABLE';
    /**
     * The number of floors the entity has from ground floor to top floor.
     *
     * Filtering Type: `integer`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    floorCount?: number;
    /**
     * Indicates whether the entity has free weights.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    freeWeights?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * A list of questions that are frequently asked about this entity
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     *
     * Filtering Type: `list of object`
     *
     * ```
     * Eligible For:
     * * atm
     * * event
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    frequentlyAskedQuestions?: Array<{
        /**
         * Filtering Type: `text`
         */
        answer?: string;
        /**
         * Filtering Type: `text`
         */
        question: string;
    }>;
    /**
     * Indicates whether the entity has a front desk.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    frontDesk?: 'FRONT_DESK_AVAILABLE' | 'FRONT_DESK_AVAILABLE_24_HOURS' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the staff is vaccinated against COVID-19.
     *
     * Filtering Type: `boolean`
     *
     * ```
     * Eligible For:
     * * healthcareFacility
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    fullyVaccinatedStaff?: boolean;
    /**
     * Indicates whether the entity has a game room.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    gameRoom?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * The gender of the healthcare professional
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * healthcareProfessional
     * ```
     */
    gender?: 'UNSPECIFIED' | 'FEMALE' | 'MALE' | 'NONBINARY' | 'TRANSGENDER_FEMALE' | 'TRANSGENDER_MALE' | 'OTHER' | 'PREFER_NOT_TO_DISCLOSE';
    /**
     * Provides additional information on where the entity can be found (e.g., `Times Square`, `Global Center Mall`)
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    geomodifier?: string;
    /**
     * Indicates whether the entity has a gift shop.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    giftShop?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has a golf couse on premise or nearby. The golf course may be independently run.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    golf?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * The unique IDs of the entity's Google Business Profile keywords, as well as the unique IDs of any values selected for each keyword.
     *
     * Valid keywords (e.g., `has_drive_through`, `has_fitting_room`, `kitchen_in_room`) are determined by the entity's primary category. A full list of keywords can be retrieved with the Google Fields: List endpoint.
     *
     * Keyword values provide more details on how the keyword applies to the entity (e.g., if the keyword is `has_drive_through`, its values may be `true` or `false`).
     *
     * * If the **`v`** parameter is before `20181204`: **`googleAttributes`** is formatted as a map of key-value pairs (e.g., `[{ "id": "has_wheelchair_accessible_entrance", "values": [ "true" ] }]`)
     * * If the **`v`** parameter is on or after `20181204`: the contents are formatted as a list of objects (e.g., `{ "has_wheelchair_accessible_entrance": [ "true" ]}`)
     *
     * **NOTE:** The latest Google Attributes are available via the Google Fields: List endpoint. Google Attributes are managed by Google and are subject to change without notice. To prevent errors, make sure your API implementation is not dependent on the presence of specific attributes.
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    googleAttributes?: {
        [key: string]: never;
    };
    /**
     * The cover photo for the entity's Google profile
     *
     *
     * Image must be at least 250 x 250 pixels
     *
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    googleCoverPhoto?: {
        /**
         * Filtering Type: `text`
         */
        alternateText?: string;
        /**
         * Filtering Type: `text`
         */
        url: string;
    };
    /**
     * Information about Google Messaging, WhatsApp and SMS, for the entity’s point of contact for messaging/chat functionality.
     *
     * NOTE: Only one, either WhatsApp or SMS is displayed on the Google listing. If both SMS Number and WhatsApp URL are provided only SMS Number will be displayed on the listing.
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    googleMessaging?: {
        /**
         * The SMS phone number of the entity's point of contact for messaging/ chat functionality.
         * Must be a valid phone number.
         * If the phone number's calling code is for a country other than the one given in the entity's countryCode, the phone number provided must contain the calling code (e.g., +44 in +442038083831). Otherwise, the calling code is optional.
         *
         * Filtering Type: `text`
         */
        smsNumber?: string;
        /**
         * A valid URL for this entity's WhatsApp account.
         * Must be a valid URL
         *
         *
         * Filtering Type: `text`
         */
        whatsappMessagingUrl?: string;
    };
    /**
     * Google Business Profile Labels help users organize their locations into groups within GBP.
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 10 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     *
     * Filtering Type: `list of text`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    googleMyBusinessLabels?: Array<string>;
    /**
     * The unique identifier of this entity on Google Maps.
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    googlePlaceId?: string;
    /**
     * The profile photo for the entity's Google profile
     *
     *
     * Image must be at least 250 x 250 pixels
     *
     * Image may be no more than 5000 x 5000 pixels
     *
     * Supported Aspect Ratios:
     * * 1 x 1
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    googleProfilePhoto?: {
        /**
         * Filtering Type: `text`
         */
        alternateText?: string;
        /**
         * Filtering Type: `text`
         */
        url: string;
    };
    /**
     * The URL you would like to submit to Google Business Profile in place of the one given in **`websiteUrl`** (if applicable).
     *
     * For example, if you want to analyze the traffic driven by your Google listings separately from other traffic, enter the alternate URL that you will use for tracking in this field.
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    googleWebsiteOverride?: string;
    /**
     * Contains the daily happy hours, holiday happy hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `happyHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday happy hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     *
     * Filtering Type: `hours`
     *
     * ```
     * Eligible For:
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    happyHours?: {
        /**
         * Filtering Type: `object`
         */
        friday?: {
            /**
             * Indicates if the happy hours are "closed" on Friday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's happy hours on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         *
         * Filtering Type: `list of object`
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             *
             * Filtering Type: `date`
             */
            date: string;
            /**
             * Indicates if the happy hours are "closed" on on the given date.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             *
             * Filtering Type: `boolean`
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for the Entity's happy hours on the specified date.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        }>;
        /**
         * Filtering Type: `object`
         */
        monday?: {
            /**
             * Indicates if the happy hours are "closed" on Monday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's happy hours on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         *
         * Filtering Type: `date`
         */
        reopenDate?: string;
        /**
         * Filtering Type: `object`
         */
        saturday?: {
            /**
             * Indicates if the happy hours are "closed" on Saturday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's happy hours on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        sunday?: {
            /**
             * Indicates if the happy hours are "closed" on Sunday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's happy hours on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        thursday?: {
            /**
             * Indicates if the happy hours are "closed" on Thursday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's happy hours on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        tuesday?: {
            /**
             * Indicates if the happy hours are "closed" on Tuesday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's happy hours on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        wednesday?: {
            /**
             * Indicates if the happy hours are "closed" on Wednesday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's happy hours on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
    };
    /**
     * A portrait of the healthcare professional
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * contactCard
     * * financialProfessional
     * * healthcareProfessional
     * ```
     */
    headshot?: {
        /**
         * Filtering Type: `text`
         */
        alternateText?: string;
        /**
         * Filtering Type: `text`
         */
        url: string;
    };
    /**
     * The organization that is hiring for the open job
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * job
     * ```
     */
    hiringOrganization?: string;
    /**
     * Indicates whether holiday-hour confirmation alerts are enabled for the Yext Knowledge Assistant for this entity
     *
     * Filtering Type: `boolean`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    holidayHoursConversationEnabled?: boolean;
    /**
     * Indicates whether the entity offers horseback riding.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    horsebackRiding?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has a hot tub.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    hotTub?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Contains the daily hours, holiday hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `hours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     *
     * Filtering Type: `hours`
     *
     * ```
     * Eligible For:
     * * atm
     * * contactCard
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    hours?: {
        /**
         * Filtering Type: `object`
         */
        friday?: {
            /**
             * Indicates if the hours of operation are "closed" on Friday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         *
         * Filtering Type: `list of object`
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             *
             * Filtering Type: `date`
             */
            date: string;
            /**
             * Indicates if the hours of operation are "closed" on on the given date.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             *
             * Filtering Type: `boolean`
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on the specified date.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        }>;
        /**
         * Filtering Type: `object`
         */
        monday?: {
            /**
             * Indicates if the hours of operation are "closed" on Monday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         *
         * Filtering Type: `date`
         */
        reopenDate?: string;
        /**
         * Filtering Type: `object`
         */
        saturday?: {
            /**
             * Indicates if the hours of operation are "closed" on Saturday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        sunday?: {
            /**
             * Indicates if the hours of operation are "closed" on Sunday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        thursday?: {
            /**
             * Indicates if the hours of operation are "closed" on Thursday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        tuesday?: {
            /**
             * Indicates if the hours of operation are "closed" on Tuesday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        wednesday?: {
            /**
             * Indicates if the hours of operation are "closed" on Wednesday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
    };
    /**
     * Indicates whether the entity offers housekeeping services.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    housekeeping?: 'HOUSEKEEPING_AVAILABLE' | 'HOUSEKEEPING_AVAILABLE_DAILY' | 'NOT_APPLICABLE';
    /**
     * A statement of the ownership and authorship of a document. Individuals or organizations based in many German-speaking countries are required by law to include an Impressum in published media.
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    impressum?: string;
    /**
     * A count of the number of indoor pools
     *
     * Filtering Type: `integer`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    indoorPoolCount?: number;
    /**
     * Valid Instagram username for the entity without the leading "@" (e.g., `NewCityAuto`)
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * organization
     * * restaurant
     * ```
     */
    instagramHandle?: string;
    /**
     * A list of insurance policies accepted by the healthcare provider
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     *
     * Filtering Type: `list of text`
     *
     * ```
     * Eligible For:
     * * healthcareFacility
     * * healthcareProfessional
     * ```
     */
    insuranceAccepted?: Array<string>;
    /**
     * The URL where consumers can download the entity's app to their iPhone or iPad
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * brand
     * * financialProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    iosAppUrl?: string;
    /**
     * Indicates whether the healthcare entity is the primary entity in its group
     *
     * Filtering Type: `boolean`
     *
     * ```
     * Eligible For:
     * * healthcareProfessional
     * ```
     */
    isClusterPrimary?: boolean;
    /**
     * Indicates whether or not the event is free
     *
     * Filtering Type: `boolean`
     *
     * ```
     * Eligible For:
     * * event
     * ```
     */
    isFreeEvent?: boolean;
    /**
     * The ISO 3166-2 region code for the entity
     *
     * Yext will determine the entity's code and update **`isoRegionCode`** with that value. If Yext is unable to determine the code for the entity, the entity'ss ISO 3166-1 alpha-2 country code will be used.
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    isoRegionCode?: string;
    /**
     * Keywords that describe the entity.
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     *
     * Filtering Type: `list of text`
     *
     * ```
     * Eligible For:
     * * atm
     * * card
     * * event
     * * faq
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * helpArticle
     * * hotel
     * * job
     * * location
     * * product
     * * restaurant
     * ```
     */
    keywords?: Array<string>;
    /**
     * Indicates if the entity is kid friendly.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    kidFriendly?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates if the property has a Kids Club.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    kidsClub?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity allows kids to stay free.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    kidsStayFree?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Contains the daily kitchen hours, holiday kitchen hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `kitchenHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday kitchen hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     *
     * Filtering Type: `hours`
     *
     * ```
     * Eligible For:
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    kitchenHours?: {
        /**
         * Filtering Type: `object`
         */
        friday?: {
            /**
             * Indicates if the kitchen hours are "closed" on Friday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen is open on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         *
         * Filtering Type: `list of object`
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             *
             * Filtering Type: `date`
             */
            date: string;
            /**
             * Indicates if the kitchen hours are "closed" on on the given date.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             *
             * Filtering Type: `boolean`
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen is open on the specified date.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        }>;
        /**
         * Filtering Type: `object`
         */
        monday?: {
            /**
             * Indicates if the kitchen hours are "closed" on Monday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen is open on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         *
         * Filtering Type: `date`
         */
        reopenDate?: string;
        /**
         * Filtering Type: `object`
         */
        saturday?: {
            /**
             * Indicates if the kitchen hours are "closed" on Saturday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen is open on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        sunday?: {
            /**
             * Indicates if the kitchen hours are "closed" on Sunday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen is open on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        thursday?: {
            /**
             * Indicates if the kitchen hours are "closed" on Thursday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen is open on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        tuesday?: {
            /**
             * Indicates if the kitchen hours are "closed" on Tuesday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen open on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        wednesday?: {
            /**
             * Indicates if the kitchen hours are "closed" on Wednesday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity's kitchen is open on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
    };
    /**
     * The IDs of the entity labels that have been added to this entity. Entity labels help you identify entities that share a certain characteristic; they do not appear on your entity's listings.
     *
     * **NOTE:** You can only add labels that have already been created via our web interface. Currently, it is not possible to create new labels via the API.
     *
     * Filtering Type: `opaque`
     *
     * ```
     * Eligible For:
     * * atm
     * * board
     * * brand
     * * card
     * * contactCard
     * * event
     * * faq
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * helpArticle
     * * hotel
     * * hotelRoomType
     * * job
     * * location
     * * organization
     * * product
     * * restaurant
     * ```
     */
    labels?: Array<string>;
    /**
     * The URL of this entity's Landing Page that was created with Yext Pages
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * atm
     * * card
     * * event
     * * faq
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * helpArticle
     * * hotel
     * * job
     * * location
     * * product
     * * restaurant
     * ```
     */
    landingPageUrl?: string;
    /**
     * The langauges in which consumers can commicate with this entity or its staff members
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     *
     * Filtering Type: `list of text`
     *
     * ```
     * Eligible For:
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    languages?: Array<string>;
    /**
     * The last name of the healthcare professional
     *
     *
     * Cannot Include:
     * * a URL or domain name
     * * HTML markup
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * healthcareProfessional
     * ```
     */
    lastName?: string;
    /**
     * Indicates whether the entity offers laundry services.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    laundry?: 'FULL_SERVICE' | 'SELF_SERVICE' | 'NOT_APPLICABLE';
    /**
     * Indicates if the property has a lazy river
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    lazyRiver?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates if the property has a lifeguard on duty
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    lifeguard?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * URL for your LinkedIn account, format should be https://www.linkedin.com/in/yourUsername
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * contactCard
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * organization
     * * restaurant
     * ```
     */
    linkedInUrl?: string;
    /**
     * location ID of the event location, if the event is held at a location managed in the Yext Knowledge Manager
     *
     * Filtering Type: `entityId`
     *
     * ```
     * Eligible For:
     * * contactCard
     * * event
     * ```
     */
    linkedLocation?: string;
    /**
     * Must be a valid, non-toll-free phone number, based on the country specified in **`address.region`**. Phone numbers for US entities must contain 10 digits.
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    localPhone?: string;
    /**
     * Indicates whether the entity offers local shuttle services.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    localShuttle?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * For atms, the external ID of the entity that the atm is installed in. The entity must be in the same business account as the atm.
     *
     * Filtering Type: `entityId`
     *
     * ```
     * Eligible For:
     * * atm
     * ```
     */
    locatedIn?: string;
    /**
     * The location where this job opening exists as either an existing location or an external location
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * job
     * ```
     */
    location?: {
        /**
         * A location entity referenced by Yext ID or Entity ID where this job opening exists
         *
         * Filtering Type: `entityId`
         */
        existingLocation?: string;
        /**
         * A location string where this job opening exists
         *
         *
         * Cannot Include:
         * * a URL or domain name
         * * HTML markup
         *
         * Filtering Type: `text`
         */
        externalLocation?: string;
    };
    /**
     * Indicates the entity's type, if it is not an event
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * atm
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    locationType?: 'LOCATION' | 'HEALTHCARE_FACILITY' | 'HEALTHCARE_PROFESSIONAL' | 'ATM' | 'RESTAURANT' | 'HOTEL';
    /**
     * An image of the entity's logo
     *
     *
     * Supported Aspect Ratios:
     * * 1 x 1
     *
     * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     *
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * atm
     * * contactCard
     * * faq
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * job
     * * location
     * * organization
     * * restaurant
     * ```
     */
    logo?: {
        /**
         * Filtering Type: `text`
         */
        clickthroughUrl?: string;
        /**
         * Filtering Type: `text`
         */
        description?: string;
        /**
         * Filtering Type: `text`
         */
        details?: string;
        /**
         *
         * Supported Aspect Ratios:
         * * 1 x 1
         *
         * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
         *
         *
         * Filtering Type: `object`
         */
        image: {
            /**
             * Filtering Type: `text`
             */
            alternateText?: string;
            /**
             * Filtering Type: `text`
             */
            url: string;
        };
    };
    /**
     * The main phone number of the entity's point of contact
     *
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * atm
     * * contactCard
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * organization
     * * restaurant
     * ```
     */
    mainPhone?: string;
    /**
     * Indicates whether the entity offers massage services.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    massage?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * The maximum age specified by the property for children to stay in the room/suite of a parent or adult without an additional fee
     *
     * Filtering Type: `integer`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    maxAgeOfKidsStayFree?: number;
    /**
     * The maximum number of children who can stay in the room/suite of a parent or adult without an additional fee
     *
     * Filtering Type: `integer`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    maxNumberOfKidsStayFree?: number;
    /**
     * Types of meals served at this restaurant
     *
     * Filtering Type: `list of option`
     *
     * ```
     * Eligible For:
     * * restaurant
     * ```
     */
    mealsServed?: Array<'BREAKFAST' | 'LUNCH' | 'BRUNCH' | 'DINNER' | 'HAPPY_HOUR' | 'LATE_NIGHT'>;
    /**
     * The number of meeting rooms the entity has.
     *
     * Filtering Type: `integer`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    meetingRoomCount?: number;
    /**
     * Information about the URL where consumers can view the entity's menu
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    menuUrl?: {
        /**
         * The URL that is shown on your listings in place of **`menuUrl.url`**. You can use **`menuUrl.displayUrl`** to display a short, memorable web address that redirects consumers to the URL given in **`menuUrl.url`**.
         *
         * Filtering Type: `text`
         */
        displayUrl?: string;
        /**
         * If set to true, only the display URL will be sent to those publishers who do not support separate display and tracking URLs for this field.
         *
         * Filtering Type: `boolean`
         */
        preferDisplayUrl?: boolean;
        /**
         * A valid URL where consumers can view the entity's menu
         *
         * Filtering Type: `text`
         */
        url?: string;
    };
    /**
     * Information about the Menu Content Lists associated with this entity
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    menus?: {
        /**
         * IDs of the Menu Lists associated with this entity
         *
         *
         * Array must be ordered.
         *
         * Array may have a maximum of 40 elements.
         *
         *
         * Filtering Type: `list of text`
         */
        ids?: Array<string>;
        /**
         * Label to be used for this entity's Menu Lists. This label will appear on your entity's listings.
         *
         * Filtering Type: `text`
         */
        label?: string;
    };
    /**
     * The middle name of the healthcare professional
     *
     *
     * Cannot Include:
     * * a URL or domain name
     * * HTML markup
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * healthcareProfessional
     * ```
     */
    middleName?: string;
    /**
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    mobilePhone?: string;
    /**
     * Indicates whether the entity is mobility/wheelchair accessible
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    mobilityAccessible?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has a nightclub.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    nightclub?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * The National Provider Identifier (NPI) of the healthcare provider
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * healthcareFacility
     * * healthcareProfessional
     * ```
     */
    npi?: string;
    /**
     * Indicates whether Knowledge Nudge is enabled for the Yext Knowledge Assistant for this entity
     *
     * Filtering Type: `boolean`
     *
     * ```
     * Eligible For:
     * * atm
     * * faq
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * helpArticle
     * * hotel
     * * job
     * * location
     * * organization
     * * product
     * * restaurant
     * ```
     */
    nudgeEnabled?: boolean;
    /**
     * The name of the office where the healthcare professional works, if different from **`name`**
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * healthcareProfessional
     * ```
     */
    officeName?: string;
    /**
     * Contains the daily online service hours, holiday online service hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `onlineServiceHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday online service hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     *
     * Filtering Type: `hours`
     *
     * ```
     * Eligible For:
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * location
     * * restaurant
     * ```
     */
    onlineServiceHours?: {
        /**
         * Filtering Type: `object`
         */
        friday?: {
            /**
             * Indicates if the online service hours are "closed" on Friday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         *
         * Filtering Type: `list of object`
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             *
             * Filtering Type: `date`
             */
            date: string;
            /**
             * Indicates if the online service hours are "closed" on on the given date.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             *
             * Filtering Type: `boolean`
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on the specified date.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        }>;
        /**
         * Filtering Type: `object`
         */
        monday?: {
            /**
             * Indicates if the online service hours are "closed" on Monday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         *
         * Filtering Type: `date`
         */
        reopenDate?: string;
        /**
         * Filtering Type: `object`
         */
        saturday?: {
            /**
             * Indicates if the online service hours are "closed" on Saturday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        sunday?: {
            /**
             * Indicates if the online service hours are "closed" on Sunday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        thursday?: {
            /**
             * Indicates if the online service hours are "closed" on Thursday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        tuesday?: {
            /**
             * Indicates if the online service hours are "closed" on Tuesday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        wednesday?: {
            /**
             * Indicates if the online service hours are "closed" on Wednesday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's online service hours on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
    };
    /**
     * The date that the entity is set to open for the first time.
     * Must be formatted in YYYY-MM-DD format.
     *
     * Date must be on or after 1970-01-01 Date must be before or on 2038-01-01
     *
     *
     * Filtering Type: `date`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    openDate?: string;
    /**
     * The list of countries the business operates in
     *
     * Filtering Type: `list of option`
     *
     * ```
     * Eligible For:
     * * organization
     * ```
     */
    operatingCountries?: Array<'AD' | 'AE' | 'AF' | 'AG' | 'AI' | 'AL' | 'AM' | 'AO' | 'AR' | 'AS' | 'AT' | 'AU' | 'AW' | 'AX' | 'AZ' | 'BA' | 'BB' | 'BD' | 'BE' | 'BF' | 'BG' | 'BH' | 'BI' | 'BJ' | 'BL' | 'BM' | 'BN' | 'BO' | 'BQ' | 'BR' | 'BS' | 'BT' | 'BW' | 'BY' | 'BZ' | 'CA' | 'CD' | 'CF' | 'CG' | 'CH' | 'CI' | 'CK' | 'CL' | 'CM' | 'CN' | 'CO' | 'CR' | 'CU' | 'CV' | 'CW' | 'CY' | 'CZ' | 'DE' | 'DJ' | 'DK' | 'DM' | 'DO' | 'DZ' | 'EC' | 'EE' | 'EG' | 'EH' | 'ER' | 'ES' | 'ET' | 'FI' | 'FJ' | 'FK' | 'FM' | 'FO' | 'FR' | 'GA' | 'GB' | 'GD' | 'GE' | 'GF' | 'GG' | 'GH' | 'GI' | 'GL' | 'GM' | 'GN' | 'GP' | 'GQ' | 'GR' | 'GT' | 'GU' | 'GW' | 'GY' | 'HK' | 'HN' | 'HR' | 'HT' | 'HU' | 'ID' | 'IE' | 'IL' | 'IM' | 'IN' | 'IQ' | 'IR' | 'IS' | 'IT' | 'JE' | 'JM' | 'JO' | 'JP' | 'KE' | 'KG' | 'KH' | 'KI' | 'KM' | 'KN' | 'KR' | 'KW' | 'KY' | 'KZ' | 'LA' | 'LB' | 'LC' | 'LI' | 'LK' | 'LR' | 'LS' | 'LT' | 'LU' | 'LV' | 'LY' | 'MA' | 'MC' | 'MD' | 'ME' | 'MF' | 'MG' | 'MH' | 'MK' | 'ML' | 'MM' | 'MN' | 'MO' | 'MP' | 'MQ' | 'MR' | 'MS' | 'MT' | 'MU' | 'MV' | 'MW' | 'MX' | 'MY' | 'MZ' | 'NA' | 'NC' | 'NE' | 'NG' | 'NI' | 'NL' | 'NO' | 'NP' | 'NR' | 'NZ' | 'OM' | 'PA' | 'PE' | 'PF' | 'PG' | 'PH' | 'PK' | 'PL' | 'PM' | 'PR' | 'PS' | 'PT' | 'PW' | 'PY' | 'QA' | 'RE' | 'RO' | 'RS' | 'RU' | 'RW' | 'SA' | 'SB' | 'SC' | 'SD' | 'SE' | 'SG' | 'SH' | 'SI' | 'SJ' | 'SK' | 'SL' | 'SM' | 'SN' | 'SO' | 'SR' | 'SS' | 'ST' | 'SV' | 'SX' | 'SY' | 'SZ' | 'TC' | 'TD' | 'TG' | 'TH' | 'TJ' | 'TL' | 'TM' | 'TN' | 'TO' | 'TR' | 'TT' | 'TV' | 'TW' | 'TZ' | 'UA' | 'UG' | 'US' | 'UY' | 'UZ' | 'VA' | 'VC' | 'VE' | 'VG' | 'VI' | 'VN' | 'VU' | 'WF' | 'WS' | 'XK' | 'YE' | 'YT' | 'ZA' | 'ZM' | 'ZW'>;
    /**
     * Information about the URL used to place orders that will be fulfilled by the entity
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    orderUrl?: {
        /**
         * The URL that is shown on your listings in place of **`orderUrl.url`**. You can use **`orderUrl.displayUrl`** to display a short, memorable web address that redirects consumers to the URL given in **`orderUrl.url`**.
         *
         * Filtering Type: `text`
         */
        displayUrl?: string;
        /**
         * If set to true, only the display URL will be sent to those publishers who do not support separate display and tracking URLs for this field.
         *
         * Filtering Type: `boolean`
         */
        preferDisplayUrl?: boolean;
        /**
         * A valid URL used to place an order at this entity
         *
         * Filtering Type: `text`
         */
        url?: string;
    };
    /**
     * Point of contact for the event organizer (not to be published publicly)
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * event
     * ```
     */
    organizerEmail?: string;
    /**
     * Point of contact for the event organizer (not to be published publicly)
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * event
     * ```
     */
    organizerName?: string;
    /**
     * Point of contact for the event organizer (not to be published publicly)
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * event
     * ```
     */
    organizerPhone?: string;
    /**
     * The number of outdoor pools the entity has.
     *
     * Filtering Type: `integer`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    outdoorPoolCount?: number;
    /**
     * Indicates whether the entity offers parking services.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    parking?: 'PARKING_AVAILABLE' | 'PARKING_AVAILABLE_FOR_FREE' | 'NOT_APPLICABLE';
    /**
     * The payment methods accepted by this entity
     *
     * Valid elements depend on the entity's country.
     *
     *
     * Filtering Type: `list of option`
     *
     * ```
     * Eligible For:
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    paymentOptions?: Array<'AFTERPAY' | 'ALIPAY' | 'AMERICANEXPRESS' | 'ANDROIDPAY' | 'APPLEPAY' | 'ATM' | 'ATMQUICK' | 'BACS' | 'BANCONTACT' | 'BANKDEPOSIT' | 'BANKPAY' | 'BGO' | 'BITCOIN' | 'Bar' | 'CARTASI' | 'CASH' | 'CCS' | 'CHECK' | 'CHEQUESVACANCES' | 'CONB' | 'CONTACTLESSPAYME' | 'CVVV' | 'DEBITCARD' | 'DEBITNOTE' | 'DINERSCLUB' | 'DIRECTDEBIT' | 'DISCOVER' | 'ECKARTE' | 'ECOCHEQUE' | 'EKENA' | 'EMV' | 'FINANCING' | 'GIFTCARD' | 'GOPAY' | 'HAYAKAKEN' | 'HEBAG' | 'IBOD' | 'ICCARDS' | 'ICOCA' | 'ID' | 'IDEAL' | 'INCA' | 'INVOICE' | 'JCB' | 'JCoinPay' | 'JKOPAY' | 'KITACA' | 'KLA' | 'KLARNA' | 'LINEPAY' | 'MAESTRO' | 'MANACA' | 'MASTERCARD' | 'MIPAY' | 'MONIZZE' | 'MPAY' | 'Manuelle Lastsch' | 'Merpay' | 'NANACO' | 'NEXI' | 'NIMOCA' | 'OREM' | 'PASMO' | 'PAYBACKPAY' | 'PAYBOX' | 'PAYCONIQ' | 'PAYPAL' | 'PAYPAY' | 'PAYSEC' | 'PIN' | 'POSTEPAY' | 'QRCODE' | 'QUICPAY' | 'RAKUTENEDY' | 'RAKUTENPAY' | 'SAMSUNGPAY' | 'SODEXO' | 'SUGOCA' | 'SUICA' | 'SWISH' | 'TICKETRESTAURANT' | 'TOICA' | 'TRAVELERSCHECK' | 'TSCUBIC' | 'TWINT' | 'UNIONPAY' | 'VEV' | 'VISA' | 'VISAELECTRON' | 'VOB' | 'VOUCHER' | 'VPAY' | 'WAON' | 'WECHATPAY' | 'WIRETRANSFER' | 'Yucho Pay' | 'ZELLE' | 'auPay' | 'dBarai' | 'Überweisung'>;
    /**
     * Performers at the event
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     *
     * Filtering Type: `list of text`
     *
     * ```
     * Eligible For:
     * * event
     * ```
     */
    performers?: Array<string>;
    /**
     * Indicates if the entity is pet friendly.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    petsAllowed?: 'PETS_WELCOME' | 'PETS_WELCOME_FOR_FREE' | 'NOT_APPLICABLE' | 'NOT_ALLOWED';
    /**
     *
     * **NOTE:** The list of photos that you send us must be comprehensive. For example, if you send us a list of photos that does not include photos that you sent in your last update, Yext considers the missing photos to be deleted, and we remove them from your listings.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 500 elements.
     *
     * Array item description:
     *
     * >Supported Aspect Ratios:
     * >* 1 x 1
     * >* 4 x 3
     * >* 3 x 2
     * >* 5 x 3
     * >* 16 x 9
     * >* 3 x 1
     * >* 2 x 3
     * >* 5 x 7
     * >* 4 x 5
     * >* 4 x 1
     * >
     * >**NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
     * >
     *
     * Filtering Type: `list of object`
     *
     * ```
     * Eligible For:
     * * atm
     * * event
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * hotelRoomType
     * * location
     * * organization
     * * product
     * * restaurant
     * ```
     */
    photoGallery?: Array<{
        /**
         * Filtering Type: `text`
         */
        clickthroughUrl?: string;
        /**
         * Filtering Type: `text`
         */
        description?: string;
        /**
         * Filtering Type: `text`
         */
        details?: string;
        /**
         *
         * Supported Aspect Ratios:
         * * 1 x 1
         * * 4 x 3
         * * 3 x 2
         * * 5 x 3
         * * 16 x 9
         * * 3 x 1
         * * 2 x 3
         * * 5 x 7
         * * 4 x 5
         * * 4 x 1
         *
         * **NOTE**: Maximum image size is 5mb after normalization and padding (if applicable). As well, there is a 6 second download limit from the image host.
         *
         *
         * Filtering Type: `object`
         */
        image: {
            /**
             * Filtering Type: `text`
             */
            alternateText?: string;
            /**
             * Filtering Type: `text`
             */
            url: string;
        };
    }>;
    /**
     * Coordinates of where consumers can be picked up at the entity, as provided by you
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * atm
     * * event
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    pickupCoordinate?: {
        /**
         * Filtering Type: `float`
         */
        latitude?: number;
        /**
         * Filtering Type: `float`
         */
        longitude?: number;
    };
    /**
     * Contains the daily pickup hours, holiday pickup hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `pickupHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday pickup hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     *
     * Filtering Type: `hours`
     *
     * ```
     * Eligible For:
     * * healthcareFacility
     * * location
     * * restaurant
     * ```
     */
    pickupHours?: {
        /**
         * Filtering Type: `object`
         */
        friday?: {
            /**
             * Indicates if the pickup hours are "closed" on Friday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         *
         * Filtering Type: `list of object`
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             *
             * Filtering Type: `date`
             */
            date: string;
            /**
             * Indicates if the pickup hours are "closed" on on the given date.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             *
             * Filtering Type: `boolean`
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on the specified date.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        }>;
        /**
         * Filtering Type: `object`
         */
        monday?: {
            /**
             * Indicates if the pickup hours are "closed" on Monday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         *
         * Filtering Type: `date`
         */
        reopenDate?: string;
        /**
         * Filtering Type: `object`
         */
        saturday?: {
            /**
             * Indicates if the pickup hours are "closed" on Saturday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        sunday?: {
            /**
             * Indicates if the pickup hours are "closed" on Sunday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        thursday?: {
            /**
             * Indicates if the pickup hours are "closed" on Thursday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        tuesday?: {
            /**
             * Indicates if the pickup hours are "closed" on Tuesday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        wednesday?: {
            /**
             * Indicates if the pickup hours are "closed" on Wednesday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for pickup on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
    };
    /**
     * URL for your Pinterest account, format should be https://www.pinterest.com/yourUsername
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * contactCard
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * organization
     * * restaurant
     * ```
     */
    pinterestUrl?: string;
    /**
     * he typical price of products sold by this location, on a scale of 1 (low) to 4 (high)
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * atm
     * * healthcareFacility
     * * healthcareProfessional
     * * location
     * * restaurant
     * ```
     */
    priceRange?: 'UNSPECIFIED' | 'ONE' | 'TWO' | 'THREE' | 'FOUR';
    /**
     * ID of the user who is the primary Knowledge Assistant contact for the entity
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * atm
     * * faq
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * helpArticle
     * * hotel
     * * job
     * * location
     * * organization
     * * product
     * * restaurant
     * ```
     */
    primaryConversationContact?: string;
    /**
     * Indicates whether the entity has access to a private beach.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    privateBeach?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity offers private car services.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    privateCarService?: 'PRIVATE_CAR_SERVICE' | 'PRIVATE_CAR_SERVICE_FOR_FREE' | 'NOT_APPLICABLE';
    /**
     * Information about the Products & Services Content Lists associated with this entity
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    productLists?: {
        /**
         * IDs of the Products & Services Lists associated with this entity
         *
         *
         * Array must be ordered.
         *
         * Array may have a maximum of 40 elements.
         *
         *
         * Filtering Type: `list of text`
         */
        ids?: Array<string>;
        /**
         * Label to be used for this entity's Products & Services Lists. This label will appear on your entity's listings.
         *
         * Filtering Type: `text`
         */
        label?: string;
    };
    /**
     * Products sold by this entity
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     *
     * Filtering Type: `list of text`
     *
     * ```
     * Eligible For:
     * * financialProfessional
     * * location
     * ```
     */
    products?: Array<string>;
    /**
     * Indicates whether Yext Knowledge Assistant question-and-answer conversations are enabled for this entity
     *
     * Filtering Type: `boolean`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * organization
     * * restaurant
     * ```
     */
    questionsAndAnswers?: boolean;
    /**
     * Information about the competitors whose search performance you would like to compare to your own
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 5 elements.
     *
     *
     * Filtering Type: `list of object`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * organization
     * * restaurant
     * ```
     */
    rankTrackingCompetitors?: Array<{
        /**
         * A name of a competitor
         *
         *
         * Cannot Include:
         * * HTML markup
         *
         * Filtering Type: `text`
         */
        name: string;
        /**
         * The business website of a competitor
         *
         *
         * Cannot Include:
         * * common domain names, e.g., google.com, youtube.com, etc.
         *
         * Filtering Type: `text`
         */
        website: string;
    }>;
    /**
     * Indicates whether Rank Tracking is enabled
     *
     * Filtering Type: `boolean`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * organization
     * * restaurant
     * ```
     */
    rankTrackingEnabled?: boolean;
    /**
     * How often we send search queries to track your search performance
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * organization
     * * restaurant
     * ```
     */
    rankTrackingFrequency?: 'WEEKLY' | 'MONTHLY' | 'QUARTERLY';
    /**
     * The ways in which your keywords will be arranged in the search queries we use to track your performance
     *
     *
     * Array must have a minimum of 2 elements.
     *
     * Array may have a maximum of 4 elements.
     *
     *
     * Filtering Type: `list of option`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * organization
     * * restaurant
     * ```
     */
    rankTrackingQueryTemplates?: Array<'KEYWORD' | 'KEYWORD_ZIP' | 'KEYWORD_CITY' | 'KEYWORD_IN_CITY' | 'KEYWORD_NEAR_ME' | 'KEYWORD_CITY_STATE'>;
    /**
     * The search engines that we will use to track your performance
     *
     * Filtering Type: `list of option`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * organization
     * * restaurant
     * ```
     */
    rankTrackingSites?: Array<'GOOGLE_DESKTOP' | 'GOOGLE_MOBILE' | 'BING_DESKTOP' | 'BING_MOBILE' | 'YAHOO_DESKTOP' | 'YAHOO_MOBILE'>;
    /**
     * Information about the URL consumers can visit to make reservations at this entity
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    reservationUrl?: {
        /**
         * The URL that is shown on your listings in place of **`reservationUrl.url`**. You can use **`reservationUrl.displayUrl`** to display a short, memorable web address that redirects consumers to the URL given in **`reservationUrl.url`**.
         *
         * Must be a valid URL and be specified along with **`reservationUrl.url`**.
         *
         * Filtering Type: `text`
         */
        displayUrl?: string;
        /**
         * If set to true, only the display URL will be sent to those publishers who do not support separate display and tracking URLs for this field.
         *
         * Filtering Type: `boolean`
         */
        preferDisplayUrl?: boolean;
        /**
         * A valid URL used to make reservations at this entity
         *
         * Filtering Type: `text`
         */
        url?: string;
    };
    /**
     * The number of restaurants the entity has.
     *
     * Filtering Type: `integer`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    restaurantCount?: number;
    /**
     * The URL given Review Invitation emails where consumers can leave a review about the entity
     *
     * ```
     * Eligible For:
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    reviewGenerationUrl?: string;
    /**
     * Indicates whether Yext Knowledge Assistant review-response conversations are enabled for this entity
     *
     * Filtering Type: `boolean`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    reviewResponseConversationEnabled?: boolean;
    /**
     * The number of rooms the entity has.
     *
     * Filtering Type: `integer`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    roomCount?: number;
    /**
     * Indicates whether the entity offers room service.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    roomService?: 'ROOM_SERVICE_AVAILABLE' | 'ROOM_SERVICE_AVAILABLE_24_HOURS' | 'NOT_APPLICABLE';
    /**
     * Destination coordinates to use for driving directions to the entity, as provided by you
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * atm
     * * event
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    routableCoordinate?: {
        /**
         * Filtering Type: `float`
         */
        latitude?: number;
        /**
         * Filtering Type: `float`
         */
        longitude?: number;
    };
    /**
     * Indicates whether the entity has a salon.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    salon?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has a sauna.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    sauna?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity offers scuba diving.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    scuba?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity offers self parking services.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    selfParking?: 'SELF_PARKING_AVAILABLE' | 'SELF_PARKING_AVAILABLE_FOR_FREE' | 'NOT_APPLICABLE';
    /**
     * Contains the daily senior hours, holiday senior hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `seniorHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday senior hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     *
     * Filtering Type: `hours`
     *
     * ```
     * Eligible For:
     * * location
     * * restaurant
     * ```
     */
    seniorHours?: {
        /**
         * Filtering Type: `object`
         */
        friday?: {
            /**
             * Indicates if the senior hours are "closed" on Friday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's senior hours on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         *
         * Filtering Type: `list of object`
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             *
             * Filtering Type: `date`
             */
            date: string;
            /**
             * Indicates if the senior hours are "closed" on on the given date.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             *
             * Filtering Type: `boolean`
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for the Entity's senior hours on the specified date.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        }>;
        /**
         * Filtering Type: `object`
         */
        monday?: {
            /**
             * Indicates if the senior hours are "closed" on Monday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's senior hours on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         *
         * Filtering Type: `date`
         */
        reopenDate?: string;
        /**
         * Filtering Type: `object`
         */
        saturday?: {
            /**
             * Indicates if the senior hours are "closed" on Saturday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's senior hours on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        sunday?: {
            /**
             * Indicates if the senior hours are "closed" on Sunday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's senior hours on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        thursday?: {
            /**
             * Indicates if the senior hours are "closed" on Thursday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's senior hours on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        tuesday?: {
            /**
             * Indicates if the senior hours are "closed" on Tuesday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's senior hours on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        wednesday?: {
            /**
             * Indicates if the senior hours are "closed" on Wednesday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for the Entity's senior hours on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
    };
    /**
     * Information about the area that is served by this entity. It is specified as a list of cities and/or postal codes.
     *
     * **Only for Google Business Profile and Bing:** Currently, **serviceArea** is only supported by Google Business Profile and Bing and will not affect your listings on other sites.
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    serviceArea?: {
        /**
         * A list of places served by the entity, where each place is either:
         * - a postal code, or
         * - the name of a city.
         *
         *
         *
         * Array must be ordered.
         *
         * Array may have a maximum of 200 elements.
         *
         *
         * Filtering Type: `list of text`
         */
        places?: Array<string>;
    };
    /**
     * Information about the area that is served by this entity. It is specified as a list of service area names, their associated types and google place ids.
     * **Only for Google Business Profile and Bing:** Currently, **serviceArea** is only supported by Google Business Profile and Bing and will not affect your listings on other sites.
     *
     *
     * Array may have a maximum of 200 elements.
     *
     *
     * Filtering Type: `list of object`
     *
     * ```
     * Eligible For:
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    serviceAreaPlaces?: Array<{
        /**
         * Filtering Type: `text`
         */
        name?: string;
        /**
         * Filtering Type: `text`
         */
        googlePlaceId?: string;
        /**
         * Filtering Type: `option`
         */
        type?: 'POSTAL_CODE' | 'REGION' | 'COUNTY' | 'CITY' | 'SUBLOCALITY';
    }>;
    /**
     * Services offered by this entity
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     *
     * Filtering Type: `list of text`
     *
     * ```
     * Eligible For:
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    services?: Array<string>;
    /**
     * Indicates if the entity is smoke free.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    smokeFreeProperty?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity offers snorkeling.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    snorkeling?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity offers a social hour.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    socialHour?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has a spa.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    spa?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Up to 100 of this entity's specialities (e.g., for food and dining: `Chicago style`)
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     *
     *
     * Array must be ordered.
     *
     * Array may have a maximum of 100 elements.
     *
     * Array item description:
     *
     * >Cannot Include:
     * >* HTML markup
     *
     * Filtering Type: `list of text`
     *
     * ```
     * Eligible For:
     * * financialProfessional
     * * location
     * * restaurant
     * ```
     */
    specialities?: Array<string>;
    /**
     * Indicates whether the entity has a sit-down restaurant.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    tableService?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Contains the daily takeout hours, holiday takeout hours, and reopen date for the Entity.
     *
     * Each day is represented by a sub-field of `takeoutHours`. (e.g. `monday`, `tuesday`, etc.) Open times can be specified per day through the `openIntervals` field and the `isClosed` flag.
     * Similarly, holiday takeout hours are represented by the `holidayHours` sub-field.
     * Setting the `reopenDate` sub-field indicates that the business is temporarily closed and will reopen on the specified date.
     * SPECIAL CASES:
     * * To indicate that an Entity is open 24 hours on a specific day, set start to 00:00 and end to 23:59 in `openIntervals` for that day.
     * * To indicate that an Entity has split hours on a specific day (e.g., open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM), supply two or more `openIntervals` values with non-overlapping sets of hours.
     * * If you are providing `openIntervals`, you may not set `isClosed` to true for that day.
     *
     * Filtering Type: `hours`
     *
     * ```
     * Eligible For:
     * * location
     * * restaurant
     * ```
     */
    takeoutHours?: {
        /**
         * Filtering Type: `object`
         */
        friday?: {
            /**
             * Indicates if the takeout hours are "closed" on Friday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for takeout on Friday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         *
         * **NOTE:** The list of Holiday Hours that you send us must be comprehensive. For example, if you send us a list of Holiday Hours that does not include Holiday Hours that you sent in your last update, Yext considers the missing Holiday Hours to be deleted, and we remove them.
         *
         *
         *
         * Array must be ordered.
         *
         *
         * Filtering Type: `list of object`
         */
        holidayHours?: Array<{
            /**
             * Date on which the holiday hours will be in effect. Cannot be in the past.
             *
             *
             * Date must be on or after 1970-01-01
             * Date must be before or on 2038-01-01
             *
             * Filtering Type: `date`
             */
            date: string;
            /**
             * Indicates if the takeout hours are "closed" on on the given date.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
             *
             * Filtering Type: `boolean`
             */
            isRegularHours?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for takeout on the specified date.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        }>;
        /**
         * Filtering Type: `object`
         */
        monday?: {
            /**
             * Indicates if the takeout hours are "closed" on Monday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for takeout on Monday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         *
         * Date must be on or after 1970-01-01
         * Date must be before or on 2038-01-01
         *
         * Filtering Type: `date`
         */
        reopenDate?: string;
        /**
         * Filtering Type: `object`
         */
        saturday?: {
            /**
             * Indicates if the takeout hours are "closed" on Saturday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for takeout on Saturday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        sunday?: {
            /**
             * Indicates if the takeout hours are "closed" on Sunday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for takeout on Sunday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        thursday?: {
            /**
             * Indicates if the takeout hours are "closed" on Thursday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for takeout on Thursday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        tuesday?: {
            /**
             * Indicates if the takeout hours are "closed" on Tuesday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for takeout on Tuesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
        /**
         * Filtering Type: `object`
         */
        wednesday?: {
            /**
             * Indicates if the takeout hours are "closed" on Wednesday.
             *
             * Filtering Type: `boolean`
             */
            isClosed?: boolean;
            /**
             * Contains the time intervals for which the Entity is open for takeout on Wednesday. Note that if isClosed is set to true, "openIntervals" cannot be provided in an update.
             *
             * Filtering Type: `list of object`
             */
            openIntervals?: Array<{
                /**
                 * The end time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                end: string;
                /**
                 * The start time of the interval in `hh:mm` format (e.g., `"06:30"`, `"17:00"`).
                 *
                 * Filtering Type: `time`
                 */
                start: string;
            }>;
        };
    };
    /**
     * Indicates whether the entity has tennis courts.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    tennis?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has a thermal pool.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    thermalPool?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Information about the availability of tickets for the event
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * event
     * ```
     */
    ticketAvailability?: 'IN_STOCK' | 'SOLD_OUT' | 'PRE_ORDER' | 'UNSPECIFIED';
    /**
     * Contains the price range for the event
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * event
     * ```
     */
    ticketPriceRange?: {
        /**
         * Three letter currency code (ISO standard)
         *
         * Filtering Type: `text`
         */
        currencyCode?: string;
        /**
         * Maximum ticket price
         *
         * Filtering Type: `decimal`
         */
        maxValue?: string;
        /**
         * Minimum ticket price
         *
         * Filtering Type: `decimal`
         */
        minValue?: string;
    };
    /**
     * The date/time tickets are available for sale (local time)
     *
     * Filtering Type: `datetime`
     *
     * ```
     * Eligible For:
     * * event
     * ```
     */
    ticketSaleDateTime?: string;
    /**
     * URL to purchase tickets for the event (if ticketed)
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * event
     * ```
     */
    ticketUrl?: string;
    /**
     * URL for your TikTok profile, format should be https://www.tiktok.com/yourUsername
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * contactCard
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * organization
     * * restaurant
     * ```
     */
    tikTokUrl?: string;
    /**
     * Contains the start/end times for the event
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * event
     * ```
     */
    time?: {
        /**
         * End date/time of the event, in local time (see timezone field)
         * Standard ISO 8601 datetime without timezone
         * Format: `YYYY-MM-DDThh:mm`
         *
         *
         * Filtering Type: `datetime`
         */
        end?: string;
        /**
         * Start date/time of the event, in local time (see timezone field)
         * Standard ISO 8601 datetime without timezone
         * Format: `YYYY-MM-DDThh:mm`
         *
         *
         * Filtering Type: `datetime`
         */
        start?: string;
    };
    /**
     * Represents the time zone offset of the entity from UTC, in `±hh:mm` format.
     *
     * For example, if the entity is 4 hours ahead of UTC time, the offset will be `+04:00`.
     *
     * If the entity is 15.5 hours behind UTC time, the offset will be `-15:30`.
     *
     * If the entity is in UTC time, the offset will be `+00:00`.
     *
     *
     * ```
     * Eligible For:
     * * atm
     * * event
     * * faq
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * job
     * * location
     * * restaurant
     * ```
     */
    timeZoneUtcOffset?: string;
    /**
     * The timezone of the entity, in the standard `IANA time zone database` format (tz database). e.g. `"America/New_York"`
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * atm
     * * board
     * * card
     * * contactCard
     * * event
     * * faq
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * helpArticle
     * * hotel
     * * hotelRoomType
     * * job
     * * location
     * * organization
     * * product
     * * restaurant
     * ```
     */
    timezone?: string;
    /**
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    tollFreePhone?: string;
    /**
     * Indicates whether the entity has a treadmill.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    treadmill?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Must be a valid phone number.
     *
     * If the phone number's calling code is for a country other than the one given in the entity's **`countryCode`**, the phone number provided must contain the calling code (e.g., `+44` in `+442038083831`). Otherwise, the calling code is optional.
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * atm
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    ttyPhone?: string;
    /**
     * Indicates whether the entity offers turndown service.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    turndownService?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Valid Twitter handle for the entity without the leading "@" (e.g., `JohnSmith`)
     *
     * If you submit an invalid Twitter handle, it will be ignored. The success response will contain a warning message explaining why your Twitter handle wasn't stored in the system.
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * organization
     * * restaurant
     * ```
     */
    twitterHandle?: string;
    /**
     * Information about the Yext-powered link that can be copied and pasted into the markup of Yext Pages where the embedded Uber link should appear
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    uberLink?: {
        /**
         * Indicates whether the embedded Uber link for this entity appears as text or a button
         *
         * When consumers click on this link on a mobile device, the Uber app (if installed) will open with your entity set as the trip destination. If the Uber app is not installed, the consumer will be prompted to download it.
         *
         * Filtering Type: `option`
         */
        presentation: 'BUTTON' | 'LINK';
        /**
         * The text of the embedded Uber link
         *
         * Default is `Ride there with Uber`.
         *
         * **NOTE:** This field is only available if **`uberLink.presentation`** is `LINK`.
         *
         * Filtering Type: `text`
         */
        text?: string;
    };
    /**
     * Information about the call-to-action consumers will see in the Uber app during a trip to your entity
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    uberTripBranding?: {
        /**
         * A longer description that will appear near the call-to-action in the Uber app during a trip to your entity.
         *
         * **NOTE:** If a value for **`uberTripBranding.description`** is provided, values must also be provided for **`uberTripBranding.text`** and **`uberTripBranding.url`**.
         *
         * Filtering Type: `text`
         */
        description: string;
        /**
         * The text of the call-to-action that will appear in the Uber app during a trip to your entity (e.g., `Check out our menu!`)
         *
         * **NOTE:** If a value for **`uberTripBranding.text`** is provided, values must also be provided for **`uberTripBranding.url`** and **`uberTripBranding.description`**.
         *
         * Filtering Type: `text`
         */
        text: string;
        /**
         * The URL that the consumer will be redirected to when tapping on the call-to-action in the Uber app during a trip to your entity.
         *
         * **NOTE:** If a value for **`uberTripBranding.url`** is provided, values must also be provided for **`uberTripBranding.text`** and **`uberTripBranding.description`**.
         *
         * Filtering Type: `text`
         */
        url: string;
    };
    /**
     * Indicates whether the entity offers valet parking services.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    valetParking?: 'VALET_PARKING_AVAILABLE' | 'VALET_PARKING_AVAILABLE_FOR_FREE' | 'NOT_APPLICABLE';
    /**
     * The date this entity is valid through.
     *
     * Filtering Type: `datetime`
     *
     * ```
     * Eligible For:
     * * job
     * ```
     */
    validThrough?: string;
    /**
     * Indicates whether the entity has a vending machine.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    vendingMachine?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Name of the venue where the event is being held
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * event
     * ```
     */
    venueName?: string;
    /**
     * Valid YouTube URLs for embedding a video on some publisher sites
     *
     * **NOTE:** Currently, only the first URL in the Array appears in your listings.
     *
     *
     *
     * Array must be ordered.
     *
     *
     * Filtering Type: `list of object`
     *
     * ```
     * Eligible For:
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * hotelRoomType
     * * location
     * * organization
     * * product
     * * restaurant
     * ```
     */
    videos?: Array<{
        /**
         *
         * Cannot Include:
         * * HTML markup
         *
         * Filtering Type: `text`
         */
        description?: string;
        /**
         * Filtering Type: `object`
         */
        video: {
            /**
             * Filtering Type: `text`
             */
            url: string;
        };
    }>;
    /**
     * Indicates whether the entity has a wading pool.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    wadingPool?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity offers wake up call services.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    wakeUpCalls?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Destination coordinates to use for walking directions to the entity, as provided by you
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * atm
     * * event
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    walkableCoordinate?: {
        /**
         * Filtering Type: `float`
         */
        latitude?: number;
        /**
         * Filtering Type: `float`
         */
        longitude?: number;
    };
    /**
     * Indicates whether the entity has a water park.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    waterPark?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity offers water skiing.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    waterSkiing?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity offers any kind of watercrafts.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    watercraft?: 'WATERCRAFT_RENTALS' | 'WATERCRAFT_RENTALS_FOR_FREE' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has a water slide.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    waterslide?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has a wave pool.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    wavePool?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Information about the website for this entity
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * atm
     * * contactCard
     * * event
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * organization
     * * restaurant
     * ```
     */
    websiteUrl?: {
        /**
         * The URL that is shown on your listings in place of **`websiteUrl.url`**. You can use **`websiteUrl.displayUrl`** to display a short, memorable web address that redirects consumers to the URL given in **`websiteUrl.url`**.
         *
         * Must be a valid URL and be specified along with **`websiteUrl.url`**.
         *
         * Filtering Type: `text`
         */
        displayUrl?: string;
        /**
         * If set to true, only the display URL will be sent to those publishers who do not support separate display and tracking URLs for this field.
         *
         * Filtering Type: `boolean`
         */
        preferDisplayUrl?: boolean;
        /**
         * A valid URL for this entity's website
         *
         * Filtering Type: `text`
         */
        url?: string;
    };
    /**
     * Indicates whether the entity has a weight machine.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    weightMachine?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates if the entity is wheelchair accessible.
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    wheelchairAccessible?: 'YES' | 'NO' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the entity has WiFi available
     *
     * Filtering Type: `option`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    wifiAvailable?: 'WIFI_AVAILABLE' | 'WIFI_AVAILABLE_FOR_FREE' | 'NOT_APPLICABLE';
    /**
     * Indicates whether the job is remote.
     *
     * Filtering Type: `boolean`
     *
     * ```
     * Eligible For:
     * * job
     * ```
     */
    workRemote?: boolean;
    /**
     * The year the entity was established.
     *
     * Filtering Type: `integer`
     *
     * ```
     * Eligible For:
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    yearEstablished?: number;
    /**
     * The most recent year the entity was partially or completely renovated.
     *
     * Filtering Type: `integer`
     *
     * ```
     * Eligible For:
     * * hotel
     * ```
     */
    yearLastRenovated?: number;
    /**
     * Coordinates where the map pin for the entity should be displayed, as calculated by Yext
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * atm
     * * event
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * job
     * * location
     * * restaurant
     * ```
     */
    yextDisplayCoordinate?: {
        /**
         * Filtering Type: `float`
         */
        latitude?: number;
        /**
         * Filtering Type: `float`
         */
        longitude?: number;
    };
    /**
     * Coordinates of where consumers can be dropped off at the entity, as calculated by Yext
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * atm
     * * event
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    yextDropoffCoordinate?: {
        /**
         * Filtering Type: `float`
         */
        latitude?: number;
        /**
         * Filtering Type: `float`
         */
        longitude?: number;
    };
    /**
     * Coordinates of where consumers can be picked up at the entity, as calculated by Yext
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * atm
     * * event
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    yextPickupCoordinate?: {
        /**
         * Filtering Type: `float`
         */
        latitude?: number;
        /**
         * Filtering Type: `float`
         */
        longitude?: number;
    };
    /**
     * Destination coordinates to use for driving directions to the entity, as calculated by Yext
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * atm
     * * event
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    yextRoutableCoordinate?: {
        /**
         * Filtering Type: `float`
         */
        latitude?: number;
        /**
         * Filtering Type: `float`
         */
        longitude?: number;
    };
    /**
     * Destination coordinates to use for walking directions to the entity, as calculated by Yext
     *
     * Filtering Type: `object`
     *
     * ```
     * Eligible For:
     * * atm
     * * event
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * restaurant
     * ```
     */
    yextWalkableCoordinate?: {
        /**
         * Filtering Type: `float`
         */
        latitude?: number;
        /**
         * Filtering Type: `float`
         */
        longitude?: number;
    };
    /**
     * URL for your YouTube channel, format should be https://www.youtube.com/c/yourUsername
     *
     * Filtering Type: `text`
     *
     * ```
     * Eligible For:
     * * contactCard
     * * financialProfessional
     * * healthcareFacility
     * * healthcareProfessional
     * * hotel
     * * location
     * * organization
     * * restaurant
     * ```
     */
    youTubeChannelUrl?: string;
};

/**
 * Location Add Request Response
 */
export type AddRequest = {
    /**
     * The Yext ID of the created add request.
     */
    readonly id?: number;
    /**
     * Whether the request is to add service for an existing location or to create a new location and add service to it.
     *
     */
    locationMode?: 'EXISTING' | 'NEW';
    /**
     * The existing location ID to add service to.
     *
     * This field is provided only when *locationMode* is EXISTING. Either this field or *newLocationId* will be provided.
     *
     */
    existingLocationId?: string;
    /**
     * The new location ID to create and add service to.
     *
     * This field is provided only when *locationMode* is NEW. Either this field or *existingLocationId* will be provided.
     *
     */
    newLocationId?: string;
    /**
     * *(Partner Portal mode)* The account ID to create a new location under.
     *
     * This field is provided only for Partner Portal and when *locationMode* is NEW.
     *
     */
    newLocationAccountId?: string;
    /**
     * *(Partner Portal mode)* The account name to create a new location under.
     *
     * This field is provided only for Partner Portal and when *locationMode* is NEW and when *newLocationAccountName* was specified in the add request.
     *
     */
    newLocationAccountName?: string;
    /**
     * *(Partner Portal mode)* The account ID to create a new account and location under.
     *
     * This field is provided only for Partner Portal, when *locationMode* is NEW and *newAccountParentAccountId* was specified in the add request.
     *
     */
    newAccountParentAccountId?: string;
    /**
     * The location profile data used in creating a new location for this add request.
     *
     * Optional. Provided only for the **Add Requests: Create (New Location)** endpoint.
     *
     */
    newLocationData?: Location;
    /**
     * The entity profile data used in creating a new entity for this add request.
     *
     * Optional. Provided only for the **Add Requests: Create (New Location)** endpoint.
     *
     */
    newEntityData?: Entity;
    /**
     * List of SKUs to sign the location up for, from among those listed in the **Available Services: List** response with Location recipientType.
     *
     */
    skus?: Array<string>;
    /**
     * The Agreement ID of the agreement that services will be added under.
     *
     */
    agreementId?: number;
    /**
     * The current status of the add request.
     */
    readonly status?: 'SUBMITTED' | 'PROCESSING' | 'COMPLETE' | 'CANCELED' | 'REVIEW' | 'FAILED';
    /**
     * The date the add request was submitted.
     */
    readonly dateSubmitted?: string;
    /**
     * The date the add request was completed. An empty string if the request has not been completed.
     */
    readonly dateCompleted?: string;
    /**
     * Results from processing.
     */
    readonly statusDetail?: string;
};

export type ExistingLocationAddRequest = {
    /**
     * ID of the location to add service for. It must already be in your account (or, if you use the Partner Portal, any of your sub-accounts).
     */
    existingLocationId: string;
    /**
     * *(Partner Portal mode only)* If the *existingLocationId* you specified is not unique among your sub-accounts, use this to specify which account the existing location is in.
     *
     */
    existingLocationAccountId?: string;
    /**
     * List of SKUs that you would like to sign the location up for, from among those listed in the **Available Services: List** response with Location recipientType.
     *
     */
    skus: Array<string>;
    /**
     * *(Advanced field)* The Agreement ID of the agreement that services will be added under. This is set automatically by Yext when you create the add request. (You can specify it yourself, but should not do so unless you have intentionally set up multiple active agreements with Yext, since this could cause your integration to break when you renew or upgrade your agreement.)
     *
     */
    agreementId?: number;
    /**
     * *(Sandbox API only)* Forces this add request into the REVIEW status if it otherwise would have completed successfully.
     *
     */
    forceReview?: boolean;
};

export type SkuQuantity = {
    /**
     * The specified SKU.
     */
    sku?: string;
    /**
     * The quantity of the specified SKU.
     */
    quantity?: number;
};

export type ExistingSubAccountAddRequest = {
    /**
     * The existing account ID to add service to.
     *
     */
    subAccountId: string;
    /**
     * List of objects representing the SKUs to be added (or increased in quantity) to the
     * account, from among those listed in the **Available Services: List** response with Sub-Account recipientType. If the sub-account already has an active service for a SKU, the service quantity will be increased. The added quantity will default to 1 for any SKU without a specified quantity.
     *
     */
    skuAdditions: Array<SkuQuantity>;
    /**
     * *(Advanced field)* The Agreement ID of the agreement that services will be added under. This is set automatically by Yext when you create the add request. (You can specify it yourself, but should not do so unless you have intentionally set up multiple active agreements with Yext, since this could cause your integration to break when you renew or upgrade your agreement.).
     *
     */
    agreementId?: number;
};

/**
 * Sub-Account Add Request Response
 */
export type SubAccountAddRequest = {
    /**
     * The Yext ID of the created add request.
     */
    readonly id?: number;
    /**
     * The existing sub-account ID to add service to.
     *
     */
    subAccountId?: string;
    /**
     * List of objects representing the SKUs to be added (or increased in quantity) to the sub-account, from among those listed in the **Available Services: List** response with Sub-Account recipientType. If the sub-account already has an active service for a SKU, the service quantity will be increased. The added quantity will default to 1 for any SKU without a specified quantity.
     *
     */
    skuAdditions?: Array<SkuQuantity>;
    /**
     * The Agreement ID of the agreement that services will be added under.
     *
     */
    agreementId?: number;
    /**
     * The current status of the add request.
     */
    readonly status?: 'SUBMITTED' | 'PROCESSING' | 'COMPLETE' | 'CANCELED' | 'FAILED';
    /**
     * The date the add request was submitted.
     */
    readonly dateSubmitted?: string;
    /**
     * Results from processing.
     */
    readonly statusDetail?: string;
};

export type CancelServicesRequest = {
    locationId: string;
    /**
     * *(Partner Portal mode only)* The account that the location receiving service is in.
     *
     */
    locationAccountId?: string;
    /**
     * The ID of the agreement under which you want services canceled. You do not need to supply this except in advanced scenarios.
     */
    agreementId?: number;
    /**
     * List of SKUs that you would like to cancel on the location. The location must have active services for at least one of the provided SKUs. If you do not provide a list of SKUs, all active services for the location will be canceled.
     */
    skus?: Array<string>;
};

export type Service = {
    /**
     * Yext ID for the service.
     */
    id?: number;
    /**
     * The location receiving service.
     */
    locationId?: string;
    /**
     * *(Partner Portal mode only)* The account that the location receiving service is in.
     *
     */
    locationAccountId?: string;
    /**
     * The ID of your agreement with Yext under which this service is being delivered. This is important to you only in advanced scenarios where you have set up multiple active agreements with Yext.
     */
    agreementId?: number;
    sku?: string;
    serviceDescription?: string;
    /**
     * The date that this service started.
     */
    started?: string;
    /**
     * Optional. The date that this service stopped.
     */
    stopped?: string;
    /**
     * Optional. Future date on which the service should be stopped if it's still active when that date arrives.
     */
    stopOnDate?: string;
    status?: 'ACTIVE' | 'STOPPED';
};

export type CancelSubAccountServicesRequest = {
    subAccountId: string;
    /**
     * List of objects representing the SKUs to be removed (or decreased in quantity) from the sub-account. The sub-account must have active services for at least one of the provided SKUs. If the decrease in quantity would result in 0 service quantity, the service will be STOPPED. The removed quantity will default to 1 for any SKU without a specified quantity. To cancel all active services provisioned on the account, use the **Services: Cancel All (Account)** endpoint.
     *
     */
    skuRemovals: Array<SkuQuantity>;
    /**
     * The ID of the agreement under which you want services canceled. You do not need to supply this except in advanced scenarios.
     */
    agreementId?: number;
};

export type SubAccountService = {
    /**
     * Yext ID for the service.
     */
    id?: number;
    /**
     * The sub-account receiving service.
     */
    subAccountId?: string;
    /**
     * The ID of your agreement with Yext under which this service is being delivered. This is important to you only in advanced scenarios where you have set up multiple active agreements with Yext.
     */
    agreementId?: number;
    sku?: string;
    serviceDescription?: string;
    /**
     * The quantity of the SKU that the service currently has.
     */
    serviceQuantity?: number;
    /**
     * The date that this service started.
     */
    started?: string;
    /**
     * Optional. The date that this service stopped.
     */
    stopped?: string;
    /**
     * Optional. Future date on which the service should be stopped if it's still active when that date arrives.
     */
    stopOnDate?: string;
    status?: 'ACTIVE' | 'STOPPED';
};

export type CancelAllSubAccountServicesRequest = {
    subAccountId: string;
    /**
     * The ID of the agreement under which you want services canceled. You do not need to supply this except in advanced scenarios.
     */
    agreementId?: number;
};

export type ProcessReviewAddRequest = {
    /**
     * ID of the add request with REVIEW status
     */
    addRequestId: number;
    /**
     * Action to take on the add request.
     */
    status: string;
};

export type CreateSubAccountRequest = {
    /**
     * The external sub-account ID for the new sub-account.
     *
     */
    newSubAccountId: string;
    /**
     * The name for the new sub-account.
     *
     */
    newSubAccountName: string;
    /**
     * The country code for the new sub-account.
     *
     */
    countryCode: string;
};

/**
 * Create Sub-Account Response
 */
export type CreateSubAccount = {
    /**
     * The external sub-account ID for the newly created sub-account.
     *
     */
    newSubAccountId?: string;
    /**
     * The name for the newly created sub-account.
     *
     */
    newSubAccountName?: string;
    /**
     * The country code for the newly created sub-account.
     *
     */
    countryCode?: string;
};

/**
 * This is an object containing the information about where the Resources are located. The attributes in this object include type, url and variables.
 *
 */
export type Source = {
    /**
     * Type of repository for resources
     */
    type: string;
    /**
     * URL path for the CaC resources. It varies with the source type.
     */
    url: string;
    /**
     * This is an object containing any resources variables that should be passed to the applier.
     */
    variables?: {
        [key: string]: unknown;
    };
};

export type ResourcesApplyRequest = {
    /**
     * ID for the account which the resources will be applied to.
     *
     */
    targetAccountId: string;
    source: Source;
};

/**
 * Resources Apply Request Response
 */
export type ResourcesApplyRequestResponse = {
    /**
     * ID for the account which the resources were applied to.
     *
     */
    targetAccountId?: string;
    source?: Source;
    /**
     * The current status of the resources apply request.
     */
    readonly status?: 'SUBMITTED' | 'PROCESSING' | 'COMPLETE' | 'CANCELED' | 'REVIEW' | 'FAILED';
    /**
     * The date the resources apply request was submitted.
     */
    readonly dateSubmitted?: string;
    /**
     * The date the resources apply request was completed. An empty string if the request has not been completed.
     */
    readonly dateCompleted?: string;
    /**
     * Results from processing.
     */
    readonly statusDetail?: string;
};

export type Account = {
    accountId?: string;
    /**
     * The number of locations in this account.
     */
    readonly locationCount?: number;
    /**
     * The number of sub-accounts directly under this account, if any.
     */
    readonly subAccountCount?: number;
    /**
     * Customer-provided ID of the account that this is a sub-account of, if any. Not provided if this is a top-level account.
     */
    parentAccountId?: string;
    /**
     * The name of this account.
     */
    accountName?: string;
    /**
     * First name of the contact user for this account.
     */
    contactFirstName?: string;
    /**
     * Last name of the contact user for this account.
     */
    contactLastName?: string;
    /**
     * Phone number of the contact user for this account.
     */
    contactPhone?: string;
    /**
     * Email address of the contact user for this account.
     */
    contactEmail?: string;
};

export type UpdateAccount = {
    /**
     * New ID of the account
     */
    accountId?: string;
    /**
     * New name of the account
     */
    accountName?: string;
};

export type NewLocationAddRequestWritable = {
    /**
     * Your ID for the location to be added.
     */
    newLocationId: string;
    /**
     * *(Partner Portal mode)* Supply your ID for the account that the new location should be created in. If this account is your main account or an existing sub-account, the location will be placed there. Otherwise, a new account will be created.
     *
     */
    newLocationAccountId?: string;
    /**
     * *(Partner Portal mode)* If a new account is created, this field's value will set the account's name. If a new account is created, and a value for this field is not provided, the new account's name will default to the location's name.
     *
     */
    newLocationAccountName?: string;
    /**
     * *(Partner Portal mode, advanced use only)* If you have a multi-layer account structure and want the new account created for this request to be under one of your sub-accounts, rather than directly under your main account, specify that sub-account's ID in this field.
     *
     */
    newAccountParentAccountId?: string;
    /**
     * If you set `newLocationId`, you must specify either `newLocationData` or `newEntityData`.
     *
     * This field allows you to set the profile data for a location, using our legacy format.
     * Profile content is specified in the same format as when creating a location with the Locations: Create endpoint.
     *
     * If the Add Request is processed successfully, this data will be used to create a new location in your account.
     *
     */
    newLocationData: LocationWritable;
    /**
     * If you set `newEntityData`, this field specifies the type of entity to be created.
     *
     */
    newEntityType?: 'atm' | 'healthcareFacility' | 'healthcareProfessional' | 'hotel' | 'location' | 'restaurant';
    /**
     * If you set `newLocationId`, you must specify either `newLocationData` or `newEntityData`.
     *
     * This field allows you to set the profile data for an entity.
     * Profile content is specified in the same format as when creating an entity with the Entities: Create endpoint.
     * You can only create entities of a type specified by `newEntityType`.
     *
     * If the Add Request is processed successfully, this data will be used to create a new entity in your account.
     *
     * **The dropdown will specify the profile content for `newEntityData`. Specify the entity type with `newEntityType` described above.**
     *
     */
    newEntityData: unknown;
    /**
     * List of SKUs that you would like to sign the location up for, from among those listed in the **Available Services: List** response with Location recipientType.
     *
     */
    skus: Array<string>;
    /**
     * *(Advanced field)* The Agreement ID of the agreement that services will be added under. This value is set automatically by Yext when you create the Add request. (You can specify it yourself, but should not do so unless you have intentionally set up multiple active agreements with Yext, since it could cause your integration to break when you renew or upgrade your agreement.)
     *
     */
    agreementId?: number;
    /**
     * *(Sandbox API only)* Forces this Add request into the `REVIEW` status if it otherwise would have completed successfully.
     *
     */
    forceReview?: boolean;
};

export type NewLocationAddRequestAtmWritable = Omit<NewLocationAddRequestWritable2, 'newEntityType'> & {
    /**
     * If you set `newLocationId`, you must specify either `newLocationData` or `newEntityData`.
     *
     * This field allows you to set the profile data for an entity.
     * Profile content is specified in the same format as when creating an entity with the Entities: Create endpoint.
     * You can only create entities of a type specified by `newEntityType`.
     *
     * If the Add Request is processed successfully, this data will be used to create a new entity in your account.
     *
     * **The dropdown will specify the profile content for `newEntityData`. Specify the entity type with `newEntityType` described above.**
     *
     */
    newEntityData?: Omit<AtmWrite, 'EntityType'> & {
        EntityType?: 'NewLocationAddRequestATMWritable';
    };
    newEntityType?: 'atm';
};

export type NewLocationAddRequestHealthcareFacilityWritable = Omit<NewLocationAddRequestWritable2, 'newEntityType'> & {
    /**
     * If you set `newLocationId`, you must specify either `newLocationData` or `newEntityData`.
     *
     * This field allows you to set the profile data for an entity.
     * Profile content is specified in the same format as when creating an entity with the Entities: Create endpoint.
     * You can only create entities of a type specified by `newEntityType`.
     *
     * If the Add Request is processed successfully, this data will be used to create a new entity in your account.
     *
     * **The dropdown will specify the profile content for `newEntityData`. Specify the entity type with `newEntityType` described above.**
     *
     */
    newEntityData?: Omit<HealthcareFacilityWrite, 'EntityType'> & {
        EntityType?: 'NewLocationAddRequestHealthcareFacilityWritable';
    };
    newEntityType?: 'healthcareFacility';
};

export type NewLocationAddRequestHealthcareProfessionalWritable = Omit<NewLocationAddRequestWritable2, 'newEntityType'> & {
    /**
     * If you set `newLocationId`, you must specify either `newLocationData` or `newEntityData`.
     *
     * This field allows you to set the profile data for an entity.
     * Profile content is specified in the same format as when creating an entity with the Entities: Create endpoint.
     * You can only create entities of a type specified by `newEntityType`.
     *
     * If the Add Request is processed successfully, this data will be used to create a new entity in your account.
     *
     * **The dropdown will specify the profile content for `newEntityData`. Specify the entity type with `newEntityType` described above.**
     *
     */
    newEntityData?: Omit<HealthcareProfessionalWrite, 'EntityType'> & {
        EntityType?: 'NewLocationAddRequestHealthcareProfessionalWritable';
    };
    newEntityType?: 'healthcareProfessional';
};

export type NewLocationAddRequestHotelWritable = Omit<NewLocationAddRequestWritable2, 'newEntityType'> & {
    /**
     * If you set `newLocationId`, you must specify either `newLocationData` or `newEntityData`.
     *
     * This field allows you to set the profile data for an entity.
     * Profile content is specified in the same format as when creating an entity with the Entities: Create endpoint.
     * You can only create entities of a type specified by `newEntityType`.
     *
     * If the Add Request is processed successfully, this data will be used to create a new entity in your account.
     *
     * **The dropdown will specify the profile content for `newEntityData`. Specify the entity type with `newEntityType` described above.**
     *
     */
    newEntityData?: Omit<HotelWrite, 'EntityType'> & {
        EntityType?: 'NewLocationAddRequestHotelWritable';
    };
    newEntityType?: 'hotel';
};

export type NewLocationAddRequestLocationWritable = Omit<NewLocationAddRequestWritable2, 'newEntityType'> & {
    /**
     * If you set `newLocationId`, you must specify either `newLocationData` or `newEntityData`.
     *
     * This field allows you to set the profile data for an entity.
     * Profile content is specified in the same format as when creating an entity with the Entities: Create endpoint.
     * You can only create entities of a type specified by `newEntityType`.
     *
     * If the Add Request is processed successfully, this data will be used to create a new entity in your account.
     *
     * **The dropdown will specify the profile content for `newEntityData`. Specify the entity type with `newEntityType` described above.**
     *
     */
    newEntityData?: Omit<LocationWrite, 'EntityType'> & {
        EntityType?: 'NewLocationAddRequestLocationWritable';
    };
    newEntityType?: 'location';
};

export type NewLocationAddRequestRestaurantWritable = Omit<NewLocationAddRequestWritable2, 'newEntityType'> & {
    /**
     * If you set `newLocationId`, you must specify either `newLocationData` or `newEntityData`.
     *
     * This field allows you to set the profile data for an entity.
     * Profile content is specified in the same format as when creating an entity with the Entities: Create endpoint.
     * You can only create entities of a type specified by `newEntityType`.
     *
     * If the Add Request is processed successfully, this data will be used to create a new entity in your account.
     *
     * **The dropdown will specify the profile content for `newEntityData`. Specify the entity type with `newEntityType` described above.**
     *
     */
    newEntityData?: Omit<RestaurantWrite, 'EntityType'> & {
        EntityType?: 'NewLocationAddRequestRestaurantWritable';
    };
    newEntityType?: 'restaurant';
};

export type PhotoWritable = {
    /**
     * Valid URL to image. Accepted formats: .jpg, .gif, .png.
     *
     * While updating this field, if the image could not be downloaded, or if its URL is invalid, the image will be ignored. The success response will contain a warning message explaining why the image was not stored in the system.
     *
     */
    url?: string;
    /**
     * Image description.
     */
    description?: string;
    /**
     * Image details.
     */
    details?: string;
    /**
     * The alternate text to be used for accessibility purposes.
     */
    alternateText?: string;
};

export type LocationWritable = {
    /**
     * Primary key. Unique alphanumeric (Latin-1) ID assigned by the Customer.
     */
    id?: string;
    /**
     * Must refer to an **account.id** that already exists.
     */
    accountId?: string;
    locationType?: LocationType;
    /**
     * Cannot include:
     * * inappropriate language
     * * HTML markup or entities
     * * a URL or domain name
     * * a phone number
     * * control characters ([\x00-\x1F\x7F])
     *
     * Should be in appropriate letter case (e.g., not in all capital letters)
     *
     */
    locationName?: string;
    /**
     * The first name of the healthcare professional
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `HEALTHCARE_PROFESSIONAL`.
     *
     */
    firstName?: string;
    /**
     * The middle name of the healthcare professional
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `HEALTHCARE_PROFESSIONAL`.
     *
     */
    middleName?: string;
    /**
     * The last name of the healthcare professional
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `HEALTHCARE_PROFESSIONAL`.
     *
     */
    lastName?: string;
    /**
     * The name of the office where the healthcare professional works, if different from **locationName**
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `HEALTHCARE_PROFESSIONAL`.
     *
     */
    officeName?: string;
    /**
     * The gender of the healthcare professional
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `HEALTHCARE_PROFESSIONAL`.
     *
     */
    gender?: 'FEMALE' | 'F' | 'MALE' | 'M' | 'UNSPECIFIED';
    /**
     * The National Provider Identifier (NPI) of the healthcare provider
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `HEALTHCARE_PROFESSIONAL` or `HEALTHCARE_FACILITY`.
     *
     */
    npi?: string;
    /**
     * Must be a valid address
     *
     * Cannot be a P.O. Box
     *
     */
    address?: string;
    /**
     * Cannot be a P.O. Box
     */
    address2?: string;
    /**
     * If true, do not show street address on listings. Defaults to false.
     */
    suppressAddress?: boolean;
    /**
     * Provides additional information to help consumers get to the location. This string appears along with the location's address (e.g., In Menlo Mall, 3rd Floor).
     *
     * It may also be used in conjunction with a hidden address (i.e., when **suppressAddress** is true) to give consumers information about where the location is found (e.g., Servicing the New York area).
     *
     * Cannot be a P.O. Box
     *
     */
    displayAddress?: string;
    city?: string;
    /**
     * For US locations, the two-character code of the location’s state, or DC for the District of Columbia
     * For non-US locations, the name of the location’s province / region / state
     *
     */
    state?: string;
    /**
     * The name of the location's sublocality.
     */
    sublocality?: string;
    /**
     * The location's postal code. For US locations, this field contains the five- or nine-digit ZIP code (the hyphen is optional).
     */
    zip?: string;
    /**
     * The two-character ISO 3166-1 code of the location's country or region. If omitted, US is used.
     */
    countryCode?: string;
    /**
     * Area that is served by this location. It may be specified as a radius from the location's address or as a list of cities and/or postal codes.
     *
     * **Only for Google Business Profile:** Currently, **serviceArea** is only supported by Google Business Profile and will not affect your listings on other sites.
     *
     */
    serviceArea?: {
        /**
         * The distance around the location that the business serves
         *
         * **NOTE:** This field is no longer supported by Google Business Profile and is deprecated. We no longer accept or store values for **`radius`**.
         *
         */
        radius?: number;
        /**
         * The unit in which radius is measured.
         *
         * **NOTE:** This field is no longer supported by Google Business Profile and is deprecated. We no longer accept or store values for **`units`**.
         *
         */
        unit?: string;
        /**
         * A list of places served by the location, where each place is either:
         * * a postal code, or
         * * the name of a city.
         *
         */
        places?: Array<string>;
    };
    /**
     * Must be a valid phone number.
     */
    phone?: string;
    /**
     * Set to true if the number listed in **phone** is a tracked phone number.
     *
     * **NOTE:** When updating **isPhoneTracked**, you must provide a value for **phone** in the same request.
     *
     */
    isPhoneTracked?: boolean;
    /**
     * Must be a valid, non-toll-free phone number.
     *
     * Required if:
     * * **isPhoneTracked** is true and the non-tracked number is a toll-free number, **OR**
     * * **isPhoneTracked** is false and **phone** is a toll-free number
     *
     */
    localPhone?: string;
    /**
     * Must be a valid phone number, based on the country specified in `countryCode`. Phone numbers for US locations must contain 10 digits.
     */
    alternatePhone?: string;
    /**
     * Must be a valid phone number, based on the country specified in `countryCode`. Phone numbers for US locations must contain 10 digits.
     */
    faxPhone?: string;
    /**
     * Must be a valid phone number, based on the country specified in `countryCode`. Phone numbers for US locations must contain 10 digits.
     */
    mobilePhone?: string;
    /**
     * Must be a valid phone number, based on the country specified in `countryCode`. Phone numbers for US locations must contain 10 digits.
     */
    tollFreePhone?: string;
    /**
     * Must be a valid phone number, based on the country specified in `countryCode`. Phone numbers for US locations must contain 10 digits.
     */
    ttyPhone?: string;
    /**
     * Yext Category IDs. A Location must have at least one and at most 10 Categories.
     *
     * IDs must be valid and selectable (i.e., cannot be parent categories).
     *
     * **NOTE:** The list of category IDs that you send us must be comprehensive. For example, if you send us a list of IDs that does not include IDs that you sent in your last update, Yext considers the missing categories to be deleted, and we remove them from your listings.
     *
     */
    categoryIds?: Array<string>;
    /**
     * The Featured Message. Default: Call today!
     *
     * Cannot include:
     * * inappropriate language
     * * HTML markup
     * * a URL or domain name
     * * a phone number
     * * control characters ([\x00-\x1F\x7F])
     * * insufficient spacing
     *
     * If you submit a Featured Message that contains profanity or more than 50 characters, it will be ignored. The success response will contain a warning message explaining why your Featured Message wasn't stored in the system.
     *
     */
    featuredMessage?: string;
    /**
     * Valid URL to which the Featured Message is linked
     */
    featuredMessageUrl?: string;
    /**
     * The URL of the location's website. This URL will be shown on your listings unless you specify a value for `displayWebsiteUrl`.
     *
     * Must be a valid URL and is required whenever `displayWebsiteUrl` is specified.
     *
     */
    websiteUrl?: string;
    /**
     * The URL that is shown on your listings in place of `websiteUrl`. You can use `displayWebsiteUrl` to display a short, memorable web address that redirects consumers to the URL given in `websiteUrl`.
     *
     * Must be a valid URL and be specified along with `websiteUrl`.
     *
     */
    displayWebsiteUrl?: string;
    /**
     * A valid URL used for reservations at this location.
     */
    reservationUrl?: string;
    /**
     * The URL that is shown on your listings in place of `reservationUrl`. You can use `displayReservationUrl` to display a short, memorable web address that redirects consumers to the URL given in `reservationUrl`.
     *
     * Must be a valid URL and be specified along with `reservationUrl`.
     *
     */
    displayReservationUrl?: string;
    /**
     * The URL of the location's menu.
     */
    menuUrl?: string;
    /**
     * The URL that is shown on your listings in place of `menuUrl`. You can use `displayMenuUrl` to display a short, memorable web address that redirects consumers to the URL given in `menuUrl`.
     *
     * Must be a valid URL and be specified along with `menuUrl`.
     *
     */
    displayMenuUrl?: string;
    /**
     * The URL used to place orders that will be fulfilled at the location.
     */
    orderUrl?: string;
    /**
     * The URL that is shown on your listings in place of `orderUrl`. You can use `displayOrderUrl` to display a short, memorable web address that redirects consumers to the URL given in `orderUrl`.
     *
     * Must be a valid URL and be specified along with `orderUrl`.
     *
     */
    displayOrderUrl?: string;
    /**
     * Hours should be submitted as a comma-separated list of days, where each day's hours are specified as follows:
     *
     * d:oh:om:ch:cm
     * * d = day of the week –
     * * 1 – Sunday
     * * 2 – Monday
     * * 3 – Tuesday
     * * 4 – Wednesday
     * * 5 – Thursday
     * * 6 – Friday
     * * 7 – Saturday
     * * oh:om = opening time in 24-hour format
     * * ch:cm = closing time in 24-hour format
     *
     * Times with single-digit hours (e.g., 9 AM) can be submitted with or without a leading zero (9:00 or 09:00).
     *
     * **Example:** open 9 AM to 5 PM Monday and Tuesday, open 10 AM to 4 PM on Saturday – 2:9:00:17:00,3:9:00:17:00,7:10:00:16:00
     *
     * SPECIAL CASES:
     * * To indicate that a location is open 24 hours on a specific day, set 00:00 as both the opening and closing time for that day.
     * * **Example:** open all day on Saturdays – 7:00:00:00:00
     * * To indicate that a location is closed on a specific day, omit that day from the list or set it as closed ("closed" is not case sensitive).
     * * **Example:** closed on Sundays – 1:closed
     * * **NOTE:** If a location is closed seven days a week, set at least one day to closed. Otherwise, **hours** is an empty string, and we assume you are not submitting hours information for that location.
     * * To indicate that a location has split hours on a specific day, submit a set of hours for each block of time the location is open.
     * * **Example:** open from 9:00 AM to 12:00 PM and again from 1:00 PM to 5:00 PM on Mondays – 2:9:00:12:00,2:13:00:17:00
     *
     * **NOTE:** To set hours for specific days of the year rather than days of the week, use **holidayHours**.
     *
     */
    hours?: string;
    /**
     * Additional information about business hours that does not fit in **hours** (e.g., Closed during the winter)
     */
    additionalHoursText?: string;
    /**
     * Holiday hours for this location.
     *
     * **NOTE:** hours must be set in order for holidayHours to appear on your listings)
     *
     */
    holidayHours?: Array<{
        /**
         * Special opening and closing times in 24-hour format (OH:OM:CH:CM, where OH:OM is the opening time and CH:CM is the closing time).
         *
         * Times with single-digit hours (e.g., 9 AM) can be submitted with or without a leading zero (9:00 or 09:00).
         *
         * Examples:
         * * 9:00:15:00 — Opening at 9:00 AM, closing at 3:00 PM
         * * "" (empty string) — Closed all day
         * * 0:00:0:00 or 0:00:23:59 — Open 24 hours
         * * 9:00:15:00,17:00:19:00 — Split hours: open from 9:00 AM to 3:00 PM and again from 5:00 PM to 7:00 PM
         *
         * **NOTE:** If **isRegularHours** is set to true, we will ignore this field.
         *
         */
        hours?: string;
        /**
         * The date on which the holiday hours will be in effect
         */
        date?: string;
        /**
         * Indicates whether the holiday hours are the same as the regular business hours for the given date. If set to true, we will update the holiday hours if the regular business hours change for the date's day of the week.
         */
        isRegularHours?: boolean;
    }>;
    description?: string;
    /**
     * A list of the conditions treated by the healthcare provider
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `HEALTHCARE_PROFESSIONAL` or `HEALTHCARE_FACILITY`.
     *
     */
    conditionsTreated?: Array<string>;
    /**
     * A list of the certifications held by the healthcare professional
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `HEALTHCARE_PROFESSIONAL`.
     *
     */
    certifications?: Array<string>;
    /**
     * A list of the types of education and training completed by the healthcare professional
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `HEALTHCARE_PROFESSIONAL`.
     *
     */
    educationList?: Array<{
        /**
         * The kind of education or training completed
         */
        type?: 'FELLOWSHIP' | 'INTERNSHIP' | 'MEDICAL_SCHOOL' | 'RESIDENCY';
        /**
         * The name of the institution where the healthcare professional received the education or training
         */
        institutionName?: string;
        /**
         * The year the healthcare professional completed the education or training
         */
        yearCompleted?: string;
    }>;
    /**
     * A list of the degrees earned by the healthcare professional
     *
     * **NOTE:**  This field is only available to locations whose **`locationType`** is `HEALTHCARE_PROFESSIONAL`.
     *
     * Valid values:
     *
     * * `ANP` (Adult Nurse Practitioner)
     * * `APN` (Advanced Practice Nurse)
     * * `APRN` (Advanced Practice Registered Nurse)
     * * `ARNP` (Advanced Registered Nurse Practitioner)
     * * `CNM` (Certified Nurse Midwife)
     * * `CNP` (Certified Nurse Practitioner)
     * * `CNS` (Clinical Nurse Specialist)
     * * `CPNP` (Certified Pediatric Nurse Practitioner)
     * * `CRNA` (Certified Registered Nurse Anesthetist)
     * * `CRNP` (Certified Registered Nurse Practitioner)
     * * `DC` (Doctor of Chiropractic)
     * * `DDS` (Doctor of Dental Surgery)
     * * `DMD` (Doctor of Dental Medicine)
     * * `DO` (Doctor of Osteopathy)
     * * `DPM` (Doctor of Podiatric Medicine)
     * * `DVM` (Doctor of Veterinary Medicine)
     * * `FNP` (Family Nurse Practitioner)
     * * `GNP` (Geriatric Nurse Practitioner)
     * * `LAC` (Licensed Acupuncturist)
     * * `LPN` (Licensed Practical Nurse)
     * * `MD` (Medical Doctor)
     * * `ND` (Naturopathic Doctor)
     * * `NP` (Nurse Practitioner)
     * * `OD` (Doctor of Optometry)
     * * `PA` (Physician Assistant)
     * * `PAC` (Physician Assistant Certified)
     * * `PHARMD` (Doctor of Pharmacy)
     * * `PHD` (Doctor of Philosophy)
     * * `PNP` (Pediatric Nurse Practitioner)
     * * `VMD` (Veterinary Medical Doctor)
     * * `WHNP` (Womens Health Nurse Practitioner)
     *
     */
    degrees?: Array<string>;
    /**
     * A list of hospitals where the healthcare professional admits patients
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `HEALTHCARE_PROFESSIONAL`.
     *
     */
    admittingHospitals?: Array<string>;
    /**
     * Indicates whether the healthcare provider is accepting new patients
     *
     * Default is true
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `HEALTHCARE_PROFESSIONAL` or `HEALTHCARE_FACILITY`.
     *
     */
    acceptingNewPatients?: boolean;
    /**
     * A set of field-value pairs indicating whether the location is closed and, if it is closed, the date of its closing.
     *
     * **NOTE:** This field does not appear in the GET response unless it has been explicitly set in a PUT request.
     *
     */
    closed?: {
        /**
         * Indicates whether the location is closed
         */
        isClosed?: boolean;
    };
    /**
     * The payment methods accepted at this location
     *
     * Valid elements depend on the location's country. For US locations, valid elements are:
     * * AMERICANEXPRESS
     * * CASH
     * * CHECK
     * * DINERSCLUB
     * * DISCOVER
     * * FINANCING
     * * INVOICE
     * * MASTERCARD
     * * TRAVELERSCHECK
     * * VISA
     * * ANDROIDPAY
     * * APPLEPAY
     * * SAMSUNGPAY
     * * BITCOIN
     * * PAYPAL
     *
     */
    paymentOptions?: Array<string>;
    /**
     * A list of insurance policies accepted by the healthcare provider
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `HEALTHCARE_PROFESSIONAL`.
     *
     */
    insuranceAccepted?: Array<string>;
    logo?: PhotoWritable;
    /**
     * Up to 50 Photos.
     *
     * **NOTE:** The list of photos that you send us must be comprehensive. For example, if you send us a list of photos that does not include photos that you sent in your last update, Yext considers the missing photos to be deleted, and we remove them from your listings.
     *
     */
    photos?: Array<PhotoWritable>;
    /**
     * A portrait of the healthcare professional
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `HEALTHCARE_PROFESSIONAL`.
     *
     */
    headshot?: PhotoWritable;
    /**
     * Valid YouTube URLs for embedding a video on some publisher sites.
     *
     * **NOTE:** Currently, only the first URL in the Array appears in your listings.
     *
     */
    videoUrls?: Array<string>;
    /**
     * Valid Instagram username for the location (e.g., NewCityFiat (without the leading "@"))
     */
    instagramHandle?: string;
    /**
     * Valid Twitter handle for the location (e.g., JohnSmith (without the leading '@')).
     * If you submit an invalid Twitter handle, it will be ignored. The success response will contain a warning message explaining why your Twitter handle wasn't stored in the system.
     */
    twitterHandle?: string;
    /**
     * The URL you would like to submit to Google Business Profile in place of the one given in **websiteUrl** (if applicable).
     *
     * For example, if you want to analyze the traffic driven by your Google listings separately from other traffic, enter the alternate URL that you will use for tracking in this field.
     *
     */
    googleWebsiteOverride?: string;
    /**
     * The cover photo for your business's Google profile
     *
     * NOTE: Your cover photo must meet all of the following requirements:
     * * have a 16:9 aspect ratio
     * * be at least 480 x 270 pixels
     * * be no more than 2120 x 1192 pixels
     *
     */
    googleCoverPhoto?: PhotoWritable;
    /**
     * The profile photo for your business's Google profile
     *
     * **NOTE:** Your profile picture must meet all of the following requirements:
     * * be a square
     * * be at least 250 x 250 pixels
     *
     */
    googleProfilePhoto?: PhotoWritable;
    /**
     * The Google Business Profile attributes for this location.
     *
     */
    googleAttributes?: Array<{
        /**
         * The unique ID Of the Google Business Profile keyword
         *
         * Keywords are determined by the location's primary category (e.g., `has_drive_through`, `has_fitting_room`, `kitchen_in_room`).
         *
         */
        id?: string;
        /**
         * The unique IDs of any options selected for the keyword.
         *
         * Keyword options provide more details on how the keyword applies to the location (e.g., if **`id`** is `has_drive_through`, **`optionIds`** may be `true` or `false`).
         *
         */
        optionIds?: Array<string>;
    }>;
    /**
     * URL for the location's Facebook Page.
     *
     * Valid formats:
     * * facebook.com/profile.php?id=[numId]
     * * facebook.com/group.php?gid=[numId]
     * * facebook.com/groups/[numId]
     * * facebook.com/[Name]
     * * facebook.com/pages/[Name]/[numId]
     *
     * where [Name] is a String and [numId] is an Integer
     *
     * If you submit a URL that is not in one of the valid formats, it will be ignored. The success response will contain a warning message explaining why the URL wasn't stored in the system.
     *
     * **NOTE:** This value is automatically set to the location's Facebook Page URL. You can only manually set **facebookPageUrl** if the location meets one of the following criteria:
     * * It is not subscribed to a Listings package that contains Facebook.
     * * It is opted out of Facebook.
     *
     */
    facebookPageUrl?: string;
    /**
     * Designates the Facebook Call-to-Action button text and value
     */
    facebookCallToAction?: {
        /**
         * The action the consumer is being prompted to take by the button's text
         */
        type?: 'NONE' | 'BOOK_NOW' | 'CALL_NOW' | 'CONTACT_US' | 'SEND_MESSAGE' | 'USE_APP' | 'PLAY_GAME' | 'SHOP_NOW' | 'SIGN_UP' | 'WATCH_VIDEO' | 'SEND_EMAIL' | 'LEARN_MORE' | 'PURCHASE_GIFT_CARDS' | 'ORDER_NOW' | 'FOLLOW_PAGE';
        /**
         * Indicates where consumers will be directed to upon clicking the Call-to-Action button (e.g., a URL). It can be a free-form string or an embedded value, depending on what the user specifies.
         *
         * For example, if the user sets the Facebook Call-to-Action as " 'Sign Up' using 'Website URL' " in the Yext platform, **`type`** will be `SIGN_UP` and **`value`** will be `[[websiteUrl]]`.
         *
         * The Call-to-Action will have the same behavior if the user sets the value to "Custom Value" in the platform and embeds a field.
         */
        value?: string;
    };
    /**
     * The cover photo for your business's Facebook profile
     *
     * Displayed as a 851 x 315 pixel image
     *
     * You must have a cover photo in order for your listing to appear on Facebook.
     *
     * **NOTE:** Your cover photo must be at least 400 pixels wide.
     *
     */
    facebookCoverPhoto?: PhotoWritable;
    /**
     * The profile picture for your business's Facebook profile
     *
     * You must have a profile picture in order for your listing to appear on Facebook.
     *
     * **NOTE:** Your profile picture must be larger than 180 x 180 pixels.
     *
     */
    facebookProfilePicture?: PhotoWritable;
    /**
     * Indicates whether the embedded Uber link for this location appears as text or a button
     *
     * When consumers click on this link on a mobile device, the Uber app (if installed) will open with your location set as the trip destination. If the Uber app is not installed, the consumer will be prompted to download it.
     *
     */
    uberLinkType?: 'LINK' | 'BUTTON';
    /**
     * The text of the embedded Uber link
     *
     * Default is "Ride there with Uber".
     *
     * **NOTE:** This field is only available if **uberLinkType** is LINK.
     *
     */
    uberLinkText?: string;
    /**
     * The text of the call-to-action that will appear in the Uber app during a trip to your location (e.g., Check out our menu!)
     *
     * **NOTE:** If a value for **uberTripBrandingText** is provided, values must also be provided for **uberTripBrandingUrl** and **uberTripBrandingDescription**.
     *
     */
    uberTripBrandingText?: string;
    /**
     * The URL that the consumer will be redirected to when tapping on the call-to-action in the Uber app during a trip to your location.
     *
     * **NOTE:** If a value for **uberTripBrandingUrl** is provided, values must also be provided for **uberTripBrandingText** and **uberTripBrandingDescription**.
     *
     */
    uberTripBrandingUrl?: string;
    /**
     * A longer description that will appear near the call-to-action in the Uber app during a trip to your location.
     *
     * **NOTE:** If a value for **uberTripBrandingDescription** is provided, values must also be provided for **uberTripBrandingText** and **uberTripBrandingUrl**.
     *
     */
    uberTripBrandingDescription?: string;
    /**
     * The year that this location was opened, not the number of years it was open
     *
     * Minimum of 1000, maximum of current year + 10.
     *
     */
    yearEstablished?: string;
    /**
     * Latitude where the map pin should be displayed, as provided by you
     *
     * Between -90.0 and 90.0, inclusive
     *
     */
    displayLat?: number;
    /**
     * Longitude where the map pin should be displayed, as provided by you
     *
     * Between -180.0 and 180.0, inclusive
     *
     */
    displayLng?: number;
    /**
     * Latitude to use for driving directions to the location, as provided by you
     *
     * Between -90.0 and 90.0, inclusive
     *
     */
    routableLat?: number;
    /**
     * Longitude to use for driving directions to the location, as provided by you
     *
     * Between -180.0 and 180.0, inclusive
     *
     */
    routableLng?: number;
    /**
     * Latitude to use for walking directions to the location, as provided by you
     *
     * Between -90.0 and 90.0, inclusive
     *
     */
    walkableLat?: number;
    /**
     * Longitude to use for walking directions to the location, as provided by you
     *
     * Between -180.0 and 180.0, inclusive
     *
     */
    walkableLng?: number;
    /**
     * Latitude to use for pickup spot for the location, as provided by you
     *
     * Between -90.0 and 90.0, inclusive
     *
     */
    pickupLat?: number;
    /**
     * Longitude to use for pickup spot for the location, as provided by you
     *
     * Between -180.0 and 180.0, inclusive
     *
     */
    pickupLng?: number;
    /**
     * Latitude to use for drop off spot for the location, as provided by you
     *
     * Between -90.0 and 90.0, inclusive
     *
     */
    dropoffLat?: number;
    /**
     * Longitude to use for drop off spot for the location, as provided by you
     *
     * Between -180.0 and 180.0, inclusive
     *
     */
    dropoffLng?: number;
    /**
     * Up to five emails addresses for reaching this location
     *
     * Must be valid email addresses
     *
     */
    emails?: Array<string>;
    /**
     * Up to 100 specialities (e.g., for food and dining: Chicago style)
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     */
    specialities?: Array<string>;
    /**
     * Up to 100 association memberships relevant to the location (e.g., New York Doctors Association)
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     */
    associations?: Array<string>;
    /**
     * Up to 100 products sold at this location
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     */
    products?: Array<string>;
    /**
     * Up to 100 services offered at this location
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     */
    services?: Array<string>;
    /**
     * Up to 100 brands sold by this location
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     */
    brands?: Array<string>;
    /**
     * Language code of the language in which this location's information is provided. This language is considered the Location's primary language in our system.
     *
     *
     * If you would like to provide your Location data in more than one language, you can create a Language Profile for each of these additional (alternate) languages.
     *
     */
    language?: string;
    /**
     * Up to 100 languages spoken at this location.
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     */
    languages?: Array<string>;
    /**
     * Up to 100 keywords may be provided
     *
     * All strings must be non-empty when trimmed of whitespace.
     *
     */
    keywords?: Array<string>;
    /**
     * Label to be used for this location’s Menus. This label will appear on your location's listings.
     */
    menusLabel?: string;
    /**
     * IDs of Menus associated with this location.
     */
    menuIds?: Array<string>;
    /**
     * Label to be used for this location’s Bio lists. This label will appear on your location's listings.
     */
    bioListsLabel?: string;
    /**
     * IDs of Bio lists associated with this location.
     */
    bioListIds?: Array<string>;
    /**
     * Label to be used for this location’s Product & Services lists. This label will appear on your location's listings.
     */
    productListsLabel?: string;
    /**
     * IDs of Product lists associated with this location.
     */
    productListIds?: Array<string>;
    /**
     * Label to be used for this location’s Event lists. This label will appear on your location's listings.
     */
    eventListsLabel?: string;
    /**
     * IDs of Event lists associated with this location.
     */
    eventListIds?: Array<string>;
    /**
     * The folder that this location is in. Must be a valid, existing Yext Folder ID
     */
    folderId?: string;
    /**
     * The IDs of the location labels that have been added to this location. Location labels help you identify locations that share a certain characteristic; they do not appear on your location's listings.
     *
     * **NOTE:** You can only add labels that have already been created via our web interface. Currently, it is not possible to create new labels via the API.
     *
     * In Locations: Update requests:
     * * If the **`v`** parameter is before `20180223`: setting the value of **`labelIds`** to an empty array has no effect on the current value
     * * If the **`v`** parameter is `20180223` or after: setting the value of **`labelIds`** to an empty array deletes the current value
     *
     */
    labelIds?: Array<string>;
    /**
     * Indicates whether Intelligent Search Tracker is enabled.
     *
     * The Intelligent Search Tracker allows you to understand your performance in local search.
     *
     */
    intelligentSearchTrackingEnabled?: boolean;
    /**
     * How often we send search queries to track your search performance.
     *
     */
    intelligentSearchTrackingFrequency?: 'WEEKLY' | 'MONTHLY' | 'QUARTERLY';
    /**
     * Keywords that we will use to track your search performance. These keywords are based on the location information you've stored in our system.
     *
     */
    locationKeywords?: Array<'NAME' | 'PRIMARY_CATEGORY'>;
    /**
     * Additional keywords you would like us to use when tracking your search performance
     *
     */
    customKeywords?: Array<string>;
    /**
     * The ways in which your keywords will be arranged in the search queries we use to track your performance
     *
     */
    queryTemplates?: Array<'KEYWORD' | 'KEYWORD_ZIP' | 'KEYWORD_CITY' | 'KEYWORD_IN_CITY' | 'KEYWORD_NEAR_ME' | 'KEYWORD_CITY_STATE'>;
    /**
     * Other names for your business that you would like us to use when tracking your search performance
     *
     */
    alternateNames?: Array<string>;
    /**
     * Other websites for your business that we should look for when tracking your search performance
     *
     */
    alternateWebsites?: Array<string>;
    /**
     * The names and websites of the competitors whose search performance you would like to compare to your own
     *
     */
    competitors?: Array<{
        /**
         * The competitor's name
         */
        name?: string;
        /**
         * The competitor's website.
         */
        website?: string;
    }>;
    /**
     * The search engines that we will use to track your performance
     *
     */
    trackingSites?: Array<'GOOGLE_DESKTOP' | 'GOOGLE_MOBILE' | 'BING_DESKTOP' | 'BING_MOBILE' | 'YAHOO_DESKTOP' | 'YAHOO_MOBILE'>;
    /**
     * Indicates whether the location is the primary location in its group
     *
     */
    isClusterPrimary?: boolean;
    /**
     * The formality of clothing typically worn at this location
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `RESTAURANT`.
     *
     */
    attire?: 'UNSPECIFIED' | 'DRESSY' | 'CASUAL' | 'FORMAL';
    /**
     * The typical price of products sold at this location, on a scale of 1 (low) to 4 (high)
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `RESTAURANT`.
     *
     */
    priceRange?: 'UNSPECIFIED' | 'ONE' | 'TWO' | 'THREE' | 'FOUR';
    /**
     * Types of meals served at this location
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `RESTAURANT`.
     *
     * Valid values:
     *
     * * `BREAKFAST`
     * * `LUNCH`
     * * `BRUNCH`
     * * `HAPPY_HOUR`
     * * `LATE_NIGHT`
     *
     */
    mealsServed?: Array<string>;
    /**
     * For ATMs, the external ID of the location that the ATM is installed in. The location must be in the same business account as the ATM.
     *
     * **NOTE:** This field is only available to locations whose **`locationType`** is `ATM`.
     *
     */
    locatedIn?: string;
    /**
     * ID of the user who is the primary Knowledge Assistant contact for the entity
     *
     */
    primaryContact?: string;
    /**
     * Indicates whether or not review response conversations are enabled for the Yext Knowledge Assistant
     *
     */
    reviewResponseConversationEnabled?: boolean;
    /**
     * Indicates whether or not holiday hour confirmation alerts are enabled for the Yext Knowledge Assistant
     *
     */
    holidayHoursConfirmationEnabled?: boolean;
};

/**
 * Location Add Request Response
 */
export type AddRequestWritable = {
    /**
     * Whether the request is to add service for an existing location or to create a new location and add service to it.
     *
     */
    locationMode?: 'EXISTING' | 'NEW';
    /**
     * The existing location ID to add service to.
     *
     * This field is provided only when *locationMode* is EXISTING. Either this field or *newLocationId* will be provided.
     *
     */
    existingLocationId?: string;
    /**
     * The new location ID to create and add service to.
     *
     * This field is provided only when *locationMode* is NEW. Either this field or *existingLocationId* will be provided.
     *
     */
    newLocationId?: string;
    /**
     * *(Partner Portal mode)* The account ID to create a new location under.
     *
     * This field is provided only for Partner Portal and when *locationMode* is NEW.
     *
     */
    newLocationAccountId?: string;
    /**
     * *(Partner Portal mode)* The account name to create a new location under.
     *
     * This field is provided only for Partner Portal and when *locationMode* is NEW and when *newLocationAccountName* was specified in the add request.
     *
     */
    newLocationAccountName?: string;
    /**
     * *(Partner Portal mode)* The account ID to create a new account and location under.
     *
     * This field is provided only for Partner Portal, when *locationMode* is NEW and *newAccountParentAccountId* was specified in the add request.
     *
     */
    newAccountParentAccountId?: string;
    /**
     * The location profile data used in creating a new location for this add request.
     *
     * Optional. Provided only for the **Add Requests: Create (New Location)** endpoint.
     *
     */
    newLocationData?: LocationWritable;
    /**
     * The entity profile data used in creating a new entity for this add request.
     *
     * Optional. Provided only for the **Add Requests: Create (New Location)** endpoint.
     *
     */
    newEntityData?: Entity;
    /**
     * List of SKUs to sign the location up for, from among those listed in the **Available Services: List** response with Location recipientType.
     *
     */
    skus?: Array<string>;
    /**
     * The Agreement ID of the agreement that services will be added under.
     *
     */
    agreementId?: number;
};

/**
 * Sub-Account Add Request Response
 */
export type SubAccountAddRequestWritable = {
    /**
     * The existing sub-account ID to add service to.
     *
     */
    subAccountId?: string;
    /**
     * List of objects representing the SKUs to be added (or increased in quantity) to the sub-account, from among those listed in the **Available Services: List** response with Sub-Account recipientType. If the sub-account already has an active service for a SKU, the service quantity will be increased. The added quantity will default to 1 for any SKU without a specified quantity.
     *
     */
    skuAdditions?: Array<SkuQuantity>;
    /**
     * The Agreement ID of the agreement that services will be added under.
     *
     */
    agreementId?: number;
};

/**
 * Resources Apply Request Response
 */
export type ResourcesApplyRequestResponseWritable = {
    /**
     * ID for the account which the resources were applied to.
     *
     */
    targetAccountId?: string;
    source?: Source;
};

export type AccountWritable = {
    accountId?: string;
    /**
     * Customer-provided ID of the account that this is a sub-account of, if any. Not provided if this is a top-level account.
     */
    parentAccountId?: string;
    /**
     * The name of this account.
     */
    accountName?: string;
    /**
     * First name of the contact user for this account.
     */
    contactFirstName?: string;
    /**
     * Last name of the contact user for this account.
     */
    contactLastName?: string;
    /**
     * Phone number of the contact user for this account.
     */
    contactPhone?: string;
    /**
     * Email address of the contact user for this account.
     */
    contactEmail?: string;
};

export type NewLocationAddRequestWritable2 = {
    /**
     * Your ID for the location to be added.
     */
    newLocationId: string;
    /**
     * *(Partner Portal mode)* Supply your ID for the account that the new location should be created in. If this account is your main account or an existing sub-account, the location will be placed there. Otherwise, a new account will be created.
     *
     */
    newLocationAccountId?: string;
    /**
     * *(Partner Portal mode)* If a new account is created, this field's value will set the account's name. If a new account is created, and a value for this field is not provided, the new account's name will default to the location's name.
     *
     */
    newLocationAccountName?: string;
    /**
     * *(Partner Portal mode, advanced use only)* If you have a multi-layer account structure and want the new account created for this request to be under one of your sub-accounts, rather than directly under your main account, specify that sub-account's ID in this field.
     *
     */
    newAccountParentAccountId?: string;
    /**
     * If you set `newLocationId`, you must specify either `newLocationData` or `newEntityData`.
     *
     * This field allows you to set the profile data for a location, using our legacy format.
     * Profile content is specified in the same format as when creating a location with the Locations: Create endpoint.
     *
     * If the Add Request is processed successfully, this data will be used to create a new location in your account.
     *
     */
    newLocationData: Location;
    /**
     * If you set `newEntityData`, this field specifies the type of entity to be created.
     *
     */
    newEntityType?: 'atm' | 'healthcareFacility' | 'healthcareProfessional' | 'hotel' | 'location' | 'restaurant';
    /**
     * If you set `newLocationId`, you must specify either `newLocationData` or `newEntityData`.
     *
     * This field allows you to set the profile data for an entity.
     * Profile content is specified in the same format as when creating an entity with the Entities: Create endpoint.
     * You can only create entities of a type specified by `newEntityType`.
     *
     * If the Add Request is processed successfully, this data will be used to create a new entity in your account.
     *
     * **The dropdown will specify the profile content for `newEntityData`. Specify the entity type with `newEntityType` described above.**
     *
     */
    newEntityData: unknown;
    /**
     * List of SKUs that you would like to sign the location up for, from among those listed in the **Available Services: List** response with Location recipientType.
     *
     */
    skus: Array<string>;
    /**
     * *(Advanced field)* The Agreement ID of the agreement that services will be added under. This value is set automatically by Yext when you create the Add request. (You can specify it yourself, but should not do so unless you have intentionally set up multiple active agreements with Yext, since it could cause your integration to break when you renew or upgrade your agreement.)
     *
     */
    agreementId?: number;
    /**
     * *(Sandbox API only)* Forces this Add request into the `REVIEW` status if it otherwise would have completed successfully.
     *
     */
    forceReview?: boolean;
};

export type AccountId = string;

/**
 * A date in `YYYYMMDD` format.
 */
export type V = string;

/**
 *
 * Number of results to skip. Used to page through results.
 * Cannot be used together with **`pageToken`**.
 *
 */
export type Offset = number;

export type ListAvailableServicesData = {
    body?: never;
    path: {
        accountId: string;
    };
    query?: {
        /**
         * A date in `YYYYMMDD` format.
         */
        v?: string;
    };
    url: '/accounts/{accountId}/availableservices';
};

export type ListAvailableServicesErrors = {
    /**
     * ErrorResponse
     *
     * Error Response
     */
    default: {
        meta?: ResponseMetaWithError;
        response?: {
            [key: string]: unknown;
        };
    };
};

export type ListAvailableServicesError = ListAvailableServicesErrors[keyof ListAvailableServicesErrors];

export type ListAvailableServicesResponses = {
    /**
     * AvailableServiceResponse
     *
     * Available Service Response
     */
    200: {
        meta?: ResponseMeta;
        response?: {
            availableServices?: Array<AvailableService>;
            count?: number;
        };
    };
};

export type ListAvailableServicesResponse = ListAvailableServicesResponses[keyof ListAvailableServicesResponses];

export type CreateNewLocationAddRequestData = {
    body: NewLocationAddRequestWritable;
    path: {
        accountId: string;
    };
    query?: {
        /**
         * A date in `YYYYMMDD` format.
         */
        v?: string;
    };
    url: '/accounts/{accountId}/newlocationaddrequests';
};

export type CreateNewLocationAddRequestErrors = {
    /**
     * ErrorResponse
     *
     * Error Response
     */
    default: {
        meta?: ResponseMetaWithError;
        response?: {
            [key: string]: unknown;
        };
    };
};

export type CreateNewLocationAddRequestError = CreateNewLocationAddRequestErrors[keyof CreateNewLocationAddRequestErrors];

export type CreateNewLocationAddRequestResponses = {
    /**
     * AddRequestResponse
     *
     * Location Add Request Response
     */
    200: {
        meta?: ResponseMeta;
        response?: AddRequest;
    };
};

export type CreateNewLocationAddRequestResponse = CreateNewLocationAddRequestResponses[keyof CreateNewLocationAddRequestResponses];

export type CreateExistingLocationAddRequestData = {
    body: ExistingLocationAddRequest;
    path: {
        accountId: string;
    };
    query?: {
        /**
         * A date in `YYYYMMDD` format.
         */
        v?: string;
    };
    url: '/accounts/{accountId}/existinglocationaddrequests';
};

export type CreateExistingLocationAddRequestErrors = {
    /**
     * ErrorResponse
     *
     * Error Response
     */
    default: {
        meta?: ResponseMetaWithError;
        response?: {
            [key: string]: unknown;
        };
    };
};

export type CreateExistingLocationAddRequestError = CreateExistingLocationAddRequestErrors[keyof CreateExistingLocationAddRequestErrors];

export type CreateExistingLocationAddRequestResponses = {
    /**
     * AddRequestResponse
     *
     * Location Add Request Response
     */
    200: {
        meta?: ResponseMeta;
        response?: AddRequest;
    };
};

export type CreateExistingLocationAddRequestResponse = CreateExistingLocationAddRequestResponses[keyof CreateExistingLocationAddRequestResponses];

export type CreateExistingSubAccountAddRequestData = {
    body: ExistingSubAccountAddRequest;
    path: {
        accountId: string;
    };
    query?: {
        /**
         * A date in `YYYYMMDD` format.
         */
        v?: string;
    };
    url: '/accounts/{accountId}/existingsubaccountaddrequests';
};

export type CreateExistingSubAccountAddRequestErrors = {
    /**
     * ErrorResponse
     *
     * Error Response
     */
    default: {
        meta?: ResponseMetaWithError;
        response?: {
            [key: string]: unknown;
        };
    };
};

export type CreateExistingSubAccountAddRequestError = CreateExistingSubAccountAddRequestErrors[keyof CreateExistingSubAccountAddRequestErrors];

export type CreateExistingSubAccountAddRequestResponses = {
    /**
     * SubAccountAddRequestResponse
     *
     * Sub-Account Add Request Response
     */
    200: {
        meta?: ResponseMeta;
        response?: SubAccountAddRequest;
    };
};

export type CreateExistingSubAccountAddRequestResponse = CreateExistingSubAccountAddRequestResponses[keyof CreateExistingSubAccountAddRequestResponses];

export type CancelServicesData = {
    body: CancelServicesRequest;
    path: {
        /**
         * The ID of the account.
         *
         * **NOTE:** If you would like to cancel location services under a sub-account, enter the ID of the *main account* rather than the sub-account.
         *
         */
        accountId: string;
    };
    query?: {
        /**
         * A date in `YYYYMMDD` format.
         */
        v?: string;
    };
    url: '/accounts/{accountId}/cancelservices';
};

export type CancelServicesErrors = {
    /**
     * ErrorResponse
     *
     * Error Response
     */
    default: {
        meta?: ResponseMetaWithError;
        response?: {
            [key: string]: unknown;
        };
    };
};

export type CancelServicesError = CancelServicesErrors[keyof CancelServicesErrors];

export type CancelServicesResponses = {
    /**
     * CancelServicesResponse
     *
     * Cancel Location Services Response
     */
    200: {
        meta?: ResponseMeta;
        response?: Array<Service>;
    };
};

export type CancelServicesResponse = CancelServicesResponses[keyof CancelServicesResponses];

export type CancelSubAccountServicesData = {
    body: CancelSubAccountServicesRequest;
    path: {
        /**
         * The ID of the account.
         *
         * **NOTE:** Enter the ID of the *main account* rather than the sub-account to cancel services provisioned on the sub-account.
         *
         */
        accountId: string;
    };
    query?: {
        /**
         * A date in `YYYYMMDD` format.
         */
        v?: string;
    };
    url: '/accounts/{accountId}/cancelsubaccountservices';
};

export type CancelSubAccountServicesErrors = {
    /**
     * ErrorResponse
     *
     * Error Response
     */
    default: {
        meta?: ResponseMetaWithError;
        response?: {
            [key: string]: unknown;
        };
    };
};

export type CancelSubAccountServicesError = CancelSubAccountServicesErrors[keyof CancelSubAccountServicesErrors];

export type CancelSubAccountServicesResponses = {
    /**
     * CancelSubAccountServicesResponse
     *
     * Cancel Sub-Account Services Response
     */
    200: {
        meta?: ResponseMeta;
        response?: Array<SubAccountService>;
    };
};

export type CancelSubAccountServicesResponse = CancelSubAccountServicesResponses[keyof CancelSubAccountServicesResponses];

export type CancelAllSubAccountServicesData = {
    body: CancelAllSubAccountServicesRequest;
    path: {
        /**
         * The ID of the account.
         *
         * **NOTE:** Enter the ID of the *main account* rather than the sub-account to cancel all of the services provisioned on the sub-account.
         *
         */
        accountId: string;
    };
    query?: {
        /**
         * A date in `YYYYMMDD` format.
         */
        v?: string;
    };
    url: '/accounts/{accountId}/cancelallsubaccountservices';
};

export type CancelAllSubAccountServicesErrors = {
    /**
     * ErrorResponse
     *
     * Error Response
     */
    default: {
        meta?: ResponseMetaWithError;
        response?: {
            [key: string]: unknown;
        };
    };
};

export type CancelAllSubAccountServicesError = CancelAllSubAccountServicesErrors[keyof CancelAllSubAccountServicesErrors];

export type CancelAllSubAccountServicesResponses = {
    /**
     * CancelAllSubAccountServicesResponse
     *
     * Cancel All Sub-Account Services Response
     */
    200: {
        meta?: ResponseMeta;
        response?: Array<SubAccountService>;
    };
};

export type CancelAllSubAccountServicesResponse = CancelAllSubAccountServicesResponses[keyof CancelAllSubAccountServicesResponses];

export type CreateProcessReviewAddRequestData = {
    body: ProcessReviewAddRequest;
    path: {
        accountId: string;
    };
    query?: {
        /**
         * A date in `YYYYMMDD` format.
         */
        v?: string;
    };
    url: '/accounts/{accountId}/processaddrequest';
};

export type CreateProcessReviewAddRequestErrors = {
    /**
     * ErrorResponse
     *
     * Error Response
     */
    default: {
        meta?: ResponseMetaWithError;
        response?: {
            [key: string]: unknown;
        };
    };
};

export type CreateProcessReviewAddRequestError = CreateProcessReviewAddRequestErrors[keyof CreateProcessReviewAddRequestErrors];

export type CreateProcessReviewAddRequestResponses = {
    /**
     * EmptyResponse
     *
     * Empty Response.
     */
    200: {
        meta?: ResponseMeta;
        response?: {
            [key: string]: unknown;
        };
    };
};

export type CreateProcessReviewAddRequestResponse = CreateProcessReviewAddRequestResponses[keyof CreateProcessReviewAddRequestResponses];

export type ListAddRequestsData = {
    body?: never;
    path: {
        accountId: string;
    };
    query?: {
        /**
         * A date in `YYYYMMDD` format.
         */
        v?: string;
        /**
         * Filters the list to add requests in a specific status.
         */
        status?: 'SUBMITTED' | 'PROCESSING' | 'COMPLETE' | 'CANCELED' | 'REVIEW' | 'FAILED';
        /**
         * (`YYYY-MM-DDThh:mm:ss` format)
         */
        submittedAfter?: string;
        /**
         * (`YYYY-MM-DDThh:mm:ss` format)
         */
        submittedBefore?: string;
        /**
         * (`YYYY-MM-DDThh:mm:ss` format)
         */
        updatedAfter?: string;
        /**
         * (`YYYY-MM-DDThh:mm:ss` format)
         */
        updatedBefore?: string;
        sku?: string;
        agreementId?: number;
        locationId?: string;
        limit?: number;
        /**
         *
         * Number of results to skip. Used to page through results.
         * Cannot be used together with **`pageToken`**.
         *
         */
        offset?: number;
    };
    url: '/accounts/{accountId}/addrequests';
};

export type ListAddRequestsErrors = {
    /**
     * ErrorResponse
     *
     * Error Response
     */
    default: {
        meta?: ResponseMetaWithError;
        response?: {
            [key: string]: unknown;
        };
    };
};

export type ListAddRequestsError = ListAddRequestsErrors[keyof ListAddRequestsErrors];

export type ListAddRequestsResponses = {
    /**
     * AddRequestsResponse
     *
     * Location Add Requests Response
     */
    200: {
        meta?: ResponseMeta;
        response?: {
            addRequests?: Array<AddRequest>;
            count?: number;
        };
    };
};

export type ListAddRequestsResponse = ListAddRequestsResponses[keyof ListAddRequestsResponses];

export type GetAddRequestData = {
    body?: never;
    path: {
        accountId: string;
        /**
         * addRequestId returned from a previous call to **Add Requests: Create (New Location)**, **Add Requests: Create (Existing Location)**, or retrieved from **Add Requests: List (Location)**
         *
         */
        addRequestId: number;
    };
    query?: {
        /**
         * A date in `YYYYMMDD` format.
         */
        v?: string;
    };
    url: '/accounts/{accountId}/addrequests/{addRequestId}';
};

export type GetAddRequestErrors = {
    /**
     * ErrorResponse
     *
     * Error Response
     */
    default: {
        meta?: ResponseMetaWithError;
        response?: {
            [key: string]: unknown;
        };
    };
};

export type GetAddRequestError = GetAddRequestErrors[keyof GetAddRequestErrors];

export type GetAddRequestResponses = {
    /**
     * AddRequestResponse
     *
     * Location Add Request Response
     */
    200: {
        meta?: ResponseMeta;
        response?: AddRequest;
    };
};

export type GetAddRequestResponse = GetAddRequestResponses[keyof GetAddRequestResponses];

export type ListSubAccountAddRequestsData = {
    body?: never;
    path: {
        accountId: string;
    };
    query?: {
        /**
         * A date in `YYYYMMDD` format.
         */
        v?: string;
        /**
         * Filters the list to add requests in a specific status.
         */
        status?: 'SUBMITTED' | 'PROCESSING' | 'COMPLETE' | 'CANCELED' | 'FAILED';
        /**
         * (`YYYY-MM-DDThh:mm:ss` format)
         */
        submittedAfter?: string;
        /**
         * (`YYYY-MM-DDThh:mm:ss` format)
         */
        submittedBefore?: string;
        /**
         * (`YYYY-MM-DDThh:mm:ss` format)
         */
        updatedAfter?: string;
        /**
         * (`YYYY-MM-DDThh:mm:ss` format)
         */
        updatedBefore?: string;
        sku?: string;
        agreementId?: number;
        subAccountId?: string;
        limit?: number;
        /**
         *
         * Number of results to skip. Used to page through results.
         * Cannot be used together with **`pageToken`**.
         *
         */
        offset?: number;
    };
    url: '/accounts/{accountId}/subaccountaddrequests';
};

export type ListSubAccountAddRequestsErrors = {
    /**
     * ErrorResponse
     *
     * Error Response
     */
    default: {
        meta?: ResponseMetaWithError;
        response?: {
            [key: string]: unknown;
        };
    };
};

export type ListSubAccountAddRequestsError = ListSubAccountAddRequestsErrors[keyof ListSubAccountAddRequestsErrors];

export type ListSubAccountAddRequestsResponses = {
    /**
     * SubAccountAddRequestsResponse
     *
     * Sub-Account Add Requests Response
     */
    200: {
        meta?: ResponseMeta;
        response?: {
            addRequests?: Array<SubAccountAddRequest>;
            count?: number;
        };
    };
};

export type ListSubAccountAddRequestsResponse = ListSubAccountAddRequestsResponses[keyof ListSubAccountAddRequestsResponses];

export type GetSubAccountAddRequestData = {
    body?: never;
    path: {
        accountId: string;
        /**
         * addRequestId returned from a previous call to **Add Requests: Create (Existing Account)** or retrieved from **Add Requests: List (Sub-Account)**
         *
         */
        addRequestId: number;
    };
    query?: {
        /**
         * A date in `YYYYMMDD` format.
         */
        v?: string;
    };
    url: '/accounts/{accountId}/subaccountaddrequests/{addRequestId}';
};

export type GetSubAccountAddRequestErrors = {
    /**
     * ErrorResponse
     *
     * Error Response
     */
    default: {
        meta?: ResponseMetaWithError;
        response?: {
            [key: string]: unknown;
        };
    };
};

export type GetSubAccountAddRequestError = GetSubAccountAddRequestErrors[keyof GetSubAccountAddRequestErrors];

export type GetSubAccountAddRequestResponses = {
    /**
     * SubAccountAddRequestResponse
     *
     * Sub-Account Add Request Response
     */
    200: {
        meta?: ResponseMeta;
        response?: SubAccountAddRequest;
    };
};

export type GetSubAccountAddRequestResponse = GetSubAccountAddRequestResponses[keyof GetSubAccountAddRequestResponses];

export type ListServicesData = {
    body?: never;
    path: {
        /**
         * The ID of the account.
         *
         * **NOTE:** To retrieve information about services provisioned on a location under a sub-account, enter the ID of the *main account* rather than the sub-account.
         *
         */
        accountId: string;
    };
    query?: {
        /**
         * A date in `YYYYMMDD` format.
         */
        v?: string;
        sku?: string;
        locationId?: string;
        /**
         * *(Portal Mode only)* Filters on the account that the location receiving service is in.
         *
         */
        locationAccountId?: string;
        /**
         * Status of the service. By default, returns only Active services, not All services.
         */
        status?: 'ACTIVE' | 'STOPPED' | 'ALL';
        agreementId?: number;
        limit?: number;
        /**
         *
         * Number of results to skip. Used to page through results.
         * Cannot be used together with **`pageToken`**.
         *
         */
        offset?: number;
    };
    url: '/accounts/{accountId}/services';
};

export type ListServicesErrors = {
    /**
     * ErrorResponse
     *
     * Error Response
     */
    default: {
        meta?: ResponseMetaWithError;
        response?: {
            [key: string]: unknown;
        };
    };
};

export type ListServicesError = ListServicesErrors[keyof ListServicesErrors];

export type ListServicesResponses = {
    /**
     * ServicesResponse
     *
     * Location Services Response
     */
    200: {
        meta?: ResponseMeta;
        response?: {
            count?: number;
            services?: Array<Service>;
        };
    };
};

export type ListServicesResponse = ListServicesResponses[keyof ListServicesResponses];

export type ListSubAccountServicesData = {
    body?: never;
    path: {
        /**
         * The ID of the account.
         *
         * **NOTE:** To retrieve information about services provisioned on a sub-account, enter the ID of the *main account* rather than the sub-account.
         *
         */
        accountId: string;
    };
    query?: {
        /**
         * A date in `YYYYMMDD` format.
         */
        v?: string;
        sku?: string;
        subAccountId?: string;
        /**
         * Status of the service. By default, returns only Active services, not All services.
         */
        status?: 'ACTIVE' | 'STOPPED' | 'ALL';
        agreementId?: number;
        limit?: number;
        /**
         *
         * Number of results to skip. Used to page through results.
         * Cannot be used together with **`pageToken`**.
         *
         */
        offset?: number;
    };
    url: '/accounts/{accountId}/subaccountservices';
};

export type ListSubAccountServicesErrors = {
    /**
     * ErrorResponse
     *
     * Error Response
     */
    default: {
        meta?: ResponseMetaWithError;
        response?: {
            [key: string]: unknown;
        };
    };
};

export type ListSubAccountServicesError = ListSubAccountServicesErrors[keyof ListSubAccountServicesErrors];

export type ListSubAccountServicesResponses = {
    /**
     * SubAccountServicesResponse
     *
     * Sub-Account Services Response
     */
    200: {
        meta?: ResponseMeta;
        response?: {
            count?: number;
            services?: Array<SubAccountService>;
        };
    };
};

export type ListSubAccountServicesResponse = ListSubAccountServicesResponses[keyof ListSubAccountServicesResponses];

export type CreateSubAccountData = {
    body: CreateSubAccountRequest;
    path: {
        accountId: string;
    };
    query?: {
        /**
         * A date in `YYYYMMDD` format.
         */
        v?: string;
    };
    url: '/accounts/{accountId}/createsubaccount';
};

export type CreateSubAccountErrors = {
    /**
     * ErrorResponse
     *
     * Error Response
     */
    default: {
        meta?: ResponseMetaWithError;
        response?: {
            [key: string]: unknown;
        };
    };
};

export type CreateSubAccountError = CreateSubAccountErrors[keyof CreateSubAccountErrors];

export type CreateSubAccountResponses = {
    /**
     * CreateSubAccountResponse
     *
     * Create Sub-Account Response
     */
    200: {
        meta?: ResponseMeta;
        response?: CreateSubAccount;
    };
};

export type CreateSubAccountResponse = CreateSubAccountResponses[keyof CreateSubAccountResponses];

export type ResourcesApplyRequestData = {
    body: ResourcesApplyRequest;
    path: {
        accountId: string;
    };
    query?: {
        /**
         * A date in `YYYYMMDD` format.
         */
        v?: string;
    };
    url: '/accounts/{accountId}/resourcesapplyrequests';
};

export type ResourcesApplyRequestErrors = {
    /**
     * ErrorResponse
     *
     * Error Response
     */
    default: {
        meta?: ResponseMetaWithError;
        response?: {
            [key: string]: unknown;
        };
    };
};

export type ResourcesApplyRequestError = ResourcesApplyRequestErrors[keyof ResourcesApplyRequestErrors];

export type ResourcesApplyRequestResponses = {
    /**
     * ResourcesApplyRequestResponse
     *
     * Resources Apply Request Response
     */
    200: {
        meta?: ResponseMeta;
        response?: ResourcesApplyRequestResponse;
    };
};

export type ResourcesApplyRequestResponse2 = ResourcesApplyRequestResponses[keyof ResourcesApplyRequestResponses];

export type GetResourcesApplyRequestStatusData = {
    body?: never;
    path: {
        /**
         * requestId returned from a previous call to **Resources Apply Requests: Create (Account)**
         *
         */
        requestId: number;
        accountId: string;
    };
    query?: {
        /**
         * A date in `YYYYMMDD` format.
         */
        v?: string;
    };
    url: '/accounts/{accountId}/resourcesapplyrequests/{requestId}';
};

export type GetResourcesApplyRequestStatusErrors = {
    /**
     * ErrorResponse
     *
     * Error Response
     */
    default: {
        meta?: ResponseMetaWithError;
        response?: {
            [key: string]: unknown;
        };
    };
};

export type GetResourcesApplyRequestStatusError = GetResourcesApplyRequestStatusErrors[keyof GetResourcesApplyRequestStatusErrors];

export type GetResourcesApplyRequestStatusResponses = {
    /**
     * ResourcesApplyRequestResponse
     *
     * Resources Apply Request Response
     */
    200: {
        meta?: ResponseMeta;
        response?: ResourcesApplyRequestResponse;
    };
};

export type GetResourcesApplyRequestStatusResponse = GetResourcesApplyRequestStatusResponses[keyof GetResourcesApplyRequestStatusResponses];

export type ListAccountsData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * A date in `YYYYMMDD` format.
         */
        v?: string;
        /**
         * Returns only accounts whose name contains the provided string
         */
        name?: string;
        limit?: number;
        /**
         *
         * Number of results to skip. Used to page through results.
         * Cannot be used together with **`pageToken`**.
         *
         */
        offset?: number;
    };
    url: '/accounts';
};

export type ListAccountsErrors = {
    /**
     * ErrorResponse
     *
     * Error Response
     */
    default: {
        meta?: ResponseMetaWithError;
        response?: {
            [key: string]: unknown;
        };
    };
};

export type ListAccountsError = ListAccountsErrors[keyof ListAccountsErrors];

export type ListAccountsResponses = {
    /**
     * AccountsResponse
     *
     * Accounts Response
     */
    200: {
        meta?: ResponseMeta;
        response?: {
            count?: number;
            accounts?: Array<Account>;
        };
    };
};

export type ListAccountsResponse = ListAccountsResponses[keyof ListAccountsResponses];

export type GetAccountData = {
    body?: never;
    path: {
        accountId: string;
    };
    query?: {
        /**
         * A date in `YYYYMMDD` format.
         */
        v?: string;
    };
    url: '/accounts/{accountId}';
};

export type GetAccountErrors = {
    /**
     * ErrorResponse
     *
     * Error Response
     */
    default: {
        meta?: ResponseMetaWithError;
        response?: {
            [key: string]: unknown;
        };
    };
};

export type GetAccountError = GetAccountErrors[keyof GetAccountErrors];

export type GetAccountResponses = {
    /**
     * AccountResponse
     *
     * Account Response
     */
    200: {
        meta?: ResponseMeta;
        response?: Account;
    };
};

export type GetAccountResponse = GetAccountResponses[keyof GetAccountResponses];

export type UpdateAccountData = {
    body?: UpdateAccount;
    path: {
        accountId: string;
    };
    query?: {
        /**
         * A date in `YYYYMMDD` format.
         */
        v?: string;
    };
    url: '/accounts/{accountId}';
};

export type UpdateAccountErrors = {
    /**
     * ErrorResponse
     *
     * Error Response
     */
    default: {
        meta?: ResponseMetaWithError;
        response?: {
            [key: string]: unknown;
        };
    };
};

export type UpdateAccountError = UpdateAccountErrors[keyof UpdateAccountErrors];

export type UpdateAccountResponses = {
    /**
     * IdResponse
     *
     * ID Response.
     */
    200: {
        meta?: ResponseMeta;
        response?: {
            id?: string;
        };
    };
};

export type UpdateAccountResponse = UpdateAccountResponses[keyof UpdateAccountResponses];
